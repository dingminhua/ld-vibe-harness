# Change — Rules

> 状态：not-created
> 主文档：`specs/22-Change-变更记录.md`

## 本文定位

本文是 Change 对象模型的 Rules 机制实践子文档，当前为 not-created 状态。

## 不创建理由

Change 事实实例以 Git commit 承载，不使用 YAML 实例文件。commit message 格式规范在主文档 §8 已完整定义，不需要 Rules 入口约束 YAML 写入、字段校验或状态机提醒。

Rules 层面的提交纪律约束已由工作区 L0 Rules（`提交纪律` 章节）和项目 L1 Rules 承接，不需要独立的 22.01-Rules.md 展开。

## 检查项

- [ ] 工作区 L0 Rules 中的提交纪律章节是否持续有效
- [ ] 如 Change 后续增加 YAML 实例或字段校验需求，重新评估是否需要创建 Rules 实体