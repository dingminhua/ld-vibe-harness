# 目录说明

> 创建日期：2026-06-08
> 定位：定义 LDVH 的项目目录边界、文档工作区映射、根目录初始化产物、docs/specs 编号分区和目录内容归属
> 适用范围：LDVH 自身项目、安装 LDVH 的管辖项目，以及 AI 在上述项目中判断文件归属、编号占用和目录事实源性质的场景
> 上位依据：`docs/specs/00-LD-Vibe-Harness理念与纲要.md`
> 相关规范：`docs/specs/02-术语规范.md`、`docs/specs/03-文档基础规范.md`、`docs/specs/03.01-规范文档规范.md`、`docs/specs/03.02-管辖项目文档规范.md`、`docs/specs/03.03-索引文档规范.md`、`docs/specs/04-规范落地与环境适配基础规范.md`、`docs/specs/07-Code实现规范.md`

---
## 1. 本文解决的问题

本文解决 LDVH 中内容应放在哪个目录、不同项目中的文档工作区如何映射、根目录初始化产物如何识别、docs/specs 编号如何分区，以及目录说明与文档规范、索引文档、环境适配和 Code 实现之间如何分工的问题。

本文只规定目录与编号边界，不直接定义文档写作细则、事实源权威性原则、工作模型字段契约、工作流程行动规则或 Code 实现细则。文档工作区规则由 `docs/specs/03-文档基础规范.md` 承接；规范落地与环境适配由 `docs/specs/04-规范落地与环境适配基础规范.md` 承接；Code 实现边界由 `docs/specs/07-Code实现规范.md` 承接。

---
## 2. 与 00 总纲的关系

00 定义 LDVH 由开发环境、工作模型、工作流程、Code 和 Web 五类构成要素共同形成 AI 工程驾驭体系，并要求稳定事实回到 Git 可追踪文件。

本文承接 00 的事实源和构成要素分工，把这些内容落实到项目目录和编号分区上，使 AI 能快速判断：

1. 哪个目录承载 LDVH 规范；
2. 哪个目录承载管辖项目文档；
3. 哪个目录承载结构化事实实例；
4. 哪个目录承载 Code、Web 和测试实现；
5. 哪些根目录文件属于项目入口或初始化结果物；
6. 哪些编号区段属于基础规范、工作模型、工作流程或其他构成要素。

---
## 3. 项目类型与工作区映射

LDVH 需要区分两类项目：

| 项目类型 | 定位 | 规范工作区 | 文档工作区 | 说明 |
|---|---|---|---|---|
| LDVH 自身项目 | 承载和维护 LDVH 规范体系、Code、Web 和 dogfood 事实实例 | `docs/specs/` | `docs/` 文档工作区、根 README.md、必要项目资料 | 当前正式基线只以 `docs/specs/` 作为规范事实源，`docs/evals/` 和 `docs/refs/` 只作为输入材料 |
| 管辖项目 | 安装并使用 LDVH 进行工程治理的用户项目 | 不默认创建 `docs/specs/` 或 `specs/` | `docs/` | 用户项目通过 `docs/` 承载项目文档、内部分析和外部引用 |

首次阅读或安装 LDVH 时，AI 应根据目标选择最小入口：

1. 理解 LDVH 正式基线：先读 `docs/specs/00-LD-Vibe-Harness理念与纲要.md`，再读本文和 `docs/specs/02-术语规范.md`；
2. 判断目录、编号或内容归属：先读本文，再按内容进入 03、04、05-12、20、40 或已登记的候选区段；
3. 完成当前环境初始化准备：先检查根目录 LDVH-ENVIRONMENT-INITIALIZATION.md，再按 `docs/specs/04.03-环境适配映射与运行投影记录模板.md` 创建、确认或更新记录，并按 `docs/specs/04.02-环境适配与运行投影规范.md`、`docs/specs/04.04-环境能力承接边界.md` 判断环境能力和降级方式；
4. 创建、修改或审计 docs/specs 正式规范：先读 `docs/specs/03-文档基础规范.md`、`docs/specs/03.01-规范文档规范.md`，再读 `docs/specs/04.01-规范落地要求规范.md` 和必要的 04 系列、07-12 规范。

管辖项目安装 LDVH 后，应优先检查根目录 LDVH-ENVIRONMENT-INITIALIZATION.md 是否存在并已适配当前项目、当前开发平台、权限边界和 Human 授权状态。该文件不存在或未适配时，按 `docs/specs/04.03-环境适配映射与运行投影记录模板.md` 处理，不得继续创建持久运行投影。

---
## 4. 目录与根文件规划

LDVH 当前识别以下目录和根文件：

| 路径 | 定位 | 主要内容 | 边界 |
|---|---|---|---|
| `docs/` | 文档工作区根目录 | LDVH 自身的规范正文、内部评估、外部引用；管辖项目的项目正文、内部分析、外部引用、说明和报告 | 具体分区由 03 系列文档规范判断 |
| `docs/specs/` | LDVH 规范正文区 | 正式规范正文 | 只属于 LDVH 自身项目；用户管辖项目不默认创建 |
| `docs/evals/` | 内部评估区 | LDVH 方案分析、内部调研；管辖项目方案比较、项目复盘、内部调研 | 不直接构成正式规范或项目稳定结论；结论需吸收到正文区、工作对象或决策后生效 |
| `docs/refs/` | 外部引用区 | 外部资料引用、副本、摘要和第三方文档 | 不直接构成正式规范或项目稳定结论；不得反向维护正文区规则 |
| `ldvh-base/` | 结构化事实实例目录 | Task、ADR、Change、Pitfall、Intent、Memo、Profile 等实例 | 事实实例目录；具体实例规则由工作模型规范定义 |
| `tools/` | Code 实现目录 | 校验器、脚本、上下文生成、受控写入和其他确定性实现 | 只放实现，不放测试；Code 输出不是最终事实源 |
| `tests/` | 自动化测试目录 | Code、Web 和其他实现的测试与夹具 | 测试内容直接放在 `tests/` 下，不另设 `tests/tools/`；测试结果不替代 Git 文件事实 |
| `web/` | Web 实现目录 | Human-facing 页面、后端、API、交互与呈现实现 | Web 状态不替代 Git 文件事实源 |
| LDVH-ENVIRONMENT-INITIALIZATION.md | 根目录环境初始化记录 | 当前项目环境适配、运行投影、能力核验和降级方式 | 不是规范文档；结构由 04.03 模板约束 |
| README.md | 项目入口说明 | 项目简介、安装、使用入口和导航 | 项目文档入口；具体规范不在 README 中定义 |

目录名称和内容归属本身不自动决定最终事实源权威性。最终事实源边界由 `docs/specs/09-事实源边界与承载规范.md` 定义；所有稳定事实仍应遵守 00 和 09 中 Git 可追踪文件和单一事实源原则。

---
## 5. docs/specs 编号分区

docs/specs 正文区使用编号分区。编号应表达文档在规范体系中的位置，而不是只表达创建顺序。

新增、删除、重命名或重排 docs/specs 根目录正式规范编号时，应评估 Human Gate。

| 编号段 | 定位 | 说明 |
|---|---|---|
| 00 | 理念与纲要 | 定义 LDVH 的存在理由、五类构成要素、价值标准和顶层治理原则 |
| 01 | 目录说明 | 定义目录边界、工作区映射、根目录初始化产物和编号分区 |
| 02 | 术语规范 | 定义术语边界、推荐用语、不推荐表达处理和术语变更治理 |
| 03 | 文档基础规范 | 定义文档工作区、规范文档、管辖项目文档和索引文档规则 |
| 04 | 规范落地与环境适配 | 定义规范落地要求、环境适配映射、运行投影、能力承接边界、LDVH 落地特别要求和个人落地特别要求 |
| 05 | 工作模型基础规范 | 定义工作模型通用规则、准入、字段契约原则和与 20-39 具体工作模型规范的关系 |
| 05.01 | 工作字段内容格式规范 | 定义工作模型字段中的叙述、检查、证据、决策、引用和日志等内容格式 |
| 06 | 工作流程基础规范 | 定义工作流程通用规则、Context、Scenario、Gate、回写、证据和与 40-59 具体工作流程规范的关系 |
| 07 | Code 实现规范 | 定义 Code 需求准入、实现、验证、修改维护和 Code 文档边界 |
| 08 | Web 信息同步规范 | 定义 Web 信息同步层、Human Gate UI、受控编辑、派生视图和 Web 文档边界 |
| 09 | 事实源边界与承载规范 | 定义 Git 文件事实源、单一事实源、事实承载介质和回写规则 |
| 10 | 运行闭环测试规范 | 定义运行闭环测试、测试用例建模、自动化测试和问题修正边界 |
| 11 | 非 LDVH 来源内容治理规范 | 定义平台、第三方、用户既有、项目既有和自动生成内容进入 LDVH 管辖时的接管规则 |
| 11.01 | 第三方 Skill 接管落地选项 | 定义第三方 Skill 和类似外部可复用 AI 能力作为默认可用接管路径时的启用、取消、降级和重新启用规则 |
| 12 | 最佳实践 | 记录不直接产生强制规则的最佳做法、案例和经验入口 |
| 13-19 | 保留基础规范扩展 | 可承接后续基础治理、实践或构成要素扩展，使用前需 Human Gate |
| 20 | 工作模型集合索引 | 维护 20-39 区段工作模型清单、状态、关系、初始化输入和审计输入 |
| 21-39 | 具体工作模型规范 | 21 ADR、22 Change、23 Pitfall、24 Intent、25 Memo、26 Profile 和 27 Task 为 active；28-34 按 20 的 removed、deferred 或 reserved 状态处理 |
| 40 | 工作流程集合索引 | 维护 40-59 区段工作流程清单、状态、关系、候选来源、初始化输入和审计输入 |
| 41-59 | 具体工作流程规范 | 41 多角色思考为 active；42-59 待创建或待讨论，后续定义产品初始化、产品审计、任务执行等具体工作流程 |
| 60-79 | 保留扩展区 | 后续构成要素或产品能力扩展，使用前需 Human Gate |
| docs/evals/ | 内部评估自然序号 | 不占用正文区编号 |
| docs/refs/ | 外部引用自然序号 | 不占用正文区编号 |

本文确认：工作模型基础规范不占用 20，工作流程基础规范不占用 40。20 和 40 用作规范型集合索引；05 和 06 分别承载工作模型、工作流程的基础规则，07 和 08 分别承载 Code 与 Web。

---
## 6. 索引文档与编号区段

20 和 40 是规范型集合索引，应按 `docs/specs/03.03-索引文档规范.md` 编写和维护。当前 20 工作模型集合索引和 40 工作流程集合索引均已在 `docs/specs/` 生效。AI 处理工作模型集合时，应读取 `docs/specs/20-工作模型集合索引.md`；处理工作流程集合时，应读取 `docs/specs/40-工作流程集合索引.md`。

集合索引只维护清单、状态、编号范围、关系、初始化输入和审计输入，不定义具体对象字段、状态机、工作流程 Context、Scenario、Gate 或行动流程。

工作模型和工作流程的分工如下：

| 区段 | 基础规范 | 集合索引 | 具体规范 |
|---|---|---|---|
| 工作模型 | 05 工作模型基础规范 | 20 工作模型集合索引 | 21-39 具体工作模型规范 |
| 工作流程 | 06 工作流程基础规范 | 40 工作流程集合索引 | 41-59 具体工作流程规范 |

新增或修改 20、40、21-39、41-59 文档时，应同步检查 01 的编号分区、03.03 的索引规则、对应基础规范和对应集合索引。若对应文件尚未创建或尚未生效，只能记录为候选事项、计划或待补齐事项，不能引用不存在的索引作为事实源入口。

---
## 7. 内容归属判断

AI 判断内容归属时，应先判断内容性质，再选择目录或编号：

| 内容性质 | 归属 |
|---|---|
| LDVH 顶层理念、构成要素和价值标准 | `docs/specs/00-LD-Vibe-Harness理念与纲要.md` |
| 目录、工作区和编号分区 | 本文 |
| 术语、命名和术语变更治理 | `docs/specs/02-术语规范.md` |
| 文档结构、引用、evals/refs 和索引文档规则 | 03 系列 |
| 规范落地、环境适配、运行投影和环境能力边界 | 04 系列 |
| Git 文件事实源和单一事实源原则 | `docs/specs/09-事实源边界与承载规范.md` |
| 工作模型通用规则 | `docs/specs/05-工作模型基础规范.md` |
| 工作字段内容格式 | `docs/specs/05.01-工作字段内容格式规范.md` |
| 工作流程通用规则 | `docs/specs/06-工作流程基础规范.md` |
| Code 需求、实现、验证和维护 | `docs/specs/07-Code实现规范.md` |
| Web 信息同步、Human Gate UI 和受控编辑 | `docs/specs/08-Web信息同步规范.md` |
| 运行闭环测试 | `docs/specs/10-运行闭环测试规范.md` |
| 非 LDVH 来源内容接管 | `docs/specs/11-非LDVH来源内容治理规范.md` |
| 第三方 Skill 接管落地选项 | `docs/specs/11.01-第三方Skill接管落地选项.md` |
| 最佳实践和经验入口 | `docs/specs/12-最佳实践.md` |
| 工作模型清单和状态 | `docs/specs/20-工作模型集合索引.md` |
| 工作流程清单和状态 | `docs/specs/40-工作流程集合索引.md` |
| 用户项目正文文档、内部分析和外部引用 | `docs/`、`docs/evals/`、`docs/refs/` |
| 结构化事实实例 | `ldvh-base/` |
| Code 实现 | `tools/` |
| Web 实现 | `web/` |
| 测试实现 | `tests/` |

如果某内容同时涉及多个归属，应优先进入其权威规则所在文档，并在其他文档中保留入口摘要或引用关系，不得复制多份规则正文。

---
## 8. 规范落地要求

本文通过以下规范落地要求说明相关要求的同步、检查或审计触发条件。

| 落地要求 | 要求内容 | 保障机制 | 同步类型 | 触发条件 |
|---|---|---|---|---|
| 上位约束承接要求 | docs/specs 新增、删除、重命名或重排正式规范时，应遵守本文目录边界和编号分区 | `docs/specs/03.01-规范文档规范.md`、Human Gate、文档检查 Code | 文档治理 | 编号分区、目录规划、根目录初始化产物或工作区映射变化时 |
| 入口可见要求 | AI 进入 LDVH 项目或用户管辖项目时，应能定位本文作为目录和编号分区入口 | 规范文档入口摘要、README、根目录环境初始化记录 | AI 执行入口提示 | 本文定位、目录表、编号表或内容归属判断变化时 |
| 确定性执行要求 | 后续 Code 应能检查 docs/specs 正文区编号、索引文档范围和关键路径引用是否符合本文 | `tools/specs_validate.py` 扩展、正反样例 | 校验实现 | 编号格式、编号分区、索引范围、路径引用规则变化时 |
| Human 交互要求 | 改变目录规划、编号分区、20/40 索引定位或根目录初始化产物时，应触发 Human Gate | Human Gate、变更前说明、人工确认 | 目录治理 | 新增目录、改变目录定位、改变编号分区、恢复已停用编号含义或删除根目录初始化产物时 |
| 生命周期触发要求 | 正式规范新增、删除、重命名或索引状态变化后，应检查本文是否需要同步 | 规范修改执行顺序、索引维护检查、人工降级检查 | 触发保障 | docs/specs 根目录结构、20-39、40-59 或文档工作区边界变化时 |

---
## 9. Human Gate 与检查要求

以下情况应评估 Human Gate：

1. 新增、删除或重命名本文定义的一级目录；
2. 改变 `docs/specs/`、`docs/`、`ldvh-base/`、`tools/`、`web/` 或 `tests/` 的定位；
3. 将用户管辖项目改为默认创建 `docs/specs/` 或 `specs/`；
4. 改变 LDVH-ENVIRONMENT-INITIALIZATION.md 的根目录定位；
5. 改变 docs/specs 编号分区；
6. 将 20 或 40 改为基础规范编号；
7. 将索引文档扩张为具体工作模型或工作流程规则；
8. 将输入材料或临时参考内容直接视为正式规范。

目录和编号检查至少包括：

| 检查项 | 标准 |
|---|---|
| 工作区映射 | LDVH 自身使用 `docs/specs/`；用户管辖项目使用 `docs/` |
| 根目录初始化记录 | LDVH-ENVIRONMENT-INITIALIZATION.md 位于根目录，缺失或未适配时按 04.03 处理 |
| 编号分区 | 文档编号符合本文 §5 |
| 索引边界 | 20/40 是集合索引，05/06 是基础规范 |
| 内容归属 | 内容进入权威归属文档，不重复多处维护 |
| 输入材料边界 | docs/evals、docs/refs、临时参考内容不直接成为正式依据 |
| Code 适配 | 可机械检查内容已评估是否需要 Code 支撑 |

---
## 10. 待补齐事项

1. 20 工作模型集合索引和 40 工作流程集合索引后续状态变化时，应继续同步本文；
2. docs/specs 编号分区是否需要 Code 结构化校验，待 20/40 最小样例稳定后补齐；
3. 13-19 保留区后续使用前，应先确认是否与 11 非 LDVH 来源内容治理、12 最佳实践或其他基础规范重复。
