target_spec: "specs/attachments/01.Att.03-环境入口类型表.md"
target_sha256: "5539fc812ec65ee8068f9deb4e88010b34a8eca90dcc947c13cd3faa272b2585"
change_type: "modification"
mapping_evidence:
  path: "_migration/28A-codex-plugin-environment-entry-alignment.md"
code_verification:
  command: "python3 code/specs_validate.py all --format text --fail-on-diagnostics && python3 -m pytest tests/code/test_formal_specs.py -q && python3 -m pytest tests/code/test_ldvh_specs_validate.py -q -k 'assurance_spec_defines_git_and_environment_hook_boundaries or environment_entry_audit'"
  passed: true
  receipt: "Formal gate covers the environment entry type closed table as an attachment subordinate to 01, including Git hook shim, generic LDVH environment plugin or extension hook and external adapter candidate entry kinds."
subagent_review:
  agent_id: "codex-local-01-environment-absorption-review"
  reviewer: "Codex"
  verdict: "pass"
  receipt: "Local review confirms 01.Att.03 only carries the entry type enum and keeps Git hook shim / LDVH environment plugin or extension hook as LDVH entry pointers, not independent rule sources; Codex remains only the current audited example."
  unresolved_blockers: []
warnings: []
