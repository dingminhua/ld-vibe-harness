# Skill 机制规范

> 创建日期：2026-05-30
> 所属主文档：`specs/11-LDVH-AI协作规范.md`
> 关系：父规范扩展
> 适用范围：LD Vibe Harness Skill 准入、命名、部署、输出、边界和变更治理
> 上位依据：`specs/11-LDVH-AI协作规范.md`
> 相关规范：`specs/01-LDVH目录说明.md`、`specs/02-LDVH术语规范.md`、`specs/03-Specs文档规范.md`、`specs/10-事实源边界与承载规范.md`、`specs/14-LDVH行动模型基础规范.md`

---

## 一、本文定位

本文定义 LD Vibe Harness 如何使用 Trae Skill 机制承载可复用流程、标准化输入输出、轻量评估、契约式行动和受控写入入口。

Skill 是可复用专业能力说明书，不是事实源。LDVH 不在本文中定义具体 LDVH 标准 Skill 清单，也不把其他项目的标准 Skill 目录迁移为 LDVH 正式规则。

## 二、父规范关系

`specs/11-LDVH-AI协作规范.md` 定义 AI 协作机制总边界、事实源回写总原则和 Rules / Skill / Agent 跨机制选择边界。本文只展开 Skill 机制。

当需要判断某项内容应进入 Rules、Skill 还是 Agent 时，必须以 `specs/11-LDVH-AI协作规范.md` 第五节为准；本文只检查已拟使用 Skill 时是否满足流程设计、命名部署、输出回写和治理要求。

Skill 与其他机制的关系：

| 对象 | 关系 |
|---|---|
| Rules | Rules 可定义 Skill 触发入口；Skill 不替代 Rules |
| Agent | Skill 不调度 Agent；需要独立上下文时只能建议用户或主控使用 Agent |
| Tools | Skill 可在授权流程中调用 Tools 执行确定性解析、校验、聚合、上下文准备或受控写入；Tools 不承载 Skill 的流程性判断 |
| 事实模型 | Skill 可按授权整理事实实例草案或编排受控写入流程，但对象字段和状态以 20-49 规范为准 |
| 行动模型 | Skill 的触发、执行和回写流程应服从 50-79 行动模型 |

## 三、Skill 内部设计检查

跨机制选择依据 `specs/11-LDVH-AI协作规范.md` 第五节，本文不另行判断某项内容是否应优先进入 Rules、Skill 或 Agent。已拟使用 Skill 时，应检查该 Skill 是否满足以下内部设计条件：

1. 可复用，不是一次性临时操作；
2. 存在多步骤流程或稳定分支判断；
3. 需要标准化输入、输出或写入格式；
4. 可在主上下文中执行；
5. 有明确事实源读取和回写边界；
6. 不承载 Human Gate 判断本身；
7. 不承载稳定规则正文；
8. 不承载完整教程或外部工具手册；
9. 具体对象流程已有事实模型规范、行动模型或模型实践子文档支撑。

## 四、命名与部署边界

LDVH 提供的通用 Skill 应使用 `ldvh-` 前缀，避免与管辖项目业务 Skill 或外部 Skill 混淆。

命名规则：

| 项目 | 规则 |
|---|---|
| 目录名 | `.trae/skills/ldvh-{skill-name}/` |
| YAML `name` | 使用与目录一致的 `ldvh-{skill-name}` |
| 命名风格 | 小写字母、数字和连字符 |
| 禁止 | 不得使用其他项目前缀，不得绑定具体业务项目名 |

部署边界：

1. LDVH 通用 Skill 可部署在工作区顶层 `.trae/skills/`；
2. 管辖项目业务 Skill 应部署在对应项目 Skill 位置，并不得使用 `ldvh-` 前缀；
3. Skill 实体文件是 Trae 执行入口，不替代 specs/、ldvh-base/或 docs/ 的事实源；
4. Skill 创建、修改、删除或迁移部署位置应评估 Human Gate。

## 五、Skill 文件结构要求

每个 Skill 至少应包含以下信息：

| 段落 | 要求 |
|---|---|
| YAML 头部 | 包含 `name` 和 `description`，description 应从 AI 视角描述触发时机 |
| 定位 | 说明 Skill 解决的问题和机制边界 |
| 触发条件 | 明确自动触发或人主动触发条件 |
| 不适用场景 | 明确不应调用该 Skill 的场景 |
| 必读文件 | 列出执行前必须读取的权威事实源 |
| 执行流程 | 步骤化流程，包含必要分支 |
| 输出格式 | 明确结构化输出和写入结果 |
| Human Gate | 明确遇到门禁时暂停并请求确认 |
| 事实源回写 | 明确哪些结果需要回写到 Git 文件事实源 |
| Agent 边界 | 明确 Skill 不调度 Agent |

Skill 必读文件中的路径必须区分 LDVH 项目规范、工作区规则和管辖项目事实源，不得用“当前项目”等模糊表述掩盖路径归属。

## 六、输出与事实源回写

Skill 输出应满足：

1. 明确输入来源；
2. 明确读取了哪些事实源；
3. 明确分析结论；
4. 明确建议动作；
5. 明确是否触发 Human Gate；
6. 明确哪些内容是事实、哪些内容是建议；
7. 涉及写入时列出待写入文件、写入原因和影响范围；
8. 写入后说明 Change 记录是否已追加。

Skill 输出不是最终事实源。Skill 产生的稳定结论、任务、备忘、证据、决策或变更，必须写入对应 Git 文件事实源后才成为稳定事实。

## 七、Skill 与 Agent 边界

Skill 不得调度 Agent。Skill 发现以下情况时，应停止升级动作并提示用户或主控判断是否使用 Agent：

1. 主上下文难以承载所需事实源；
2. 需要不同角色独立形成结论；
3. 需要并行委派多个专业角色；
4. 需要与主上下文不同的权限边界；
5. 需要长期维护 Agent Prompt 或权限配置。

Skill 可在输出中说明“建议主控或用户评估是否使用 Agent 或进入对应行动模型”的理由，但不得自行创建、调用、编排、修改或删除 Agent。涉及子 Agent 流程的，应回到对应 Rules 入口和行动模型主文档，由主控依据该行动模型编排。

## 八、具体 Skill 清单原则

本文不维护 LDVH 标准 Skill 清单。具体 Skill 只有在满足以下条件后，才可进入模型实践子文档或实体定义：

1. 对应事实模型规范、行动模型或工具实践已稳定；
2. Skill 的输入、输出和事实源回写边界明确；
3. Skill 不复制 specs 正文或外部资料；
4. Skill 创建已通过 Human Gate；
5. 已记录 Change YAML；
6. 已验证不会与管辖项目 Skill 或外部 Skill 命名冲突。

## 九、Human Gate 与检查要求

以下情况应评估 Human Gate：

1. 创建、修改、删除、重命名或启用/禁用 Skill；
2. 改变 Skill 部署位置、命名前缀或触发条件；
3. 将 Skill 输出提升为稳定事实但未回写 Git 文件事实源；
4. 允许 Skill 修改 Rules、specs 规范、Agent、跨项目契约或其他高影响资产；
5. 允许 Skill 调度 Agent；
6. 在正式规范中新增具体 LDVH 标准 Skill 清单。

Skill 检查至少包括：

| 检查项 | 标准 |
|---|---|
| 准入 | 满足可复用、多步骤、标准输入输出、契约式行动和主上下文可执行 |
| 命名 | LDVH 通用 Skill 使用 `ldvh-` 前缀 |
| 边界 | 不修改 Rules、specs 规范、Agent 或跨项目契约 |
| Agent 边界 | 不调度 Agent |
| 事实源 | Skill 不自成事实源，稳定事实已回写 |
| Human Gate | 创建、修改、删除和高影响写入已识别门禁 |
| Change 记录 | Skill 变更已记录到 `ldvh-base/changes/` |

## 十、待补齐事项

1. 20-49 事实模型规范稳定后，补齐对象写入型 Skill 的字段级检查；
2. 50-79 行动模型稳定后，补齐 Skill 触发与场景识别流程；
3. 如未来创建具体 LDVH Skill，应新增对应模型实践子文档或实体定义，不在本文维护清单。
