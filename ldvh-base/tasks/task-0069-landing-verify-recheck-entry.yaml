id: task-0069
type: task
title: 实现 landing verify 与复检聚合入口
status: closed
created: '2026-06-11T00:38:22.091927'
updated: '2026-06-11T02:40:00'
description: 实现 landing verify 与 recheck 聚合入口，复用 fact/spec/web/test 结果，作为 apply/repair
  后进入 review_needed 的验证证据来源。
source: intent-0012；docs/research/21-LDVH下一阶段推进方向-受控落地执行闭环.md；用户确认创建任务批次。
acceptance: '- [x] verify 能聚合 fact_validate、specs_validate、pytest 或等价命令结果；- [x] verify
  输出通过、失败、degraded、需要 Human Gate 等状态；- [x] verify 结果能被 review_needed 队列或 Web 消费；-
  [x] 验证失败时不得进入 closed；- [x] 测试覆盖成功、失败和 degraded 输出。'
blocked_by:
- task-0066
deliverables:
- tools/specs_validate.py
- tests/test_specs_validate.py
- .trae/specs/landing-verify-recheck-entry/tasks.md
source_intent: intent-0012
related_adrs:
- adr-0007
related_docs:
- docs/research/21-LDVH下一阶段推进方向-受控落地执行闭环.md
verification: |
  ## 验证计划

  先补 landing-verify 失败测试，再实现聚合入口；运行聚焦测试、全量 pytest、fact_validate、ldvh-landing-check、landing-verify。

  ## 验证结果

  - `python3 -m pytest tests/test_specs_validate.py -k landing_verify` 先失败于 `landing_verify_build` 和 CLI 子命令缺失，完成实现后结果 5 passed。
  - `python3 -m pytest` 结果 202 passed。
  - `python3 tools/fact_validate.py ldvh-base` 结果 files=104 errors=0 warnings=0。
  - `python3 tools/specs_validate.py landing-verify --format json` 正常输出 landing-verify/v1 JSON，因既有 landing-report/spec_validate open 缺口返回 1；输出 `review_queue.status=blocked`、`next_steps.task_status=review_needed`、`can_close=false`。
  - `python3 tools/specs_validate.py ldvh-landing-check` 因既有 landing-report open、Human Gate degraded、spec_validate open 返回 1。
  - `python3 tools/specs_validate.py all docs/specs --specs-dir docs/specs` 因既有平台入口 Markdown 路径 warning 返回 1，未发现本次 landing-verify 相关测试失败。

  ## 结论

  landing-verify 聚合入口已实现，可复用 ldvh-landing-check 的 fact/spec 结果并接入 pytest 等价结果，输出 review_needed 队列证据；验证失败场景不会进入 closed，而是进入 blocked review 队列。
closed_at: '2026-06-11T02:40:00'
closure_evidence: |
  ## 验证结果

  - `python3 -m pytest tests/test_specs_validate.py -k landing_verify` 结果 5 passed。
  - `python3 -m pytest` 结果 202 passed。
  - `python3 tools/fact_validate.py ldvh-base` 结果 files=104 errors=0 warnings=0。
  - `python3 tools/specs_validate.py landing-verify --format json` 输出 landing-verify/v1 JSON；当前项目存在既有 open/degraded 缺口时返回 1，并保留 review_needed blocked 队列。
  - `python3 tools/specs_validate.py ldvh-landing-check` 因既有 landing-report/spec_validate open 与 Human Gate degraded 返回 1。
  - `python3 tools/specs_validate.py all docs/specs --specs-dir docs/specs` 因既有平台入口 Markdown 路径 warning 返回 1。

  ## 结论

  task-0069 已完成；landing-verify 聚合入口已覆盖成功、失败、degraded、需要 Human Gate 和 CLI JSON 输出，并将失败验证保留为 review_needed 证据而非 closed。
