# Code诊断码表

```yaml
v2_attachment:
  attachment_id: "04.Att.04"
  title: "Code诊断码表"
  status: "draft"
  authority: "not_active_until_parent_and_human_approved"
  parent_spec: "specs-v2/04-Code确定性执行规范.md"
  canonical_path: "specs-v2/attachments/04.Att.04-Code诊断码表.md"
  purpose: "承载 04 授权的 Code 诊断字段和等级"
  migration_sources:
    - "specs/07-Code确定性执行实现规范.md"
  code_consumption:
    - "code_diagnostic_schema"
```

> 文件状态：本文当前位于 `specs-v2/`，尚未切换为 active；正式 Code 诊断规则仍以 active `specs/07-Code确定性执行实现规范.md` 和当前实现为准。本文是 `04-Code确定性执行规范.md` 授权附件，必须随父规范经过 Human 单篇确认后才可生效，不定义诊断实现或关闭判断。

## 1. 定位

本文承载 04 授权的 Code 诊断字段和等级。

## 2. 诊断字段

| 字段 | 含义 |
|---|---|
| `severity` | `error`、`warning` 或 `info` |
| `code` | 稳定诊断码 |
| `message` | 人类可读诊断说明 |
| `source_refs` | 来源路径、章节、对象、字段或 commit 回指 |
| `suggested_owner` | 建议归口；无法判断时可为空，但必须通过降级说明暴露 |

## 3. 使用规则

诊断只暴露缺口、冲突、漂移、降级和建议回写方向，不直接关闭问题，不替代 Human Gate。`--fail-on-diagnostics` 的失败边界由具体命令实现和测试确认。

## 4. 待补齐事项

1. 当前 v2-check 并非所有诊断都稳定填充 `suggested_owner`，后续应补实现或记录降级；
2. 诊断码闭集和正反样例仍需 Code 测试补齐。
