# LDVH 环境初始化记录

> 创建日期：2026-06-08
> 文件名：`LDVH-ENVIRONMENT-INITIALIZATION.md`
> 当前记录适用项目：LD Vibe Harness
> 当前记录适用环境：Codex
> 当前适配状态：已适配 LDVH 当前项目的 Codex 环境；若本文件被安装到其他项目或其他开发平台，必须重新确认并改写
> 模板来源：`docs/specs/04.03-环境适配映射与运行投影记录模板.md`
> 文件性质：项目根目录环境初始化结果文件，不是 docs/specs 正式规范

---
## 1. 这个文件是什么

这个文件用于告诉 Human 和 AI：当前项目的 LDVH 环境初始化结果是什么。

它记录：

1. 当前使用的开发环境；
2. 已核验的平台能力；
3. LDVH 规范落地要求如何映射到当前环境；
4. 哪些运行投影已经存在、缺失、仅作为会话级降级方式存在；
5. 创建、修改或删除持久运行投影前，需要检查什么、由谁确认。

这个文件放在项目根目录，是为了让用户可以直接看见和审阅当前 LDVH 环境设置。它不是隐藏工具状态，不是缓存，不是 docs/specs 正式规范，不是外部参考文档，也不能替代 Git 文件事实、Human Gate、Code 验证或 docs/specs 规则。

本文件结构由 `docs/specs/04.03-环境适配映射与运行投影记录模板.md` 约束。已有内容应增量更新；除非 Human 明确要求重置，不得覆盖已有记录。

---
## 2. 适配状态与持续提醒

本文件可能随 LDVH 安装进入用户项目。安装后的默认文件不等于已经适配用户的当前开发平台。

AI 每次准备为用户执行以下动作前，必须先读取并检查本节：

1. 映射或实现环境功能；
2. 创建、修改或删除 Rules / Instructions、Skill、Agent、Hook、CI、Web、命令或配置；
3. 把某项规范落地要求登记为运行投影；
4. 判断某个环境能力已经可以承接 LDVH 要求；
5. 将会话级降级方式升级为持久运行投影。

检查标准如下：

| 检查项 | 当前记录 |
|---|---|
| 记录适用项目 | LD Vibe Harness |
| 记录适用环境 | Codex |
| 用户当前项目 | LD Vibe Harness |
| 用户当前开发平台 | Codex |
| 权限边界 | 当前 Codex 会话允许在工作区内读写文件、运行本地校验和发起必要审批；创建项目级持久运行投影前仍需单独确认 |
| Human 授权状态 | 已授权本次 docs/specs 文档修复和校验；未授权自动创建项目级指令、配置、Skill、Hook、automation 或 plugin 投影 |
| 适配状态 | 已适配当前项目与当前环境 |
| 最近 Human 确认 | 2026-06-08，本项目当前会话确认 |
| 安装到其他项目后的默认判断 | 待用户确认，不能直接视为已适配 |

如果当前项目、当前开发平台、环境能力、权限边界或 Human 授权状态与上表不一致，AI 必须持续提醒用户修改本文件，直到用户完成确认或改写。

在本文件未适配当前用户环境前，AI 可以做以下事情：

1. 说明不匹配原因；
2. 草拟本文件的修改建议；
3. 调研当前平台能力；
4. 执行不产生持久运行投影的临时分析；
5. 记录会话级降级方式和残留风险。

在本文件未适配当前用户环境前，AI 不得做以下事情：

1. 宣称某项环境适配映射已经完成；
2. 把当前环境能力当成已核验事实；
3. 创建、修改或删除持久运行投影；
4. 把会话级降级方式当成项目级配置；
5. 让未确认的平台能力替代 docs/specs 正式规范或 Human Gate。

---
## 3. 初始化摘要

| 字段 | 内容 |
|---|---|
| 当前环境 | Codex |
| 初始化目标 | 记录 LDVH 仓库的环境适配映射和运行投影状态 |
| 初始化日期 | 2026-06-08 |
| 模板来源 | `docs/specs/04.03-环境适配映射与运行投影记录模板.md` |
| 能力来源 | 2026-06-08 通过 openai-docs skill 获取并核验的 OpenAI Codex Manual |
| 权限边界 | 当前会话可进行文档修复、校验和必要审批；项目级持久投影创建需另行 Human 确认 |
| Human 授权状态 | 本次授权限于修复 docs/specs 内部自洽问题和同步校验 |
| 当前项目投影状态 | 尚未建立 Codex 项目级持久运行投影 |
| 降级模式 | 当前会话直接读取 docs/specs，手动运行 LDVH Code 检查，并通过对话或审批请求 Human 确认 |

---
## 4. 能力核验来源

本记录中的 Codex 能力事实来自 OpenAI Codex Manual，地址为 `https://developers.openai.com/codex/codex-manual.md`。该文档于 2026-06-08 通过 openai-docs skill 获取，并确认本地缓存为当前来源。

已覆盖的手册范围：

1. Agent Skills；
2. Custom instructions with AGENTS；
3. Subagents；
4. Hooks；
5. Advanced Configuration；
6. Model Context Protocol；
7. Automations；
8. Plugins。

能力摘要：

| 能力 | 核验摘要 | LDVH 使用边界 |
|---|---|---|
| 指令入口 | Codex 支持通过 AGENTS 类文件、项目指令或用户指令形成持久指导 | 适合承接入口可见要求，但不应复制 docs/specs 长正文 |
| Skills | Codex Skills 支持渐进披露的可复用工作流 | 适合承接流程复用要求；不是最终事实源，也不是子 Agent 调度器 |
| Subagents | Codex 支持显式调用子 Agent 完成独立、专项或并行工作 | 适合承接独立/专项/并行思考要求，但必须由主控汇总 |
| Hooks | Codex 支持围绕工具调用、用户提示、子 Agent 事件、压缩和停止事件设置生命周期钩子 | 适合承接生命周期触发要求，但需要项目配置和可执行检查支撑 |
| 配置 | Codex 支持用户级和可信项目级配置 | 只有经过信任、权限和 Human Gate 检查后，才适合承接持久项目行为 |
| MCP | Codex 支持 MCP 作为外部工具和上下文来源 | 只作为工具来源，不是 LDVH 自身核心机制 |
| Automations | Codex App 支持后台、定时或线程自动化 | 适合承接周期性检查或后续跟进，结果仍需回写事实源 |
| Plugins | Codex Plugins 可以打包 skills、apps 和 MCP servers | 是分发单元，不是独立的规范落地要求 |

---
## 5. 环境适配映射

| 规范来源 | 规范落地要求 | 保障机制 | Codex 承接方式 | 运行投影实体或位置 | 运行投影状态 | 验证方式 | 降级方式 |
|---|---|---|---|---|---|---|---|
| `docs/specs/04.01-规范落地要求规范.md` §4 | 入口可见要求 | 入口摘要、项目说明、规则入口映射、目录入口 | 项目或用户指令、仓库入口摘要、当前任务提示 | AGENTS 类项目入口、项目指令或会话入口摘要 | 项目级缺失；会话级存在 | 检查项目入口文件、配置和会话启动上下文 | 当前任务显式读取 docs/specs 入口规范 |
| `docs/specs/04.01-规范落地要求规范.md` §4 | 流程复用要求 | Skill、命令、工作流程、操作清单 | Codex Skill、命令流程或可复用人工流程 | 项目或用户 Skill、命令入口或文档化流程 | 项目级缺失 | 检查 Skill 可发现性、描述匹配和脚本可执行性 | 手动执行模板步骤，并记录缺失投影 |
| `docs/specs/04.01-规范落地要求规范.md` §4 | 子 Agent 思考要求 | 子 Agent、独立审阅、并行审查、专项检查 | 显式 Codex 子 Agent 或并行审阅流程 | 会话级子 Agent 调用；项目级模板或角色配置仅在后续创建后存在 | 项目级缺失；会话级可用 | 子 Agent 返回独立结果，主控汇总 | 使用人工二次审阅或主控自审，并记录缺口 |
| `docs/specs/04.01-规范落地要求规范.md` §4、`docs/specs/07-Code实现规范.md` | 确定性执行要求 | Code、测试、校验命令、CI、脚本 | LDVH Code 命令、脚本、测试、CI 或受控写入入口 | `tools/specs_validate.py`、后续漂移检查 Code、CI 或命令入口 | LDVH Code 部分存在；漂移检查缺失 | 运行命令、测试、正反样例或等价检查 | 按 `docs/specs/07-Code实现规范.md` 记录 Code 缺口、降级检查和残留风险 |
| `docs/specs/04.01-规范落地要求规范.md` §4 | Human 交互要求 | Human Gate、Web、对话确认、审批提示 | 对话确认、权限审批、Codex App 交互、Web 或 Human Gate 提示 | Human Gate 提示、审批请求、Web 工作台或对话记录 | 项目级缺失；会话级可用 | Human 确认、审批记录或可追溯验收 | 暂停并请求 Human 决策，不得用 AI 自报替代确认 |
| `docs/specs/04.01-规范落地要求规范.md` §4、`docs/specs/04.02-环境适配与运行投影规范.md` | 生命周期触发要求 | 触发映射、CI、自动运行、定时任务、降级检查 | Codex hooks、automations、CI、命令约束或人工生命周期检查 | Hooks 配置、自动化、CI、停止前检查或降级记录 | 项目级缺失；人工降级可用 | 检查触发配置、执行记录、命令输出或人工清单 | 变更后手动执行生命周期检查，并记录缺失的自动触发 |

---
## 6. 当前运行投影状态

当前 LDVH 仓库 Codex 项目级运行投影状态如下：

| 投影类型 | 来源映射 | 当前状态 | 维护方式 | 验证方式 | 漂移风险 | 回写出口 | 后续处理 |
|---|---|---|---|---|---|---|---|
| 入口可见投影 | 入口可见要求 | 缺失 | 待 Human 确认后创建 | 检查项目入口文件、配置和会话启动上下文 | AI 只能依赖会话显式读取，容易遗漏入口 | 当前环境初始化记录或后续项目入口投影 | 待 docs/specs 00、03、04、14 稳定后再评估 |
| 项目配置投影 | 生命周期触发要求、Human 交互要求 | 缺失 | 仅在确有必要且授权后创建 | 检查项目级 Codex 配置 | 未经确认创建配置会扩大权限或自动行为 | 当前环境初始化记录、Task 或 Memo | 只有项目级 hooks、MCP、模型或运行约束确有必要时才创建 |
| Skill 投影 | 流程复用要求 | 缺失 | 待流程稳定后创建 | 检查 Skill 可发现性、描述匹配和脚本可执行性 | 流程未稳定前创建会固化过早抽象 | 当前环境初始化记录或 Skill 说明 | 待 specs 编辑流程稳定后再决定 |
| 子 Agent 投影 | 子 Agent 思考要求 | 项目级缺失；会话级可用 | 会话级人工调用；项目级模板待定 | 子 Agent 返回独立结果，主控汇总 | 会话级能力不等于项目级持久投影 | 审计结果、Task、Memo 或正式规范 | 复杂审计使用会话级子 Agent 并记录为降级；稳定重复流程再创建投影 |
| Hook 投影 | 生命周期触发要求 | 缺失 | 待检查 Code 和触发时机稳定后创建 | 检查 hooks 配置和执行记录 | 生命周期要求只能人工降级检查，可能遗漏自动触发 | 当前环境初始化记录、Task 或 Code 需求 | 生命周期触发要求和可执行 Code 检查稳定后才创建 |
| Code 命令投影 | 确定性执行要求 | LDVH Code 部分存在 | 手动运行现有 LDVH Code；后续可接入 CI 或 hooks | 运行 `tools/specs_validate.py` 和测试 | 现有 Code 只覆盖部分结构检查，漂移检查缺失 | Code 规范、测试、Task 或正式规范 | 必要时补充环境适配映射和后续运行投影漂移检查 |
| Human Gate 投影 | Human 交互要求 | 项目级缺失；会话级可用 | 当前会话通过对话确认和审批请求执行 | Human 确认、审批记录或可追溯验收 | 缺少项目级投影时依赖会话纪律 | 当前环境初始化记录、Task 或审计记录 | 高影响变更继续保留显式 Human 决策，并记录项目级投影缺失 |

---
## 7. 初始化动作

本记录已完成以下动作：

1. 确认当前仓库尚无 Codex 项目级指令入口、项目配置、项目 Skill 目录或 hooks 配置；
2. 记录 Codex 能力来源和能力摘要；
3. 将 LDVH 规范落地要求类型映射到 Codex 承接方式；
4. 记录当前运行投影状态和降级方式；
5. 将稳定 LDVH 规则保留在 docs/specs，将本文件保留为初始化结果物。

---
## 8. 更新规则

以下情况发生时，应更新本文件：

1. 当前项目执行环境变化；
2. Codex 项目级指令、配置、skills、hooks、automations 或 plugins 被创建、修改或删除；
3. docs/specs 的规范落地要求、环境适配规则或 Code 规则变化；
4. 当前环境能力核验结果变化；
5. 会话级降级方式升级为持久运行投影；
6. Human 明确要求重新初始化 LDVH 环境设置；
7. 本文件被安装到其他项目或其他开发平台。

已有记录应读取后增量更新。除非 Human 明确要求重置，不得在初始化时覆盖本文件。

---
## 9. Human Gate 与检查

以下情况应评估 Human Gate：

1. 创建、修改或删除项目级指令入口、配置、skills、hooks、automations 或 plugin 设置；
2. 启用自动触发、自动写入或大范围后台自动化；
3. 将会话级降级方式视为持久项目投影；
4. 删除唯一可用的 Code 检查或降级检查；
5. 将本文件从项目初始化记录改成正式规范或外部参考；
6. 将安装默认记录视为已适配用户当前开发平台。

本记录检查项：

| 检查项 | 标准 |
|---|---|
| 用途清晰 | 用户打开本文件即可知道它是 LDVH 环境初始化记录 |
| 根目录可见 | 文件位于项目根目录，不隐藏为工具私有状态 |
| 中文可读 | 正文使用中文，英文文件名只作为稳定入口 |
| 模板可追溯 | 记录可追溯到 `docs/specs/04.03-环境适配映射与运行投影记录模板.md` |
| 适配状态 | 已说明当前记录适用项目、适用环境、适配状态和最近 Human 确认 |
| 能力来源 | 平台能力事实有来源和日期 |
| 映射 | 规范落地要求映射到环境承接方式 |
| 投影状态 | 项目级投影与会话级降级方式分开 |
| 事实源边界 | 本文件不替代 docs/specs、Code 验证、Human Gate 或 Git 文件事实 |

---
## 10. 未决限制与后续事项

当前未决限制：

1. 尚未创建 Codex 项目级指令入口、项目配置、项目 Skill、项目子 Agent 工作流、hook、automation 或 plugin 投影；
2. 根目录环境初始化记录结构检查已由 `tools/specs_validate.py env-init` 覆盖，但运行投影漂移检查 Code 尚未实现；
3. Codex 官方文档、本地配置或项目级运行投影变化时，应重新核验 Codex 能力事实；
4. 其他项目或其他环境安装本文件后，必须先按当前项目和当前开发平台改写本文件，再创建持久运行投影。
