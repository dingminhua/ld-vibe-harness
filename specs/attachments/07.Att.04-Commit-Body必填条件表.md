# Commit-Body必填条件表

```yaml
v2_attachment:
  attachment_id: "07.Att.04"
  title: "Commit-Body必填条件表"
  status: "active"
  authority: "active_with_parent_spec"
  parent_spec: "specs/07-事实源边界与Git追溯规范.md"
  canonical_path: "specs/attachments/07.Att.04-Commit-Body必填条件表.md"
  purpose: "承载 07 授权的 commit body 必填条件和正文质量标准"
  migration_sources:
    - "history/specs-v1/10-Git提交规范.md"
  code_consumption:
    - "commit_body_required_conditions"
    - "commit_body_quality_warnings"
```

> 文件状态：本文是 active 授权附件；随父规范生效，不独立改变父规范正文授权之外的原则、状态机、事实源边界或流程 Gate。

## 1. 定位

本文承载 07 授权的 commit body 必填条件和正文质量标准。

07 正文定义 commit message 契约；本文只维护 body 何时必填、应包含哪些语义和哪些正文不合格，不得定义 type/scope 枚举、提交样例、AI 提交流程、Hook 接入或 Web 页面契约。

## 2. Body 必填条件

以下提交必须包含非空 body：

1. 修改 specs、rules、Code、Web、测试、配置、AI 入口或能力资产；
2. 修改 `ldvh-base/` 工作对象事实源的状态、关键字段、归档、废弃、关闭或删除；
3. 一次提交涉及两个及以上文件，或同时影响两个及以上 scope；
4. 修改会影响 Human Gate、事实源边界、状态流转、字段契约、Web 呈现、校验逻辑、复制上下文或 AI 执行入口；
5. 有用户确认、设计取舍、残留风险、兼容性影响、验证证据或后续约束需要保留；
6. description 无法让不了解上下文的人判断本次提交的目的、边界和影响。

只有以下提交可以省略 body：

1. 单文件、低风险、语义完全由 description 表达清楚的 typo、格式、注释或微小文案修正；
2. 纯机械版本号、锁文件、生成物或等价维护变更，且不改变事实源语义、Web 行为或校验结果；
3. 回退提交的工具自动生成正文已经足够说明被回退提交，且没有额外人工取舍。

## 3. Body 语义要求

必须写 body 的提交，其正文应优先使用 Markdown 小标题或列表形成稳定清单，至少覆盖以下语义：

| 语义 | 内容 |
|---|---|
| 动机 | 为什么需要这次修改，解决了什么问题或收敛了什么偏差 |
| 关键变更 | 本次实际改变了哪些行为、契约、展示或事实源语义，不重复逐文件清单 |
| 影响边界 | 影响哪些对象、规范、Code/Web 能力、用户体验、AI 执行或后续维护 |
| 验证结论与风险 | 本次变更已经确认到什么程度，仍有什么未验证或残留风险 |

推荐小标题为 `动机`、`关键变更`、`影响边界`、`验证结论`、`风险与后续`。检查命令可以作为辅助证据出现，但不得替代验证结论。

## 4. 不合格 Body

以下 body 不合格：

1. 只写“更新”“优化”“完善”“调整样式”“按要求修改”等无法判断语义的空泛表述；
2. 只重复文件名、diff stat、命令输出或 changed files；
3. 只堆 `npm run web:check`、`python3 code/specs_validate.py ...`、`git diff --check` 等命令，而不说明验证结论、动机、关键变更和影响边界；
4. 把 Git 提交伪装成工作对象记录，用 `Human-Gate:`、`Verification:`、`Risk:` 等 LDVH 私有 trailer 替代 body 语义清单；
5. 为了满足格式而堆砌模板句，实际没有提供 Human 可审查的信息。

## 5. 辅助核对表

| 核对项 | 标准 |
|---|---|
| 必填判断 | 命中 §2 条件时存在非空 body |
| 语义完整 | body 覆盖动机、关键变更、影响边界、验证结论与风险 |
| 不堆命令 | 命令输出没有替代验证结论 |
| 不用私有 trailer | 未用 `Human-Gate:`、`Verification:`、`Risk:` 替代正文 |
| 不虚构验证 | 未实际验证时明确写出未验证原因 |

## 6. 待补齐事项

1. Code v2 commit validator 确认后，应检查 body 必填 error 与正文质量 warning 的边界；
2. Web v2 提交详情确认后，应检查 body 展示是否保留本文语义层；
3. 需要改变 body 必填条件时，必须回到 07 主文档 Human Gate。
