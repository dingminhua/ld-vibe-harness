# Code命令入口表

```yaml
v2_attachment:
  attachment_id: "04.Att.02"
  title: "Code命令入口表"
  status: "active"
  authority: "active_with_parent_spec"
  parent_spec: "specs/04-Code确定性执行规范.md"
  canonical_path: "specs/attachments/04.Att.02-Code命令入口表.md"
  purpose: "承载 04 授权的 Code 命令入口表"
  migration_sources:
    - "history/specs-v1/07-Code确定性执行实现规范.md"
  code_consumption:
    - "code_command_entries"
```

> 文件状态：本文是 active 授权附件；随父规范生效，不独立改变父规范正文授权之外的原则、状态机、事实源边界或流程 Gate。

## 1. 定位

本文承载 04 授权的 Code 命令入口表。

## 2. 入口表

| 命令 | 当前用途 | 读写属性 | v2 边界 |
|---|---|---|---|
| `python3 code/specs_validate.py all` | active specs 综合校验 | 只读诊断 | 当前默认读取 active `specs/` |
| `python3 code/specs_validate.py v2-check` | active specs 结构诊断和只读知识地图预览 | 只读诊断 | 当前默认读取 active `specs/`；不替代完整运行时知识地图能力 |
| `python3 code/specs_validate.py governed-projects` | 管辖项目配置检查 | 只读诊断 | 不修改 `LDVH-GOVERNED-PROJECTS.yaml` |
| `python3 code/commit_validate.py --check-message...` | commit message 预检 | 只读诊断 | 不创建 commit，不替代 Human Gate |
| `python3 code/hook_dispatch.py run git.commit-msg` | Hook 统一 dispatcher 入口 | 只读/阻断型诊断 | 不替代 Git commit records 或 CI |

## 3. 使用规则

删除、重命名或改变命令行为前，必须检查 Rules、Skills、Hooks、README、测试、Web 和行动编排成员是否引用该入口。

## 4. 待补齐事项

1. v2 Code 命令稳定后，应补充参数、默认范围、exit code 和示例；
2. 受控写入前检查命令尚未实现，不得在本文登记为 active 命令。
