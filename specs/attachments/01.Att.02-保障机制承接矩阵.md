# 保障机制承接矩阵（最小化版）

```yaml
ldvh_attachment:
  attachment_id: "01.Att.02"
  title: "保障机制承接矩阵"
  status: "active"
  canonical_path: "specs/attachments/01.Att.02-保障机制承接矩阵.md"
  parent_spec: "specs/01-保障与衔接.md"
  relation: "authorizes_attachment"
```

> 文件状态：active；只给出最小承接映射，不替代实现细节。

| 需求ID | 触发消费时机 | 行动指南承接 | Hook 承接 |
|---|---|---|---|
| AI-BEH-001 | `session_start` | 入口 read_plan 与 required_paths | AI lifecycle Hook 会话入口 |
| AI-BEH-002 | `acknowledge_read_plan` | `ldvh.acknowledge_read_plan` read_plan_consumed 回执；后续写入和提交门禁通过显式 `acknowledged_paths` 或同 session runtime cache 消费 | LDVH 内部 runtime receipt / validator bridge；不要求任何目标环境原生 Hook 支持 |
| AI-BEH-003 | `pre_tool_use` | 写前阻断检查 | 工具调用封装层 |
| AI-BEH-004 | `git_commit_msg` | 提交前校验 | Git 提交校验入口 |
| AI-BEH-005 | `human_facing_output` | 对 Human 的输出必须带价值门判断 | 主控输出前置检查 |
| AI-BEH-006 | `external_output_intake` | 过程输出和证据分类与采纳边界 | 外部输出接收层 |
| AI-BEH-007 | `diagnostic_disposition` | warning/blocker/follow-up/unverifiable 分派 | 诊断流转层 |
| AI-BEH-008 | `completion_claim` | 完成声明闭环检查 | 完成前置校验层 |
