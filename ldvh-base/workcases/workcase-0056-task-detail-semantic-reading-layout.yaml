id: workcase-0056
type: workcase
title: Task 详情语义阅读结构重构
goal: |
  Task 详情语义阅读结构重构
status: closed
created: '2026-06-07T00:39:37.004998'
updated: '2026-06-07T00:51:45.709239'
workarea: workarea-0006
priority: P2
description: 由历史 Task task-0056 迁移生成的任务计划。计划目标以关联 Task 的 description 和 acceptance
  为准，避免复制多个权威事实。
success_criteria: '- [x] 关联任务 task-0056 满足其 acceptance、verification 和 closure_evidence。'
source: |-
  Legacy TaskPlan migration from taskplan-0056; source path: ldvh-base/taskplans/taskplan-0056-task-detail-semantic-reading-layout.yaml
  Human Gate confirmed by dmh2002: User said 继续吧 after TaskPlan-to-WorkCase migration preview on 2026-06-18; write WorkCase facts and WorkArea backlinks only, preserve legacy TaskPlan/Task facts.
orchestration:
  mode: single
  execution_items:
  - id: task-0056
    title: Task 详情语义阅读结构重构
    role: legacy-task
    mode: single
    input_refs:
    - task-0056
    expected_output: |
      - [x] Task 详情页不再仅按 YAML 字段平铺，而是形成任务主线阅读结构
      - [x] description/source/source_intent 聚合为任务目标与来源区域
      - [x] acceptance 使用清晰的验收区域，展示完成进度和检查项
      - [x] verification 与 closure_evidence 形成验证/关闭证据区域，Markdown 标题和列表层级清晰
      - [x] deliverables/related_docs/affected_docs 形成产出与文档区域
      - [x] 原 YAML 源码折叠入口保留
      - [x] 运行 TypeScript 类型检查通过
    status: done
    result_summary: |-
      verification: 打开 /objects/task/task-0052 和 /objects/task/task-0056，检查 Task 页面是否能按目标、验收、验证、证据、产出形成清晰阅读结构；运行 npx tsc --noEmit。

      ## 验证结果

      Task 详情页按任务工作流主线组织页面，目标、验收、验证、证据、产出形成清晰阅读结构，npx tsc --noEmit 类型检查通过。

      ## 结论

      验证通过。

      closure_evidence: ## 完成证据

      1. TaskReadingLayout 组件已实现，按任务工作流主线组织页面：任务目标→验收标准→验证/关闭证据→产出与文档→其他字段
      2. TaskSection 组件提供语义色调分区（primary/checklist/evidence/docs/default）
      3. description+source+source_intent 聚合为任务目标区域
      4. acceptance 使用 ChecklistCard 展示验收进度
      5. verification+closure_evidence 使用 EvidenceBlock 渲染，双列布局
      6. deliverables/related_docs/affected_docs 形成产出与文档三列区域
      7. YAML 源码折叠入口保留在页面底部
      8. npx tsc --noEmit 类型检查通过
      9. 同步修复 task-0052 编号冲突（Hermes→task-0055）和 fact_cli.py update \n 转义 bug

      ## 验证结果

      TaskReadingLayout 组件已实现，Task 详情页按任务工作流主线组织页面，npx tsc --noEmit 类型检查通过，同步修复了编号冲突和转义 bug。

      ## 结论

      任务完成，人工确认关闭。
    evidence_refs: []
    blocking_reason: null
  review:
    controller_self_check: true
    specialist_review:
      required: false
      role: null
      expected_output: null
    human_closure_review: true
verification_evidence: |
  ## 验证计划

  历史证据格式规范化：检查原 验证证据 是否保留可复查的验证对象、结果或关闭判断；本次仅补齐四段式结构，不新增或改变历史事实语义。

  ## 验证命令

  历史记录未单独记录完整的验证命令；如原字段包含命令、脚本、检查入口或人工检查动作，保留在下方原始证据摘录中。

  ## 验证结果

  原始证据摘录：

  原文标题：验证结果

  历史任务计划迁移聚合证据：
  - task-0056: ## 完成证据

  ## 结论

  本次仅完成证据字段四段式格式规范化；对象状态、关闭判断、验证结果和历史事实语义以原始证据摘录为准，未额外伪造历史验证过程。
closure_evidence: |
  ## 验证计划

  历史证据格式规范化：检查原 关闭证据 是否保留可复查的验证对象、结果或关闭判断；本次仅补齐四段式结构，不新增或改变历史事实语义。

  ## 验证命令

  历史记录未单独记录完整的验证命令；如原字段包含命令、脚本、检查入口或人工检查动作，保留在下方原始证据摘录中。

  ## 验证结果

  原始证据摘录：

  原文标题：验证结果

  历史任务计划迁移聚合证据：
  - task-0056: ## 完成证据

  ## 结论

  本次仅完成证据字段四段式格式规范化；对象状态、关闭判断、验证结果和历史事实语义以原始证据摘录为准，未额外伪造历史验证过程。
review_requested_at: '2026-06-07T00:51:45.709245'
closed_at: '2026-06-07T00:51:45.709245'
related_docs: []
related_adrs: []
related_sparks: []
related_pitfalls: []
related_workcases: []
