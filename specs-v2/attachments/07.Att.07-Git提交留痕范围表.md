# Git提交留痕范围表

```yaml
v2_attachment:
  attachment_id: "07.Att.07"
  title: "Git提交留痕范围表"
  status: "draft"
  authority: "not_active_until_parent_and_human_approved"
  parent_spec: "specs-v2/07-事实源边界与Git追溯规范.md"
  canonical_path: "specs-v2/attachments/07.Att.07-Git提交留痕范围表.md"
  purpose: "承载 07 授权的 Git 提交留痕适用范围"
  migration_sources:
    - "specs/10-Git提交规范.md"
  code_consumption:
    - "git_commit_trace_scope"
    - "commit_preflight_inputs"
```

> 文件状态：本文当前位于 `specs-v2/`，尚未切换为 active；正式 Git 提交留痕规则仍以 active `specs/10-Git提交规范.md` 为准。本文是 `07-事实源边界与Git追溯规范.md` 授权附件，必须随父规范经过 Human 单篇确认后才可生效，不独立改变 commit message 契约、提交流程或 Hook 接入。

## 1. 定位

本文承载 07 授权的 Git 提交留痕适用范围。

07 正文定义 Git commit records 的事实源修改追溯性质；本文只说明哪些修改应通过符合 07 契约的 Git commit records 留痕，不得定义 commit message 字段、提交流程、Hook 接入、Code 输出 Schema 或 Web 页面契约。

## 2. 应留痕范围

以下修改应通过符合 07 契约的 Git commit records 留痕：

1. 修改 specs 正式规范、规则入口、能力资产、Code、Web、测试或配置；
2. 修改 `ldvh-base/` 下的工作对象事实源；
3. 修改 Study Markdown、docs 正文、docs/studies、docs/sources 或经授权沉淀的项目文档；
4. 修改会影响 Human Gate、状态流转、事实源边界、校验逻辑、Web 呈现或 AI 入口的文件；
5. 完成 WorkCase、ADR、Spark、Study、Pitfall 等对象的创建、关键字段改写、状态变化、归档或删除；
6. 回退、修正或补充之前已提交的事实源修改。

未进入 Git 的临时实验、构建产物、缓存或本地草稿不形成正式提交记录；一旦提交，就应尽量符合 07 的 commit message 契约。

## 3. 辅助核对表

| 核对项 | 标准 |
|---|---|
| 事实源修改 | 涉及 Git 文件事实源的稳定修改已通过 commit 留痕 |
| 高影响修改 | 影响 Human Gate、状态流转、事实源边界或 AI 入口的修改已写 body |
| 临时内容 | 未提交的临时实验、缓存或草稿未被解释为正式提交记录 |
| 不替代流程 | 本文未定义 AI 提交流程、拆提交流程或 Hook 接入 |

## 4. 待补齐事项

1. 行动编排迁移后，应检查提交准备流程是否正确引用本文范围；
2. Code v2 commit preflight 确认后，应检查 staged files 判断是否覆盖本文范围；
3. 多仓库管辖项目规则确认后，应补充跨仓库提交留痕边界。
