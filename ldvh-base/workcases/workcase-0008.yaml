title: 真 Windows 上 spark-0004 的 preflight 与 core-readonly 真实验收
change_log:
- signature:
    agent_id: Human
    host_environment: Human
  session_id: human-authorized-legacy-compat-0008
  at: '2026-07-30T13:35:03.915242+08:00'
  summary: Human授权兼容旧数据：为 change_log 机制落地前形成的遗留对象建立首条可信流水起点
status: open
priority: P2
goal: 在 Human 指定并授权的原生 Windows（win32/nt，NTFS 固定卷）runner 上，通过 tools/verify_native_windows.py 的 preflight 与 core-readonly 两阶段，为 spark-0004 「原生 Windows 支持范围与真实验收」形成可读、可复核的真实验收证据；明确记录未验证范围， 不把只读/策略阶段结果扩大为写能力通过。
scope: 覆盖 tools/verify_native_windows.py 的 preflight（8 个基础探针）与 core-readonly （追加 public_write_fail_closed 策略探针及 ruff 检查）两阶段在真 Windows 上的实际运行与证据收集； 覆盖 evidence 落盘（源码树外固定 NTFS 卷、干净 Git worktree）与 core-summary.json 等产物的读取。 排除 core-full 写矩阵（受 file_only 耐久性 Human Gate 阻塞，不在本案范围）、adapter-handoff （独立 Human Gate，本案只记录不执行），以及任何 Git commit/push/PR 与远端发布动作。 本 WorkCase 推进 spark-0004 的「真实支持范围/未支持范围/验收结论」中的只读与公开写策略部分； 写能力的耐久性等 Human 决定项保持为 spark-0004 的未决风险，不在本案关闭。
success_criterion_definitions:
- criterion_id: criterion-01
  statement: 在真 Windows（sys.platform=win32, os.name=nt, NTFS 固定卷）上，preflight 阶段的 8 个基础探针全部 passed：native_environment_and_console、junction_rejection、symlink_rejection、 lock_kill_recovery、file_only_atomic_boundaries、git_linked_worktree_temp_index、 drive_case_alias、unc_fail_closed。
- criterion_id: criterion-02
  statement: core-readonly 阶段 public_write_fail_closed 探针 passed：未经 Human 授权的公开 create/update 在真实 Windows 上不可用且没有任何副作用（fail-closed）。
- criterion_id: criterion-03
  statement: 证据由 tools/verify_native_windows.py 产出并落盘于源码树外固定 NTFS 卷；source_root 为干净 Git worktree，core-summary.json 等产物可被独立回读，且明确标注 contract=ldvh-native-windows-verification/1。
- criterion_id: criterion-04
  statement: 结果如实记录未验证范围：core-full 写耐久性 Human Gate 未接受、adapter-handoff 未执行， 不在本案中声明为通过或失败；只读/策略证据不被扩大为写能力验收。
phase: human_plan_confirming
plan_version: 2
work_items:
- item_id: item-01
  goal: 在真 Windows 上执行 preflight 验证并收集基础探针证据。
  expected_result: tools/verify_native_windows.py --phase preflight 退出码 0，core-summary.json 中 8 个基础探针状态均为 passed；除预定 evidence-dir 的证据落盘外，不提交、不推送、不修改受管源码树或远端。
  status: pending
  approach_summary: 使用隔离 Python 创建临时 venv 并 pip install --isolated [dev]；evidence-dir 置于源码树外固定 NTFS 卷 （E:/ldvh-evidence/preflight-001）；以 argv + 有界超时 + 最小子环境运行 test_native_windows.py； 结果由工具写回 core-summary.json、commands.jsonl 等。
- item_id: item-02
  goal: 在真 Windows 上执行 core-readonly 验证并收集公开写策略证据。
  expected_result: tools/verify_native_windows.py --phase core-readonly 退出码 0，core-summary.json 中追加的 public_write_fail_closed 探针为 passed，ruff check/format 通过；写策略证据与 preflight 合并形成 只读/策略阶段验收结论。
  status: pending
  depends_on:
  - item-01
  approach_summary: 使用 E:/ldvh-evidence/core-readonly-001 的独立 _work/venv 重新执行隔离 pip install --isolated .[dev]，不复用 preflight 的 evidence 目录或临时 venv；追加运行 test_native_windows_write_policy.py 与 ruff，并由工具写回该 phase 自己的 core-summary.json。
creation_reviews:
- reviewer: Codex independent reviewer (/root/workcase0018_v2_review)
  reviewed_at: '2026-07-30T13:28:40+08:00'
  subject_version: 2
  scope: 复核候选 plan_version=2 唯一变更：item-02 改为使用 E:/ldvh-evidence/core-readonly-001 的独立 _work/venv 重新隔离安装并运行 core-readonly，不复用 preflight 的目录或 venv；核对其与当前 Gate1 授权包的双目录边界一致性。未修改文件。
  conclusion: pass
execution_authorization:
  authorized_actions:
  - action_id: authorization-windows-preflight
    summary: 在 Human 指定的原生 Windows runner 执行 preflight 并形成证据。
    target_scope: 仅经确认的 win32/nt runner、其干净 source worktree、事前不存在的 E:/ldvh-evidence/preflight-001 及工具在该目录 _work/venv 创建的临时隔离环境。
    effect_scope: 允许 pip install --isolated .[dev] 到该临时 venv，以 argv、有界超时、最小子环境运行 tools/verify_native_windows.py --phase preflight，并写规定证据；不得修改受管源码或远端。
    risk_summary: 隔离安装、测试进程、锁恢复和证据写入会影响临时环境；外部副作用不保证全部可逆。
    rollback_summary: 失败时停止进程并删除临时 venv；证据按审计需要保留，后续删除须另有 Human 决定。
    rule_refs:
    - specs/21-WorkCase-工作项.md
    - tools/verify_native_windows.py
  - action_id: authorization-windows-core-readonly
    summary: 在同一指定 Windows runner 的独立目录执行 core-readonly。
    target_scope: 仅经确认的 win32/nt runner、其干净 source worktree、事前不存在的 E:/ldvh-evidence/core-readonly-001 及工具在该目录 _work/venv 创建的独立临时隔离环境。
    effect_scope: 允许独立 pip install --isolated .[dev]、运行 --phase core-readonly、public_write_fail_closed 探针及 ruff check/format check；公开 create/update 必须 fail-closed；禁止 core-full 与 adapter-handoff。
    risk_summary: 隔离安装、测试进程、临时文件和探针失败可能影响该临时目录，但不得扩大为写能力验收。
    rollback_summary: 失败时停止进程并删除本 phase 临时 venv；保留证据与诊断，不修改源码或远端。
    rule_refs:
    - specs/21-WorkCase-工作项.md
    - tools/verify_native_windows.py
  - action_id: authorization-evidence-readback
    summary: 回读并交叉核对 preflight 与 core-readonly 两套证据。
    target_scope: 仅读取两个固定 evidence 目录中的 core-summary.json、commands.jsonl 与对应 probe 产物。
    effect_scope: 允许形成当前 WorkCase 结果与验证摘要；未运行或证据不完整项必须记录 not_verified，不得写成通过或失败。
    risk_summary: 证据缺失、格式错误或误读会造成结论扩张。
    rollback_summary: 停止结论形成，保留原证据并如实报告不可确认范围。
    rule_refs:
    - specs/21-WorkCase-工作项.md
    - specs/07-Code 工程实践规范.md
  - action_id: authorization-bounded-delegation
    summary: 委派有界 subagent 做只读核对或独立 result review。
    target_scope: 仅 workcase-0008 当前结果和两套 Windows evidence。
    effect_scope: 允许本次运行内只读委派；独立 result reviewer 不得是实施者或 Controller，不替 Human 决定，也不新增环境动作。
    risk_summary: 职责不独立或超出证据的结论会污染结果。
    rollback_summary: 主控拒绝不可回指或不独立的输出，不写入结果事实。
    rule_refs:
    - specs/06-行动模板基础规范.md
    - specs/21-WorkCase-工作项.md
  action_ceiling: 仅 Human 指定的原生 Windows runner、每个 phase 开始前确认干净的 source worktree、事前不存在的 E:/ldvh-evidence/preflight-001 与 E:/ldvh-evidence/core-readonly-001、各自 _work/venv、两阶段测试进程及有界只读复核。
  prohibited_actions:
  - 运行 core-full 或 adapter-handoff
  - 修改受管源码树、Git commit、push、PR、发布或远端配置
  - 写入两个固定 evidence 目录以外的验收路径
  - 复用已存在的 evidence 目录或前一 phase 的临时 venv
  - 执行未知、不可逆或未列的高风险环境动作
  allowed_adjustments: 仅允许有界超时/进程重试、各 phase 临时 venv 重建、同一已授权命令参数的安全修正和有界重新委派；不得改变 phase、runner、volume、evidence path、probe 集合、写能力范围或风险上限；包内 PlanDelta 仍需 fresh review。
  verification_and_rollback: 每个 phase 前重新确认未来 Windows runner 的 source worktree clean 和对应 evidence 目录不存在；逐 phase 检查退出码、JSON contract 与 probes，再交叉回读两套证据并报告未验证范围。失败时停止、保留诊断、删除临时 venv，不删除审计证据，不修改源码或远端。
  out_of_bounds_handling: 不执行超界动作，不请求第三次扩权；取消受影响 item 的未授权部分，并以实际证据安全进入结果链与 Gate2。
  human_prerequisites:
  - Gate1 前由 Human 指定并确认真实 native Windows win32/nt runner 与干净 source worktree
  - 'Gate1 前由 Human 确认固定 NTFS E: 卷以及两个事前不存在的 evidence 目录'
  - Gate1 前由 Human 确认隔离 pip 安装所需权限与包源访问
resume_from: 等待 Human Gate1 确认当前 plan_version 2；获批后按两个独立 evidence-dir 自动推进。
waiting_on: Human 在 Gate1 确认 workcase-0008 plan_version 2、完整 execution_authorization 与已列 Windows 前置条件。
object_id: workcase-0008
fact_type_key: workcase
created_at: '2026-07-21T04:23:03+08:00'
updated_at: '2026-07-30T13:35:03.915242+08:00'
