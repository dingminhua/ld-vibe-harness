# 安装配置与验证行动模板

```yaml
ldvh_spec:
  spec_id: "30"
  spec_kind: "action_template_spec"
  title: "安装配置与验证行动模板"
  status: "active"
  authority: "active"
  canonical_path: "specs/30-安装配置与验证行动模板.md"
  parent_spec: "specs/06-行动模板基础规范.md"
  relation: "refines"
  positioning: "定义安装、配置、接入方式选择、验证、断点恢复、用户告知清单、Human-facing 编排和交还闭环的正式行动模板"
  scope: "安装、接入、初始化、确认目标工作区根目录下的管辖项目配置位置、登记管辖项目、组织安装向导 Code facade 输出、检查旧插件旧路径、写入完成后的断点引导、LDVH 影响验证和回滚信息"
  basis:
    - "specs/00-理念与构成.md"
    - "specs/01-保障与衔接.md"
    - "specs/02-AI行为规范.md"
    - "specs/03-事实源与Git溯源规范.md"
    - "specs/04-Specs基础规范.md"
    - "specs/06-行动模板基础规范.md"
  related_specs:
    - "specs/07-Code确定性执行规范.md"
    - "specs/09-测试与验证规范.md"
    - "specs/10-安装与配置规范.md"
  migration_sources:
    - "v2:specs/33-ldvh-install-action-LDVH安装行动编排.md"
    - "v2:specs/32-environment-entry-adaptation-环境入口落地与适配检查.md"
  code_consumption:
    - "ldvh_spec_metadata"
    - "ldvh_install_initialization_action_template"
    - "install_action_stage_contract"
    - "install_user_disclosure_checklist"
    - "environment_plugin_install_boundary"
    - "governed_project_config_location_gate"
    - "install_verification_handoff"
    - "breakpoint_recovery_protocol"
    - "lifecycle_verification_handoff"
    - "stop_conditions"
  role_sections:
    value_judgment: "1. 价值判断"
    authority_basis: "2. 权威依据"
    jurisdiction_boundary: "3. 归口边界"
    scope: "4. 适用范围"
    rule_body:
      - "5. 模板定位与来源"
      - "6. 用户告知清单"
      - "7. 安装行动阶段与 Human Gate"
      - "8. Context、Scenario、Gate 与交还"
    assurance_measures: "9. 保障措施"
    verification_method: "10. 验证方法"
    human_gate: "11. Human Gate"
    stop_conditions: "12. Stop Conditions"
    next_queries: "13. 待补齐事项"
```

> 文件状态：active；本文是 V3 独立正式行动模板，承接 LDVH 安装、接入、初始化、首次管辖项目配置、写入完成后的断点引导和 LDVH 影响验证闭环。本文不越过 Human Gate 直接安装或升级环境插件或管辖项目 Git Hook，不直接写入用户环境 Hook 系统文件，不声明任何环境入口 integrated，不恢复 Rules / Skill 顶层机制。

## 1. 价值判断

本文存在的价值，是把安装、配置、接入方式选择和验证这类高影响行动沉淀成可复用模板，让 AI 在执行前能稳定说明写入位置、影响范围、验证方式和回滚路径，减少遗漏告知、误装入口、误判 integrated 或绕过 Human Gate 的风险。

该模板主要服务 V1、V2、V3、V4、V5、V6、V7 和 V8：它让 AI 快速定位安装、配置与验证入口，理解写入对象和环境边界，正确判断受管项目、环境入口和 integrated 状态，按固定 Gate 稳定执行，在高影响写入、配置位置确认或风险接受前等待 Human 确认，执行指定验证，按格式回写配置和残留风险，并用范式交互向 Human 呈现待确认事项。安装、配置或接入方式变化会改变环境入口、配置位置或受管项目边界；如果没有模板，AI 容易把“方案存在”“样例包存在”“旧插件存在”误写成“已部署”或“已接管”，也容易把系统检查结果当成用户决策、把后置缺口放进主选项，或在最终确认前边问边写。

## 2. 权威依据

本文承接 `specs/06-行动模板基础规范.md` 的 Context、Scenario、Gate、执行、验证、回写和交还结构。行动模板不是规则源，也不是授权器；本文的职责是把来源规则、Code 输出、Human Gate、验证证据和交还内容组织为可复用行动路径。

本文承接 `specs/01-保障与衔接.md` 的环境入口、Hook 分类、插件 / 扩展包安装口径和 integrated 声明边界；承接 `specs/10-安装与配置规范.md` 的配置事实源和 target-first 解析边界；承接 `specs/07-Code确定性执行规范.md` 和 `specs/09-测试与验证规范.md` 的实现域与验证边界。

安装向导 CLI 或等价 Code facade 已出现时，本文仍保持正式行动模板角色：10 归口安装与配置的机器契约，Code facade 归口确定性 `check -> plan -> apply -> verify` 执行壳，本文归口 Human-facing 五阶段编排、告知清单、最终确认、断点恢复、失败信息包、验证交还和缺口分流。CLI 不替代本文；本文也不得复制 CLI、10、01 或 07 的机器规则形成第二规则源。

本文受 V3 01 的环境入口边界约束，也受 V3 01、06、10 的共同约束：01 管环境入口、06 管行动模板结构、10 管管辖项目配置契约。本文不声明 integrated；只有真实自动触发、失败可阻断、安装与接入证据可复核、回滚方式明确且验证证据齐备时，才可由对应环境入口规则判断是否 integrated。

若本文与 01、06、10 或 Human Gate 冲突，应回到上位规范和 Human Gate，不得由本模板自行覆盖。

## 3. 归口边界

本文归口定义安装、配置与验证行动的步骤组织、用户告知清单、Gate、验证、断点恢复和交还。断点后验证的统一标准，是 runtime 循环在真实工作流里产出可复核 receipt；环境是否支持 Hook 只影响触发方式，不影响验证标准。

本文不归口定义环境入口判定分类、Hook 安装实现、插件 manifest schema、管辖项目配置字段、Code 命令细节、Web 页面、测试框架或真实用户环境配置。环境入口边界归 01，状态归口原则归 04，Code 实现实践归 07 和 `code/docs/`，管辖项目配置契约归 10，验证声明归 09。

本文可以组织“先告知、再确认、再执行、再验证、再断点恢复、再交还”的行动顺序，也可以把安装向导 Code facade 的输出翻译为 Human 可理解的安装阶段、净变化、风险和下一步；但不得把行动步骤写成环境入口安装授权、配置事实源契约、Code 实现细节、机器规则源或 integrated 状态证明。

本文不得登记 Git 提交、WorkCase 推进或其它后续行动模板候选的迁入判断；跨模板候选筛选统一回到 `specs/06-行动模板基础规范.md`。

## 4. 适用范围

本文适用于：

1. 用户要求安装 LDVH、接入 LDVH 或初始化 LDVH；
2. 用户要求配置、生成或迁移 `LDVH-GOVERNED-PROJECTS.yaml`；
3. 用户要求把项目登记为管辖项目，或检查某项目是否已被 LDVH 管辖；
4. 用户要求检查安装是否生效、修复旧插件、旧路径或 stale V2 path；
5. 后续真实安装、升级、禁用、卸载环境插件或 Git hook shim 前的 Human Gate 准备。

用户只是询问概念、职责或边界时，只回答 01、06、10 和本文边界，不写入配置、不安装插件、不修改 Hook。

## 5. 模板定位与来源

本文是 06 之后的第一个独立正式行动模板，编号为 `30`。它不是 06 父规范的正文示范，也不是 Code 实现域文档；它是可被 AI 直接引用的行动模板成员。

因此，30 的成败不取决于是否亲自执行底层安装逻辑，而取决于是否能按 06 的行动模板结构稳定组织安装行动：进入场景识别、Context 收集、Gate 与 Human Gate、五阶段交互、验证入口、回写边界、交还和断点恢复。底层确定性执行由安装向导 Code facade 承接；30 必须消费其输出并交还主控 AI 与 Human 判断。

本文承接安装、初始化、首次管辖项目配置和验收交还能力。历史行动模板、环境适配材料或外部包装只能作为来源线索，不能让本文复制其全文、恢复 Rules / Skill 顶层机制或越过 Human Gate。

当前最小闭环包括：安装入口发现、目标工作区确认、管辖项目登记、用户告知清单、安装方案预览、最终确认、写入后验证、断点引导、失败信息包和交还。真实环境插件写入、管辖项目 Git Hook 写入、完整部署自动化、用户级配置解析、项目内配置迁移和批量项目接入仍需按来源规则进入 Human Gate 与最终确认。

## 6. 用户告知清单

安装、部署、初始化、配置或卸载前，本模板必须先形成用户告知清单，并把清单交给 Human 确认。

告知清单至少包括：

1. 要安装、生成、修改、覆盖、禁用、卸载或删除的对象；
2. 写入位置及其级别：用户级、工作区级、项目级、Git 本地目录、插件 cache、仓库内样例或只读检查；
3. 受影响项目、不受影响项目、跨项目范围和混合非管辖 target 风险；
4. 会接入哪些 Git hook shim、环境 Hook、plugin lifecycle event、manual entrypoint 或 repo instruction；
5. 是否需要创建或检查管辖项目根下的 `ldvh-base/` 事实源目录，以及每个子目录的用途；
6. 哪些事件可能阻断，哪些只是 diagnostic，哪些仍是 `manual_ready` 或 `deferred`；
7. 旧插件、旧路径、旧配置、stale V2 path、历史 trust 或旧 Hook 的处理方式；
8. 验证入口、预期结论、不可验证范围和失败处理；
9. 回滚或卸载入口，以及卸载后如何确认环境不再自动触发 LDVH；
10. 写入完成后需要在重启 App、新窗口或新会话中继续确认的 lifecycle 验证内容；
11. 残留风险、source_refs 和下一步 Human Gate。

管辖项目事实源目录告知只说明是否需要按 10 §8 检查或创建 `ldvh-base/` 及其事实源子目录、创建是否进入 Human Gate、目录存在不代表事实实例或环境入口 integrated。目录用途、不得替代边界和研究材料边界回指 `specs/10-安装与配置规范.md` §8 及 20-24 成员规范，本文不复制目录用途表。

缺少用户告知清单时，不得安装、部署、初始化、写入配置、声明接入或要求 Human 验收。告知清单不是授权本身；授权只能来自 Human Gate。

## 7. 安装行动阶段与 Human Gate

### 7.1 行动阶段与交互边界

安装、接入、初始化或配置管辖项目时，AI 必须使用安装行动阶段组织 Human-facing 交互。安装行动不是测试模式；它是正常安装流程。是否执行只由最终确认决定。

安装行动固定为五个阶段：

| 阶段 | 名称 | 说明 | 写入边界 |
|---|---|---|---|
| 1/5 | 路径确认 | 展示完整流程，确认 LDVH 本体路径和目标工作区路径；从当前 AI 运行上下文识别目标环境并锁定，后续不再询问 | 不写入 |
| 2/5 | 安装前检查 | 只读检查当前项目、目标工作区配置、事实源目录、环境入口、旧插件和旧路径证据 | 不写入 |
| 3/5 | 安装选项 | 只收集真正需要 Human 决定的安装选项；配置位置不作为选项；目标环境已锁定时不再次询问 | 不写入 |
| 4/5 | 安装方案预览 | 汇总用户告知清单、检查结果、Human 选择、将执行动作、后置项、验证方式和回滚方式 | 不写入 |
| 5/5 | 最终确认 | Human 选择执行或不执行；只有选择执行后才可写入；写入完成后交还断点恢复入口和 lifecycle 验证清单 | 确认执行后才可写入 |

每次向 Human 展示安装行动时，交互输出应表达当前阶段、已确认决策 / 结果和未发生事项。本文不得新增安装运行时状态闭集，不得把交互展示项写成环境入口判定事实、事实对象状态或安装事实记录。具体图标、表格列、控件、展示样式和交还摘要模板归实现域文档或运行时输出。

安装行动应按场景复杂度精简呈现，不得对所有操作堆砌完整表格集。内部摘要检查可用于组织回答，但默认不在用户主界面逐字展示；raw diagnostic 只进入技术明细附录或命令输出。需要 Human 作出选择时，每次只问一个问题，不把检查事实、后置项或返回修改混入主选项；最终确认只允许“执行方案”和“不执行，停止安装”两个主选项。

安装行动必须区分方案确认和执行确认。4/5 安装方案预览只能询问“是否进入最终确认”，该选择不是执行授权，不得用“执行方案”或其它会让 Human 误以为已经授权写入的措辞。5/5 最终确认必须直接询问“执行方案”或“不执行，停止安装”，不得再次要求 Human 确认只读检查、重复展示安装前检查事实，或把断点后验证包装成新的安装授权选项。Human 在 5/5 选择 `1 执行方案` 后，AI 必须立即执行已确认写入并进入执行后验证；写入完成后必须交还恢复入口语、lifecycle 验证清单和失败信息包，不得继续解释流程或再次索要同一授权。

安装前检查必须区分检查结论和是否阻断；注意、需安装、需升级或阻断类结论的来源和具体展示形态回指 01、10、09、Code facade 和实现域文档。图标、表格列和示例形态不得替代结论文字、Human Gate 或验证结论。

### 7.2 用户告知、用户选择与检查事实

用户告知清单必须作为 4/5 安装方案预览的必含内容交给 Human 确认；若 2/5 安装前检查发现 blocking，AI 也必须在暂停交还时同步列出已能确认的告知清单项、缺失项和解除条件。告知清单不得替代 5/5 最终确认。

安装前检查只能输出系统已检查事实，不得把事实伪装成 Human 选项。目录、配置、环境入口、旧插件旧路径、外部 Hook、Git worktree 和 Git Hook 检查结果应按 01、10、09 和 Code facade 输出归类为检查事实、需授权动作、后置项或阻断项；需要安装、升级或阻断时进入方案预览或暂停交还，不在本文重写具体判定规则。

### 7.3 路径发现、配置位置与管辖项目清单

目标工作区配置已存在且方案倾向保留时，安装前检查必须展示当前配置摘要和项目候选，供 Human 决定是否保留或按编号调整。配置字段、配置正确性、路径解析、target-first 结论和层级冲突判定回指 10、10 附件和 Code facade；本文只规定需要让 Human 能比较当前配置与拟写入配置，不维护配置校验规则摘要。

安装入口发现必须先从 LDVH 本体读取本文。目标工作区没有 LDVH 文件时，AI 可以执行有限、只读、有证据的 bootstrap discovery：依次检查当前对话或进程已知 LDVH root、`LDVH_ROOT` / `LDVH_HOME` 环境变量、已安装插件 / Hook / wrapper 中记录的 LDVH root、当前 cwd 向上的 LDVH 本体证据、常见工作区父目录的有限深度搜索；找到候选后必须展示候选路径和证据，多个候选必须让 Human 选择，找不到时必须要求 Human 提供 LDVH 本体路径，不得要求目标工作区已经存在 `specs/30`。

安装选项只包含当前可执行且需要 Human 取舍的决策。管辖项目配置位置、目标工作区默认值、项目内 / 用户级 / LDVH 本体目录边界和配置层级冲突回指 10；本文只规定路径确认必须让 Human 看清 LDVH 本体路径、目标工作区根目录和配置文件完整路径的含义、建议、证据和可复制路径，具体表格列和展示形态归实现域。

### 7.4 安装方案预览与最终确认

安装方案预览必须合并所有 Human Gate。主界面只展示普通用户作出下一步判断所需的信息：会改变什么、不会改变什么、执行后还需要验证什么、是否存在阻断；技术明细可以附后或折叠展示，但不得丢失。写入对象、写入位置、将执行动作、不会执行的事项、后置项、验证入口、失败处理和回滚方式都必须在此步可见或可展开，具体检查项和命令由 01、10、09、Code facade 和实现域文档承接。

完整安装方案必须覆盖目标环境入口承接和每个已选择管辖项目的 Git Hook 承接。非 Git worktree、配置层级冲突、目标不明确、非管辖或混合 target、Human 未确认等阻断按来源规则暂停，不得继续进入最终执行或声明完成。真实 lifecycle、外部环境触发或卸载后自动触发状态无法在当前回合验证时，必须写入不可验证范围，不得混入“验证通过”；只读安装检测是否足以支持写入完成结论回指 01、09 和 Code facade。最终确认前，AI 只能只读检查、展示结果和收集选择，不得写入配置、创建目录、安装插件、修改 Hook、修改用户环境或声明 integrated。

### 7.5 环境入口承接与统一验证边界

30 不按“支持 Hook / 不支持 Hook”拆成两套写入完成流程。环境是否支持 Hook 只影响写入或承接内容：支持 Hook 的目标环境可以安排目标环境插件 / 工具入口插件、扩展包或 package；暂不支持 Hook 的目标环境可以安排 repo instruction、manual entrypoint、thin reference 或 external_adapter_candidate 类型候选。两类承接都必须回到同一套 Runtime Protocol、同一套消费时机和同一套验证标准，不得形成两套规则体系。

AI 必须读取 01、`01.Att.03`、`01.Att.04` 和环境审计结果，并把 raw diagnostic 翻译为可行动语言。环境审计字段、配置检查字段、Hook status 字段和 source_refs 只允许放在技术明细、命令输出或诊断附录中；主界面应表达为用户可理解的触发方式、待验证范围、阻断原因和下一步。

目标环境确认暂不支持自动 Hook 时，30 仍按统一安装流程组织写入、验证和交还；不得把它写成降级、失败、不能安装或独立分支。安装方案预览必须回指 Runtime Protocol 生成薄引用或 manual entrypoint 承接内容，给出可复制内容和写入位置。薄引用只负责引导 AI 读取 Runtime Protocol 并自觉调用运行时入口；具体入口、命令和文件名由 Runtime Protocol 与 Code 实现域承接。

统一验证标准不是“插件页面可见”或“薄引用文件存在”，而是 runtime 循环在真实工作流里产出可复核证据。无论触发来源是环境 lifecycle event、Git hook shim、repo instruction 还是手动入口，验证都必须回指 Runtime Protocol、01 和 09 的证据要求，并能结合 Git `commit-msg` 正反例、写入前检查或完成声明检查说明 LDVH 已在当前工作流中被消费。文件存在、插件缓存、历史 trust、薄引用文本或聊天印象都不得单独写成 integrated 或完整生效证明。

环境 Hook 或插件提示必须按当前目标环境命名。AI 能从当前运行上下文识别当前 AI 运行环境名称时，必须优先使用该环境名称，不得沿用示例环境名称；只有当前运行环境、环境审计或 Human 明确目标环境为 Codex 时，才可在用户主界面写 `Codex/工具入口插件`。目标环境不是当前运行环境，或换成 Trae、IDE、Agent runner、CI、repo instruction、未知环境时，必须改写为对应的目标环境名称，或使用环境无关的 `目标环境插件 / 工具入口插件`。

AI 提示用户安装、升级、授权或验收环境插件时，必须说明目标环境、用户需要执行的动作、断点后验证入口、失败后应返回给 AI 的信息，以及后续诊断来源。具体插件页面、命令、输出字段和提示模板归 01、Code 实现域文档或运行时输出承接。未真实写入插件包、未进入插件页面或未获得授权证据前，不得写成“插件已安装”，只能写当前证据和待验证范围。

### 7.6 写入完成后的断点引导与 lifecycle 验证（LDVH 影响验证）

安装验证是 30 的固有组成，不是可选升级，也不交给独立验收模板。30 的验证分为两类：当前回合可完成的安装检测，以及写入完成后因重启 App、新窗口或新会话产生的断点后 LDVH 影响验证。断点是物理事实，不是流程缺陷；AI 必须把断点设计成可识别、可恢复、可复跑。

当前回合可完成的安装检测至少覆盖 10、01、09 和 Code facade 要求的配置、事实源目录、Git Hook、环境入口、运行时直测、旧路径诊断和不可验证范围说明。具体检查项、命令和输出字段由来源规则、Runtime Protocol 和实现域文档承接。安装检测通过后仍不得把文件存在、插件可见、薄引用存在或直测通过写成 integrated。

LDVH 影响验证必须回答“LDVH 会在什么时候影响当前工作流、会得到什么结果、不会产生什么副作用”。它应按 `plugin_hook`、`thin_reference`、`manual_entrypoint` 等接入方式组织，而不是只问 Hook 是否可用。支持 Hook 时可验证 lifecycle event、写入前阻断和 Stop 提醒；薄引用或 manual entrypoint 时应验证 Runtime Protocol 可读、manual runtime adapter 可运行和 receipt 可复核。缺少自动 Hook 只表示该接入方式未 integrated，不得把薄引用验收写成 Hook 验收失败。

LDVH 影响验证默认只读，不得写 `spark-0046`、`ldvh-base/`、specs 或其它正式事实源。真实工作流需要覆盖写入前检查时，优先使用 payload fixture、dry-run 或只读探针；确需 harmless scratch target 时，必须显式说明 scratch 路径、写入目的、清理方式和删除确认，且删除失败时本次影响验证不得通过。

写入完成交还时，AI 必须输出断点恢复协议：

1. 恢复入口语：用户在任何新窗口或新会话粘贴后，AI 必须识别为继续 30 验证；
2. 可复制的只读可见性探针：回指 Runtime Protocol 或等价运行时入口，并说明正常输出的判定来源；
3. 授权 / trust 检查：目标环境需要插件、扩展包或 package 时，必须先提示用户确认插件已授权、已信任或无待处理授权，再进入重启、新窗口、新会话或真实触发检查；
4. Hook 触发证据检查：支持 Hook 的目标环境必须提示用户查看插件 Hook / lifecycle 触发记录、Hook 输出、插件事件日志或等价证据；若目标环境没有可见记录，必须把“未取得 Hook 触发证据”列入未完成触发项，不得用手动探针替代真实 Hook 触发证据；若出现 PreToolUse 阻断，尤其是 `RUNTIME_READ_PLAN_CONSUMED_EMPTY`、`PREFLIGHT_TARGET_UNKNOWN` 或等价前置检查错误，必须分类为“Hook 已触发，但 read_plan 消费证据链路未通过”，不得继续写成 lifecycle 未触发；
5. LDVH 影响验证清单：至少覆盖一次真实管辖项目 Git `commit-msg` 正反例、一次写入前检查或等价 harmless scratch target 检查、一次完成声明或交还前验证证据检查，并说明接入方式、触发时机、预期结果和是否写入；
6. 失败反馈包：目标环境名称和版本、入口结果、统一安装验证输出、环境审计输出、失败步骤编号、是否发生实际写入、scratch target 路径和文件状态、相关错误文本；
7. 当前不可验证范围和复跑方式：用户回来后 AI 先复跑只读统一安装验证入口重建上下文，不依赖聊天记忆、不写长期过程状态文件。

断点后用户回来时，AI 不得重新启动安装向导或要求用户重新确认已完成写入；必须先复跑只读统一安装验证入口重建当前证据，再按恢复入口语和验证清单逐项引导。需要 Human 观察的内容必须写成“你是否看到了上述正常表现”或“请贴出完整输出”，由 AI 根据本文判断通过、失败或暂停诊断；不得要求 Human 自行理解专业“通过 / 失败”。

LDVH 影响验证的主结论只能表达“本次验证通过”“本次验证失败”或“本次未验证”。该结论是当前验证输出，不写成长效事实状态；但只要后续复跑只读验证显示 LDVH root、管辖项目配置、Git Hook managed marker、环境入口指向和 runtime adapter 输出未变化，AI 可以消费当前证据继续行动。环境变化、插件升级、Hook 改动、配置变化、stale path 或验证冲突出现时，必须重新验证或写为未验证。

### 7.7 写入完成交还、断点恢复与失败信息包

执行完成后必须先交还“写入完成”结论，再交还断点恢复协议和技术明细。支持 Hook、薄引用、manual entrypoint 或外部 adapter 候选都不得产生不同的完成分支；差异只体现在触发来源、写入对象和不可验证范围。用户主结论必须明确写入是否完成、下一步恢复入口语和真实工作流验证要求，不得把“断点后验证待完成”写成停止后续验证的理由。

交还摘要至少覆盖：实际写入对象、明确未写入对象、01.Att.04 环境接入判定分类及证据、环境入口触发来源、每个管辖项目 Git `commit-msg` Hook 验证摘要、断点恢复入口语、LDVH 影响验证清单、下一步 Human Gate、不可验证范围、回滚或卸载入口和 source_refs。交还摘要不得形成新的安装状态闭集，也不得把 01.Att.04 分类写成长效事实状态。

交还必须拆成两个块：`Runtime 入口与 lifecycle 验证`（对用户解释为 `Runtime 入口与 LDVH 影响验证`）和 `提交消息检查`。前者说明环境插件、薄引用、manual entrypoint、runtime adapter receipt、写入前检查、完成声明检查和副作用边界；后者说明每个管辖项目 Git Hook、正例放行和反例阻断。不得用一个“Hook 已通过”覆盖两类入口。

用户下一步待办最多五项：打开插件页面 / 扩展页面 / 插件管理器并确认已授权 / trust 或无待处理授权；重启 App 或新开目标环境会话；查看插件 Hook / lifecycle 触发记录，若没有可见记录则返回“未取得 Hook 触发证据”；运行或请求 AI 运行只读可见性探针；按引导触发真实工作流验证（Git `commit-msg` 正反例、harmless scratch target 写入前检查或完成声明检查）；若失败，复制失败信息包。目标环境需要插件页面或授权 / trust 时，授权检查必须排在重启或新开会话之前；支持 Hook 的目标环境必须把 Hook 触发证据检查排在手动 runtime 探针之前，不另开独立验收模板。

失败或暂停时必须提供可复制失败信息包，不得只要求“截图给我”。失败信息包至少列出：目标环境名称和版本、入口结果、统一安装验证输出、环境审计输出、失败步骤编号、是否发生实际写入、scratch target 路径和文件状态、相关错误文本。若某项拿不到，写“未取得”并说明原因。

最终确认只展示两个主选项：`1 执行方案` 和 `2 不执行，停止安装`。最终确认前必须明确提示选择执行后才会开始写入；最终确认摘要只列出将写入对象和不写入对象，不得重复安装前检查表或把已完成的只读检查再次作为待确认事项。Human 选择不执行时，AI 必须停止在方案预览或最终确认状态，不得写入；Human 要求调整方案时，AI 必须回到对应前一步，但不得把返回修改作为第三个主选项；只有 Human 选择执行后，AI 才能按方案执行已确认的仓库内动作，并在写入后执行验证、回滚说明和残留风险交还。

安装执行完成后必须输出写入完成交还总结，不得只给零散验证结果。交还总结必须单列实际写入、未写入对象、当前回合安装检测摘要、断点后 lifecycle 验证入口、不可验证范围和撤销 / 恢复入口。缺少断点恢复入口语、可复制探针或失败信息包时，不得声明安装交还完整。

## 8. Context、Scenario、Gate 与交还

| 结构 | 最小要求 |
|---|---|
| Context | 读取用户目标、目标环境、LDVH 本体路径、目标工作区根目录、管辖项目候选、目标工作区配置、管辖项目事实源目录状态、环境入口审计结果、每个已选择管辖项目的 Git Hook 状态和 source_refs，并回指 01、06、10、07、09、Runtime Protocol、Git Hook 实践文档和安装向导实践文档。 |
| Scenario | 用户要求安装 LDVH、接入 LDVH、初始化 LDVH、配置管辖项目、把项目登记为管辖项目、检查安装是否生效或修复旧插件 / 旧路径时适用；用户只是询问概念或规则时，只回答 01/06/10 边界，不写入配置、不安装插件、不修改 Hook。 |
| Gate | 写入或迁移环境入口、插件 / 扩展包、管辖项目 Git Hook、管辖项目配置或事实源目录，确认 LDVH 本体路径和目标工作区根目录，处理配置层级冲突，声明环境入口 integrated，目标环境 Hook 能力不明，多项目或混合非管辖 target，缺少用户告知清单、安装方案预览或最终确认时，必须暂停或进入 Human Gate。 |
| 执行 | 先用有限、只读、有证据的 bootstrap discovery 找到 LDVH 本体并从本体读取本文；再按五阶段组织路径确认、安装前检查、安装选项、安装方案预览和最终确认。执行阶段只编排来源规则和 Code facade 输出：环境入口按 01，配置和事实源按 10，验证按 09，具体命令、输出字段和实现入口按 Code / hooks 实现域文档。 |
| 验证 | 验证覆盖当前回合安装检测和断点后 lifecycle 验证引导。本文只列验证入口、不可验证范围和 09.Att.01 字段名；具体验证命令、输出字段、正反样例和通过标准由 01、10、09、Runtime Protocol、Code facade 和实现域测试承接。 |
| 回写 | 安装和初始化检查输出默认是过程输出；配置写入只落在 Human 确认的管辖项目配置事实源并受 10 约束；事实源目录创建只建立入口，不创建事实实例；旧插件、旧路径、环境适配缺口或长期风险按 03/05/09 分流，不得把 runtime receipt、环境观察或聊天结论写成事实源。 |
| 交还 | 交还安装方式、路径和配置确认结果、管辖项目选择、环境接入判定来源、用户告知清单及 Human 确认结果、实际写入和未写入对象、验证摘要、回滚或卸载入口、残留风险、下一步 Human Gate、source_refs 和未完成分流；写入完成后必须交还断点恢复入口语、可复制只读可见性探针、真实工作流验证清单和失败信息包。阻断时交还阻断原因、缺少证据、缺少告知项和建议的下一步。 |

## 9. 保障措施

| 要求 | 机制 | 触发 | 证据 | 缺口处理 |
|---|---|---|---|---|
| 安装行动阶段要求 | 安装交互必须使用五个阶段表达当前阶段、已确认决策 / 结果和未发生事项；由本文、04、06、09 保障，最终确认前不得写入，不得新增安装运行时状态闭集 | 安装、接入、初始化或配置管辖项目时 | §7.1 五阶段交互记录、5/5 最终确认记录、§7.7 安装交还摘要 | 缺少阶段或最终确认时停止写入，回到对应阶段重做 |
| 告知清单要求 | 安装、部署、初始化、配置或卸载前必须形成用户告知清单并交给 Human 确认；由本文、01、06、09 保障 | 涉及写入、入口、配置或卸载时 | §6 告知清单、4/5 安装方案预览记录、Human 确认记录、source_refs | 缺少告知项时暂停，不进入最终确认 |
| 环境入口边界要求 | 插件、扩展包、Hook 或 manual entrypoint 只能作为入口承载，不得声明未验证 integrated；由本文、01 和环境入口审计保障 | 检查或改变环境入口时 | 环境入口审计输出、01.Att.04 分类记录、§7.5 安装检测摘要 | 缺少真实自动触发、payload 或失败处理证据时交还未 integrated，不声明自动接入 |
| Git Hook 安装验证要求 | 完整安装必须覆盖每个已选择管辖项目的 Git `commit-msg` Hook，并在安装后通过统一验证入口或等价入口验证 status、managed hook、正例放行和负例阻断；由本文、01、Code adapter、tests 保障 | 安装、升级或修复 LDVH 时 | 每个管辖项目 Git Hook status 记录、managed marker 记录、正反例验证结果 | 任一项目未通过时阻断写入完成交还，回到 Git Hook 修复 |
| lifecycle 验收闭环要求 | 安装检测通过但尚未 integrated 时，必须提供重启 App、新会话只读可见性探针、插件页面授权、真实触发或受控写入正反例的 Human 验收路径，并复跑统一验证入口形成本次验收总结；不得写入或消费长期过程状态 | 环境插件安装检测通过但后续逻辑需要 integrated 时 | 30 断点后 lifecycle 验证输出、统一验证摘要、不可写成长效状态说明 | 证据不足时写本次未验证，不写长期状态 |
| 环境插件验收提示要求 | 环境插件安装、升级或复核必须提示插件页面、重启 App、授权 / trust、新窗口或新会话测试、正常判断标准和失败反馈；由本文、01、Code audit、tests 保障 | 安装、升级、复核或交还目标环境插件时 | §7.7 用户下一步待办、正常判断标准、§7.7 失败信息包 | 缺少提示时补交交还摘要，不声明安装验收完整 |
| Git 项目准入要求 | 每个已选择管辖项目必须是有效 Git worktree；非 Git 目录必须阻断安装并说明管辖项目必须是 Git 仓库；由本文、10、Code 后端和 tests 保障 | 安装、初始化或接入管辖项目时 | Git worktree 检查记录、Git common-dir 线索、§7.4 阻断诊断 | 非 Git 目录停止安装，要求更换项目或先初始化 Git |
| 工作区配置 Gate 要求 | 生成或修改管辖项目配置前必须确认 LDVH 本体路径、目标工作区根目录和配置文件完整路径；配置位置固定为目标工作区根目录；由本文、10、Human Gate 保障 | 创建或迁移 `LDVH-GOVERNED-PROJECTS.yaml` 时 | §7.3 路径确认表、配置层级检查记录、5/5 Human 最终确认 | 路径或层级不清时停止写入，要求 Human 选择保留、迁移或删除冲突配置 |
| 事实源目录告知要求 | 首次启用管辖项目时必须按 10 说明 `ldvh-base/` 及事实源子目录用途；由本文、10、20-24 保障 | 创建或检查管辖项目事实源目录时 | §6 事实源目录告知、§7.2 检查结果、Human Gate 记录 | 用途未说明或目录创建未授权时不创建目录，不声明初始化完成 |
| 验证交还要求 | 完成声明前必须交还验证摘要、回滚或卸载入口和残留风险；由本文、09 保障 | 声称安装、初始化或配置完成时 | §7.7 验证摘要、回滚或卸载入口记录、残留风险、source_refs | 缺少验证或回滚信息时写为未完成交还，补齐后再声明完成 |
| 写入完成总结要求 | 安装执行完成后必须单列实际写入、未写入对象、Hook 接入后测试、不可验证范围和撤销 / 恢复入口；由本文、09 和 Code 审计入口保障 | 执行安装、初始化或配置写入后 | §7.7 写入完成总结、实际写入清单、Hook 测试摘要 | 缺少总结时不得只给零散测试结果，必须重做交还摘要 |

## 10. 验证方法

验证本文规则时应检查：

| 检查类别 | 检查内容 | 不满足时 |
|---|---|---|
| 行动阶段检查 | 是否使用五阶段安装行动、表达当前阶段与已确认决策 / 结果、覆盖安装方案预览和最终确认；是否把检查事实、用户决策和后置项分开；是否避免把交互展示项写成新的状态闭集 | 不得执行写入或要求 Human 验收 |
| 清单检查 | 是否在安装、部署、初始化、配置或卸载前列出用户告知清单 | 不得执行写入或要求 Human 验收 |
| Gate 检查 | 是否识别配置位置、环境入口、插件安装、integrated 声明和多项目 target 的 Human Gate | 暂停并交还 Human |
| 环境检查 | 是否按 01、09、Runtime Protocol 和 Code facade 区分安装检测、接入判定、用户侧冒烟检查和不可验证范围；是否避免把用户侧观察、文件存在或插件可见写成 integrated | 不得声明环境 integrated；安装检测未通过时不得声明写入完成 |
| Git Hook 检查 | 是否把每个已选择管辖项目 Git Hook 纳入安装 / 升级方案，并通过统一安装验证入口或等价入口验证当前管辖项目 Hook 结果 | 不得声明完整写入完成 |
| Git 项目检查 | 是否检查每个已选择管辖项目是有效 Git worktree；非 Git 目录是否阻断并说明管辖项目必须是 Git 仓库 | 不得继续执行安装 |
| 配置检查 | 是否按 10 解析和验证管辖项目配置，并确认配置固定在目标工作区根目录，且展示 LDVH 本体路径、目标工作区根目录和配置文件完整路径 | 不得声明管辖项目配置已生效 |
| 配置层级检查 | 是否检查目标工作区根目录到目标项目路径链上已有项目内配置 | 不得继续执行工作区安装 |
| 事实源目录检查 | 是否回读管辖项目 `ldvh-base/` 及五个子目录状态，并说明每个目录用途 | 不得声明管辖项目事实源初始化完成 |
| 回滚检查 | 是否说明回滚或卸载入口，并验证卸载后不再自动触发 LDVH | 不得声明部署闭环完整 |
| 交还总结检查 | 是否在写入完成后单列实际写入、未写入对象、统一安装验证入口或等价验证结果、Hook 接入后测试、不可验证范围和撤销 / 恢复入口 | 不得声明写入完成 |

## 11. Human Gate

以下情况必须进入 Human Gate：

1. 安装、升级、禁用、卸载或迁移 LDVH 插件 / 扩展包 / package；
2. 写入、覆盖、删除或迁移环境入口、Hook、plugin lifecycle event 或 repo instruction；
3. 创建、修改、迁移目标工作区根目录下的 `LDVH-GOVERNED-PROJECTS.yaml`，或确认 LDVH 本体路径 / 目标工作区根目录；
4. 创建、删除、迁移或重命名管辖项目 `ldvh-base/` 及事实源子目录；
5. 删除、迁移、合并、覆盖或保留同一路径链上的多个 `LDVH-GOVERNED-PROJECTS.yaml`；
6. 接受用户级配置目录后置缺口或其它当前 Code 不支持的配置候选；
7. 声称环境入口 integrated、接受阻断行为、接受残留风险、确认安装方案预览、选择执行或不执行、要求调整方案或要求 Human 验收；
8. 多项目、跨项目或混合非管辖 target 的安装、配置或验证范围不清。

## 12. Stop Conditions

出现以下情况时，AI 必须暂停：

1. 目标环境、写入对象、写入位置级别或影响范围不清；
2. 缺少安装行动阶段展示、用户告知清单、安装方案预览或最终确认，或这些内容未交给 Human 确认；
3. 插件、Hook、repo instruction、旧路径或历史 trust 被写成 integrated 证明；
4. LDVH 本体路径、目标工作区根目录或目标工作区配置文件完整路径不清；
5. 目标项目内已有 `LDVH-GOVERNED-PROJECTS.yaml` 尚未删除、迁移或明确保留；
6. 管辖项目 `ldvh-base/` 目录用途、写入影响或 Human Gate 状态未说明，或系统已检查事实被重新包装成 Human 选项；
7. Code、环境审计或工具输出正在替代 Human Gate、事实源或完成判断；
8. 验证、回滚或卸载路径不可复现，却要求声明安装部署完成。

## 13. 待补齐事项

1. 本模板用于真实安装、升级、禁用、卸载或迁移 LDVH 插件 / 扩展包 / package 前，必须重新生成用户告知清单并进入 Human Gate；
2. 用户级配置目录和项目内配置不是本模板支持的主位置；如已存在，只能作为 legacy / conflict 或后置缺口处理；
3. 本模板当前只承接最小安装、初始化、管辖项目配置和事实源目录初始化闭环；若要扩展到批量管辖项目配置、跨项目安装或完整卸载记录，必须先修订本文并重新进入 Human Gate；
4. 本模板声明安装、升级或卸载完成前，需要 Code 实现域提供可复现的验证和回滚证据；
