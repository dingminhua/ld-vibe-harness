# 保障消费时机表

```yaml
ldvh_attachment:
  attachment_id: "01.Att.01"
  title: "保障消费时机表"
  status: "candidate"
  canonical_path: "specs/attachments/01.Att.01-保障消费时机表.md"
  parent_spec: "specs/01-保障与衔接.md"
  relation: "authorizes_attachment"
```

> 文件状态：candidate；本文只承载保障需求的消费时机闭集。

| 消费时机 | 触发点 | 消费主体 | 用途 |
|---|---|---|---|
| `session_start` | 会话开始、恢复、上下文压缩后、子 Agent 启动 | Hook 或 Rules 入口、dispatcher、Code read_plan | 加载保障需求所需的 P0/P1 事实源 |
| `acknowledge_read_plan` | AI 消费 P0/P1 read_plan 后、首次实质行动前 | dispatcher、session receipt | 记录 AI 已消费入口读取计划 |
| `pre_tool_use` | Write、Edit、apply_patch、明确写入型命令前 | Hook 或 Rules 路径、dispatcher | 写入前检查入口读取计划消费证据、target 和阻断条件 |
| `git_commit_msg` | Git commit message 校验或提交前 | Git hook 或 Rules 路径、dispatcher、commit validator | 提交前检查入口读取计划消费证据、提交说明和 Human Gate / 验证缺口 |
| `human_facing_output` | AI 交还判断、计划、修改说明、完成声明或请求 Human 放行时 | 主控 AI、Web 或过程输出 | 向 Human 呈现价值门判断、依据、理由、风险和待确认事项 |
| `external_output_intake` | AI 接收 Code、Skill、Agent、review、测试、Web、Hook 或命令输出时 | 主控 AI、行动模板、过程输出 | 定性过程证据，防止外部输出替代主控判断 |
| `diagnostic_disposition` | 出现 warning、blocker、follow-up、unverifiable、失败、风险或能力缺口时 | 主控 AI、行动模板、事实模型、对应缺口归口 | 对诊断和缺口分级、阻断、分流或交还 Human |
| `completion_claim` | AI 声称完成、通过、关闭、可交付或验收前 | 主控 AI、测试治理、Human Gate、事实源回写 | 检查验证声明、残留风险、证据回指和未验证范围 |
