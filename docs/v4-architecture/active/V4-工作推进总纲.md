# V4 工作推进总纲

> 记录性质：本文是 V4 唯一当前工作推进控制面，统一回答“现在在哪里、下一步做什么、以何种方式做、完成凭什么”。它不是规则源，不授权产品行为，不替代当前有效 Specs。
> 核验时间：2026-07-15

Code、Web、环境、事实类型、行动模板和 dogfood 都在本文中维护当前状态、顺序、Gate、验证证据和未完成边界。`docs/code/README.md` 与 `docs/web/README.md` 只是目录和运行入口；已完成增量的详细规划由 Git 历史保留，不再与本文并行证明当前设计。提案、审计记录和浏览器观察只作为本文回指的决策或证据输入。

## 0. 唯一来源与维护边界

| 信息类型 | 当前唯一责任位置 | 其它文档不得承担 |
|---|---|---|
| V4 规则、领域语义与授权 | 当前有效 `specs/` 与授权附件 | 本文、Code、tests、提案或实现说明不得补造 |
| 当前阶段、顺序、活跃增量、协作方式、Gate、状态、证据、缺口和下一步 | 本文 | README、提案、审计记录、设计资料和源码注释不并行维护 |
| 决策理由、候选比较与独立复核 | `docs/v4-architecture/` 中明确身份和状态的提案/审计记录 | 不自动改变本文当前状态或取得规则效力 |
| 稳定实现架构、目录和运行方式 | 当前源码、`docs/code/README.md`、`docs/web/README.md` 及相应实现说明 | 不复制当前状态、顺序或验证通过数 |
| 已完成增量详细与历史方案 | Git 历史或明确标记为历史的 `archive/` | 不与本文并行证明当前设计 |

任何增量如果改变当前状态、顺序、实现规划或证据，必须在同一增量内更新本文。其它文档只更新自己承担的稳定信息或回指，不同步抄写状态段落。

## 1. 当前结论

V4 已建立规范模型、事实模型、Helper CLI 和首批行动模板的确定性基础。规范候选/内容、管辖范围、事实候选/读取/草案/创建/单对象更新，以及行动模板候选/内容读取共十个 Helper 公开操作已经实现。05 定义事实共同协议，20–24 唯一定义具体类型的合法初态、状态/阶段、转换、关系和证据条件；WorkCase `blocked` 创建初态、F1 `phase`/`work_item_counts` 及 05 §11.7 重复枚举冲突已经修复。30–34 五个当前行动模板分别承担 Git 提交、事实对象判定与受控创建、事实对象生命周期变更与承接处置、环境接入安装与验证、事实对象来源重建与批量迁移；统一 Helper 只返回机械候选、精确定义和完整来源，不判断语义适用、不执行模板也不扩张授权。09 已定义环境接入共同规则；首个 Codex `SessionStart startup|resume` 薄 adapter 已在本机当前用户的 Codex 0.144.2 中完成安装、信任、显式配置、真实成功/失败触发及停用/恢复验证。

2026-07-15 三份独立架构审计的共同方向判断已完成事实核对：根架构和确定性内核继续保留，但真实 AI 消费与跨会话接续仍落后于规范与测试闭合；普通发行交付已在后续增量中闭合当前 macOS/Python 3.12 范围。Web 共享创建事务和未挂载的 `web-direct-capture` Python 应用服务已完成，但生产 `POST /api/sparks` 与读取仍使用 V3。Human 随后调整当前目标：Web V4 bridge 未提交工作连同审核结论停放在独立本地分支，不合入 `dev-v4`、不推送；原生 Windows 验证同时暂停，两者列为发布前后续事项。当前主线改为完成全部 92 个 V3 事实对象的逐条 V4 迁移与验证，随后建立 bootstrap/resume 并完成真实跨会话 dogfood。迁移首阶段已经冻结 commit/tree、92 项内容指纹、Git blob、原状态和原始时间，并进一步以 decision-independent source characterization 机械覆盖 1,882 个顶层字段、797 个递归时间和 3,353 个递归旧引用；所有语义审核和 target 决定仍为零，`facts/**` 继续为 0。正式 successor 身份、时间与旧终态边界已由 Human 决定和 05 §7.4/34 建立；但逐对象语义决定、双向映射 ledger、无写 dry-run、可恢复执行与完整验证尚未完成，因此仍不得写生产实例。与该迁移无关的高级多对象事务、自动 `supersedes`、删除能力和跨 clone 唯一分配继续后移。每个阶段实施前继续 subagent PRE review，完成后形成独立 Git 提交。

## 2. 当前推进方式：主线、subagent 与独立并行开发

本节记录 Human 当前确认的 V4 工作方式，不是正式 Specs，也不把开发环境中的 subagent、新对话、模型或 worktree 定义为 LDVH 架构概念。是否创建新对话，先由任务能否形成真正独立的本地 Git 开发与提交决定，不能为了并行或使用不同模型而反向制造 worktree。

### 2.1 主线责任

主线单线承担：

1. 依据 00 作架构判断和范围取舍；
2. 起草、修改、复核并激活正式 Specs；
3. 定义或修改公共 Schema、Helper 共同契约和跨模块共享接口；
4. 确定并行实现的文件所有权、输入、输出、验收标准和合并顺序；
5. 审核独立提交、处理冲突、完成跨模块集成、完整验证和最终结论。

主线默认使用 `gpt-5.6-sol` 与 `high` 推理强度，不使用 `xhigh` 或 `pro`。正式来源、公共契约和最终结论不得因其它任务已经输出而自动成立。

### 2.2 subagent 使用范围

单线程推进中不需要独立 Git 提交的内部工作使用 subagent，包括：

1. 只读调查与历史证据盘点；
2. 辅助分析、方案比较和 Code 影响分析；
3. 术语审核、表达检查和独立复核；
4. 不需要独立提交、结果直接回到主线上下文的其它内部子任务。

这些工作不创建新对话、不人为建立 worktree，也不要求切换模型。subagent 只提供待主线核对的输入，不修改正式 Specs、公共 Schema、共享接口或最终结论；需要写文件或独立提交时，必须重新判断是否已经满足下一节的全部条件。

### 2.3 新对话、不同模型与 worktree 的准入条件

只有某项工作能够通过独立本地 Git worktree 真正并行开发，并同时满足以下条件，才创建新对话并设置不同模型：

1. 文件修改范围与主线及其它并行实现互斥；
2. 输入、输出和验收标准已经明确；
3. 不依赖主线尚未形成的架构、Specs、公共 Schema 或共享接口决定；
4. 能够独立实现、测试和形成提交，并由主线按明确顺序审核与合并。

一般独立实现可以使用 `gpt-5.6-terra` + `high`；低风险、机械性强且验收确定的独立实现可以使用 `gpt-5.6-luna` + `high`。模型不可用或当次工具不能指定模型时必须报告实际安排。新对话的完成声明只证明其分支结果，不能替代主线审核、合并、完整回归或能力完成声明。

任务不满足任一条件时保持主线单线推进，必要的只读或复核工作交给 subagent。worktree 是已经确认能够独立开发后的隔离手段，不是新对话、模型切换或表面并行的目的。

### 2.4 共同边界与本次修正

无论采用哪种方式，都不得降低 00、当前有效 Specs、Human Gate、Stop Conditions、术语审计和未覆盖范围如实声明的要求。架构不确定、来源冲突、需要 Human 选择或会修改共同维护位置时，受影响范围回到主线。

2026-07-13 Human 明确修正了此前对“多模型并行”的扩大理解：V3 规则读取经验审核、V4 事实类型候选盘点和 L3/L4 Code/tests 影响分析属于只读内部工作，本应使用 subagent，不应创建新对话和 worktree。三项任务已经完成，其结果可以保留为主线输入，但后续不得复用这种执行方式。V4 推进网页原计划能够独立修改 Web 文件、独立测试和提交，因此创建新对话与 worktree 的判断本身合理；Human 后续接受了仓库外临时 HTML 并停止该实现，不形成 V4 Web 功能完成声明。

## 3. V4 全局推进路线与并行边界

V4 不是把 V3 文件和实现复制到新目录，而是按照 00 重新建立规则、事实、行动、确定性能力和 Human 呈现之间的完整工作链路。V3 资产逐项作为历史问题、实践证据和实现参考接受保留、重写、合并、取消或延后判断；只有进入当前规则源、实现、验证和实际接入的范围，才能形成 V4 能力声明。

目标工作链路为：

```text
当前任务与工作对象
→ 解析管辖范围
→ 定位相关规则、事实、状态和能力
→ 展开足够的规则上下文
→ 判断适用要求、Human Gate 和 Stop Conditions
→ 使用适用行动结构和 Code 能力
→ 执行、验证、回写事实源
→ Web 向 Human 呈现状态、风险和证据
→ 后续 AI 从事实源恢复并接续
```

### 3.1 建设阶段

| 阶段 | 建设对象 | 当前状态 | 阶段完成边界 |
|---|---|---|---|
| 1 | 元架构与基础 Specs | 已完成当前基线 | 00–09 激活，基础责任、关系、术语和机器契约成立；不表示具体能力已经实现 |
| 2 | Helper 服务主干 | 已完成首段 | CLI、共同请求响应、能力发现、L0–L2 规范候选读取和管辖范围解析具有实现与验证 |
| 3 | 规则信息服务 | 已完成当前范围 | Helper 能读取足够的 L3/L4 当前规则内容；AI 依据当前来源承担自然语言语义判断；Code 只执行来源已定义的结构化契约 |
| 4 | 具体事实类型 | 已完成 | 统一字段与结构治理、Spark → WorkCase → ADR → Pitfall → Study 串行定义、逐类型 Gate 和五类型全局归并均已通过 |
| 5 | 事实服务能力 | 已完成 | Helper 能按实际管辖项目分层发现、召回、展开、机械校验、受控创建和单对象 CAS 更新已经准入的事实对象，AI 承担语义消费；其它回写另行准入 |
| 6 | 具体行动模板 | 30–34 与统一 Helper 读取机制已完成；34 依 Human 全量迁移目标和重复批次价值专项准入 | 以中央规则源维护内部行动能力，由统一 Helper 发现和读取；新增模板必须有真实重复价值证据，用户不逐模板安装或启用 Skill |
| 7 | Code 工程与开源交付 | 普通安装已闭合当前 macOS/Python 3.12 范围；原生 Windows 暂停并后移至发布前 Gate | LDVH 自身的模块、测试、配置、安装、普通部署、错误边界、开源协作检查和跨平台验证能够支持明确声明的 Helper 能力；不负责管辖项目的 CI 或工程质量管理 |
| 8 | 环境接入 | 09/33 已完成；本机 Codex 0.144.2 最小接入增量已真实闭合，其它环境未开始 | 目标环境的 Hook、插件或 adapter 真实调用同一 Helper 操作，并保留环境能力缺口和实际触发证据 |
| 9 | Web V4 适配 | 表现层冻结；bridge 工作已停放且不合入 `dev-v4`，发布前恢复；其它写 API 暂不建设 | 保持当前视觉、布局、文案、字段和交互，通过共享 Code 受控创建 V4 Spark，并从 V4 当前来源读取和投影页面所需信息 |
| 10 | 端到端 dogfood | 0 个当前事实实例，未验证；提升为近期主线 Gate | 在真实且适合记录的受管辖工作中，经 Human 选择建立事实/WorkCase，从任务定位、规则与事实消费、行动、验证、回写到跨会话接续形成实际证据；不得制造 mock 或无真实责任的事实来证明流程 |
| 11 | V3 覆盖与事实迁移收口 | 92 项 immutable baseline 与 decision-independent source characterization 已建立；语义审核/target 决定/生产实例均为 0 | 49 Spark、24 WorkCase、17 Study、2 Pitfall 全部逐条形成合法 V4 承接、验证和 Git 锚点，包括已关闭对象；其它 V3 资产和已知问题也均有可回指的保留、重建、取消或延后结论 |
| 12 | 首个 V4 可用版本验收 | 未开始 | 安装、公开调用、失败恢复、环境、Web、文档和真实项目测试支持明确且不过度扩大的发布范围 |

阶段 4 与阶段 5 已关闭。统一字段与结构治理、Spark、WorkCase、ADR、Pitfall、Study 完整类型 Gate 和五类型全局归并均已完成机械验证与两路独立复核；事实候选发现、精确读取、单对象受控创建和单对象 CAS 更新已形成来源契约、实现与范围匹配测试。V4 由 05 与 `05.Att.01` 统一登记全部正式字段与结构；任何新增前必须全局查重，已有 type 登记被第二类需要时先提升到 foundation，不得复制或自动降级，有限共享字段与结构必须有且只有一个 `promote` 来源。Human 的保留决定、字段登记或类型规范成立都不使 V3 字段、状态、目录、实例当前性或未实现回写自动取得 V4 效力。WorkCase 专项外部审计已经完成并逐项处置；处置同时发现并修正关闭前结果包与 open/blocked 状态条件冲突，七个 phase 均有有效快照回归，WorkCase Gate 保持关闭。两份全架构方向审计已逐项处置；Human 后续校正了建设顺序：内部建设问题直接在 Specs、Code 和 tests 中解决，不为了 dogfood 制造事实对象。其它事实回写能力仍须各自重新准入，不能从单对象更新能力外推。

### 3.2 当前工作的协作方式

| 工作线 | subagent 可以辅助 | 新对话与 worktree 的准入点 | 必须由主线完成 |
|---|---|---|---|
| L3/L4 规则读取 | 审核 V3 经验、Code 影响、术语和正式契约草案 | 01 激活、实现规划固定，且片段读取、领域适配和专项 tests 能形成互斥文件与独立提交后 | 正式 01、公共操作注册、共享 Helper 边界、集成和完整验证 |
| 具体事实类型 | 盘点历史字段、提供语义反例和独立复核 | 当前五类不进入并行开发；未来其它互斥实现仍须另行满足新对话与 worktree 条件 | 统一字段登记，以及 Spark → WorkCase → ADR → Pitfall → Study 的串行准入、正式来源、字段归一化、跨类型关系和最终处置 |
| 具体行动模板 | 盘点重复行动、比较净价值、审核 Gate/Stop | 具体模板已经由当前来源定义，且实现文件、测试和提交与其它工作互斥时 | 模板准入、正式来源、共同执行边界和跨能力取舍 |
| Code 与 tests | 分析模块责任、风险测试和文件所有权 | Specs 与共享接口已经固定，独立模块可以自行实现、测试和提交时 | Code 实现规划、公共 Schema、共享接口、操作注册、合并和全量回归 |
| Web | 调查现有表现、数据来源、响应式和可访问性 | 页面或组件功能具有稳定来源，修改范围与主线互斥，并能独立测试和提交时 | Web 共同数据边界、跨页面取舍、行为变更授权和最终验收 |
| 环境与部署 | 调查平台能力、安装方式和验证条件 | Helper 与部署契约已固定，某一环境 adapter 或安装增量能独立开发、真实验证和提交时 | 跨环境共同语义、公共安装契约、集成与完成声明 |
| V3 覆盖审核 | 持续核对历史资产、问题与遗漏 | 只有出现满足 §2.3 的独立重建实现时才创建新对话 | 处置结论、规则归口和每阶段最终覆盖收口 |

### 3.3 必须保持单线的工作

以下内容保持主线单线处理：00 与正式 Specs、Helper 共同请求响应 Schema、公开操作注册、跨模块共享接口、同一生产文件、分支冲突与集成、完整回归、真实环境完成声明和 Human Gate 交还。subagent 或独立实现发现来源冲突、架构不确定、需要新增正式术语或必须改变共同维护位置时，必须返回主线，不能在子任务或分支中自行决定。

### 3.4 已发生的协作批次

2026-07-13 曾从 `dev-v4` 启动以下任务。本表保留实际执行方式和修正结论，不把历史执行方式自动转为后续惯例：

| 工作 | 实际执行方式 | 状态与修正结论 | 任务回指 |
|---|---|---|---|
| L3/L4 公开操作契约收敛 | 主线；后续使用 `high` | 正式 Specs 与公共契约继续由主线单线处理 | 当前主任务 |
| V3 规则读取经验审核 | 新对话 + worktree；`terra` + `xhigh` | 只读输出已完成并保留；按修正后边界本应使用 subagent，后续不重复该方式 | `019f595b-35b5-7392-ba4b-81e9970e1cb3` |
| V4 事实类型候选盘点 | 新对话 + worktree；`terra` + `xhigh` | 只读输出已完成并保留；按修正后边界本应使用 subagent，后续不重复该方式 | `019f595b-35b7-74b1-98b5-453b595a3521` |
| L3/L4 Code 与 tests 影响分析 | 新对话 + worktree；`luna` + `xhigh` | 只读输出已完成并保留；按修正后边界本应使用 subagent，后续不重复该方式 | `019f595b-35b4-73e2-b270-9895a022b687` |
| V4 推进总览网页 | 新对话 + worktree；`terra` + `xhigh` | 原任务具备独立 Web 修改、测试和提交边界，创建方式合理；Human 后续接受仓库外临时 HTML 并停止实现 | `019f5958-990f-7422-956b-6264c5640863` |

上述输出和临时可视化仍只覆盖各自实际结果。正式 Specs、Code/Web 集成、验证证据和阶段状态必须由主线回读实际来源、提交与结果后另行更新。

## 4. 已激活规则源

当前 `specs/` 中实际发现的 24 个候选文件均声明为 `active`：

| 类型 | 数量 | 当前范围 |
| --- | ---: | --- |
| 根规范 | 1 | `specs/00-理念与构成.md` |
| 普通规范（active） | 19 | `specs/01-规范模型基础规范.md` 至 `specs/09-环境接入规范.md`、`specs/20-Spark-火花.md` 至 `specs/24-Study-研究报告.md`，以及 `specs/30-Git提交行动模板.md` 至 `specs/34-事实对象来源重建与批量迁移行动模板.md` |
| 授权附件 | 4 | `01.Att.01`、`02.Att.01`、`04.Att.01`、`05.Att.01` |

这里的 active 表示文档声明并通过当前实现已覆盖的机械检查，不替代 Human 对规范责任、承载净价值、附件正文授权和 `supersedes` 语义的审核。

## 5. 已实现并验证的 Code 范围

`code/ldvh/` 当前包括：

- Git worktree 内规范候选发现，含 ignore、Working Tree、符号链接和路径边界；
- Markdown/YAML 固定结构读取；
- 根规范、普通规范和授权附件的身份与结构检查；
- 规范依据、结构归属、附件授权、替代关系和影响范围检查；
- 事实类型声明、统一字段与结构登记、唯一定义、适用范围、准入记录、类型绑定和定义闭包检查；
- L0–L2 派生信息及来源位置回指；
- `Helper 公开操作` 声明候选读取与契约引用检查；
- 诊断对象和不完整范围表达。
- `ldvh` 进程入口、共同 JSON 请求与响应、退出码和基础诊断；
- `capabilities` 与 `call` 的基础分流，不由 Code 或 tests 补造领域公开操作；
- 从实际导入包路径定位共置 LDVH Working Tree 规则源，不从 `cwd`、隐藏环境变量、缓存或 `HEAD` 猜测。
- `read-specification-candidates` 的领域请求解析、精确 key 选择、L0–L2 累积结果、来源、范围、验证、资格缺口和局部问题隔离；
- 来源声明与显式 Code 实现映射的交集绑定；无来源实现不取得公开身份，实际实现异常形成有边界的服务错误。
- 管辖项目配置的候选发现、当前 YAML 内容读取、字段闭集校验和真实路径去重；
- 工作对象路径定位、Git worktree 根和 common-dir 的实时观察，以及 Git 技术失败与确定性非 worktree 的区分；
- 逐对象管辖结果、确定性聚合、来源回指，以及 `resolve-governance-scope` 的领域请求与 Helper 服务结果；
- 以登记 worktree 路径确认项目身份，并使同一 common-dir 下的 main/linked worktree 归属同一项目；分支、HEAD、remote 和目录名不作为身份。
- 从 `05.Att.01` 与 20–24 实时派生五类事实对象 Schema，读取四类 YAML 与 Study Markdown 当前 Working Tree 载体，并检查身份、状态、时间、引用、关系、正文骨架和仓库内来源。
- `read-fact-objects` 只接受精确事实引用；一次调用绑定一个管辖项目、一个实际 worktree 和一个 common-dir，不搜索、不迁移、不写回，并逐对象保留 invalid、not_found 与 unavailable。
- `find-fact-object-candidates` 直接扫描五类型当前权威对象并形成 F0；F1 固定返回全部 active ADR 与 open/blocked WorkCase，F2 使用类型化权威字段和确定性过滤；分页 cursor 绑定查询与对象集指纹，invalid/unavailable 和 partial coverage 可观察。
- `read-fact-objects` 为 mechanically valid 对象返回完整载体 SHA-256；`update-fact-object` 只接受精确引用、该指纹和排除托管字段后的完整目标，在类型锁内复核当前对象、转换、关系与来源，原子替换后再按同一项目边界回读。

当前工作树运行结果：无规范仓库或公开操作声明结构诊断、无不完整来源，发现的 10 项公开操作均已接入实现。新增 09 后，`read-specification-candidates` 可精确返回 `environment-integration` 的 L0–L2 身份、直接依据和结构父关系；`read-specification-content` 可按精确标题返回其 L3 当前原文。事实候选、读取、草案、创建和单对象更新，以及行动模板候选/内容读取继续使用同一当前规则源和 Helper envelope。结果继续报告尚未由 Code 自动证明的规则源资格条件和语义缺口，不因实现存在而冒充全部资格已获证明。

2026-07-13 已完成一次早期规则信息消费 dogfood 探针：AI 先调用 `read-specification-candidates` 精确定位 `ldvh-root`，再调用 `read-specification-content` 请求 00 的“以规范模型、事实模型、行动模板、Code 和 Web 为构成要素”章节。两次调用均返回 `ok`，来源回指和未机械证明的资格缺口得到保留；L3 请求按 01 契约展开为 265 行的 L4 完整来源。该证据证明当前规则定位与安全读取链路可以被 AI 实际消费，同时确认当前尚无能够缩小返回范围的通用 L3 机械判据。它只覆盖规则信息读取，不替代阶段 10 对事实、行动、验证、回写和跨会话接续的完整端到端 dogfood。

2026-07-14 16:40+08:00 对 `resolve-governance-scope` 的实时 CLI 复测中，`/Users/dmh2002/poker_hud_projects/LDVH-GOVERNED-PROJECTS.yaml` 为 `valid`：当前 V4 worktree 通过登记路径归属 `ldvh`，V2 linked worktree 通过相同 `git_common_dir` 归属同一项目，结果为 `governed_single`、0 gaps、0 diagnostics。该文件不属于当前 Git 仓库，本段只是带时点的当次观察，不是长期可用性承诺；后续 AI 必须以调用时的 Helper 结果为准。

09 当次新增验证证据：规范仓库和 Helper 定向回归 4 passed；真实 CLI 的 L2 候选与 L3 精确内容读取均返回 `ok`。配套 33 与 Codex adapter 加入后已经完成范围匹配及完整回归，结果见本纲后续当前增量说明；本段定向结果不替代完整回归。

Helper CLI 当前同时支持两种互斥规则源视图：源码或 editable install 从导入 Code 所属同一 Working Tree 读取当前 `specs/`；普通 wheel 从同一 Python distribution 内与版本和集合摘要绑定的安装规则快照读取。直接 wheel、无 `.git` 的 sdist→wheel、版本替换、强制重装、卸载及十项公开操作进程矩阵已在当次 macOS/Python 3.12 临时环境验证；Linux、原生 Windows、公共索引发布和环境 adapter 自动接入不由该证据外推。

### 5.1 已完成增量：事实对象只读服务

阶段 5 的首个实现增量已经获准进入契约冻结与实现，范围只包括：按实际管辖项目和实际 Git Working Tree 发现五类当前事实对象，解析 YAML 或 Study Markdown 载体，从当前统一字段登记、类型绑定和具体类型来源派生机械 Schema，逐对象校验并返回有效对象、局部失败、来源和诊断。它不包含创建、更新、迁移、规范化重写、Web、环境 adapter、跨 Working Tree 聚合，也不声称 Code 能判断事实语义、来源真实性、证据充分性、自然语言查重、状态决定或关系是否在领域上成立。

公共契约按以下顺序固定，未完成前不得由并行实现线自行发明接口或字段：

1. 05、`05.Att.01` 与 20–24 继续唯一承担字段、结构、类型、载体、状态、引用和关系语义；Code 只从这些来源派生模型，不建立第二份手工 Schema；
2. 由 05 定义一个只读 Helper 公开操作、闭集 `arguments` 和 `result`，复用 02 的管辖解析与 04 的共同请求响应；
3. 一次调用只读取一个实际 Git Working Tree；未提供 `work_object_locators` 时使用 Helper 实际 `cwd`，提供时全部 locator 必须解析到同一管辖项目的同一 `git_worktree_root`；不得退回登记项目路径、其它 worktree 或缓存内容；
4. 对象发现、载体解析、单对象 Schema 检查和需要全对象集的关系图检查分层；首段只能声明实际实现并通过测试的机械范围，局部坏文件不得吞掉同次请求中其它可验证对象；
5. YAML 四类型和 Study Markdown 可以在上述接口冻结后按互斥模块并行实现；公开操作注册、共同响应映射、跨类型关系检查、全量回归和完成声明由主线串行集成。

首增量的模块责任为：`ldvh.facts` 承担来源派生模型、载体解析、发现和机械校验；`ldvh.helper.operations` 只承担领域请求解析、管辖绑定、结果映射与公开操作适配；既有 `ldvh.specs` 继续只读取规则源，`ldvh.governance` 继续只解析管辖范围。实现不得把 Helper DTO、test fixture 或 Python 常量提升为事实语义来源。

最低风险与测试映射必须覆盖：未知字段、缺失与禁止字段、JSON 类型、非空值、对象 ID/文件名/目录/类型不一致、时间格式与顺序、嵌套引用闭集、Study frontmatter 与六个正文 H2、合法对象和损坏对象并存、缺失精确 selector、符号链接或路径逃逸、同项目不同 worktree 歧义，以及各类型当前来源明确列出的状态、引用和关系机械条件。冻结首版对象 Schema 前，还必须回放本节下表指定的 V3 Study、WorkCase、Pitfall 和 Spark 代表样本；无法无损承接的信息只能报告并重新进入字段准入，不得由 Code 私增字段。

本增量完成条件是：当前来源声明与 Code 实现一一绑定；五类型均有有效和无效对象级 tests；实际 CLI 从当前 Working Tree 返回可回指结果并保留局部失败；全量 Code tests、Ruff 和差异检查通过；独立复核没有未解决的 P1/P2。受控回写必须作为后续独立增量，在只读模型稳定后另行规划原子写入、冲突检测、授权、写后回读和失败恢复，不能由本增量顺带开放。

上述完成条件已经满足；最终独立复核未发现未解决的 P1/P2。该增量完成时仓库外治理配置仍指向旧路径，因此当时只证明隔离治理夹具中的真实 CLI；配置后续修复及带时点的实时结果以 §5 当前基线为准。

V3 代表样本回放已经按“信息处置而非字段照搬”冻结以下结论：

| 样本 | 历史信息 | 当前处置 | 自动迁移边界 |
|---|---|---|---|
| Study 0011 | 14 项 `urls.ref/title/summary` | `ref` 进入 source/evidence locator；`title` 只作正文链接显示；用途、读取范围与限制进入方法、发现或结论正文 | 缺 observed_at、version、applicability 或 validation_summary 时迁移未完成；只有稳定信息无法无歧义承接时才按 05 重新字段准入 |
| WorkCase 0001 | 4 项 `execution_items` | 逐项判断：共同服务同一关闭责任且具有稳定阶段目标者重建为 work_items；独立关闭责任拆为其它 WorkCase；内部命令与临时步骤退出；结果进入 item result/evidence 或顶层验证 | 不恢复通用 orchestration；不得把所有执行项静默丢弃或原样继承 |
| Pitfall 0001/0002 | 4 + 8 项 `tags` | 只作历史索引提示；当前语义由 title、症状、触发、根因、解决、避免和适用边界承担 | 不恢复 tags；语义无法由现有内容承接时重新准入 |
| Spark 0039 | 22 项 evolution 且历史时间缺时区 | 当前 evolution 上限保持 8；迁移时压缩、拆分或仅保留历史，且必须修复可验证时间来源 | Code 只拒绝不合规旧 shape，不自动选择压缩或拆分方式 |

对应回放测试逐项记录 `mapped`、`split-or-plan`、`history-only-index-hint` 或 `compress-split-or-history`。这些记录不执行迁移，也不把 V3 实例当前化。

### 5.2 已完成增量：事实对象受控创建

第二增量只实现“无副作用草案准备 → AI 填写语义内容 → Code 最终分配身份并原子创建 → 写后回读”链路，不实现更新、删除、状态推进、多对象事务、`supersedes`、提交或跨 clone 分配。05 负责两个公开操作的领域契约；04 的共同请求响应保持不变；07 的既有 Code 实现规划要求约束锁、allocator、原子 no-overwrite、回滚和测试；20–24 继续唯一承担类型语义与初始状态。该增量建立时行动模板尚未形成，这不阻塞事实服务基础契约；后续模板只能调用这些操作，不能成为创建语义的唯一来源。

实现按以下顺序串行集成：

1. 冻结 `prepare-fact-object-draft` 与 `create-fact-object` 的来源契约、草案绑定、托管字段、初始状态和拒绝边界；
2. 实现无副作用 Schema/候选身份投影，不在准备阶段创建 canonical 文件或持久 reservation；
3. 在 `git_common_dir` 内实现项目 + 类型共享锁、单调且永不复用的顺序 allocator，并以原子 no-overwrite 写入目标；
4. 写入前复用当前派生 Schema 和项目级关系检查，写入后复用事实读取链路；失败只在目标仍为本次内容时回滚；
5. 测试同 worktree 并发、linked worktree 并发、候选过期、allocator 跳号、手工目标碰撞、Schema/worktree 漂移、Study 载体、写后回读失败与回滚；不同 clone 只建立合并前冲突检测边界；
6. 全量回归与差异审核通过后声明本增量完成；若后续要求独立复核，再将其作为额外审计，不倒推当前机械证据失效。

本增量不允许 Code 先在 `facts/` 正式位置写入空壳或占位对象。Code 只生成草案依据；AI 填写除四个托管字段外的完整内容；正式文件只能在最终机械检查通过后一次出现。

上述实现与测试已经完成。两个 AI 草案可以观察到同一候选编号，但正式创建在项目 + 类型共享锁中重新分配最终编号；同 worktree 并发和 linked worktree 已验证不会重复，手工目标碰撞不会覆盖，写后回读失败会在内容仍匹配时回滚且不复用编号。不同 clone 或机器仍不共享本地锁，只保留后续合并前冲突拒绝边界。Study 已验证 Markdown 正文骨架与 Git 可追踪来源；其它四类共用同一来源派生 Schema 与创建链路，不据此宣称 AI 语义判断已经自动化。

### 5.3 已完成增量：事实对象候选发现与召回

第三增量新增 `find-fact-object-candidates`。它在一个管辖项目和一个实际 worktree/common-dir 中直接扫描五类型当前权威对象，形成包含 Schema/对象集指纹、类型状态计数和 invalid/unavailable 清单的 F0；F1 固定分页返回全部 `active` ADR 与 `open`/`blocked` WorkCase 的最小权威卡；F2 按类型、状态、精确引用、关系目标、source/evidence locator 和类型允许字段的区分大小写字面 substring 过滤。卡片只投影 20–24 已定义字段，不生成 AI 摘要、`tags`、权重或语义分数。

分页使用查询与当前对象集共同绑定的 opaque cursor；对象增加、删除、修改、worktree 改变、查询改变、cursor 损坏或越界都拒绝续页并要求从第一页重启。显式精确引用可以召回终态对象，但不能绕过同时给出的状态过滤。任何 invalid/unavailable 对象都会进入 F0、共同未完成范围和 `partial` coverage；不会静默从“全量恢复”声明中消失。F3 仍由 `read-fact-objects` 承担，F4 仍按具体来源需要展开。

实现与范围匹配测试覆盖 F1 全集和分页、F2 Pitfall 无 tags 投影、终态精确召回、关系/locator/字段组合过滤、对象变化后的旧 cursor、无效对象 partial、linked worktree 隔离、非法字段与真实 CLI。Code 只证明扫描、机械检查、过滤、投影、排序和分页；AI 仍负责语义相关性、applicability、来源与证据充分性及行动授权。阶段 5 因此在“候选发现、精确展开、受控创建”当前边界内关闭；事实更新、状态推进、多对象事务和其它回写不从该结论外推。

### 5.4 已完成增量：事实对象单对象 CAS 更新

后续建设直接在 05、Code 和 tests 中增加 `update-fact-object`，没有为了证明流程而创建 Pitfall、Spark 或其它事实对象。调用者必须先用 `read-fact-objects` 精确取得 mechanically valid 完整对象及 SHA-256，再提交同一稳定引用、当前指纹和排除四个 Code 托管字段后的完整目标；它不是 patch、merge、删除或多对象事务。

Code 在项目 + 类型共享锁内重新解析管辖、Git 身份、当前载体和项目级关系，保留 `object_id`、`fact_type_key`、`created_at`，仅在实际变化时生成 `updated_at`。状态和 WorkCase phase 只允许来源明示的边；计划或结果结构变化必须递增相应版本并使旧审核、执行/关闭批准及结果包按来源失效。任何 `supersedes` 边变化或进入 `superseded` 都被拒绝，留待未来多对象原子能力。

替换前再次比较完整旧 bytes，写入使用同目录原子替换并保留文件 mode；写后按同一事实读取和项目级检查回读，失败时只在目标仍等于本次新载体时 CAS 回滚。全部通过受控入口的同类型写入由共享锁串行；不共享该锁的外部工具只能尽可能检测，当前不声明跨工具或跨 clone 的线性化 CAS。关系检查同时修正为允许已经合法建立的 `supersedes` source 后续进入 retired 而继续保留边；受控创建和单对象更新仍禁止建立或改写该边。

### 5.5 已完成增量：共享创建事务与未挂载 Web direct capture 服务

提交 `1d39a4c4` 将事实创建抽成不依赖 Helper envelope 的 application transaction；Helper wrapper 仍保持现有公开行为。该提交保留“锁外准备、外部分配锁下完成创建”的 seam，并由共享应用边界完成既有的最终校验、分配、no-overwrite 创建、写后回读与条件回滚。

提交 `4fd2935b` 进一步对 schemas 与递归 supplied 输入做防御性冻结，建立精确载体 attempt preview、allocator 只读 preview + CAS commit、精确 raw-text 写后回读，并增加保留 invalid/unavailable 的单类型完整原始扫描和未挂载的 `ldvh.facts.web_direct_capture` 应用服务。服务只接收已解析的 `CreationBoundary`、当前 schemas 和闭集 `{title, description, priority}` 请求；它完成规则定义的 canonicalization、自包含 source ref 恢复与验证、全状态历史 identity 与 open 当前 identity 精确重复检查、coverage/integrity fail-closed、4 MiB 双层预算、并发/冲突后完整重扫、no-overwrite、精确回读与残留分类。linked worktree 共享 common-dir counter，重复扫描范围仍是已选定的实际 worktree。

本增量没有建立 HTTP/TypeScript bridge，不解析 loopback、认证、唯一管辖项目或 worktree/common-dir，不提供 V4 read/list/detail DTO projector，也未挂载生产路由。`web/src/**`、`web/api/**`、路由和表现资产均为零变化；现有 V3 POST/读取仍在生产。Windows public writes 继续在 durability gate 前 fail closed，所以这些 Code 回归不构成原生 Windows 通过证据。

## 6. 行动模板工作线

### 阶段 6 调查增量：V3 行动资产盘点与内部能力归并

阶段 6 已完成第一项只读调查。`docs/v4-architecture/investigations/V3-行动资产盘点与V4内部行动能力设计.md` 覆盖 V3 三份正式行动模板、18 个 Python 模块责任簇、Git Hook 与 Codex/WorkBuddy 插件、10 份 Skill/团队材料，以及既有下游清单已完整登记的 92 个事实实例，并按当前 00、06 和相邻责任重新归并。

调查没有按旧文件恢复模板。Human 已确认七项都是 LDVH 内部需要覆盖的行动能力：事实对象判定与受控创建、独立复核、Git 提交、委派与并行执行、事实对象状态转换与承接处置、安装配置验证、环境 adapter 编写与更新。前四项之外的后置能力仍因 Code 或环境前置而按顺序延后，但不再被表述为等待用户逐项取舍的可选模板。结构化研究/专业文档交付不在本次 Human 确认的七项中，暂不建立 LDVH 核心模板；通用验证、lifecycle 事件链、Action Guide 编译、固定角色和输出格式明确不单独建立模板。

具体模板定义继续串行。30 已完成 V3 对照、独立复核和真实 Git dogfood；31、32 在 05 与 20–24 的唯一权威冲突修复后，依据 `V4-事实对象行动模板准入与边界调查.md` 的承载比较和独立复核转为 `active`。Code 只机械解析声明和读取当前内容，不判断模板语义适用或行动授权。32 的多对象原子分支明确保持能力缺口，不得由多个单对象写入拼接。

首次真实提交验证已经暴露规则层缺口：03 原来不定义 message 和拆分政策，30 初版又把共同格式下放给未定义的“项目政策”，导致 LDVH 没有为受管辖项目提供统一提交基线。Human 已明确全部受管辖项目都应使用同一 LDVH 格式，否则管辖失去共同能力价值。`docs/v4-architecture/investigations/V4-LDVH仓库Git提交规则准入与闭环调查.md` 已完成 V3 资产迁移矩阵、承载位置再审和 03/30 最终边界复核。最终方案保持 06 边界，由 03 §9 承接受管辖项目共同提交契约，30 只承担实际创建行动；不建立 09 或 10。纯 Validator、来源投影、风险 tests、Web token 同步与真实 worktree/Index 的只读输入绑定已经实现；现有 commit 仍不改写。

统一消费模型已经进入 06：行动模板只在 LDVH 当前规则源维护一份；`read-action-template-candidates` 从声明形成机械候选，`read-action-template-content` 按稳定 key 返回精确定义、完整来源及双指纹，AI 完成语义适用判断。两个操作已经实现并通过五模板真实 CLI 调用。用户在一个环境只安装、启用或配置一次 LDVH adapter/Hook 包；不同环境可以有不同薄协议 adapter，但不得复制模板正文、选择语义或领域规则。Codex `SessionStart` 最小 adapter 已形成仓库内实现；其它事件和环境 adapter 仍未定义或实现。2026-07-15 三份独立审计处置后，原定第 34 项以后暂停准入；2026-07-16 Human 的全量 92 项迁移目标和专项 PRE review 已为 34 证明真实重复价值、前置契约和独立失败边界，因此仅 34 恢复准入，其它候选仍需等待实际价值和前置能力。

2026-07-14 三路独立只读审议对五类创建后生命周期形成初步架构结论，随后结合 V3 WorkCase 设计意图分析和 Human 对真实控制需求的澄清完成修正：20–24 唯一定义状态与承接语义，AI 承担自然语言成立性判断，一份跨五类的“事实对象状态转换与承接处置”候选模板只组织一次受控变更，Code 分层执行单对象 CAS 或同项目同 worktree 多对象原子变更集。WorkCase 不是环境步骤运行引擎；Human 明确选择将一项工作作为 WorkCase 建立项目记录后，对象必须保存阶段性工作项、可恢复当前快照、创建审核与主控处置、Human 执行授权、主控结果自检、独立结果审核、最终报告、分流建议和 Human 关闭确认。该完整质量控制链不因工作持续时间、复杂度或 work item 数量而缩减；单个工作项内部的命令、临时 todo、工具日志、推理和自由实现步骤仍不进入事实。21、统一登记、准入审计、机械校验、F1 阶段与工作项计数投影及 WorkCase 封闭记录已经按该结论修订；更新服务、跨类型状态转换模板、多对象事务和 Web 展示仍未因此取得效力。

## 7. Web 工作线

当前 Web 是由 `web/package.json` 管理的 Node/TypeScript 工程：`web/api/` 承载 Express API，`web/src/` 承载 React 页面与组件，`web/tests/` 承载 Web 自有测试，`web/docs/` 保留既有产品实践与实现说明。运行和检查命令由 `docs/web/README.md` 导航，但所有当前顺序、状态和完成声明只在本节维护。

未经明确当前来源或 Human 决定，Web 工作只能作行为保持型整治，不得改变受当次改动影响的视觉、布局、路由、导航、交互、文案、多语言、API、数据展示、错误/空态/加载态、响应式、键盘和已有可访问性行为。需要有意改变时，必须把行为变化与结构整治分开，同时取得当前来源支持和 Human 对明确范围的决定。本文不授权恢复 V3 事实类型、字段、状态或写入契约。

`web/tests/` 已建立首批 Web 自有 API 特征测试，覆盖 health、404、`no-store`、ProjectFiles 文件读取边界和 Changelog/Dashboard/ProjectFiles 共享 commit DTO。当次未修改 `web/src/`、`web/api/`、路由、样式或视觉资产。

验证证据：

- `npm test`：6 passed、0 failed、0 skipped；
- `npm run check`：通过；
- `npm run build`：通过，保留现有大 bundle 警告；
- `npm run lint`：仍有既存 11 errors、14 warnings。

这些结果只证明首批 API 特征和当前构建/类型检查范围，不证明页面、组件、视觉、响应式、辅助技术或 V4 领域适配已经完成。

2026-07-13 已完成代表性桌面与窄屏的实际浏览器观察，覆盖 Dashboard、Project Files、Spark 列表与创建面板、Changelog、未知路由、对象 404、中英文和加载/空态/错误态。当次确认窄屏没有等价导航，Spark 创建入口仍作为 V3 产品实现存在，Project Files 当次指向 V3 项目；这些是现状与缺口，不授予 V4 能力身份。键盘焦点、屏幕阅读器、浅色主题和其它视口仍为未验证；详见 `docs/web/V4-Web-浏览器表现基线.md`。

当前已知风险包括：大型页面同时承担数据转换、领域判断和呈现；生产前后端仍硬编码 V3 类型、字段、状态和目录；部分读取失败可能被空集合或零计数隐藏；Docs/ProjectFiles 存在真实路径、符号链接和允许目录边界风险；未挂载 Python service 尚无 TypeScript transport、HTTP 边界和 V4 DTO projector；小屏没有经真实测试确认的等价导航；lint 债务和 bundle 警告仍在。具体实现文件和历史设计证据由 `web/docs/` 与当前源码承担，不在本文复制。

三份独立审计进一步确认：`POST /api/sparks` 当前仍直接构造 `id/type/created/updated` 等 V3 shape，并以 `writeFileSync(..., flag: 'wx')` 写入 `ldvh-base/sparks`。这不是只读历史兼容，而是能够产生 V4 Helper 不认可对象的现实写入口。Human 随后明确修正处置：Spark 创建 UI 是保留的产品设计意图，不得关闭、隐藏或改变其视觉、布局、文案、字段与交互表现；后端 POST 和读取接口应改为依据 V4 Spark 与共享受控 Code 能力创建、回读和投影 V4 对象。其它事实类型的 Web 写入 API 当前不建设。08/20/31 与提交 `1d39a4c4`、`4fd2935b` 已闭合稳定 source ref、精确重复和不经 Helper CLI 的共享 Python 调用边界；剩余工作是将它们接入未挂载 bridge/DTO，然后原子接管生产 POST 与读取。

Web 工作线按以下 Gate 顺序推进：

1. 持续冻结 Spark 创建表现层，以前端源文件零目标变化和现有浏览器观察保留视觉、布局、文案、表单字段和交互时序；
2. 建立未挂载 TypeScript 内部 bridge/transport、V4 Spark read/list/detail reader 和 V4→现有前端 DTO projector；此切片不挂生产路由、不改 `web/src/**`；
3. 在新的实施前审核后，同一增量内原子切换 V4 Spark POST 与 list/detail 读取，强制 loopback 本机单用户和唯一 project/worktree/common-dir 边界，并永久删除 V3 writer；
4. 保持已恢复的 `web/tests/` 和首批 API 特征测试可执行，补齐 HTTP/旧 error shape 映射、POST 成功/失败、并发、回滚、创建后可见与前端契约回归；
5. 执行 API 与实际浏览器冻结表现验证，对实际声明支持的语言、视口、键盘和辅助技术范围补齐证据，未覆盖部分继续标记未验证；
6. 其它事实类型写入 API 和表现层重构继续不进入范围。

当前活跃实现增量将是未挂载 TypeScript bridge 与 V4 DTO reader/projector。每个 Web 增量开始前继续记录当次保持对象、观察范围、已知缺口、实现边界、模块责任、依赖、风险匹配测试与完成条件，并先取得 subagent 只读审核结论。

## 8. 尚未完成或未声明的范围

以下范围不能因当前 Code 存在或测试通过而标记为 available、integrated 或 completed：

- 通用领域契约章节完整性读取器，以及能够缩小返回范围的通用 L3 机械判据；
- 阶段 7 的 LDVH 自身持续集成尚未建立。它只检查本仓库的源码、Specs、tests、构建产物、普通安装、Helper CLI、Web 和环境 adapter 的静态/契约边界；不得读取、修改或评价用户的管辖项目，不替代管辖项目自己的 CI，也不得在 fork PR 中使用个人环境、真实账号或机密。需要项目样本时只能使用仓库内 fixture 或测试进程创建的隔离临时仓库；
- 原生 Windows 支持尚未成立。核心 CLI 已形成锁、安全读取、原子写入、路径和 Git 边界的 macOS 候选证据，Codex 插件也已分离 POSIX `command` 与 Windows `commandWindows` 并固定 UTF-8 transport；7A 又建立了非 CI 的原生手工 runner、native-only probes 与 evidence contract。runner 只接受 clean Git top-level，以白名单环境、isolated Pip、命令 timeout、fixed NTFS 观察、JUnit 逐项矩阵和失败/清理收口防止假通过；当前 fail-closed policy 与未来获批写入矩阵分离，六进程 allocator、linked counter 和条件更新单赢家在政策未接受时显式 blocked。本契约阶段的当前 macOS 完整回归为 802 passed / 10 native-only skipped / 1 deselected；被显式排除的是已知暂停 Web direct-capture 问题 `test_open_current_identity_is_checked_after_legal_content_change`，该结果既不把被排除项算作通过，也不改变原生证据范围。但当前没有 Human 指定并授权的 Windows runner，Windows file-only 耐久尚未获接受，Codex 安装/信任/触发仍是独立 Human Gate，所以没有任何原生通过结论。UNC 当前默认 fail-closed，WSL 不等同于原生 Windows 完成证据；
- 事实对象删除、字段 patch/merge、多对象事务、`supersedes` 事务、迁移、跨 clone 唯一分配、语义消费决定和超出 §5.4 的其它受控回写；
- 管辖配置写入、worktree 创建/切换/删除等生命周期管理，以及跨平台真实环境验证；
- 独立复核、委派与并行执行、环境 adapter 编写与更新等其它候选模板仍须按 01/06 逐项准入；30–34 已为 `active`，统一候选/内容读取已实现，但 Helper 不自动选择或执行模板，32 的多对象原子分支和 34 的批量迁移 executor 仍不可执行；
- 本机当前用户 Codex 0.144.2、`ldvh@personal` 当前版本和 `SessionStart startup|resume` 之外的环境插件、Hook、adapter、真实触发与回滚；
- Web `web-direct-capture` 的 08/20/31 规则契约、共享事实创建 application transaction 和未挂载 Python direct-capture service 已形成，但内部 TypeScript bridge/transport、V4 read/list/detail reader 与 DTO projector、loopback/唯一管辖边界、HTTP 映射、POST 与读取原子接管、V3 writer 删除、创建后可见及页面/组件/视觉/可访问性/响应式验证仍未实现；未挂载 Code 成立不等于 Web 创建已经 available；
- 真实事实创建、跨会话恢复、Human 交还与 WorkCase 关闭的端到端 dogfood；当前 Git 跟踪的 `facts/**` 为 0，既有 Code/tests 与单环境接入不能替代该证据；
- V3 92 个事实对象的全量逐条迁移；不可变机器基线和 decision-independent source characterization 已闭合机械输入，successor 身份/时间、旧终态边界和 34 行动承载也已建立，但 semantic reviewed/target decided 仍均为 0，仍没有完整语义 ledger、dry-run/可恢复写入能力或任何实际 V4 承接结果，已关闭对象也不得跳过；
- Index 提交前验证、提交后历史锚点和跨 Git 历史 `retired` 职责检查；
- 五类型准入记录重组后的路径漂移已于 2026-07-15 关闭：运行时唯一常量和测试 fixture 已同步到实际 `active/` 路径；09 与 33 加入后的规范、模板和 Helper 精确集合断言已同步，没有恢复旧文件、复制审计记录或放宽检查；完整回归结果以本增量最终复跑为准；
- 规范责任重复、承载净价值、附件正文授权和 `supersedes` 语义的 Human/独立复核。

## 9. 当前下一步

`read-specification-content` 已完成 Code、tests、真实 CLI 验证和独立复核。规则适用判断的 V4 当前来源、V3 经验与失败模式、行业模型及术语已完成只读调查和三路独立复核。《V4 规则适用判断职责边界调查记录》已依据 00 收口：Code 不得解释自然语言规则，AI 承担语义判断，来源已定义的结构化契约可由 Code 执行。这不是待 Human 确认的建设取舍。

阶段 5 已完成并关闭。`find-fact-object-candidates`、`read-fact-objects`、`prepare-fact-object-draft`、`create-fact-object` 与 `update-fact-object` 已分别承担候选发现、精确展开、草案准备、单对象受控创建和单对象 CAS 更新；F0–F4、五类特有时机、ADR/WorkCase 恢复基线、Pitfall 不恢复 `tags`、分页 coverage、旧 cursor 失效、过期更新拒绝、状态/phase/version 转换和写后回读均有来源契约、实现和范围匹配测试。WorkCase 专项审计与后续两份全架构方向审计已分别按 `V4-WorkCase专项外部审计处置记录.md` 和 `V4-全架构方向双审计处置记录.md` 逐项处置。

Helper 高频读取消费成本增量已完成：`ldvh-helper-cli/2` 默认 `compact` 档聚合同范围重复资格、来源和验证信息，显式 `diagnostic` 档保留逐项审计证据；L3 按精确 heading path 返回机械切片，不再因 Code 无法证明语义完整性自动展开 L4。AI 对语义适用、上下文是否充分及是否继续补读承担判断；Code 只证明精确标题匹配、行范围、来源回指和已实际返回内容。该增量当次真实 CLI 的默认规范候选响应为 12,074 bytes，诊断档为 46,517 bytes，精确 L3 为 6,408 bytes；当时 Ruff 与 466 项 Code tests 通过。这些是带时点的历史增量证据，不承担当前测试总数或 AI 判断质量声明。

`environment-integration` 与 `environment-integration-installation-verification` 已分别形成 09 和 33 当前来源，并通过规范/模板解析及 Helper 精确读取。首个 Codex 插件 adapter、显式 `PLUGIN_DATA` 配置、plan/apply/check/verify 入口已形成；历史 macOS 候选当时有 17 项范围匹配 tests。Human 授权后，旧同名 V3 插件已完成只读备份，V4 以 `ldvh@personal` 版本 `0.1.0+codex.20260715021623` 原位安装，来源与缓存一致，Hook Trusted/Enabled，配置与 Helper 静态验证通过。Codex 0.144.2 新任务已经取得正常 `ok/governed_single`、缺失配置显式 unresolved、停用后无注入和恢复后再次成功四条真实证据；marketplace 未变、历史 receipt 前后均为 190。Windows 第 6 切片又形成含 `commandWindows`、UTF-8 strict 和平台测试 helper 的仓库候选，当前 22 项 adapter tests 与 684 项完整 tests 通过，但没有替换个人安装；所以旧 cache/trust/真实触发不覆盖新候选。核心 CLI、仓库 adapter 候选和各平台真实接入继续分别报告。

三份独立审计与当前工作树的合并复核见 `V4-三份独立审计处置记录.md`。2026-07-15 在提交 `d64381263000` 加当前文档工作树上重新运行：567 项 Code tests 全部通过，Ruff check 与 format check 通过，`capabilities` 实时返回 10 项已绑定公开操作。三份报告中的 549 passed / 1 failed、8 项操作、21 个候选和 3 个模板只描述各自审计时点或旧工作树，不再承担当前状态。该复跑仍不证明普通安装、Web、原生 Windows、真实事实消费或跨会话恢复。

普通发行安装增量已经依次完成规则契约、快照构建与定位、Helper 来源身份投影和生命周期矩阵。当前规则集加入 34 后，wheel 内包含 25 项清单成员及另一个 `manifest.json`，合计 26 项快照资源；直接 wheel 与无 `.git` sdist→wheel 的集合摘要一致。隔离测试从带旧版专属 RECORD 成员的 `0.0.0` 临时包真实升级到当前 `0.1.0`，证明旧成员、旧 metadata、旧版本与旧快照身份退出；当前快照篡改后 Helper fail closed，`--force-reinstall` 恢复；卸载后 LDVH console script、package、dist-info 和快照消失，依赖留存不误报为 LDVH 残留。直接 wheel 与 sdist-derived wheel 都在隔离受管辖 Git 项目中完成十项操作的 `capabilities <key>`、`call <key>`、操作专属 invalid request、退出码和单一 JSON stdout 矩阵；全项目文件树与 Git status/index 指纹证明 capabilities、prepare 和 invalid request 无持久副作用，有效 create/update 只改变预期 canonical 事实文件。规则/实现来源保持 installed distribution 身份，facts/governance/Git 保持临时项目 Working Tree 身份，快照内容前后不变。该证据只覆盖当次 macOS/Python 3.12 普通 distribution，不证明 PyPI、Linux、原生 Windows、CI 或环境 adapter 真实触发。

Human 最新目标调整后，当前不再继续 Web bridge 或原生 Windows。后续按以下 Gate 顺序推进：

1. **V3 迁移冻结基线与 source characterization。** 已完成：固定 92 项 snapshot commit/tree、路径、状态、byte size、SHA-256、Git blob 和原始时间，并机械递归覆盖时间、旧引用、字段 shape 和 Study body 结构；所有语义/target 决定保持未开始/未决。
2. **正式 successor 契约与 34 行动模板。** 已完成：Human 确认新 V4 身份/创建时间、不猜历史时区、严格五类和单项交还策略；05 §7.4、05.Att.01 和 `fact-object-source-reconstruction-migration` 已定义合法初态 successor、semantic/materialized digest、可恢复应用、Git anchor 与 no-op 边界，但不表示 executor 可用。
3. **语义 ledger 与无写机械基础。** 下一 Gate：建立独立 ledger Schema、source↔logical-target 双向校验、`semantic_payload_sha256` dry-run，并把永久 source-integrity 与首阶段 zero-facts Gate 拆分；仍不实施生产写入。
4. **92 项逐条迁移与验证。** 完成逐源语义审核和覆盖终态、缺时区、拆分、合并、Study URL 与关系修复的 pilot，再按 target 依赖拓扑分批写入；每批独立 PRE review、验证和 Git 提交，最终闭合 92 项、全图关系、no-op rerun 与 Git anchors。
5. **低成本 bootstrap/resume。** 优先用薄 adapter 机械组合现有管辖解析、能力摘要、F0/F1 和来源指针；只有现有操作不能形成稳定边界时才准入新的 Helper 公开操作。使用迁移后的真实事实记录调用数、响应体量、空字段与无产出判断。
6. **真实纵向 dogfood。** 选择真实受管辖工作并由 Human 明确决定是否建立 WorkCase；完成事实创建、读取、CAS 更新、强制中断、换会话恢复、验证、Git 溯源、结果交还和关闭。不得仅为测试流程制造内部事实。记录读取文件数、调用数、字节数、Human 等待点、版本失效、重复调查和恢复遗漏。
7. **发布前恢复项。** 恢复已停放的 Web V4 bridge，保持表现层冻结并原子接管 Spark POST/读取；完成原生 Windows 验证、只检查 LDVH 自身的三平台 CI、其它 V3 覆盖、支持矩阵、License、贡献、安全、版本、发布和排障资料，再进入最终验收。
8. **证据驱动减法。** dogfood 后再决定是否简化 WorkCase 完整质量链、字段准入、响应信封或规范披露。当前 Human 已确认的 WorkCase 设计不因审计意见自动失效；任何改变重新进入 Human Gate。

第 1、2 项已闭合；当前 Gate 是独立 semantic ledger Schema、双向 logical-target 校验、无写 dry-run 与 baseline mode split，仍必须保持 semantic reviewed/target decided 为 0 和零 V4 生产实例。Web bridge 的停放结论不等于 bridge 可合入，生产 POST/读取仍是 V3；发布前恢复时仍必须完成 bridge/DTO、loopback/唯一管辖边界、HTTP 映射和创建+读取原子接管，并在同一切换删除 V3 writer。原生 Windows 候选与手工入口同样不等于原生通过。CI 不是迁移、bootstrap 或 dogfood 的前置条件，也不负责读取、修改或评价 LDVH 所管辖项目；它在发布前只检查 LDVH 自身。在 92 项迁移、bootstrap/resume、真实 dogfood、Web V4 Spark/读取闭环、原生 Windows、其它 V3 覆盖、三平台 CI、开源资料和最终验收完成前，不能发布。

## 10. 当前决策与证据文档索引

| 文档 | 当前身份 | 与本文的关系 |
|---|---|---|
| `investigations/V4-规则适用判断职责边界调查记录.md` | 已完成的职责边界调查 | 保留依据和候选比较；不形成 Human Gate 或待建设的通用判断能力 |
| `investigations/V4-L3L4规则内容读取公开操作提案.md` | 已确认并完成实现 | 只保留决策理由和实现输入；当前结果以 §5 为准 |
| `investigations/V4-首个Helper公开操作候选评估.md` | 已完成的候选评估 | 只保留历史决策证据，不承担当前顺序 |
| `investigations/V3-行动资产盘点与V4内部行动能力设计.md` | 阶段 6 已完成的只读调查与机制设计 | 支持 §6 的能力覆盖、统一消费模型与建设顺序；不授予具体模板身份或效力 |
| `investigations/V4-事实对象行动模板准入与边界调查.md` | 已完成的两模板承载比较与独立复核处置 | 支持 31/32 的独立责任、数量、边界及 05/20–24 冲突修复；不替代模板正文 |
| `investigations/V4-LDVH仓库Git提交规则准入与闭环调查.md` | 当前责任调整与迁移复核记录 | 记录 V3 迁移、复核异议及 30、独立来源、03 三种承载方案的最终处置；不使 validator 或任何 commit 合规 |
| `active/V4-Git提交契约-Validator-Code实现规划.md` | 当前 Code 实现规划 | 覆盖 03 §9 来源投影、纯 validator、风险 tests 与 Web token 同步缺口；不覆盖 Git 写入、Hook 或环境安装 |
| `active/V4-行动模板声明解析-Code实现规划.md` | 当前 Code 实现规划 | 覆盖通用行动模板声明解析、派生诊断和风险测试；不覆盖 Git 写入、Helper 操作或环境接入 |
| `investigations/V4-环境接入规范与安装验证行动模板准入调查.md` | 已完成的 09/33 责任准入与边界调查 | 支持环境共同规则与安装行动结构的独立承载；不替代 Specs 或真实环境证据 |
| `active/V4-Codex环境接入-Code实现规划.md` | 已完成首个 Codex adapter 的实现边界记录 | 保存最小事件范围、配置契约、风险 tests 和实现结果；不自行证明安装或触发 |
| `active/V4-Codex环境接入-安装与真实验证记录.md` | 本机 Codex 首个环境增量的当次证据 | 支持 §8/§9 的安装、信任、成功/失败触发及停用/恢复结论；不外推其它环境 |
| `active/V4-三份独立审计处置记录.md` | 当前三审计合并处置依据 | 记录三份 2026-07-15 审计意见的事实核对、接受/延后/拒绝与顺序修正；不替代本文当前状态或 Specs |
| `active/V4-V3事实对象全量逐条迁移决定.md` | 当前 V3 实例迁移范围决定 | 记录 92 个 V3 事实对象全部逐条迁移、包含已关闭对象的 Human 决定、库存、完成含义和未实施边界；不自行授权迁移写入 |
| `investigations/V4-V3事实迁移基线与契约调查.md` | 当前迁移首阶段调查与 PRE review 处置 | 记录不可变机器基线、直接写入 blocker、后续契约候选和 Human 待决定项；不定义正式迁移语义或授权实例写入 |
| `active/V4-Web表现冻结与V4-Spark创建决定.md` | 当前 Web 产品边界决定 | 记录表现层不得修改、Spark 创建保留并改接 V4 受控 Code、其它写 API 暂不建设的 Human 决定，以及已完成的未挂载 service 与尚未接管的生产边界 |
| `closures/V4-WorkCase设计外部审计交接包.md` | 已完成外部审计的交接输入 | 保留当次资料、问题和输出要求；不再承担当前下一步，不取得 WorkCase 或实现定义权 |
| `closures/V4-WorkCase专项外部审计处置记录.md` | 已完成的审计意见处置 | 记录接受、降级、拒绝、额外发现和后置测试边界；不取得当前规则定义权 |
| `active/V4-全架构方向双审计处置记录.md` | 已完成的双审计意见处置 | 记录两份根目录审计的逐项事实核对、实测基线与合并后顺序；不取得规则、字段或操作定义权 |
| `investigations/V3-设计覆盖与V4下游审核清单.md` | 当前下游覆盖审核入口 | 向本文的 V3 收口阶段提供待核对项，不决定当前实施顺序 |
| `active/V4-架构重建基线.md` | 稳定的非继承与历史边界 | 不作为当前计划 |
| `docs/web/V4-Web-浏览器表现基线.md` | 当次浏览器观察证据 | 支持 §7 的已观察和未验证范围，不是 Web 计划 |

其它 Specs 责任、命名、术语、规范依据和结构审计记录保留各自已标明的调查或历史作用，不纳入当前工作推进控制面。

## 11. 状态口径

本文和 README 只报告当前工作树与当次验证证据。规范已激活、Code 已实现、测试已通过、环境已接入、当前可用和工作已完成是不同结论，后续文档必须分别表述，不能互相替代。
