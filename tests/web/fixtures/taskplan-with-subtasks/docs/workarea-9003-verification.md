# WorkArea 9003 Verification Notes

This document is a fixture for checking how WorkArea detail pages preview related Markdown documents.

## Purpose

- Verify that `related_docs` appears in the WorkArea related materials section.
- Verify that clicking a local Markdown document opens the right reading panel.
- Verify that Markdown headings, lists, code spans, and paragraphs use the shared Markdown renderer.

## Expected Behavior

The WorkArea detail page should keep the main route on `workarea-9003` while the document opens in the extended reading panel.
