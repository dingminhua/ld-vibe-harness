# v1-v2-Code消费双读映射矩阵

```yaml
v2_attachment:
  attachment_id: "04.Att.08"
  title: "v1-v2-Code消费双读映射矩阵"
  status: "draft"
  authority: "not_active_until_parent_and_human_approved"
  parent_spec: "specs-v2/04-Code确定性执行规范.md"
  canonical_path: "specs-v2/attachments/04.Att.08-v1-v2-Code消费双读映射矩阵.md"
  purpose: "承载 04 授权的 v1-v2 Code 消费双读映射"
  migration_sources:
    - "specs/07-Code确定性执行实现规范.md"
  code_consumption:
    - "v1_v2_code_dual_read_mapping"
```

> 文件状态：本文当前位于 `specs-v2/`，尚未切换为 active；正式 Code 默认消费仍以 active `specs/` 和当前实现为准。本文是 `04-Code确定性执行规范.md` 授权附件，必须随父规范经过 Human 单篇确认后才可生效，不决定 active 切换。

## 1. 定位

本文承载 04 授权的 v1-v2 Code 消费双读映射。

## 2. 映射矩阵

| v1 消费对象 | v2 目标 | 双读目的 |
|---|---|---|
| `ldvh_doc` | `v2_spec` | 规范身份兼容与迁移差异诊断 |
| `ldvh_member` | `v2_fact_model_member` / `v2_action_member` | 成员身份兼容与集合差异诊断 |
| 保障要求表 | v2 五字段保障要求表 | 保障结构兼容 |
| Human Gate 章节 | v2 Human Gate 章节 | Gate 触发边界兼容 |
| 待补齐事项 | v2 待补齐事项 | 迁移缺口保留 |
| 字段注册表 | v2 字段注册与字段矩阵附件 | 字段契约差异诊断 |
| Web DTO / Spark 创建白名单 | v2 05 与事实模型成员 | Web 消费回归 |
| 测试入口 | v2 08 与 04 回归入口 | 校验入口兼容 |

## 3. 使用规则

双读只用于兼容和迁移审计，不得把 v2 草案解释为 active 规范，也不得让 v2 草案覆盖 v1 active 规则。

## 4. 待补齐事项

1. 当前 v2-check 尚未完整解析 active `specs/` 的 `ldvh_doc` 和 `ldvh_member` 做差异诊断；
2. active 切换前必须补齐双读策略、正反样例和降级诊断。
