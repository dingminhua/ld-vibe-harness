# Code完整验证入口表

```yaml
v2_attachment:
  attachment_id: "08.Att.05"
  title: "Code完整验证入口表"
  status: "active"
  authority: "active_with_parent_spec"
  parent_spec: "specs/08-测试基础规范.md"
  canonical_path: "specs/attachments/08.Att.05-Code完整验证入口表.md"
  purpose: "承载 08 授权的 LDVH Code 当前完整验证入口和降级说明"
  migration_sources:
    - "history/specs-v1/11-测试基础规范.md"
    - "code/docs/01-Code参考实现结构规范.md"
  code_consumption:
    - "code_complete_test_entry"
```

> 文件状态：本文是 active 授权附件；随父规范生效，不独立改变父规范正文授权之外的原则、状态机、事实源边界或流程 Gate。

## 1. 定位

本文承载 08 授权的 LDVH Code 当前完整验证入口。

本文只记录当前参考实现的验证入口和降级说明，不限定未来 Code 的实现语言、测试框架、包管理器、命令组织或技术栈。若 Code 实现域调整验证入口，应同步本文、`code/docs/` 和 08 主体，但不得把当前命令反向写成长期技术选型义务。

## 2. 入口表

| 入口 | 当前性质 | 最低要求 |
|---|---|---|
| `npm run test:code` | Code 日常完整测试入口 | 同时运行 `tests/code` 自动化测试和 `python3 code/specs_validate.py all --fail-on-diagnostics` |
| `npm run test:code:unit` | Code 快速单元测试入口 | 只覆盖 `tests/code` 时，验证声明必须说明未覆盖综合入口的风险 |
| `python3 code/specs_validate.py all --fail-on-diagnostics` | active specs 综合检查入口 | 当前默认读取 active `specs/` |
| `python3 code/specs_validate.py v2-check --fail-on-diagnostics --format text` | active specs 诊断和只读知识地图预览入口 | 不替代完整运行时知识地图能力、Web 接入验证或历史记录价值提取 |
| `git diff --check` | 空白和补丁格式检查 | 只能证明 diff 格式无空白错误，不能替代业务测试 |

## 3. 降级规则

只运行局部入口时，应在验证声明中说明未运行完整入口的原因、影响范围和残留风险。Code 关键 CLI、诊断 Schema、目录假设、写入行为或下游消费合同变化时，不得只用 smoke test 作为完整验证。

## 4. 待补齐事项

1. Code 第二版实现继续推进后，应检查本文入口是否需要补充知识地图和受控写入 preflight 专项测试；
2. package scripts 变化时，应同步本文、`code/docs/` 和 08 主体。
