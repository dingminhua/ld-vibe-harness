# LDVH Install Wizard Practice

本文是 Code 实现域文档，承接 `specs/30-LDVH安装初始化管辖项目配置行动模板.md` 的安装向导交互实践。本文不定义新的安装授权，不替代 Human Gate，不表示任何插件、Hook 或用户环境已经安装。

## 展示原则

安装向导是正常安装流程，不区分测试版和正式版。是否执行只在最终确认时由 Human 决定。

用户主流程使用普通词：

| 用词 | 用途 |
|---|---|
| 流程概览 | 展示完整步骤 |
| 安装前检查 | 只读检查当前状态 |
| 安装选项 | 收集需要 Human 决定的选项 |
| 安装方案预览 | 汇总检查结果、选择、后置项、验证和回滚 |
| 最终确认 | Human 选择执行、不执行或返回修改 |

避免把内部检查、环境接管状态等技术口径作为用户主标题。这些术语只应出现在技术细节、验证摘要或环境状态说明中。

## 状态表模板

每次展示向导时，先给流程状态表。当前步骤使用箭头，已经完成的步骤把决策或结果写进最后一列，尚未发生的步骤保持空白。

|  | 步骤 | 名称 | 决策 / 结果 |
|---|---|---|---|
| ➜ | 1/5 | 📋 流程概览 |  |
|  | 2/5 | 🔍 安装前检查 |  |
|  | 3/5 | 📝 安装选项 |  |
|  | 4/5 | 🔒 安装方案预览 |  |
|  | 5/5 | 🛠️ 最终确认 |  |

状态列不要写“待进行 / 待选择 / 待确认”。空白已经表示尚未发生。

## 信息分类

安装向导必须分开展示三类信息：

| 类型 | 来源 | 是否问 Human |
|---|---|---|
| 系统检查事实 | 只读检查、Code 输出、目录回读 | 不问 |
| 用户决策 | Human 需要取舍的安装选项 | 问 |
| 本次后置 | 当前不支持、未授权或需要单独 Gate 的事项 | 不作为主选项 |

示例：

| 内容 | 分类 | 表达 |
|---|---|---|
| `ldvh-base/` 已存在 | 系统检查事实 | 写入安装前检查结果，不再询问是否创建 |
| 目标工作区根目录 | 用户决策 | 确认工作区，不询问配置文件放项目内 |
| 项目内或用户级配置目录 | 本次后置 | 说明不是支持位置，不放入主选项 |
| 用户环境插件安装 | 本次后置 | 说明需要单独 Human Gate |

## 选择表模板

需要 Human 选择时，每次只问一个问题。配置位置固定为目标工作区根目录；选择表只用于确认 LDVH 本体路径、目标工作区根目录或多个候选。

| 项目 | 路径 | 含义 | 限制 | 建议 |
|---|---|---|---|---|
| LDVH 本体 | `<ldvh-root>` | 提供 specs、Code 和安装模板 | 不等于目标工作区 | 候选必须有 `specs/30...` 和 `code/specs_validate.py` 证据 |
| 目标工作区 | `<workspace-root>` | 放置工作区配置 | 项目内配置不是支持位置 | 确认后配置为 `<workspace-root>/LDVH-GOVERNED-PROJECTS.yaml` |
| 工作区配置 | `<workspace-root>/LDVH-GOVERNED-PROJECTS.yaml` | 登记该工作区管辖项目 | 不放项目根或用户级目录 | 默认且唯一支持位置 |

如果当前界面支持选择框 / 单选控件，多个 LDVH 本体候选或多个目标工作区候选应使用 UI 控件；不可用时使用等价编号选项。

目标工作区没有 LDVH 文件时，AI 仍应从 LDVH 本体读取安装模板。允许的 bootstrap discovery 是有限、只读、有证据的搜索：优先使用当前已知 LDVH root、`LDVH_ROOT` / `LDVH_HOME`、插件 / Hook / wrapper 中记录的 LDVH root、cwd 向上的 LDVH 本体候选、常见工作区父目录有限深度搜索；多个候选必须展示证据并让 Human 选择，找不到时要求 Human 提供 LDVH 本体路径。

确认目标工作区根目录时，必须检查从工作区根目录到当前项目路径链上的 `LDVH-GOVERNED-PROJECTS.yaml`。如果工作区根目录和项目内同时存在配置，安装方案预览应显示配置层级冲突，并停止在最终确认前；用户需要先删除、迁移或明确保留其中一个配置文件。

## 最终确认

最终确认前不得写入。安装方案预览必须列出：

1. 将写入或修改的文件；
2. 将不会执行的事项；
3. 本次后置项；
4. 验证命令和预期结果；
5. 回滚方式；
6. 残留风险。

最终确认至少提供三类选择：

| 选项 | 含义 | 结果 |
|---|---|---|
| 1 | 执行 | 按方案执行并验证 |
| 2 | 不执行 | 停止，不写入 |
| 3 | 返回修改 | 回到安装选项 |

## 示例

### 1/5 流程概览

|  | 步骤 | 名称 | 决策 / 结果 |
|---|---|---|---|
| ➜ | 1/5 | 📋 流程概览 |  |
|  | 2/5 | 🔍 安装前检查 |  |
|  | 3/5 | 📝 安装选项 |  |
|  | 4/5 | 🔒 安装方案预览 |  |
|  | 5/5 | 🛠️ 最终确认 |  |

进入最终确认前不会写文件。用户环境、插件和外部 Hook 需要单独授权。

| 选项 | 结果 |
|---|---|
| 1 | 进入安装前检查 |
| 2 | 暂停 |

### 2/5 安装前检查

|  | 步骤 | 名称 | 决策 / 结果 |
|---|---|---|---|
|  | 1/5 | 📋 流程概览 | 已开始 |
| ➜ | 2/5 | 🔍 安装前检查 |  |
|  | 3/5 | 📝 安装选项 |  |
|  | 4/5 | 🔒 安装方案预览 |  |
|  | 5/5 | 🛠️ 最终确认 |  |

| 检查项 | 结果 | 影响 |
|---|---|---|
| 当前项目 | 已识别 | 可以继续 |
| 事实源目录 | 已存在 | 无需创建 |
| 用户环境插件 | 未授权处理 | 本次后置 |
| 外部项目 Hook | 未授权处理 | 本次后置 |

本步不需要选择。下一步进入安装选项。

### 3/5 安装选项

|  | 步骤 | 名称 | 决策 / 结果 |
|---|---|---|---|
|  | 1/5 | 📋 流程概览 | 已开始 |
|  | 2/5 | 🔍 安装前检查 | 配置已存在；目录已存在；插件后置 |
| ➜ | 3/5 | 📝 安装选项 |  |
|  | 4/5 | 🔒 安装方案预览 |  |
|  | 5/5 | 🛠️ 最终确认 |  |

需要确认：LDVH 本体和目标工作区。

| 项目 | 路径 | 含义 | 限制 | 建议 |
|---|---|---|---|---|
| LDVH 本体 | `/Users/dmh2002/poker_hud_projects/ld-vibe-harness-v3` | 提供 specs、Code 和安装模板 | 不等于目标工作区 | 候选证据应包含 `specs/30...` 和 `code/specs_validate.py` |
| 目标工作区 | `/Users/dmh2002/poker_hud_projects` | 放置工作区配置 | 项目内配置不是支持位置 | 本例使用该工作区 |
| 工作区配置 | `/Users/dmh2002/poker_hud_projects/LDVH-GOVERNED-PROJECTS.yaml` | 登记该工作区管辖项目 | 不放项目根或用户级目录 | 默认且唯一支持位置 |

用户级配置目录和项目内配置不是支持位置。

如果本例中目标工作区根目录和项目内都已有配置文件，下一步安装方案预览应阻断，不得继续执行工作区安装。
