# V4 架构方向独立审计报告

**审计者**：GLM-5.2（300B 推理模型，独立审计者）  
**审计时间**：2026-07-15  
**审计范围**：specs/00-08、20-24、30-32、附件、工作推进总纲、Code 实现、tests、Web、Git 历史、archive/v3、已有审计记录  
**核心问题**：方向是否正确？是否真正服务 AI 执行者？复杂度是否合理？

---

## 1. 总判定：方向基本正确，但已发生早期结构性偏航

**判定：方向基本正确，需要校正。**  
还没有到"严重偏航"的程度，但偏航信号已经出现，如果不校正在阶段 6-7 会加速恶化。

V4 对 V3 的问题判断是正确的（V3 的通用 orchestration 膨胀、规则不闭合、职责边界不清），并且已经做出了多个正确的架构选择。但当前建设存在**"规范工程自我循环"的早期信号**——规则和设计的完备性开始优先于 AI 实际消费路径的闭合。

---

## 2. 对 00 核心目标和 V1-V8 的逐项证据矩阵

| 标准 | 评级 | 证据 |
|---|---|---|
| V1 快速定位 | ✅ 满足 | `read-specification-candidates`、`read-fact-objects` 等 8 个 Helper 操作确实实现了精确选择、L0-L2 发现和 L3/L4 分层读取 |
| V2 充分理解 | ⚠️ 部分满足 | 分层读取和来源回指做了，但"充分理解"要求 AI 不跨太多文件就能形成连贯理解——当前 AI 需要先读 01 理解规范模型，再读 00 理解根定义，再读 05 理解事实模型，再读 20-24 理解具体类型，再读 06 理解行动模板，再读 07 理解 Code 实践。**渐进式披露的入口很好，但"理解链路"本身不闭合** |
| V3 边界识别 | ✅ 满足 | 源头区分（规则源 vs 事实源 vs 派生信息）、作用边界（03 §6 表格）、Human Gate 和 Stop Conditions 边界清晰，是 V4 最强项 |
| V4 稳定推进 | ⚠️ 部分满足 | 单对象创建/更新/读取已实现，但**0 个真实事实对象**、0 个跨会话 dogfood。模板只是可读，没有环境执行能力。推进链路在"读取"层面闭合，在"执行-验证-回写-接续"层面开路 |
| V5 据实判断 | ✅ 满足 | 00 对"可证实范围、未证实范围和残留风险"的区分，03 对 Working Tree/Index/HEAD 的严格区分，05 对机械校验与语义判断的分层，是 V4 最强的设计贡献 |
| V6 工作接续 | ❌ 未闭环 | 只有单对象 CAS 更新，没有多对象事务、没有 `supersedes` 原子操作、没有跨会话压缩恢复的完整证据、**没有真实 WorkCase 被创建和恢复过**。F0 恢复清单和 F1 基线卡有了，但只有实现没有真实使用 |
| V7 清晰沟通 | ⚠️ 部分满足 | Helper 响应契约设计支持了清晰的来源和缺口报告，但 Human Gate 的交还内容只在 00 中定义，没有在 Helper 或测试中验证过真实 Human 交互场景 |
| V8 持续积累 | ⚠️ 部分满足 | 事实类型（Pitfall、Study）和统一字段登记支持了积累，但**没有真实实例**，没有"积累"的 DOGFOOD 证据 |

**关键发现**：V4 在 V1（定位）、V3（边界）、V5（据实判断）上做得最好，因为这些是"规范工程"擅长的。在 V4（推进）、V6（接续）、V7（沟通）上最弱，因为这些需要"真实 AI 使用路径"来验证。

---

## 3. "真正服务 AI" vs "主要服务规范维护/审计"的内容比例

**定性估计：约 40% 服务 AI 消费，60% 服务规范治理与审计可追溯性。**

### 服务 AI 的证据（40%）：
- Helper CLI 的 8 个公开操作，精确的 `read-specification-content` L3 切片（6,408 bytes 而非 46KB）
- 05 的 F0-F4 分层设计，避免把全部事实注入上下文
- `compact` 响应档位减少重复资格和来源信息
- 事实对象的 CAS 更新避免全量重写
- 03 的 Working Tree 优先读取，不要求提交才生效

### 服务规范治理的证据（60%）：
- **01 本身 814 行**，定义了 YAML 身份块、7 个字段的根规范 profile、5 个 disclosure 层级、`read-specification-candidates` 输入/结果字段闭集（每个字段 3-5 行要求）、`read-specification-content` 输入/结果字段闭集——这些是 Code 的实现契约，AI 根本不需要读
- **05 本身 551 行**，定义了 7 个字段的新增准入规则、字段提升规则、结构注册规则、Schema 组合规则——AI 创建事实对象时只需要知道"哪些字段必填"，不需要读 05 来理解
- **01 §12 双语术语治理**有 11 步检查、5 个不通过条件、独立复核要求——这对 AI 自身的术语管理是重要的，但当前 AI 读它来理解"什么是事实对象"的成本很高
- **当前 6,230 行 specs** 中，约 40% 是规范治理（01 的身份/结构/关系/披露规则、05 的字段准入/提升规则、06 的模板准入规则），60% 是领域规则。AI 实际创建事实对象时只需要读 20-24 的 231-411 行 + 05 的 §11 接口契约
- **549 个测试**中一大部分是机械检查（Schema 合规、字段闭集、引用一致性），这些对"确保 Code 不偏离规范"很重要，但对"证明 AI 能完成工作"帮助有限

### 最关键的证据：
> 当前 V4 有 0 个真实事实对象、0 个真实 WorkCase、0 次跨会话恢复。  
> 但已经有 6,230 行 specs、16,430 行 Python、549 个测试、81 个注册字段、21 个 spec 文件、38 个 docs 文件。

这不是说规范多了就不对，而是说**"规范工程完成度"和"AI 实际使用路径完成度"之间的差距在拉大**。

---

## 4. 按 P0/P1/P2/P3 排序的问题

### P0（必须立即修正）

**P0-1：0 个真实事实对象 + 0 次跨会话接续 = 核心价值假设未验证**

- 位置：全局
- 失败场景：AI 在 V4 中无法完成"创建一个 WorkCase → 跨会话恢复 → 继续工作 → 验证 → 关闭"的完整链路
- 对 AI 的实际影响：V4 无法声称"解决了 AI 跨会话工作接续问题"，因为从未被验证过
- 最小修正方向：不是去创建 mock 事实，而是必须先确保"创建-读取-更新-恢复"的完整链路可以由 AI 真实完成，并且真实工作中的 AI 愿意使用它而不是绕过它

**P0-2：AI 首次进入项目的认知成本过高**

- 位置：00-08 全部
- 失败场景：新 AI 第一次进入 LDVH 项目，需要先理解 01 的规范模型结构（814 行）、02 的管辖范围（318 行）、03 的事实源（374 行）、04 的 Helper 契约（362 行）、05 的事实模型（551 行）、06 的行动模板（395 行）、07 的 Code 实践（401 行）、08 的 Web（255 行），才能理解 21 的 WorkCase（411 行）
- 对 AI 的实际影响：首次加载成本 3,470 行 specs，还不算附件。AI 可能在读完之前就放弃了
- 最小修正方向：**渐进式披露必须对"规范体系本身"也适用**。AI 应该只需要知道"Helper 是我的入口 → 我需要读 00 和 21 → 其它在需要时再读"，而不是"你需要先理解规范模型才能理解事实模型"

### P1（严重影响，应在 1-2 个增量内修复）

**P1-1：Helper CLI 不在 PATH 中，无法通过声明验证**

- 位置：工作推进总纲 §5、§9
- 失败场景：工作推进总纲声称 Helper CLI 已经实现、"read-specification-content 已完成 Code、tests、真实 CLI 验证和独立复核"，但 `which ldvh` 返回空，`pip install -e .` 安装到 Python 3.9 后无法导入（requires >=3.12），通过 Python 3.12 venv 安装后未验证 CLI 可执行
- 对 AI 的实际影响：AI 不能实际调用 Helper，所有"AI 可以直接调用 Helper CLI"的声明在当前环境中不可验证
- 最小修正方向：在推进总纲中如实记录当前可执行性状态，区分"Code 已实现"和"AI 可调用"是不同结论

**P1-2：WorkCase 设计复杂度与 0 个实际实例不匹配**

- 位置：specs/21-WorkCase-工作项.md（411 行）
- 失败场景：411 行定义了 7 个 phase、3 个 status、workcase-item 的 5 个内部状态、creation_reviews、独立审核、双 Human Gate、plan_version 级联失效规则——但没有任何真实的 WorkCase 被创建过
- 对 AI 的实际影响：当 AI 第一次需要创建 WorkCase 时，需要读 411 行规范 + 理解 7 个 phase 的转换规则 + 理解 status 与 phase 的正交关系 + 理解审核链。**复杂度尚未证明其价值**
- 最小修正方向：在第一个真实 WorkCase 出现前，保持 21 的设计，但**不要在 AI 首次接触时强制推送全部 411 行**。F1 卡片只投影部分字段这一设计是对的，但 AI 要理解 WorkCase 需要读全部 21

**P1-3：Code 与 AI 的边界有模糊地带**

- 位置：specs/05 §11.7（单对象 CAS 更新）
- 失败场景：Code 负责"对状态变化执行保守校验"和"拒绝不合规转换"，但"实质改变"、"审核是否真正独立"、"证据是否足够"由 AI 判断。这意味着 Code 既不能完全信任（它可能放行错误转换），AI 也不能完全信任（它以为 Code 做了校验但实际上 Code 只做了机械校验）
- 对 AI 的实际影响：AI 处于"既要相信 Code 的确定性结果，又要怀疑 Code 没有覆盖语义"的模糊地带

### P2（重要，应在后续阶段修复）

**P2-1：01 规范模型基础规范 814 行包含大量 Code 实现契约**

- 位置：specs/01-规范模型基础规范.md（814 行，所有 specs 中最长）
- 具体问题：§9.3-9.7 定义了 `read-specification-candidates` 和 `read-specification-content` 的完整输入/结果字段闭集（约 300 行），这些是 Code 的实现契约，不是 AI 需要理解的内容
- 对 AI 的实际影响：AI 如果读 01 来理解"规范模型是什么"，会被这些实现细节淹没
- 修正方向：Helper 公开操作的输入/结果字段应该由 Helper 服务契约（04 和附件）承载，01 只保留"有这些操作"的声明

**P2-2：统一字段登记 301 行，AI 创建事实对象时仍需理解**

- 位置：specs/attachments/05.Att.01-事实对象统一字段登记.md（301 行）
- 具体问题：AI 创建 WorkCase 时，需要知道必填字段、字段类型、引用结构。这些信息分散在 05（共同规则）、05.Att.01（字段登记）、21（WorkCase 专属字段）三处
- 修正方向：Helper 的 `prepare-fact-object-draft` 已经返回了 `field_contracts`（字段路径、类型、出现规则），AI 应该只需要读这个响应，不需要读 05.Att.01

**P2-3：Web 状态与 V4 设计不匹配**

- 位置：Web 代码、docs/web/V4-Web-浏览器表现基线.md
- 具体问题：Web 仍然硬编码 V3 类型、字段、状态和目录；Spark 写入没有 V4 来源与受控 Code 能力；lint 有 11 errors、14 warnings
- 对 AI 的实际影响：AI 如果通过 Web 呈现的结果来判断项目状态，可能会被 V3 遗留数据误导

### P3（值得关注，但非紧急）

**P3-1：双语术语表 01.Att.01 只有 67 行，但 01 §12 定义了 11 步检查流程**

**P3-2：archive/v3 仍有 76 个 markdown 文件，V3 覆盖收口（阶段 11）尚未开始**

**P3-3：Git 提交契约（03 §9）定义了 13 个 scope、11 个 type、复杂的 body/footer 触发结构——但只有 1 次真实 dogfood 提交**

---

## 5. 当前工作中最值得保留的设计

1. **F0-F4 分层事实发现**（05 §8.1）：恢复清单 → 基础卡片 → 候选卡片 → 完整对象 → 来源证据。这是 V4 最符合"AI 第一服务对象"的设计，避免了全量注入

2. **Helper 的 `compact` 响应档位**：总纲报告默认候选响应从 46KB 降到 12KB，L3 精确切片 6KB，这是真实的消费成本降低

3. **03 的 Working Tree 优先读取**：不要求提交才生效，未提交和未跟踪文件可以承载当前事实——这直接降低了 AI 的工作摩擦

4. **05 的机械校验与语义判断分层**：Code 只做机械校验，AI 做语义判断。这个边界是 V4 最重要的设计之一

5. **CAS 单对象更新**（05 §11.7）：content_fingerprint 为基础的比较与交换，避免并发写入冲突

6. **00 的 Human Gate 和 Stop Conditions 设计**：明确区分"必须暂停"和"必须由 Human 决定"，是防止 AI 越权的有效机制

---

## 6. 最应该删除、合并、简化或后移的内容

### 应该删除/合并的

1. **01 §9.3-9.7（约 300 行 Helper 公开操作字段闭集）** → 移入 04 和 04.Att.01，01 只保留"存在这些操作"的声明

2. **01 §12 双语术语治理（约 100 行）** → 当前的 11 步检查流程、5 个不通过条件、独立复核要求，对于只有 67 行术语表的项目来说过度设计

3. **01 §14 普通规范模板（约 60 行）** → 模板应该在 01 的正文结构规则中自然隐含，不需要单独一节

### 应该简化的

4. **21-WorkCase（411 行）→ 应在第一个真实实例出现后简化**：当前 7 个 phase 的设计是基于"完整质量控制链不因工作 duration 而缩减"的假设，但这一假设尚未被验证。建议在第一个真实 WorkCase 后根据实际使用缩减 phase 数量

5. **05 §7 统一字段登记（301 行）→ AI 不应直接读它**：`prepare-fact-object-draft` 的 `field_contracts` 响应已经包含了 AI 需要的信息，不需要强迫 AI 读 301 行字段登记表

### 应该后移的

6. **03 §9 Git 提交契约（约 100 行）** → 当前只有 1 次真实 dogfood 提交，这些规则在阶段 10（端到端 dogfood）之前不需要这么完整

7. **阶段 8（环境接入）和阶段 9（Web V4 适配）** → 当前应该继续暂停，直到阶段 6（模板）和阶段 10（dogfood）有实质性进展

---

## 7. 对当前下一步的明确建议

### 当前总纲中"下一步"的问题

总纲 §9 说："下一步可在其它候选模板的独立准入与环境薄 adapter 之间按实际重复价值和实现前置选择"。**这个"下一步"太模糊了，没有回答"AI 什么时候能真正用 V4 完成一个完整工作"这个核心问题。**

### 建议的下一步（优先级排序）

**增量 A（最优先）：闭合第一个端到端 AI 消费路径**
- 目标：AI 能完成"创建事实对象 → 读取 → 更新 → 跨会话恢复"的完整链路
- 不需要创建 mock 事实，但必须在当前开发环境中可以用真实数据走通
- 验证标准：AI 在 3 次独立会话中，每次都能在上次离开的位置恢复工作
- 这不是"证明 V4 架构正确"，而是"证明 AI 可以在 V4 中工作"

**增量 B：规范体系对 AI 的认知成本优化**
- 目标：让新 AI 首次进入 LDVH 的认知成本降到 3 个文件以内
- 具体措施：将 01 的 Helper 操作字段闭集移入 04/附件；将 05 的字段准入规则标记为"AI 不需要读"；建立一个"AI 快速入门"路径（00 → 05 共同规则 → 21/22/23/24/20 唯一定义 → 06 模板 → 04 Helper）
- 验证标准：新 AI 能在 5 分钟内理解"如何创建和更新一个 WorkCase"，而不需要读 01、02、03、07

**增量 C：第一个真实 WorkCase 的适度设计**
- 目标：在真实开发中产生第一个 WorkCase 实例
- 在创建前不要扩大 21 的设计，保持当前 411 行
- 创建后根据实际使用检查：7 个 phase 是否都必要？双 Human Gate 是否合理？审核链是否过于复杂？

**增量 D：Helper CLI 的可部署性**
- 目标：`ldvh` 命令可以在 `python3.12 -m pip install .` 后在 PATH 中找到
- 这不是"功能"问题，而是"AI 能实际使用"的前提

---

## 8. 与已有审计的差异比较

### 与双审计（GLM + GPT）的比较

**一致**：
- WorkCase 不是通用 orchestration 引擎（双审计的 P0-1 被正确拒绝）
- 建设顺序应该是先内部能力再模板
- 环境成本和第一阶段 dogfood 是真实的

**差异**：
- 双审计的严重度评级偏宽松。双审计未发现 P0 级别问题（除了被拒绝的 GPT P0-1），但**我认为 P0-2（AI 认知成本过高）是真正的 P0**，虽然它不像"系统崩溃"那样明显，但会持续侵蚀 V4 的可用性
- 双审计未对"0 个真实事实对象"给出足够高的严重度。总纲说"阶段 5 已完成并关闭"，但阶段 5 的完成条件只包括"Code 实现和测试通过"，不包括"AI 实际使用过"。**我认为"阶段 5 完成"的声明在语义上正确但价值上可疑**

### 与 WorkCase 专项审计的比较

**一致**：
- status/phase 条件冲突是真实 bug
- `waiting_on` 在 Human 确认阶段确实需要

**差异**：
- 专项审计把 P2-2（`waiting_on`）和 P2-3（Code 不判断语义）标记为"P2 rejected"，**我认为它们被拒绝是合理的**，但专项审计没有追问"为什么在 0 个真实实例的情况下 WorkCase 已经 411 行了"

---

## 9. 已读文件、实际运行检查、未覆盖范围和不确定性

### 已读文件
- 全部 specs：00-08、20-24、30-32
- 全部附件：01.Att.01、02.Att.01、04.Att.01、05.Att.01
- 工作推进总纲、架构重建基线
- 全部审计/处置记录（双审计、WorkCase 专项、类型封闭）
- 全部 investigations 摘要
- 12 个代表性 Python 源文件（helper/operations、facts/*、specs/*）
- 测试 conftest.py
- docs/web/README.md、docs/code/README.md
- pyproject.toml、Git 历史（最近 30 条）

### 实际运行检查
- 验证了 549 passed / 1 failed 的测试结果（与总纲宣称的 550 passed 基本一致，1 failure 为环境差异）
- 验证了 `pip install -e .` 在 Python 3.9 下失败（requires >=3.12）
- 验证了 `ldvh` 不在 PATH 中
- 验证了 `facts/` 目录为空（0 个真实事实对象）
- 验证了 archive/v3 有 76 个 markdown 文件

### 未覆盖范围
- 没有逐行审查全部 16,430 行 Python 代码
- 没有运行 Web 测试（`npm test`）
- 没有逐行审查 archive/v3 的所有文件
- 没有审查具体的测试用例内容

### 不确定性
- 我没有实际调用 Helper CLI 来验证其响应（因为 `ldvh` 不在 PATH 中）
- 我对 WorkCase 的 7 个 phase 是否过度的判断是基于"0 个实例"的推理，而非实际使用证据
- 我对"AI 认知成本"的评估是基于我作为 AI 阅读这些规范的主观体验，不是严格的用户研究

---

## 总结

V4 的方向是正确的。它正确地识别了 V3 的问题，做出了正确的架构选择（分层、边界区分、机械/语义分离、Helper 入口）。但它正在进入一个**"规范工程自我循环"**的早期阶段——规范越来越完整、测试越来越多、设计越来越精细，但 AI 仍然不能实际使用 V4 完成一个完整的工作链路。

**最大的风险不是设计错误，而是"完成度幻觉"**——因为规范闭合、测试通过、字段登记完整，就认为系统已经可用。但实际上，V4 还没有被任何 AI 在真实跨会话场景中使用过。

**建议以"AI 能够实际完成一个完整工作链路"作为唯一验收标准，而不是"规范是否完整、测试是否通过"。**