# Gate与Validate阶段边界矩阵

```yaml
v2_attachment:
  attachment_id: "05.Att.05"
  title: "Gate与Validate阶段边界矩阵"
  status: "draft"
  authority: "not_active_until_parent_and_human_approved"
  parent_spec: "specs-v2/05-Web信息同步规范.md"
  canonical_path: "specs-v2/attachments/05.Att.05-Gate与Validate阶段边界矩阵.md"
  purpose: "承载 05 授权的 Gate/Validate 阶段边界"
  migration_sources:
    - "specs/08-Web信息同步实现规范.md"
  code_consumption:
    - "web_gate_validate_boundaries"
```

> 文件状态：本文当前位于 `specs-v2/`，尚未切换为 active；正式 Gate/Validate Web 边界仍以 active `specs/08-Web信息同步实现规范.md` 为准。本文是 `05-Web信息同步规范.md` 授权附件，必须随父规范经过 Human 单篇确认后才可生效。

## 1. 定位

本文承载 05 授权的 Gate 与 Validate 阶段边界。

## 2. 阶段边界

| 阶段 | 可以展示 | 不得执行 |
|---|---|---|
| Gate | 待确认项、确认对象、影响范围、风险、选项 | 直接完成行动 Gate、自动授权、自动回写 |
| Validate | 验证结果、错误态、降级说明、来源命令、修复线索 | 自动修复、自动关闭任务、自动写入验证结论 |
| Gate 升级写入 | Human 选择、回写目标、风险说明 | 未补齐白名单、对象规范和证据落点前写入事实源 |
| Validate 升级写入 | 验证声明候选、失败诊断 | 绕过 08 验证声明或 07 回写边界 |

## 3. 待补齐事项

1. 行动编排候选计划形成或成员 active 后，应核对 Gate 页面是否需要消费具体成员 Gate 定义；
2. Validate 写入能力如未来出现，必须先更新 05 主体、08 和 07。
