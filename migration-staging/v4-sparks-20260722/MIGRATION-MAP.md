# Spark 迁移对照

本表只记录 V3 与暂存 V4 的语义比较和重建判断，不是当前事实对象，也不替代任何 Spark 的 `source_refs`、`summary` 或 `evolution`。

## 第一对：V3 `spark-0005` 与暂存 V4 `spark-0005`

| 项目 | 判断 |
|---|---|
| V3 输入 | `ld-vibe-harness-v3/ldvh-base/sparks/spark-0005-ldvh-web-markdown-rendering-discovery.yaml` |
| 当前 V4 输入 | `ldvh-base/sparks/spark-0005.yaml`（曾在本次迁移准备中暂存，现已恢复） |
| 身份判断 | 同号不是依据；两者都讨论 Web 对事实对象长文本的 Human 阅读，且 V4 的完整阅读边界自然承接 V3 的 Markdown 呈现发现，应合并为一个当前 Spark。 |
| 保留 V3 内容 | YAML/结构化字段仍是权威事实；长文本字段可用 Markdown 表达并由 Web 忠实阅读；Web 是事实对象阅读器而不是原始 YAML 查看器；引入外部 Markdown 正文会改变事实源组织，不能由本 Spark 默认决定。 |
| 保留暂存 V4 内容 | Web 独立完整服务 Human；Helper 的 F0–F4 渐进式投影服务 AI；可复用只读读取核心但不得形成第二事实权威；错误、来源、诊断与未解析引用必须如实呈现。 |
| 不承接 V3 内容 | `task-0052`、V3 `specs/12`、旧 Validator/Tools 路径及其“已执行/待验收”断言没有当前 V4 证据，不能写入重建对象。 |
| 重建判断 | 重建为一个 `open` V4 Spark：当前问题是 Web 完整阅读、Markdown 忠实呈现与 Helper 渐进式阅读如何共同保持单一事实权威；不把两份对象原文拼接，也不把暂存 V4 的旧 ID 视为新对象身份依据。 |
| 待验证 | 用实际 V4 页面检查 Markdown 段落、标签、编号/无序列表、演变和窄屏阅读；再判断共享只读读取核心是否值得实现。 |
| 迁入状态 | 2026-07-22：已将仍适用的 V3 Markdown 阅读边界并入当前 V4 `spark-0005`；未带入旧 Task、旧路径或旧验收断言。 |

## 第二对：V3 `spark-0034` 与当前 V4 `spark-0004`

| 项目 | 判断 |
|---|---|
| V3 输入 | `ld-vibe-harness-v3/ldvh-base/sparks/spark-0034-windows-platform-support.yaml` |
| 当前 V4 输入 | `ldvh-base/sparks/spark-0004.yaml` |
| 身份判断 | 编号不同；两者都限定原生 Windows 支持的范围与验收。V4 的“真实原生 runner 证据”是 V3 跨平台运行链路清单的当前承接，合并为一个 Spark。 |
| 保留 V3 内容 | Windows 支持应覆盖 CLI、路径与管辖判定、Git hook、AI Hook/Rules、Web/npm、测试矩阵和安装文档；它不是“Python 能跑”即可成立。 |
| 保留 V4 内容 | 只有 Human 指定并授权的原生 Windows runner 才能产生支持结论；其它平台结果不得替代；耐久性等需要 Human 决定的风险保持显式。 |
| 不承接 V3 内容 | V3 中具体脚本、旧规范路径、旧对象编号和“当前已怎样实现”的仓库状态断言未经 V4 当前来源核实，不写入当前对象。 |
| 重建判断 | 保持一个 `open`、P2 的 V4 Spark：先形成最小 Windows 验收矩阵及各面证据，再判断实施范围和是否建立 WorkCase。 |
| 迁入状态 | 2026-07-22：已将仍适用的 V3 验收面和 V4 原生 runner 证据门槛合并到当前 V4 `spark-0004`；未新增 Git 来源。 |

## 第三对：V3 `spark-0035` 与当前 V4 `spark-0002`

| 项目 | 判断 |
|---|---|
| V3 输入 | `ld-vibe-harness-v3/ldvh-base/sparks/spark-0035-adapter-payload-gap-workbuddy.yaml` |
| 当前 V4 输入 | `ldvh-base/sparks/spark-0002.yaml` |
| 身份判断 | 编号不同；两者都关注 adapter 未把真实工作对象交给核心时，target-first 与管辖判定会失去依据。V4 以通用输入完整性承接 V3 的具体现场发现。 |
| 保留 V3 内容 | adapter 与核心判定是不同层；必须验证原始 payload/target/cwd 的端到端交接；未知输入不能静默伪装成明确非管辖；受管 adapter 与现场脚本需要分层。 |
| 保留 V4 内容 | adapter 不得猜测或补全输入；结论需要多环境、关键事件的真实输入证据。 |
| 不承接 V3 内容 | WorkBuddy 的具体目录、旧脚本/规范路径、旧 workcase 结论和旧行为分级没有作为 V4 当前实现状态写入；它们只用于识别待验证问题。 |
| 重建判断 | 保持一个 `open`、P2 的 V4 Spark：先建立最小实际环境矩阵，再判断何种 adapter、诊断或安装验收工作需要进入 WorkCase。 |
| 迁入状态 | 2026-07-22：已将 V3 的适配层断裂经验合入当前 V4 `spark-0002`；未新增 Git 来源。 |

## 第四对：V3 `spark-0032` 与当前 V4 `spark-0001`

| 项目 | 判断 |
|---|---|
| V3 输入 | `ld-vibe-harness-v3/ldvh-base/sparks/spark-0032-multi-environment-hook-compatibility.yaml`（V3 已 resolved） |
| 当前 V4 输入 | `ldvh-base/sparks/spark-0001.yaml` |
| 身份判断 | 并非同一历史状态：V3 处理多环境 Hook 的适配与分流，V4 处理目标环境中 lifecycle 与真实工作对象范围。两者共享“协议不等于环境适配”的边界，后者仍有当前未决问题。 |
| 保留 V3 内容 | 环境事件、payload、信任、注册和阻断语义不能互相假设；协议层应与 adapter 分层；Hook 触发不应替代对实际工作对象的判断。 |
| 保留 V4 内容 | 不预设固定事件闭集；宽 workspace 下必须以实际范围证据和 Human 歧义决策收敛受管项目。 |
| 不承接 V3 内容 | 已 resolved 的 probe/workcase、旧环境矩阵、插件分发路径、旧 Hook-only 结论及其环境能力断言没有被作为当前 V4 事实重写。 |
| 重建判断 | 不新建对象；将可复用的分层边界补入仍 `open` 的 V4 `spark-0001`，保持它只追踪当前对象范围与恢复契约。 |
| 迁入状态 | 2026-07-22：已迁入适配分层的有效边界；V3 已完成的部署结论留在审计材料，未冒充为 V4 当前验证。 |

## 第五对：V3 `spark-0023` 与当前 V4 `spark-0003`

| 项目 | 判断 |
|---|---|
| V3 输入 | `ld-vibe-harness-v3/ldvh-base/sparks/spark-0023-ldvh-capability-assurance-environment-landing.yaml` |
| 当前 V4 输入 | `ldvh-base/sparks/spark-0003.yaml` |
| 身份判断 | V3 的范围更宽，讨论能力资产与环境落地责任链；其中安装、验证、现场状态分层的部分直接支撑 V4 的安装与配置拓扑议题。 |
| 保留 V3 内容 | 规范变化不能自行证明环境部署；能力资产、环境配置和实际安装状态必须分开；只读检查可以暴露同步影响但不越权改写环境。 |
| 保留 V4 内容 | 安装前/后、配置位置、停用/卸载和故障定位都要形成可诊断闭环；有限证据不得外推为跨环境完成。 |
| 不承接 V3 内容 | 旧 `capability-environment` 命令、旧 Rules/Skill/Hook 登记、旧环境状态和“后续所有工作”的优先级结论没有作为 V4 当前事实保留。 |
| 重建判断 | 不新建对象；将与环境落地直接相关的责任链收敛到 `open` 的 V4 `spark-0003`，其余宽泛能力治理不由此对象承接。 |
| 迁入状态 | 2026-07-22：已迁入安装责任链的有效边界；未新增 Git 来源。 |

## 第六对：V3 `spark-0033` 与当前 V4 `spark-0018`

| 项目 | 判断 |
|---|---|
| V3 输入 | `ld-vibe-harness-v3/ldvh-base/sparks/spark-0033-runtime-operation-assurance-hook-agent-deployment.yaml` |
| 当前 V4 输入 | `ldvh-base/sparks/spark-0018.yaml` |
| 身份判断 | V3 讨论 Hook 与 Agent 入口如何形成持续运行链路；V4 单独追问无 Hook 时的主动约束。后者承接前者关于恢复、委派和事实消费的未验证部分，但不接受旧双入口结论。 |
| 保留 V3 内容 | 需要在新会话、子 Agent、压缩恢复、工作树切换和 Skill 调用中验证事实消费、STOP/Human Gate 与证据回收；脚本触发不等于 Agent 已实际消费约束。 |
| 保留 V4 内容 | Hook 的自动注入、阻断和审计不能由规则文档或 Agent 自律冒充；无 Hook 运行必须单独证明主动链路及其失效面。 |
| 不承接 V3 内容 | 旧 Hook/Rules 双入口、环境部署状态、receipt 路径、旧 P0 排序、旧具体 WorkCase 和“已具备”实现断言均未作为 V4 当前事实迁入。 |
| 重建判断 | 不新建对象；将有效的连续运行与恢复场景补入 `open` 的 V4 `spark-0018`，保持其对“无 Hook 不等于有强制力”的独立边界。 |
| 迁入状态 | 2026-07-22：已迁入持续消费与恢复场景；未新增 Git 来源。 |

## 第七对：V3 `spark-0004` 与当前 V4 `spark-0025`

| 项目 | 判断 |
|---|---|
| V3 输入 | `ld-vibe-harness-v3/ldvh-base/sparks/spark-0004-multi-project-governance.yaml` |
| 当前 V4 输入 | `ldvh-base/sparks/spark-0025.yaml`；其 Web 子问题由 `spark-0021` 单独承接。 |
| 身份判断 | V3 提出项目边界与项目集合缺口，V4 `spark-0025` 已是跨层多项目成熟度入口；V3 的 Web 聚合子问题不复制到该对象。 |
| 保留 V3 内容 | 不预设新增项目集合对象；需要先说明什么构成项目，以及现有管辖配置、事实对象与消费视图能否承接分组和聚合。 |
| 保留 V4 内容 | 多项目必须同时审视聚合与隔离、跨层隐含单项目假设及独立缺口分流。 |
| 不承接 V3 内容 | 已废弃的 Profile 路径和“通过某字段扩展”的历史候选不作为当前模型方案。 |
| 重建判断 | 不新建对象；将项目边界/集合的约束迁入 `open` 的 V4 `spark-0025`，Web 具体作用域仍由 `spark-0021` 保持独立。 |
| 迁入状态 | 2026-07-22：已迁入项目边界与“不预设新集合对象”的判断；未新增 Git 来源。 |

## 第八对：V3 `spark-0004` 的 Web 子问题与当前 V4 `spark-0021`

| 项目 | 判断 |
|---|---|
| V3 输入 | `ld-vibe-harness-v3/ldvh-base/sparks/spark-0004-multi-project-governance.yaml`（仅项目集合/聚合子问题） |
| 当前 V4 输入 | `ldvh-base/sparks/spark-0021.yaml` |
| 身份判断 | V3 的一个宽泛多项目问题分解到多个 V4 对象；此处只承接 Web 聚合不应自行创建项目集合模型的约束。 |
| 保留内容 | “全部”是 Web 消费作用域，不预设新事实类型；归属标识、同号消歧、读写边界和数据覆盖必须直接呈现。 |
| 不承接内容 | 项目定义、配置拓扑和跨层成熟度不复制到 Web 对象，分别保持在 `spark-0007`、`spark-0025`。 |
| 重建判断 | 保持 `open` 的 V4 `spark-0021`；不建立新 Spark。 |
| 迁入状态 | 2026-07-22：已补入项目集合约束并明确与相邻 Spark 的分工；未新增 Git 来源。 |

## 第九对：V3 `spark-0025` 与当前 V4 `spark-0024`

| 项目 | 判断 |
|---|---|
| V3 输入 | `ld-vibe-harness-v3/ldvh-base/sparks/spark-0025-entry-trigger-chain.yaml` |
| 当前 V4 输入 | `ldvh-base/sparks/spark-0024.yaml` |
| 身份判断 | V3 讨论入口触发与先路由再行动；V4 聚焦 AI 已语义选中项目后的 Helper-first 续接。后者承接 V3 的顺序和分层约束，但不继承其旧 Hook/知识地图方案。 |
| 保留 V3 内容 | AI 的语义判断不能交给 Code；Code/Helper 只能校验可观察事实并暴露不确定性；先路由、再读取、再行动、再验证需要可观察。 |
| 保留 V4 内容 | 选中候选项目后仍须以显式 worktree locator 重新解析；只有 `governed_single` 与实际能力范围成立后才能 Helper-first 续接。 |
| 不承接 V3 内容 | 旧 Hook-only、具体命令、semantic claim 字段、receipt 形式、Rules/AGENTS 重构、环境安装和部署结论没有被作为 V4 当前方案。 |
| 重建判断 | 不新建对象；将顺序和职责分层迁入 `open` 的 V4 `spark-0024`。 |
| 迁入状态 | 2026-07-22：已迁入顺序保障与语义/可观察事实分层；未新增 Git 来源。 |

## 第十对：V3 `spark-0004`、`spark-0041` 与当前 V4 `spark-0007`

| 项目 | 判断 |
|---|---|
| V3 输入 | `spark-0004-multi-project-governance.yaml`（管辖子问题）与 `spark-0041-user-local-ldvh-config-directory.yaml` |
| 当前 V4 输入 | `ldvh-base/sparks/spark-0007.yaml` |
| 保留内容 | 工作区主登记与用户级便利层必须分开；用户级位置不能自动成为主登记或环境状态；多项目不预设新集合事实类型。 |
| 不承接内容 | 旧 Git 迁移记录、目录变量、跨平台默认路径、旧配置解析顺序和任何已安装/已写入断言。 |
| 重建判断 | 保持 `open` 的 V4 `spark-0007`，只追踪当前产品边界与 Human 决定；具体安装和实现保持由 `spark-0003` 承接。 |
| 迁入状态 | 2026-07-22：已移除机械 Git 来源，补入独立 intent、可读摘要与演变。 |

## 第二十一对：V3 `spark-0001` 与当前 V4 `spark-0001`、`spark-0024`

| 项目 | 判断 |
|---|---|
| 保留内容 | 会话开始与用户输入是不同阶段：前者恢复当前可见范围，后者由 AI 判断任务目标后以真实 target 重新解析。 |
| 分流 | lifecycle、工作对象范围与宽 workspace 歧义进入 `spark-0001`；已选中项目后的 Helper-first 续接进入 `spark-0024`。 |
| 不承接内容 | 旧 user-input canonical event、receipt 字段、acknowledge 写入、旧 Hook 机制及任何已实现结论。 |
| 重建判断 | 不新建对象；分别补入两个已有 V4 Spark 的独立边界。 |
| 迁入状态 | 2026-07-22：已将会话/用户输入分层补入 V4；未新增 Git 来源。 |

## 第二十对：V3 外部网页调研线索与当前 V4 `spark-0017`

| 项目 | 判断 |
|---|---|
| 保留内容 | 获取方式、来源记录、成本、隐私、robots、许可证和失败语义必须分开；外部输入不等于事实。 |
| 不承接内容 | 过期 API/价格/功能、插件接入、provider 结论、Git 版本和任何实际服务已验证断言。 |
| 重建判断 | 保持 `open` 的 V4 `spark-0017`，先证明现有研究手段存在证据缺口，再决定是否受限接入外部网页能力。 |
| 迁入状态 | 2026-07-22：已移除机械 Git 来源，补入独立 intent、可读摘要与演变。 |

## 第十九对：V3 路由与索引线索与当前 V4 `spark-0016`

| 项目 | 判断 |
|---|---|
| 保留内容 | 导航与索引只能缩小阅读范围；事实召回、正式关系和代码索引具有不同来源与失效边界。 |
| 不承接内容 | 旧 Knowledge Map、tool-plan、会话记忆、第三方产品能力、测试与 Git 版本。 |
| 重建判断 | 保持 `open` 的 V4 `spark-0016`，分别证明三类导航能力的实际需求。 |
| 迁入状态 | 2026-07-22：已移除机械 Git 来源，补入独立 intent、可读摘要与演变。 |

## 第十八对：V3 Web 阅读线索与当前 V4 `spark-0015`

| 项目 | 判断 |
|---|---|
| 保留内容 | 完整事实应具有 Human 可扫描的顺序、层次和来源回指；Web 不能把视觉呈现升级为事实或证据。 |
| 不承接内容 | 旧页面、字段模型、WorkCase 状态、视觉方案、测试结果与 Git 版本。 |
| 重建判断 | 保持 `open` 的 V4 `spark-0015`，按事实类型分别设计阅读表达；WorkCase 最后单独处理。 |
| 迁入状态 | 2026-07-22：已移除机械 Git 来源，补入独立 intent、可读摘要与演变。 |

## 第十七对：V3 输出格式研究线索与当前 V4 `spark-0014`

| 项目 | 判断 |
|---|---|
| 保留内容 | 原始事件、执行结果、最终状态、证据与 Human 表达须分层，同一可观察结果可有不同消费投影。 |
| 不承接内容 | 旧 Envelope、字段清单、产品比较、审计结论、Git 版本和已实现断言。 |
| 重建判断 | 保持 `open` 的 V4 `spark-0014`，只追踪稳定输出契约与失败语义。 |
| 迁入状态 | 2026-07-22：已移除机械 Git 来源，补入独立 intent、可读摘要与演变。 |

## 第十六对：V3 lifecycle 分类线索与当前 V4 `spark-0013`

| 项目 | 判断 |
|---|---|
| 保留内容 | 动作分类必须基于真实副作用、target 与当前 Gate，并以目标环境事件做正反例和停用恢复验收。 |
| 不承接内容 | 工具/命令白名单、旧 receipt、旧插件、事件能力、Git 版本与任何具体 adapter 已实现结论。 |
| 重建判断 | 保持 `open` 的 V4 `spark-0013`，只在 Human 选定环境、事件和对象范围后进入具体分类。 |
| 迁入状态 | 2026-07-22：已移除机械 Git 来源，补入独立 intent、可读摘要与演变。 |

## 第十五对：V3 渐进式披露线索与当前 V4 `spark-0012`

| 项目 | 判断 |
|---|---|
| 保留内容 | 结果优先、按需精确展开、来源与缺口可回指；短投影不能替代正式规则源。 |
| 不承接内容 | 旧 action-guide、旧 Git 版本、旧“已完全承接”判断与一次性恢复路径的泛化结论。 |
| 重建判断 | 保持 `open` 的 V4 `spark-0012`，区分已完成的恢复投影和未解决的一般 Helper 渐进式消费。 |
| 迁入状态 | 2026-07-22：已移除机械 Git 来源，补入独立 intent、可读摘要与演变。 |

## 第十四对：V3 Skill 封装线索与当前 V4 `spark-0011`

| 项目 | 判断 |
|---|---|
| 保留内容 | 能力资产需要明确职责、触发、验证与部署边界，且必须由实际重复痛点和净收益证明。 |
| 不承接内容 | 旧 registry、trigger schema、外部产品能力、环境状态与 Git 版本。 |
| 重建判断 | 保持 `open` 的 V4 `spark-0011`，不因历史工具或文件存在自动建设能力资产。 |
| 迁入状态 | 2026-07-22：已移除机械 Git 来源，补入独立 intent、可读摘要与演变。 |

## 第十三对：V3 协作与 worktree 线索与当前 V4 `spark-0010`

| 项目 | 判断 |
|---|---|
| 保留内容 | 主控必须定义委派边界、验证与结果回收；子 Agent、独立复核、新对话和 worktree 都是条件化协作手段。 |
| 不承接内容 | 固定角色、自动团队、旧 WorkCase、旧状态机、Git 版本与任何跨客户端能力结论。 |
| 重建判断 | 保持 `open` 的 V4 `spark-0010`，先形成当前环境的协作治理，再由具体目标进入 WorkCase。 |
| 迁入状态 | 2026-07-22：已移除机械 Git 来源，补入独立 intent、可读摘要与演变。 |

## 第十二对：V3 Harness 研究线索与当前 V4 `spark-0009`

| 项目 | 判断 |
|---|---|
| 保留内容 | Harness 成熟度取决于发现、读取、能力调用、验证、回收、恢复和清理的真实闭环，不取决于 Agent 数量或某个外部产品。 |
| 不承接内容 | 旧 Git 版本、Symphony/Linear/Codex App Server 方案、旧实验顺序与任何已接入结论。 |
| 重建判断 | 保持 `open` 的 V4 `spark-0009`，先形成成熟度证据，再决定最小持续编排实验。 |
| 迁入状态 | 2026-07-22：已移除机械 Git 来源，补入独立 intent、可读摘要与演变。 |

## 第十一对：V3 `spark-0006`、`spark-0022` 与当前 V4 `spark-0008`

| 项目 | 判断 |
|---|---|
| 保留内容 | WorkCase 需要稳定的目标、阶段、等待、审核与证据契约；协作运行结果必须由主控回收，不得把运行期细节写成事实状态。 |
| 不承接内容 | V3 的 Task/TaskPlan 状态机、旧 WorkCase、旧 worktree 实现与 Git 历史断言。 |
| 重建判断 | 保持 `open` 的 V4 `spark-0008`，只验证现有 WorkCase 状态是否足够，不引入调度器或外部任务系统。 |
| 迁入状态 | 2026-07-22：已移除机械 Git 来源，补入独立 intent、可读摘要与演变。 |
