# 行动编排规范

```yaml
v2_candidate_doc:
  doc_id: "07"
  status: candidate_notes
  authority: none_until_human_approved
  planned_domain: "行动编排框架、Context、Scenario、Gate、执行、证据"
  source_candidate:
    - "specs/06-行动编排基础规范.md"
    - "specs/03.03-行动编排文档规范.md"
    - "specs/30-action-orchestration-governance-行动编排创建审核与治理.md"
    - "specs/41-fact-model-audit-事实模型审核.md"
    - "specs/42-specs-audit-规范审核.md"
    - "specs/43-assurance-readiness-audit-保障准备度审核.md"
    - "specs/44-git-commit-orchestration-Git提交编排.md"
```

> 候选状态：本文是 v2 候选记录文件，不承载正式规范正文。正式行动编排规则仍以 v1 `06`、`03.03` 和 `30-59` active 主文件为准。

## 1. 定位

本文拟承接行动编排通用规则和行动编排文档机制，但不得把 30-59 具体流程简单合并成一个总流程。

v2 应保留以下分层：

| 层 | 候选职责 |
|---|---|
| 07 主规范 | Context、Scenario、Gate、主控调度、Skill/Agent、Code 协作、流程回写触发、证据原则 |
| 30-59 成员主文件 | 具体流程的准入、场景、执行步骤、Gate、回写、证据、可测试性锚点 |
| 30 | 行动编排资产创建、审核和治理流程 |
| 41 | 事实模型审核流程 |
| 42 | 基础规范审核流程 |
| 43 | 保障准备度审核流程 |
| 44 | Git 提交编排流程 |

## 2. 不得丢失的机制

若 v2 不保留 `03.03` 子文档形式，07 必须承接：

1. 30-59 active 判定；
2. 成员自描述契约；
3. Context、Scenario、Gate、执行、回写、证据、可测试性锚点；
4. `assurance_takeover`；
5. `capability_assets`；
6. Code 派生集合索引输入。

## 3. 与 04 的分工

04 维护全局事实源、证据和回写原则。07 只维护行动执行中的触发、Gate 联动、主控交还、Skill/Agent 输出分流和流程特有证据要求。

07 不得重定义最终事实源原则。

## 4. 待核对事项

1. 是否将过程输出和回写通用规则移交给 v2 04；
2. 本文只保留行动编排特有判断、调度和交还规则；
3. 30-59 具体行动编排如何保留流程特有规则、成员自描述和可测试性锚点；
4. 30、41、42、43、44 是否继续作为具体流程主文件逐篇核对；
5. Code v2 如何兼容 workflow member、`assurance_takeover` 和 `capability_assets`。
