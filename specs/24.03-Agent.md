# Intent — Agent 实践

> 所属主文档：`specs/24-Intent-意图.md`
> 子文档槽位：24.03-Agent
> 状态：not-created

## 1. 不创建理由

Intent 对象不需要独立 Agent。Intent 的创建、分析和状态流转可在主上下文中完成，不需要独立上下文或并行分析。待多角色思考工作流程（51）稳定后，如需 Intent 特定的 Agent 实践，再创建本子文档。

## 2. 检查项

1. Intent 创建和分析是否可在主上下文中完成；
2. 是否存在需要独立上下文处理 Intent 的场景。
