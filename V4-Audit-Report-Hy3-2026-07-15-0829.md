# V4 架构方向独立审计报告

**审计模型**：Hy3
**审计时间**：2026-07-15 08:29 (GMT+8)
**审计性质**：严格只读（未修改/创建/删除/暂存/提交任何文件；本文件为按用户要求落盘的审计报告，非仓库既有产物）
**审计对象**：`/Users/dmh2002/poker_hud_projects/ld-vibe-harness-v4`
**审计方法**：先独立读 `specs/00` 形成自己的判断 → 再读总纲/规范/字段登记/WorkCase → 抽查 Code/tests → 自查 Git 状态 → 形成结论 → 最后读两份外部审计做差异比对（不继承其结论与严重度）→ 意图层比对 `archive/v3`。

---

## (1) 总体判定

**判定：基本正确，但需要校正（在"构建顺序 / 消费闭环验证"轴上已偏航，核心设计方向正确）。**

理由：V4 的**内核机制**（Helper 作为统一 AI 入口、渐进式披露 L0–L4/F0–F4、Code/AI 边界、事实模型五型与墓碑机制、compact/diagnostic 响应档）与 `specs/00` 的"AI 执行体是第一服务对象"高度自洽，且我已**在代码中验证**这些机制确实落地（见第 4、8 节）。但项目在"规范闭合 / 字段治理 / 审计自证"上投入过重，而价值前提——"端到端 AI 消费闭环能真正降低 AI 成本"——**至今未验证**（总纲 Phase 10 标注"未验证"），且下一步仍在继续扩张能力面（模板、适配器）而非先补上这关键一环。**设计站在 AI 这边，但工程的重心已滑向"把规范体系本身做严谨"，这与 00 的初衷出现了张力。**

---

## (2) V1–V8 价值准则证据矩阵

八项价值准则来自 `specs/00`：快速定位 / 充分理解 / 边界识别 / 稳定推进 / 据实判断 / 工作接续 / 清晰沟通 / 持续积累。

| 准则 | 服务 AI 的设计证据（已验证/可核验） | 偏"服务规范治理"的信号 | 判定 |
|---|---|---|---|
| **V1 快速定位** | Helper `specification_candidates`(L0 累积索引)、`fact_candidate`(F0 恢复清单+F1 基线卡)、`action_template_candidates`；代码已验证 `specification_content.py:111-170` 的 L3 精确标题切片 | 定位需先穿越 01+02+03 治理范围链才能处理一个受管提交 | ✅ 机制在，路由被治理门控 |
| **V2 充分理解** | L3 精确切片返回整节；F3 全量对象、F4 源头证据；L0–L4 阶梯 | 每条规则仍嵌在"7 列验证表 + Human Gate + Stop Conditions"的工序话术中，理解被工序噪声淹没 | ⚠️ 内容在，但阅读负担高 |
| **V3 边界识别** | `00 §5`、 `04 §9 line 259`、 `05` Code/AI 切分明确："Code 不得自行补充或重新解释来源规则…不得以确定性结果替代 AI 语义判断" | 边界本身靠庞大治理机器强制， enforcement 成本偏高 | ✅ 边界真实，成本高 |
| **V4 稳定推进** | WorkCase 7 阶段 + 正交 status/phase、plan_version/result_version 绑定、双 Human Gate | 生命周期机器本身可能成为"被推进的对象"而非工作（GPT P0-1：WorkCase 50+ 字段有复辟 V3 编排之嫌） | ⚠️ 需裁剪到被消费字段 |
| **V5 据实判断** | 事实模型 F0–F4、准入审计证据文件、source-of-truth=Git Working Tree（非 HEAD/cache） | 准入审计本身 5 步机器（准入引用→准入记录→独立复核→绑定→专属定义）偏重 | ✅ 意图对，机器重 |
| **V6 工作接续** | F0 恢复清单、WorkCase `supersedes/depends-on/routed-to`、plan_version 绑定 | 机制存在，但**未经 dogfood 证明** | ⚠️ 未验证 |
| **V7 清晰沟通** | compact/diagnostic 响应档（12KB/46KB），代码验证 `responses.py:58,74-75` 默认 compact 且枚举校验 | GLM P0-2：响应信封 13 个顶层字段仍偏复杂，AI 解析成本高 | ⚠️ 档位对，信封重 |
| **V8 持续积累** | 事实五型（Spark/WorkCase/ADR/Pitfall/Study）+ 统一字段墓碑(promote/retired) | `05 §7.1.8` 字段变更需锁步重评所有受影响类型；`§7.2` 每增一字段需全局去重审计+独立语义复核 → 积累被高门槛门控 | ⚠️ 设计好，门槛早 |

---

## (3) "真正服务 AI" vs "主要服务规范/审计" 的定性比例

基于上述证据与代码抽查，我的独立估计：

- **真正服务 AI 消费**：约 **45%**。集中在 Helper 统一入口、渐进式披露、Code/AI 边界、事实模型意图、响应档——这些是 AI 每轮真实能用到的。
- **主要服务规范维护/审计自证**：约 **55%**。集中在：36 处 Human Gate/Stop/Verification 表（几乎每个非附件规范重复 7 列验证表）、字段登记 5 步准入机器、WorkCase 50+ 字段生命周期、响应信封复杂度、以及"先规范闭合再证明消费"的构建顺序。

**与外部审计的差异比对（不继承其结论）**：
- GLM5.2（"基本正确，需要校正"，AI≈40–50%）：其 P0-1"渐进式披露未在代码落地"**已被项目自身的审计→校正闭环推翻**——我实测 L3→L4、F0/F1/F2 均已实现，且总纲日期晚于该审计。说明项目**确有可用的校正回路**，这是加分项。
- GPT-5.6（"结构性偏航"，AI≈30–40%）：其根因（规范闭合/审计优先于真实消费路径、Phase 10 未验证）与我独立结论**收敛**，但我对其"偏航"的严重度定性偏乐观——核心设计并未背离 00，只是工程重心滑移。

**结论**：两份审计在根因上一致，严重度上我取中间值；我的 45/55 落在二者之间，但在"方向"上支持 GLM（内核正确），在"紧迫性"上支持 GPT（消费闭环必须立即补证）。

---

## (4) 问题清单（P0–P3，含 file+line / 失效场景 / AI 影响 / 最小修复）

### P0（结构性，须在继续扩容前解决）
**P0-1｜AI 消费闭环（Phase 10 dogfood）至今未验证**
- 位置：`docs/v4-architecture/active/V4-工作推进总纲.md` Phase 10 标注"未验证"；构建顺序显示下一步是模板/适配器。
- 失效场景：12 个构建阶段 + ~550 测试通过后，仍无一次真实多轮 AI 任务跑通 Helper 来证明"闭环降低了 AI 的 定位/理解/判断/接续/验证/交接/恢复 成本"。所有"服务 AI"主张仍是设计意图，非实测。
- AI 影响：价值前提悬空——可能建出一套精美但 AI 尚未真正受益的 harness。最高风险。
- 最小修复：在加模板/适配器前，跑一次真实多轮任务端到端穿过 Helper，发布"前后 AI 成本"实测对照。

**P0-2｜单动作治理成本相对"已证明的 AI 价值"过高（且过早苛刻）**
- 位置：`specs/05-事实模型基础规范.md` §7.1.8（字段变更锁步重评所有受影响类型）、§7.2（每增一字段全局去重审计+独立语义复核）；`specs/attachments/05.Att.01` 增一字段=跨 2–3 文件 5 处同步编辑；字段登记约 115 行。
- 失效场景：当前事实体量未知/可能不大，却已施加"企业级"治理税。每加一个字段就触发全类型重评+独立复核+5 处同步，AI 在真实任务中为了记一条 Pitfall 要付出远超其收益的流程成本。
- AI 影响：把"降低 AI 成本"做成"为降低未来成本而先抬高当前成本"，在价值未证前是净负担。
- 最小修复：在 dogfood 证明价值前，将 §7.1.8 / §7.2 降级为"按需触发"而非"每次强制"；字段增改走轻量快速通道。

### P1（重要，应在一个校正增量内处理）
**P1-1｜WorkCase 相对 00 意图过度铺陈**
- 位置：`specs/21-WorkCase-工作项.md`（50+ 字段、7 阶段、双 Human Gate）。00 明确要避免 V3 编排膨胀。
- 失效场景：AI 在真实接续时只需少数字段，却要解析 50+ 字段生命周期；且 GPT P0-1 指其有"复辟广义编排"之嫌。
- AI 影响：接续理解成本被自身生命周期吞掉。
- 最小修复：审计 50+ 字段中哪些被 AI 实际消费，未消费字段降级为可选/移除。

**P1-2｜响应信封复杂度**
- 位置：`code/ldvh/helper/responses.py`（默认 compact 但顶层仍为 13 字段，GLM P0-2 真实）。
- AI 影响：每轮解析 13 顶层字段，解析 token 与出错面增大。
- 最小修复：压平至 <8 个必要字段，元数据下沉到信封副层。

**P1-3｜总纲测试数内部不一致**
- 位置：`V4-工作推进总纲.md` 各处 462 / 466 / 550 不一致（我实测 549 通过 + 1 失败）。
- AI 影响：削弱对"已验证"主张的信任，间接污染 V1–V8 的"据实判断"。
- 最小修复：统一为单一来源数字。

**P1-4｜每规范重复 7 列验证表 + Human Gate + Stop Conditions（约 36 处）**
- 位置：所有非附件规范（01–08、20–24 等，子代理映射确认）。
- AI 影响：AI 为理解"一条规则"需跨文件吞入大量工序话术，V2 充分理解被噪声淹没。
- 最小修复：抽一份唯一规范验证附录，各规范以链接引用，删除内联重复。

### P2（中等，可合并简化）
**P2-1｜Code/AI 边界在 00/04/05 重复陈述** → 收为一份唯一声明，其余引用（P2）。
**P2-2｜字段登记 5 步准入机器**（`05.Att.01` 准入引用→记录→复核→绑定→专属定义）在现有事实体量下偏重 → 合并为更少步骤或单文件（P2）。
**P2-3｜治理范围链 01+02+03+30** 才能处理一个受管提交 → 提供"受管提交快速通道"或折叠（P2）。

### P3（轻微）
**P3-1｜`code/tests/helper/test_service.py:256`** 失败：对操作键**列表顺序**的脆弱断言（实际多了 `resolve-governance-scope`、`read-action-template-candidates` 两项）。非功能缺陷，但污染"全绿"信号。最小修复：改为子集/排序断言。
**P3-2｜命名不一致**：`ldvh` / `LDVH` / `LaoDing Vibe Harness` 在文档间混用 → 定一个规范名。

---

## (5) 最值得保留的设计（守住了 00 的灵魂）

1. **Helper CLI 作为统一 AI 入口**（8 操作 + 2 行动模板操作）——正确的抽象，AI 只有一个门。
2. **渐进式披露 L0–L4 / F0–F4**——已代码验证（`specification_content.py:111-170`、`fact_candidate_operation.py`），真正降 token 成本。
3. **Code/AI 边界**（`00 §5`、`04 §9`）——项目之魂，防止 Code 伪造语义判断。
4. **compact/diagnostic 响应档**——已验证（`responses.py:58,74-75`）。
5. **事实模型五型 + 墓碑(promote/retired)**——支撑 V8 持续积累且不膨胀。
6. **source-of-truth = Git Working Tree（非 HEAD/cache）**——正确。
7. **验证纪律陈述**（spec 活跃 ≠ 代码实现 ≠ 测试通过 ≠ 环境集成 ≠ 工作完成）——应作为常驻横幅。

---

## (6) 最可删 / 合并 / 简化 / 推迟的内容

- **推迟**：行动模板（Phase 6–7）、适配器——在 dogfood 证明核心闭环前不扩容（对应 P0-2）。
- **简化**：36 处重复验证/Gate/Stop 表 → 抽唯一附录以链接引用（P1-4）。
- **合并**：字段登记 5 步准入机器 → 更少步骤/单文件（P2-2）。
- **简化**：WorkCase 字段集 → 裁剪到被消费字段（P1-1）。
- **推迟**：`05 §7.1.8` 全类型锁步重评 / `§7.2` 每字段独立复核 → 价值证前改为按需触发（P0-2）。
- **删除**：04/05 中对 Code/AI 边界的冗余重述 → 收为唯一声明（P2-1）。

---

## (7) 下一步建议与推荐增量顺序

**核心建议**：先把"价值证明"补上，再谈"能力扩张"。当前最危险的是在闭环未证的情况下继续堆规范面。

**推荐增量（按优先级）**：
1. **增量 A（证明价值，最高优先）**：用一次真实多轮 AI 任务端到端跑通 Helper，实测"前后 AI 成本"并发布为 Phase 10 证据。这是解锁所有"服务 AI"主张的唯一动作。
2. **增量 B（降治理税）**：合并重复验证/Gate/Stop 表为唯一附录；响应信封压到 <8 必要字段；WorkCase 裁到被消费字段；§7.1.8/§7.2 改按需触发。
3. **增量 C（仅 A+B 之后）**：恢复模板/适配器建设——此时已有被证明的消费路径来锚定范围，避免再次滑向"为规范本身而规范"。

---

## (8) 审计覆盖与不确定项

**已读文件**：
- `specs/00-理念与构成.md`、`docs/v4-architecture/active/V4-工作推进总纲.md`、`specs/04`、`specs/21`、`specs/05`、`specs/06`、`specs/attachments/05.Att.01-事实对象统一字段登记.md`
- 经子代理映射（未逐字深读）：`specs/01,02,03,07,08,20,22,23,24` 及附件，共约 15 文件 ≈ 3,945 行、~23 枚举登记、~36 处 Gate/Stop/Verification 表
- 两份根级外部审计：`V4-Audit-Report-GLM5.2-2026-07-14-1524.md`、`V4-Audit-Report-GPT-5.6-SOL-极高-2026-07-14-1533.md`

**已运行检查**：
- `pip install -e .` 后可编辑安装，解决 `ModuleNotFoundError: No module named 'ldvh'`
- `pytest`：**549 passed / 1 failed**（`test_service.py:256` 列表顺序脆弱断言，非功能缺陷）
- `ruff`：All checks passed
- 代码核验：`specification_content.py:111-170`（L3 精确切片 ✅）、`fact_candidate_operation.py`（F0/F1/F2 ✅）、`responses.py:58,74-75`（compact 默认+枚举校验 ✅）

**未覆盖 / 仅浅层**：
- **`archive/v3` 深度比对未完成**：仅通过 `specs/21` 对"拒斥 V3 编排膨胀"的显式声明做了意图层判断，未逐文件深读 v3 规范以量化"保留了什么意图 / 只是加了抽象"。这是用户 prescribed 顺序 (e) 的缺口，建议补做。
- **`web/`（17,669 文件）仅点查**，未评估其对 AI 消费的实质贡献。
- **适配器尚未构建**，无法审计其范围是否合理。
- **真实 dogfood 运行我未执行**（只读且需真实 AI 消费），故"闭环降本"仅能依据总纲状态标注判断为"未验证"。

**不确定项**：
- 当前真实测试数以我实测为准（549+1），总纲 550 略高估。
- WorkCase 50+ 字段中有多少被 AI 实际消费，需增量 B 中实测确认（决定是否裁剪）。
- 字段治理税在当前事实体量下是否"过早"取决于未知的事实数量——若事实已多则 P0-2 可降级。

---

**一句话收尾**：方向对、灵魂在、机制已落地；但工程重心滑向了"把规范体系做严谨"而非"先证明 AI 真的省了成本"。先补 Phase 10 dogfood 实测，再把治理税降下来，最后才扩张模板/适配器——这是把 V4 拉回 00 的最短路。
