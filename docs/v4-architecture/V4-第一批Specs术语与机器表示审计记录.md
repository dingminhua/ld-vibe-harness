# V4 第一批 Specs 术语与机器表示审计记录

> 记录性质：本文按 `specification-model-foundation` §12 检查第一批 04–08 草案中新出现、重复使用或进入机器接口的名称。本文不是规则源；候选名称只有被相应定义来源吸收、完成独立复核并按 01 登记后，才可能成为 `current`。

## 1. 检查范围与方法

本次检查覆盖：

1. `specs/04-LDVH Helper CLI 服务契约规范.md` 的公开操作、能力发现结果、服务结果和规则适用判断结果；
2. `specs/05-事实模型基础规范.md` 的“事实类型 / Fact Type”和“事实对象 / Fact Object”；
3. 06–08 的标题、H2、表头和重复出现的责任名称；
4. Human 后续确认的 07、08 中文标题及其英文名称和 `spec_key`。

检查顺序为：先说明概念和使用目的，再判断是否已有术语可承担，随后比较中英文名称、机器表示、正反例、相邻概念、替换影响和是否需要双语术语表登记。外部工程表达不因常见而自动成为 LDVH 正式术语。

## 2. 04 Helper 公共接口

### 2.1 公开操作标识 / Public Operation Identifier

预期含义：稳定识别 Helper CLI 某一项公开可调用操作，使 AI、Code、tests 和环境入口引用同一服务行为，而不依赖内部函数、文件路径或展示名称。04 当前由 `operation_key` 承载。

- 来源分类：LDVH Helper 服务特定的接口概念；不是行业标准术语。
- 现有术语不足：`spec_key`、`attachment_key` 是规范模型身份；命令名可能包含通用 `call`，都不能稳定识别被调用的具体服务行为。
- 中文候选：`公开操作标识`。
- 英文候选：`Public Operation Identifier`。
- 机器表示：字段 `operation_key`；具体值由定义该公开操作的当前来源确定。
- 正例：规则读取、管辖解析或未来事实读取能力分别具有稳定且来源明确的操作 key。
- 反例：Python 函数名、CLI 别名、厂商 Hook 事件、尚未实现的路线图名称。
- 相邻概念：公开 CLI 命令负责路由；能力发现报告当次可调用性；操作标识不证明实现、授权或调用成功。
- 替换检查：若去掉该概念，环境入口和 tests 只能依赖命令文本或内部函数，无法稳定引用同一公开服务行为。
- 独立复核结论：概念必要、名称自然、边界明确。已以 `proposed` 登记双语术语表；`operation_key` 只作为该概念的机器表示，不扩张为所有 LDVH 对象的通用 key。04 进入当前规则源时才可同步转为 `current`。

### 2.2 能力发现结果 / Capability Discovery Result

预期含义：说明某项已经被询问的公开操作，在来源、实现和当次条件三个方面实际处于什么范围。04 当前使用：

```text
defined_not_implemented
implementation_without_contract
unavailable_for_request
partially_available
available_for_request
```

- 来源分类：LDVH Helper 服务特定的请求结果；不是长期状态模型，也不是行业标准枚举。
- 现有术语不足：00 只禁止把未实现、未接入和未验证冒充为可用，没有提供机器响应需要的当次分流值。
- 中文候选：`能力发现结果`。
- 英文候选：`Capability Discovery Result`。
- 机器表示：字段 `availability`，使用下述五个值。
- 正例：来源已定义但没有实现；来源和实现存在但本次缺少配置；只有部分对象范围可调用。
- 反例：项目长期“能力状态”、环境 `integrated`、功能路线图阶段、调用执行结果。
- 相邻概念：服务结果描述一次实际调用；环境接入结果描述真实环境连接；能力发现结果只覆盖本次发现输入和观察时间。
- 替换检查：若只返回布尔 `available`，无法区分契约缺失、实现缺失、当次条件缺失和部分可用，AI 会错误选择下一步。
- 决策顺序：未知名称且来源、实现都不存在时不进入该枚举，`call` 返回 `invalid_request`；有来源无实现为 `defined_not_implemented`；有实现无来源为 `implementation_without_contract`；来源与实现均存在但缺少可复核能力依据或零个请求子范围可调用时为 `unavailable_for_request`；至少一个子范围可调用且至少一个不可调用时为 `partially_available`；全部请求范围可调用时为 `available_for_request`。
- 独立复核结论：概念必要，上述顺序消除了未知操作、测试依据缺失、零/部分/全部范围之间的重叠。若保持为稳定公开接口，应在 04 的定义来源和术语表中登记概念；不把五个值提升为跨 LDVH 状态闭集。

### 2.3 服务结果 / Service Outcome

预期含义：一次公开 Helper 调用在其请求范围内形成的外层结果，用于机器调用方先分流，再读取范围、来源、变化、验证和缺口。04 当前使用：

```text
ok
no_change
partial
rejected
unavailable
invalid_request
error
```

- 来源分类：LDVH Helper 服务特定的接口概念；不是领域完成状态或行业标准枚举。
- 中文候选：`服务结果`。
- 英文候选：`Service Outcome`。
- 正例：结构有效但授权不足为 `rejected`；只有部分对象完成为 `partial`；请求完成但没有变化为 `no_change`。
- 反例：事实对象状态、测试结论、Human Gate 结果、环境集成状态、项目完成状态。
- 相邻概念：进程退出码只是快速分流镜像；完整语义还包括 `scope`、`sources`、`gaps`、`changes` 和 `verification`。
- 替换检查：只使用 exit code 或 `success: true/false` 会折叠拒绝、不可用、部分结果和无变化，导致调用方扩大解释。
- 互斥顺序：先判断请求结构，不成立为 `invalid_request`；至少一个独立子范围完成且至少一个未完成时为 `partial`；没有子范围完成时，规则/授权拒绝为 `rejected`，实现、可复核能力依据、配置、依赖或环境不足为 `unavailable`，实现异常或结果不可信为 `error`；全部范围执行并形成结果为 `ok`；只有允许执行且检查完成的状态变更请求在目标已满足或无需变化时使用 `no_change`。
- 独立复核结论：概念及七值区分必要，互斥顺序成立。退出码映射和 `outcome` 字段属于同一公开契约，需在 04 转为 `active` 前登记；不得把 `ok` 或 `no_change` 解释为上层工作完成。

### 2.4 规则适用判断结果 / Rule Applicability Evaluation Result

预期含义：Helper 针对一条已定位来源规则、一个明确对象范围和当次输入，报告是否已经完成适用判断及其结果。04 当前使用：

```text
applies
does_not_apply
insufficient_information
not_evaluated
```

- 来源分类：LDVH Helper 服务对来源规则判断的接口概念；不是新的规则效力模型。
- 中文候选：`规则适用判断结果`。
- 英文候选：`Rule Applicability Evaluation Result`。
- 机器表示：字段 `applicability`；不复用 01 已用于规范依据的 `basis`。
- 正例：依据足以判断命中；依据足以判断不命中；依据不足；尚未执行检查。
- 反例：规则优先级、规则有效/无效、规则已自动保障、AI 是否遵守、行动是否完成。
- 相邻概念：01 定义当前规则源和披露层级；具体来源定义适用条件和效力；02 提供对象范围；本概念只报告某次判断。
- 替换检查：如果把依据不足和尚未检查都写成“不适用”，AI 会把未知范围误当成已排除规则。
- 独立复核结论：收窄后的名称准确表达规则对对象和当次输入的适用判断；`insufficient_information` 与规范 `basis` 不再混淆。四分结果具有明确净价值，但必须维持“来源规则语义不变、结果绑定当次对象和输入”的边界。04 转为 `active` 前登记概念。

### 2.5 04 的其它字段

`task`、`arguments`、`scope`、`sources`、`gaps`、`changes`、`verification`、`diagnostics` 等在 04 中是普通接口字段，并已由表格直接限定语义；当前没有证据要求把每个字段分别提升为双语架构术语。若未来字段被跨多个规范作为独立概念复用、出现名称歧义或形成机器枚举，必须重新执行 01 §12，而不能凭本记录自动取得 `current`。

## 3. 05 事实模型候选术语

### 3.1 事实类型 / Fact Type

预期含义：对一类准备在项目中稳定保留、具有相同事实语义和实例边界，并需要按共同规则接受对象化、状态化或证据化管理的事实内容所作的语义分类。

- 来源分类：LDVH 特定的描述性术语；不声称行业标准。
- 现有术语不足：事实模型是构成要素，稳定事实是已位于事实源并满足来源规则的内容；二者都不能表达类型级准入和唯一定义来源。
- 中文候选：`事实类型`。
- 英文候选：`Fact Type`。
- 正例：重新准入后具有共同实例语义、身份和状态要求的一类决策记录。
- 反例：文件扩展名、目录、Schema 名、前端筛选项、V3 遗留类型名称。
- 相邻概念：类型成立前的内容仅就类型准入而言是候选输入；它是否已经是普通稳定事实仍按 03 判断。类型成立不等于其中每个事实对象的当前内容有效；对象身份可以在当前内容无效时继续作为定位和修复目标。
- 替换检查：如果只有“事实模型”和具体对象，就无法在恢复 Spark、WorkCase 等名称之前判断是否值得建立一类事实及其共同来源。
- 初步结论：概念必要，名称基本自然。active 03 使用过普通描述“事实对象类型”，需要在 05 转为 `active` 前决定统一为本候选还是登记别名，不能保留两套未说明表达。

### 3.2 事实对象 / Fact Object 与事实实例 / Fact Instance

预期含义：已准入事实类型之下，按该类型唯一定义来源规定的身份、结构和位置承载、可跨行动与会话独立定位和更新的一项稳定事实单位。

- 来源分类：LDVH 特定术语。
- 中文候选 A：`事实对象`；英文 `Fact Object`。
- 中文候选 B：`事实实例`；英文 `Fact Instance`。
- A 的优点：与 00“对象化管理”和现有架构工作记录一致；能强调稳定身份、关系和操作边界。
- A 的风险：容易与 `工作对象 / Work Object` 混淆，英文 `Fact Object` 不是通行标准术语。
- B 的优点：清楚表达 type/instance 关系，并与 active 03 的普通描述“事实实例”一致。
- B 的风险：可能让人误解为运行时或内存实例，弱化其事实源中的稳定身份。
- 正例：已准入类型下、位于唯一当前权威位置、可独立定位和校验的一项记录。
- 反例：任意文件或 YAML 映射、命令输出、缓存、Web 视图、未准入的 V3 实例，以及仅因当前任务指向而成为工作对象的普通资源。
- 相邻概念：稳定事实不一定需要成为这种单位；当当前事项指向该单位时，同一资源可以同时承担工作对象作用。
- 替换检查：如果只称“对象”，会与工作对象混淆；如果只称“稳定事实”，无法表达类型下唯一身份、关系和变更边界。
- 独立复核结论：首选 `事实对象 / Fact Object`，别名 `事实实例 / Fact Instance`。首选名称更能表达稳定身份、关系与当前权威位置；别名保留 active 03 已有 type/instance 表达。二者都属于 LDVH 特定术语，不声称行业标准。05 转为 `active` 前必须统一 03、05、06、架构记录和术语表中的首选名称与别名，不能让两者承担不清楚的不同语义。

## 4. 06–08 标题和重复表达

### 4.1 06

“参与者、能力与授权”分别指谁承担行动、需要什么实际能力、允许影响什么范围，替代了过于宽泛的“执行资源”。它们都是当前语境中的普通名词，不需要合成新的正式术语。具体行动模板的“唯一定义来源、稳定身份和发现入口”复用 01 的定义来源与身份治理，不建立新的模板状态模型。

06 为具体行动模板提出 `template_key`，用于在当前规则源中稳定识别模板，并把它映射到当前定义来源 `spec_key` 与正文位置。它是行动模板特定的机器身份，不是规范职责标识符，也不证明模板适用、已执行或已验证。

- 候选中文：`行动模板标识`。
- 候选英文：`Action Template Identifier`。
- 来源分类：LDVH 行动模板特定接口概念，不声称行业标准。
- 正例：模板改名或移动时 key 保持，拆分、合并、替代时旧 key 能回到承接说明。
- 反例：规范 `spec_key`、文件名、标题、编号、某次模板运行 ID。
- 初步结论：稳定身份概念必要；具体正文承载格式尚未成立。第一份具体模板准入前必须完成机器格式和独立复核，若该名称需要跨 Helper、Code 和模板定义来源复用，则同步登记双语术语表。

### 4.2 07

Human 确认中文标题为 `Code 实践与测试规范`，英文采用 `Code Practices and Testing Specification`。`code-engineering-practices` 保持表示已审定的统一 Code 工程责任；测试从一开始就是该责任的一部分，标题显式化不构成职责拆分。

07 的“Code 规划、模块责任、依赖方向、接口与 Schema 维护、诊断、重构评估、风险驱动测试、测试检查范围”是正文直接说明的工程表达。除下述 `Code 规划` 外，当前没有把这些表达登记为 LDVH 架构构成要素、机器状态或行业标准术语。

独立复核确认 `Code 规划 / Code Planning` 已经承担稳定形成时机、最小内容、同步、偏移、验证与停止条件，因此不能继续只作为普通短语。候选审查如下：

- 准确定义：面向一个实际准备实现或变更范围的版本化实现说明，在编码前记录来源、模块责任、依赖与调用方向、接口与 Schema 维护责任、错误与诊断、风险、测试和已知缺口，并在这些内容变化时同步更新。
- 来源分类：LDVH Code 实践中的特定描述性术语；不声称是行业标准。
- 中文候选：`Code 规划`。
- 英文候选：`Code Planning`。
- 替代名称：`Implementation Plan` 容易落入任务步骤和排期；`Code Design Description` 弱化先于实现、持续同步和测试映射；`Code Engineering Plan` 容易被理解为项目级工程计划，因此均不采用。
- 现有术语不足：单次实施计划关注本次步骤和进度；架构描述可能覆盖更广产品或系统；实现说明没有自动包含“先于相应实现并与边界变化同步”的责任。
- 正例：首个 Helper 增量开始前，对实际准备实现的模块、依赖、接口、Schema、诊断和测试形成版本化说明。
- 反例：任务排期、当前进度、未来路线图、源码文件列表、事后为既成实现补写的合理化说明。
- 相邻概念：它不是规则源、产品契约、具体事实类型或项目排期；可以使用架构图等表达形式，但不因此等同完整架构描述标准。
- 替换检查：取消该概念而只要求“先想清楚”无法稳定定位实现前输入、维护责任和设计偏移；拆成多项零散记录会增加同步遗漏。
- 唯一定义来源：候选 07 §5；在 07 为 `draft` 时只具有 proposed 状态。
- 使用扫描与影响范围：当前只命中 07、07 调查记录、本记录和关系/建设记录，没有与当前规则源既有术语冲突；未来会影响版本化 Code 规划文件、Helper 实现前置和 Code/tests 交付检查。
- Human 确认：概念来源于 Human 已明确要求 Code 在首次实现前完成系统规划。
- 独立复核：一名未参与 07 起草的执行者确认概念必要、中英文范围一致，并确认上述三个替代名称均不如当前名称准确；候选可以登记为 `proposed`，但只有 07 进入当前规则源且术语表同步满足 `current` 条件后才能生效。

07 原有四组斜杠测试名称未通过复核，因为把检查范围、测试对象和测试目的误写成近义词。草案已改为“局部组件行为、接口契约、真实组合、公开入口贯通”四种 LDVH 本地检查范围，不使用英文稳定名称，也不称其为行业 test levels，因此当前不登记新的测试层次术语。

### 4.3 08

Human 最终确认中文标题为 `Web 呈现与交互规范`，英文采用 `Web Presentation and Interaction Specification`，`spec_key` 为 `web-presentation-interaction`。标题以面向 Human 的呈现和交互概括产品责任；独立读取、来源范围和派生当前性仍由 YAML `positioning`、`scope`、职责边界和正文明确，因此标题简化不删除读取责任。

一次未参与 08 起草的独立复核已经确认该名称在最新 Human 决定下中英文自然、范围一致。名称本身是规范文档身份，不需要另登记为跨规范架构术语。

## 5. 当前结论和激活前置

1. 07、08 的名称和 `spec_key` 已按 Human 决定收敛；
2. 06 的 `template_key` 是必要的机器身份候选，稳定身份与承接语义已经独立复核，具体正文承载格式仍等待第一份具体模板准入前完成；已以 `行动模板标识 / Action Template Identifier` 登记为 `proposed`；
3. 04 的四组公共机器接口概念及互斥决策已经完成独立复核并登记为 `proposed`，等待 04 激活时同步为 `current`；
4. 05 的“事实类型 / Fact Type”和“事实对象 / Fact Object”概念必要，独立复核建议分别以“事实对象类型”和“事实实例 / Fact Instance”为别名；已登记为 `proposed`，等待 05 激活时同步 active 03 和术语表；
5. 07 的 `Code 规划 / Code Planning` 已通过独立名称复核并登记为 `proposed`，等待 07 激活时同步为 `current`；
6. 04、05、06、07 中的候选未完成对应准入前，不得把相应名称和机器表示扩散到 Code、tests、事实对象、具体行动模板、环境 adapter 或 Web 实现；
7. 本记录不阻止 04–08 作为 `draft` 完成第一批起草、结构检查和职责复核，但阻止 04、05、07 在候选术语未完成激活同步时转为 `active`；06 的机器承载格式则阻止第一份具体行动模板的自动发现声明。
