# Change — Web

> 状态：not-created
> 主文档：`specs/22-Change-变更记录.md`

## 本文定位

本文是 Change 对象模型的 Web 机制实践子文档，当前为 not-created 状态。

## 不创建理由

Change 事实实例以 Git commit 承载，读取和查询通过 `git log` 命令在终端执行。当前不需要 Web 信息同步、变更看板、Human Gate UI 或受控编辑入口。

## 检查项

- [ ] 是否未出现需要在 Web 端展示 commit 历史或变更看板的需求
- [ ] 如后续需要 Web 变更看板或协同编辑入口，重新评估是否需要创建 Web 子文档