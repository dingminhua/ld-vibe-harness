# Change — 契约

> 状态：not-created
> 主文档：`specs/22-Change-变更记录.md`

## 本文定位

本文是 Change 对象模型的 Contract 契约子文档，当前为 not-created 状态。

## 不创建理由

Change 不使用 YAML 实例文件承载，其字段契约映射为 commit message 格式规范，在主文档 §8 已完整定义（type 枚举、scope 枚举、subject 格式、body 格式、Refs 格式）。不需要独立的 YAML schema 契约子文档。

## 检查项

- [ ] 主文档 §8 的 commit message 格式规范是否持续为 Change 字段契约的权威位置
- [ ] 如 Change 后续增加 YAML 实例承载或需要结构化 schema，重新评估是否需要创建 Contract 子文档