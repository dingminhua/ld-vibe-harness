# Change — Agent

> 状态：not-created
> 主文档：`specs/22-Change-变更记录.md`

## 本文定位

本文是 Change 对象模型的 Agent 机制实践子文档，当前为 not-created 状态。

## 不创建理由

Change 的 commit 操作在单一会话中执行，不需要 Agent 独立上下文、并行处理或上下文分发。commit message 格式校验是轻量级检查，不需要独立 Agent 上下文。

## 检查项

- [ ] 是否未出现需要并行处理多个仓库 commit 的场景
- [ ] 如 Change 后续需要跨仓库并行提交或独立上下文处理，重新评估是否需要创建 Agent