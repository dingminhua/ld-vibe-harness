# V4 根规范规范源章节变更提案

> 记录性质：本文是 00《理念与构成》新增「以规范源为边界系统与流程规范」章节及 §1–§3 联动改动的变更提案、决定记录与执行依据。本文不是规则源。
> 形成时间：2026-07-18
> 当前状态：v3。独立复核（5 项 P1、4 项 P2、1 项 P3 及 1 条建议）已全部处置；Human 已完成 §10 全部五项决定并逐段批准文本；同一增量已按 §8 执行完毕，验证记录见 §11。残留事项：独立术语复核、§6 回指改写、Git 提交（§12）。

## 1. 起因与问题

00 §1 设计理念共六项，其中第 3 项「以规范源为行动边界与流程规范」在正文中长期没有承接章节：§2 承接第 1 项（第一服务对象）、§3 承接第 2 项（三类语义构成要素）、现 §4 承接第 4 项（事实源）、现 §6 承接第 5 项（V1-V8）。本提案为第 3 项补齐正文承接，并在评审与独立复核中完成三轮修正：

1. Human 指出规范源负责的不只是行动边界，各种边界都由规范源规定，章节作用从「行动边界」泛化为「边界系统」；
2. Human 定夺章标题为「以规范源为边界系统与流程规范」，§2 第 1 项保留四字命名「明确边界」并延伸内容；
3. 独立复核（2026-07-18）发现效力层次混同、行动授权定义缺漏、附件回指遗漏、术语流程缺失和 Human Gate 误判五类问题，全部处置后经 Human 逐段批准落地。

## 2. 评审与复核依据

- 00 全文；01（生效模型、「进入 LDVH 当前规则源」、`active` 语义、具体规则效力由来源规范定义、术语治理与准入流程、附件与父规范冲突处理）；06（§5.1 准入、当前具体行动模板集合、§6.1 适用判断、§6.2 授权与规则边界、不另建模板状态闭集）；
- `01.Att.01`（现有条目、既有漂移、对 00 的章节回指）；specs 全文 `ldvh-root` 引用扫描；specs 全文「系统」用词扫描；`code/` 对 00 的识别方式（路径与 H1，H2 仅通用连续编号检查）；code/tests 断言扫描（无对 00 章节号或 H2 标题的旧断言）；
- 独立复核报告（2026-07-18，外部 AI 按只读复核提示词执行，引用均带文件与行号）。

## 3. §4 落地全文（v3，与 00 正文一致）

已插入 `specs/00-理念与构成.md` 第 105–123 行（现 §4 与现 §5 之间）。与 Human 批准的 v2 文本的唯一差异：按 01 双语名称要求，在五个登记术语的首次定义处补加中英文规范名称（规范源（Specification Source）、行动边界（Action Boundary）、流程规范（Process Specification）、边界系统（Boundary System）、行动允许（Action Permission）），属格式级合规补充，不改语义。

```markdown
## 4. 以规范源为边界系统与流程规范

LDVH 以规范源为边界系统与流程规范。规范源（Specification Source）由全部 Specs 及其授权附件构成，是 LDVH 规范内容的统一承载，并对 LDVH 发挥两类基础作用：

1. 界定 LDVH 边界。规范源规定 LDVH 全部规范边界的含义、对象、范围、条件和效力，包括但不限于行动边界（Action Boundary）、事实源边界、构成要素职责边界、价值边界、授权与 Human 决定边界、验证与声明边界、Helper 与各环境入口的服务边界、能力边界，以及停止与恢复边界。这些边界相互关联，共同构成 LDVH 的边界系统；
2. 规范行动流程。规范源通过行动模板规定重复场景中可复用的行动流程（流程规范，Process Specification），使重复行动能够按照稳定结构推进，而不必反复重建流程。行动模板的语义构成和职责边界由本文构成要素章节定义；本章只说明其作为规范源内容所产生的流程效力。

这里的边界系统（Boundary System），是规范源中各类边界相互关联形成的整体，不是新的语义构成要素。

凡要取得 LDVH 规范效力的边界或流程要求，都必须由规范源规定。Human 对话、当次实际信息和工具结果，可以分别提供当前目标、授权、决定、条件输入以及执行与验证依据；这些内容是否成立、产生何种作用，仍须按照适用规范判断。它们不因此成为规范源，也不得自行新增、改写或扩张 LDVH 的规范边界与流程要求。

规范收录于规范源，不等于已经生效；已经生效，也不等于适用于当前事项。规范文档满足规范模型规定的身份、状态、结构和关系等条件时，进入当前规则源，取得规范效力；具体规则的适用范围和效力由其来源规范定义，其对象、场景、条件和作用范围与当前事项相符时，适用于当前事项。适用条件由规范内容定义，Human 对话、当前目标、工作对象和当次实际信息是判断这些条件是否成立的输入，AI 据此完成适用判断。来源规范可以把授权、环境和能力等条件规定为适用条件、执行前置条件或两者兼具；适用判断与行动允许按来源规定分别作出，互不替代。

规范的生效、适用与行动允许（Action Permission），是三个依次判断的问题：生效决定规范内容能否作为依据，适用决定具体规则是否约束当前事项，行动允许则要求该行动处于 Human 当前指令给予的授权范围内，并且适用于该行动的全部来源规则许可条件已经成立。规范源规定哪些行动可以直接推进，哪些行动须由 Human 作出决定，以及相应决定或授权的主体、对象、范围和成立条件。凡规范要求 Human 授权的行动，只有取得符合这些要求的授权，才属于规范允许执行的范围。

行动模板在其唯一定义来源进入当前规则源、来源规定的准入条件成立后，进入当前具体行动模板集合；进入当前具体行动模板集合的行动模板适用于当前事项时，其流程要求对当次范围形成约束。AI 应在授权、可用能力和停止条件允许的范围内按其规定推进，并在执行每一步前满足该步规定的前置条件；条件不足或命中暂停、交还条件时，应按模板暂停或分流。规范未限定实现方法或没有适用的行动模板时，AI 在规范允许的范围内保持求解自主。

规范模型定义规范源的组织方式和生效条件，具体规范定义自身内容和适用范围，行动模板的共同规则由承担相应基础职责的规范定义。索引、摘要、结构化表示、缓存、视图和检查结果只用于辅助定位、读取与检查；发生语义判断时必须能够回指相应规范内容，不得改写规范的含义、生效状态、适用范围或行动允许范围，也不得成为第二规范源。
```

## 4. §1–§3 联动改动（已落地）

### 4.1 §1（一处）

设计理念第 3 项：`3. 以规范源为行动边界与流程规范；` → `3. 以规范源为边界系统与流程规范；`

### 4.2 §2（三处）

1. 所需支撑第 1 项：→ `1. 明确边界——行动、事实源、职责、授权、验证等各类边界模糊时，AI 容易扩大、遗漏或误解工作范围；各类边界均由规范源规定，构成边界系统；`
2. 能力优势第 3 项：→ `3. 约束判断——能完成规范生效、适用与行动允许判断，识别规则边界、风险、阻塞、冲突和需要交由 Human 判断、授权或决定的节点；`
3. 支撑方向句：「清晰的行动边界」→「清晰的边界系统」（Human 决定，§5 第 5 项）。

### 4.3 §3（一处实质修改）

模板作用段 → `行动模板不重新定义适用规范或事实语义，也不自行产生行动授权；其进入当前具体行动模板集合、适用与对行动的约束方式，由本文规范源章节定义。`

行动模板定义所列语义内容（适用条件、必要输入、关键步骤与分支、失败处理、验证、回写和交还）继续由 §3 唯一承担；原句「授权、可用能力和停止条件允许的范围」三个限定已在 §4 模板段逐字恢复。

### 4.4 frontmatter（已落地）

- `positioning` 增加「规范源作为边界系统与流程规范」；
- `scope` 的「规则作用边界」扩为「规范源作用与规则作用边界」；
- `code_consumption` 增加 `specification_source_principles`。

## 5. Human 决定记录

以下决定由 Human 在 2026-07-18 评审与逐段批准过程中作出，本文即为其书面记录：

1. 边界泛化：规范源负责的不只是行动边界，各种边界都由规范源规定（同时构成 00 §8 验证要求第一行所要求的设计理念修改决定）；
2. 章标题定夺为「以规范源为边界系统与流程规范」；
3. §2 第 1 项保留四字命名「明确边界」，内容延伸覆盖各类边界；
4. 批准 §4 定稿 v1 的三处微调（「价值边界」复用；「由本文构成要素章节定义」；「生效，取得规范效力」）；
5. §2 支撑方向句选用「清晰的边界系统」（否决「规范系统」候选：无定义且与规范源、规范模型、边界系统相撞）；
6. 逐段批准 §1–§3 联动改动与 §4 全文七段（复核后 v2 文本）；
7. Gate 项 10：接受恢复根级「可用能力」限定（v2 已按此修订），权威层级下降取舍消失；
8. Gate 项 9：批准 §4 第 3 段与末段对 Human 对话、当次信息、工具结果、索引、缓存、视图、检查结果的上位效力约束；
9. Gate 项 2：确认 v2 行动允许表述（Human 当前指令 + 来源规则许可双重成立）不改变 Human、AI、Code 基本职责关系；
10. 术语路线：批准路线 A（5 个正式候选术语完成 01 准入审计与登记，独立术语复核后闭合）；
11. Gate 项 1 补充决定：§1 最终目标第 1 项「清晰的行动边界」改为「清晰的边界系统」，与 §4 章名及边界泛化决定（§5 第 1 项）保持一致；
12. §3 行动模板定义第 3 条「按模板实际生效的步骤与分支」改为「按模板对当次范围实际约束的步骤与分支」，与 §4 行动模板效力表述（模板成立不等于对当次范围形成约束）对齐；
13. §5 章名由「以事实源为信息锚点」改为「以事实源为信息锚点与判断依据」，首句与功能段同步承接（与 §1 设计理念第 4 项对齐）；
14. 废除「五类构成要素」统称：规范模型、事实模型、行动模板与 Code、Web 不作并列归类；Code 保留为确定性执行能力的上位概念（下属交付入口为 Helper、Hook、Gate），Web 独立、不属于 Code；全文 6 处相关表述同步改写；
15. 章节重组：§6 定为「以 V1-V8 为八项价值评判标准」，新增 §7「以 AI 执行者主动调用、环境 Hook/Gate 自动触发和 Web 交互协作为三种价值交付方式」，原 Helper 章内容并入 §7；§1–§7 与设计理念第 1–6 项一一对应；
16. §8–§10 的进一步修改留待后续讨论，当前已知残留：验证表第 2 行「命中/生效」旧表述、Gate 第 3 项引文标点与 §7 原句不严格一致。

## 6. 独立复核处置记录（2026-07-18）

复核结论：修正后执行。逐项处置如下，全部接受：

| 发现 | 处置 |
|---|---|
| P1-1 规范文档生效、具体规则适用、行动模板成立混成同一模型 | 接受。§4 拆清三层：规范文档满足 01 条件进入当前规则源取得规范效力；具体规则适用范围和效力由来源规范定义；行动模板按 06「唯一定义来源进入当前规则源 + 准入条件成立 → 进入当前具体行动模板集合」表述 |
| P1-2 行动允许漏掉 Human 当前指令 | 接受。行动允许改为双重成立，与 00 §2、06 §6.2 对齐 |
| P1-3 「specs 内无对 00 章节号的交叉引用」为错误事实 | 接受并更正：01.Att.01 有 16 条 `ldvh-root §「N. 标题」` 回指（含 Helper、构成要素两处既有漂移）；01.Att.01 全部受影响回指纳入同一增量 |
| P1-4 术语清单不完整、非无冲突、未满足 01 准入流程 | 接受。事实源边界已是 current 条目；「规则源」「当前规则源」均已登记；术语路线 A 经 Human 批准，审计记录见 `V4-规范源章节术语准入审计.md` |
| P1-5 Human Gate 判断错误、执行决定未闭合 | 接受。§7 改为「触发—决定状态—作用范围」格式；未决项收拢为 §10 五项，全部决定后执行 |
| P2-1 规范源与当前规则源实为两个集合 | 接受。§4 明示「进入当前规则源，取得规范效力」；规范源（全部承载）⊇ 当前规则源（生效子集） |
| P2-2 三层模型与 06 适用条件交叉分类 | 接受。§4 适用段增加「来源规范可以把授权、环境和能力等条件规定为适用条件、执行前置条件或两者兼具；适用判断与行动允许按来源规定分别作出，互不替代」 |
| P2-3 可用能力从根级降为下位规范要求 | 接受。§4 模板段恢复根级限定「授权、可用能力和停止条件允许的范围」 |
| P2-4 边界清单漏「能力边界」 | 接受。§4 第 1 条改「包括但不限于」并补入能力边界 |
| P3 「九项枚举」计数不准确 | 接受。改为「行动模板定义所列语义内容」 |
| 建议：为 §4 增加 L3 精确读取回归样例 | 接受。已新增 `test_root_specification_source_chapter_l3_returns_exact_slice` |

## 7. Human Gate 逐项判断（终态）

| 00 §9.1 项 | 触发判断 | Human 决定状态 | 作用范围 |
|---|---|---|---|
| 1 存在理由/设计理念/最终目标 | 触发（设计理念第 3 项修改） | 已决定（§5 第 1、2 项） | §1 第 3 项、§4 标题与首句 |
| 2 第一服务对象与三方职责关系 | 触发（v1 行动允许措辞缺 Human 当前指令） | 已确认不改变（§5 第 9 项） | §4 三层判断段 |
| 3 Helper 基础服务形式与作用边界 | 不触发（命中/未命中边界在 §4 保留并上位化） | — | — |
| 4 事实源定位与事实源边界 | 不触发 | — | — |
| 5 五类构成要素 | 不触发（§4 明示边界系统不是新的语义构成要素） | — | — |
| 6 V1-V8 | 不触发 | — | — |
| 7 规范模型定义职责授权 | 不触发（§4 末段维持既有授权表述） | — | — |
| 8 验证要求/Human Gate/Stop Conditions 上位语义 | 不触发 | — | — |
| 9 上位效力约束 | 触发（新增对 Human 对话、当次信息、工具结果、索引、缓存、视图、检查结果的效力约束） | 已批准（§5 第 8 项） | §4 第 3 段与末段 |
| 10 重大取舍 | 触发（v1 删除根级「可用能力」限定） | 已决定接受恢复，取舍消失（§5 第 7 项） | §4 模板段、§3 移交段 |

## 8. 同一增量执行范围（已执行）

1. §1 理念第 3 项措辞 ✅；
2. §2 三处（含支撑方向句「清晰的边界系统」）✅；
3. §3 模板作用段移交 ✅；
4. 插入 §4 落地全文 ✅；
5. 后续章节重编号：现 §4–§9 → §5–§10（含 8.x/9.x 小节）✅；
6. frontmatter 同步 ✅；
7. `01.Att.01` 全部受影响回指同一提交 ✅（映射见下，含既有漂移修复；「规则源」并入「规范源」作为其他有效表达）；
8. 术语路线 A：5 个候选的准入审计记录（`V4-规范源章节术语准入审计.md`）、双语规范名称与登记 ✅；独立术语复核待进行（§12）；
9. code/tests：无旧断言需同步（复核已核实）；新增 §4 L3 精确标题读取回归测试 ✅。

回指映射（已执行）：

| 条目 | 原回指 | 新回指 | 性质 |
|---|---|---|---|
| LDVH、AI 执行者 | §1、§2 | 不变 | 无需改 |
| 构成要素、规范模型、事实模型、行动模板、Code、Web | §「5. 以规范模型、事实模型、行动模板、Code 和 Web 为构成要素」 | §「3. 以规范模型、事实模型和行动模板为三类语义构成要素」 | 既有漂移修复 |
| 规范源、边界系统、行动边界、流程规范、行动允许 | — | §「4. 以规范源为边界系统与流程规范」 | 新增登记 |
| 事实源、事实源边界 | §「4. 以事实源为信息锚点」 | §「5. 以事实源为信息锚点」 | 顺延 |
| LDVH Helper CLI、Helper | §「3. 以 LDVH Helper CLI 为基础服务形式」 | §「6. 以 LDVH Helper CLI 为基础服务形式」 | 既有漂移修复 + 顺延 |
| V1-V8 价值标准 | §「6. …」 | §「7. 以 V1-V8 价值标准为行动标尺」 | 顺延 |
| Human Gate | §「8. Human Gate」 | §「9. Human Gate」 | 顺延 |
| 停止条件 | §「9. Stop Conditions」 | §「10. Stop Conditions」 | 顺延 |

## 9. 暂不处理与后续事项

1. 现 §6（Helper CLI 章）「命中具体规则时落实、未命中时普通求解路径保持自主」段改为回指 §4——按 Human 指示另行处理；
2. 本增量的 Git 提交：按 30（Git 提交行动模板）需 Human 当次授权，见 §12；
3. 本增量的变更记录按当前工作推进控制面的维护规则写入（随 Git 提交增量）。

## 10. 待 Human 决定事项（全部已决定）

| # | 事项 | 决定 |
|---|---|---|
| 1 | 支撑方向句 | 「清晰的边界系统」（§5 第 5 项） |
| 2 | Gate 项 10 | 接受恢复根级「可用能力」限定（§5 第 7 项） |
| 3 | Gate 项 9 | 批准上位效力约束（§5 第 8 项） |
| 4 | Gate 项 2 | 确认不改变基本职责关系（§5 第 9 项） |
| 5 | 术语路线 | 路线 A（§5 第 10 项） |

## 11. 执行与验证记录（2026-07-18）

执行内容：

1. `specs/00-理念与构成.md` 整文重写：§1–§3 四处联动改动、插入 §4（105–123 行）、现 §4–§9 顺延为 §5–§10（含小节号）、frontmatter 三项同步；
2. `specs/attachments/01.Att.01-LDVH双语术语表.md`：8 组回指映射（含 2 处既有漂移修复）、5 条新术语登记、「规则源」并入「规范源」、行序按章序重排；
3. `docs/v4-architecture/investigations/V4-规范源章节术语准入审计.md`：5 条术语的 01 准入审计（必要性、中英文候选、正反例、相邻概念、替换与使用扫描）、既有条目处置与残留事项；
4. `code/tests/helper/test_specification_content.py`：新增 `test_root_specification_source_chapter_l3_returns_exact_slice`。

验证证据（当次工作树，2026-07-18）：

- Code tests 全量回归（分组执行）：477 + 216 + 112 = **805 passed，10 skipped**（10 个均为既有 native Windows 条件跳过，与本增量无关）；
- Ruff `check` 与 `format --check`：全部通过（含新增测试文件）；
- 规范仓库机械检查随 specs 测试组通过（新 §4 结构、H2 编号连续、附件与父规范一致、5 条新登记）；
- 真实 CLI：`capabilities` 发现 10 项公开操作（`ok`）；`read-specification-content` 按精确标题 `["4. 以规范源为边界系统与流程规范"]` L3 切片返回 `ok`，切片范围为 00 第 105–123 行，内容与落地文本一致；响应保留既有「7 项当前规则源资格条件尚未由 Code 机械证明」的常驻 gaps（非本增量引入）；
- 新增回归测试所在文件 16 passed。

未验证与残留范围：

1. 独立术语复核未进行（路线 A 的最后一个完成条件）；
2. Git 提交已执行：主体增量 `353d4b25`；§1 与 §3 两处措辞统一见下述补充执行；
3. 普通发行快照未重新构建（本增量只改 Working Tree 规则源；wheel/sdist 快照将在下次构建时包含新文本）；
4. §6 回指改写、总纲变更记录随后续增量。

补充执行（2026-07-18，Human 批准后）：

1. `specs/00-理念与构成.md` §1 最终目标第 1 项「清晰的行动边界」改为「清晰的边界系统」（§5 第 11 项决定）；
2. §3 行动模板定义第 3 条「按模板实际生效的步骤与分支」改为「按模板对当次范围实际约束的步骤与分支」（§5 第 12 项决定）；
3. 验证：`pytest code/tests/specs code/tests/helper` 393 passed（含规范结构检查与 §4 L3 精确读取回归）。

补充执行（2026-07-18 第二轮，Human 逐章批准后）：

1. §5 章名改为「以事实源为信息锚点与判断依据」，首句与功能段同步（§5 第 13 项）；
2. §3 尾段改写：Code 作为确定性执行能力上位概念引出（下属 Helper、Hook、Gate），Web 独立（§5 第 14 项）；§3 第 1 条「行动边界」微打磨为「各类边界」；
3. 章节重组落地：原 §6（Helper）与 §7（V1-V8）整块替换为新 §6（V1-V8）+ 新 §7（三种价值交付方式）（§5 第 15 项）；
4. 去「五类构成要素」全文同步：frontmatter positioning、验证表第 4 行（改「构成内容归口」）、Gate 第 5 项、Stop 第 5 项、Gate 第 3 项引文同步新 §7 表述；
5. 验证：全量回归 393 + 320 + 93 = **806 passed，10 skipped**（10 个均为既有 native Windows 条件跳过）；「五类」「第六类」「行动标尺」「命中具体规则」在 §1–§7 零残留（验证表第 2 行残留见 §5 第 16 项，随 §8–§10 讨论处理）；
6. 00 前七章通读复核：结构、设计理念映射、语义链、回指均自洽。

## 12. 下一步

1. 独立术语复核（可对 `V4-规范源章节术语准入审计.md` 与 `01.Att.01` 执行只读复核）；
2. Human 授权后按 30 完成本增量 Git 提交，并按当前工作推进控制面规则写入变更记录。
