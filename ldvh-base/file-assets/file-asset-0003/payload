LDVH 过度设计评估
先说总量：生产代码约 3.57 万行 Python + 1.59 万行 TS，规范 9574 行 markdown，测试 3.37 万行（测试比 ≈0.95，健康，不算过度测试）。问题集中在生产代码的分层和"自我证明"机制上。

🔴 明显过度的地方
1. 每个 Helper 操作强制拆成 _operation.py + _request.py 两个文件——仪式大于收益

helper/operations/ 下 41 个文件，几乎全是成对结构。以最小的 fact_integrity 为例：

fact_integrity_request.py（79 行）只做一件事：把 CommonRequest 校验成一个 3 字段的 frozen dataclass；
fact_integrity_operation.py（193 行）里 _validated_request 调一次解析，然后才是真正的逻辑。
这套模式被复制到 18 个操作上。Request 解析函数大多 50–150 行、只服务一个调用点，却每个都配两个 dataclass（XxxRequest + XxxRequestParseResult）和一个 __all__。这是把"每个操作都要可独立发现、可独立声明来源"的规范要求，机械地落到了文件结构上，结果是导航一个操作要跳两个文件，而解析逻辑简单到内联在 operation 里也完全清晰。

2. "声明在规范、实现在代码、运行时再绑定"的三元绑定机制，对 18 个操作过重

service.py（563 行）+ operation_runtime.py（162 行）+ operation_sources.py（209 行）+ source_refs.py（212 行）构成一套运行时机制：

从 markdown 规范里解析出"公开操作声明"（OperationDeclarationCandidate）；
与代码里的 IMPLEMENTATIONS 字典做交集绑定（bind_operation_implementations）；
对"有代码没声明"的实现发 diagnostic，对"有声明没代码"的发 gap；
每次调用还要带 evidence 元组、source_refs、qualification_gaps。
这套机制要防的是"代码偷偷暴露规范没声明的操作"。但实际操作集合就 18 个、全在一个 __init__.py 里手写注册（79–101 行的 import 墙），没有任何第三方扩展点、没有插件、没有动态注册。用一个静态字典 + 启动期断言就能达到同样的安全保证，却为此维护了一个运行时绑定器 + 规范文本解析器 + source reference 绑定器（set_reference_binder/contextvar 那套）。这是典型的为不存在的扩展面付出框架成本。

3. source_refs / gap / qualification 证据系统渗透到每个返回值

几乎每个操作都在手工拼：

sources = (_INPUT_CONTRACT, _RESULT_CONTRACT) + tuple(plain(s) for s in run.sources) + _IMPLEMENTATION_EVIDENCE

然后每个返回分支（ok/unavailable/error）重复构造 OperationExecution，带上 requested_scope / completed_scope / not_completed_scope / gaps / verification / governance_resolution / disclosure / diagnostics / follow_up 十几个字段。

fact_integrity_operation.py 193 行里真正干活的就 snapshot → assess → 返回 status 约 20 行（124–151 行），其余全是 scope/sources/gaps 的包装。证据可追溯是这个项目的核心价值，方向对；但当前实现是"每个字段手工串接"，导致每加一个操作就要写 200–1200 行样板（fact_creation_operation.py 1266 行、workcase_update_operation.py 964 行）。这一层应该由一个统一的 result builder 收敛，而不是每个操作重新拼。

4. governance/resolver.py 814 行，管辖解析的状态空间爆炸

一个"给定路径 → 找到它属于哪个被管辖项目 + 哪个 worktree + common-dir"的功能，产生了：TechnicalOutcome / TechnicalNonCompletion / ResolutionGap / ResolutionDiagnostic / GovernanceResolutionRun / _ValidatedProject 6 个 dataclass，以及 _resolve_object / _governed_resolution / _unknown_result / _technical_run_for_failures / _global_technical_run / _non_valid_configuration_run 等 6 条并行的"构造一次运行结果"路径，外加 _freeze_source / _freeze_value / _freeze_sources / _deduplicate_sources / _plain_value / _source_time / _observation_sources / _registered_project_sources / _configuration_sources 一整组 source 冻结/去重工具。

它把"找到了 / 没找到 / 配置坏了 / git 失败 / 全局失败 / 技术失败"当成 6 种并列结局，每种都有独立的构造函数和 scope 算法。实际上这些结局在调用点绝大多数被统一折叠成 unavailable。这里的抽象是在为"管辖配置可能以 N 种方式坏掉"建模，但调用方只关心"能不能形成唯一边界"。

5. facts 层 creation/update 与 creation_application/update_application 的分层价值存疑

update.py 只有 28 行，是 atomic_replace_relative_if_equal 的一层薄包装；
creation.py（337 行）做 ID 分配 + 锁 + 原子创建；
然后 creation_application.py（422 行）和 update_application.py（310 行）在上面再叠一层"应用"逻辑；
同时 transitions.py（1274 行）、validation.py（408 行）、project_validation.py（284 行）、workcase_validation.py（997 行）各自校验。
"application" 这一层的命名暗示有 DDD 风格的应用服务/领域服务分离，但读下来 application 层主要是在编排 transitions + validation + repository，没有独立的业务规则。transitions.py 1274 行和 workcase_validation.py 997 行说明状态机和校验本身就很重，application 层更像是为了分层而分层。facts/ 8597 行里，我估计有 1500–2000 行是可以通过合并 _application 与编排层、合并 update.py 进 repository 来消掉的。

6. 规范即运行时配置：9574 行 markdown 被代码解析

specs/markdown.py（476 行）+ field_registry.py（841 行）+ identity.py（445 行）+ discovery.py（604 行）在运行时用正则（全仓 279 处 regex/heading 解析命中）从 markdown 里抽字段、抽操作声明、抽事实类型 schema。

这是项目最根本的架构选择（"规范驱动，而非硬编码"），有其合理性——AI 读规范和代码读规范同源。但代价是：

改一行规范措辞可能破坏正则；
代码里大量断言"规范必须包含某标题/某表格列"，本质是在用测试保护一个自然语言文档的格式稳定性；
field_registry.py 841 行就是在解析规范里的字段表并机械校验，这些字段如果用 YAML/Python schema 单源声明，几百行就能做完。
这一条我不主张改方向，但值得意识到：它是项目里最大的一笔"为了让 AI 和代码读同一份文本"的税。

🟡 值得讨论但不算硬伤
Web 层 1.59 万行 TS、10 个页面，对应 6 种事实对象。CognitionCenter / Cognition 等认知中心类页面对"5 种事实类型 + 提交记录"的领域来说偏重，但 Web 是面向 Human 的呈现层，UI 代码本来就容易膨胀，且未读内部不能下定论。
commits/git_adapter.py 735 行 + validation.py 592 行做提交信息校验。Git gate 是真实需求，但 candidate_index.py（454 行）在 commit 时重建事实候选索引的复杂度，需要确认是否每次 commit 都值得。
filesystem.py 1634 行——大量原子写/锁/跨平台封装，在多 AI 环境并发写同一事实库的场景下是合理的，不算过度。
🟢 合理的复杂度
并发控制（creation.py 里的 common-dir 锁 + 原子 compare-and-replace）是真实需求，多 AI 环境会并发写。
测试比 0.95 健康，最大的测试文件（workcase_application 2655 行）对应的也是最复杂的状态机。
跨 worktree / common-dir 的 git 身份解析（governance/git.py 322 行）在 git worktree 场景下是真问题。
规范与代码同源这个核心取舍是有意为之的产品决策，不是无意的过度设计。
如果要收敛，优先级建议
最高 ROI：用一个统一的 OperationResultBuilder 接管 sources/gaps/scopes 串接，能直接砍掉每个 operation 里 30–40% 的样板（helper 模块预期可减 2000–3000 行）。
合并 _request.py 进 _operation.py（或反过来，只留一个文件），18 个操作少跳一半文件。
把运行时"声明↔实现绑定"简化为启动期一次性校验，删掉 operation_sources/source_refs 里的运行时动态绑定部分（source 追踪可保留为构造器辅助，不必是 contextvar 绑定器）。
governance/resolver.py 的 6 条结局路径折叠成 2–3 条（成功 / 不可用带原因）。
合并 facts 的 update.py 进 repository，评估 creation_application/update_application 是否能与编排它们的 helper operation 合并。
总体判断：LDVH 不是那种"为了用设计模式而用模式"的过度设计，它的核心抽象（事实对象、规范同源、受控写入、可验证证据）都服务于明确的产品目标。但它在"让每个操作都自证合规"这件事上走到了极端——证据、来源、scope、gap 的逐字段手工拼接渗透到了每一层，导致一个本质上是"读/写 6 种 markdown/yaml 文件 + 校验 commit"的系统长出了 5 万行代码。税交得最重的是 helper 操作层和 governance resolver，这两处也是最值得收敛的。