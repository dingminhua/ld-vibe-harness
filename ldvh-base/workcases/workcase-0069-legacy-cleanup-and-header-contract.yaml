id: workcase-0069
type: workcase
title: 清退旧工作对象事实源与兼容入口
goal: |
  清退旧工作对象事实源与兼容入口
status: review_needed
created: '2026-06-18T09:08:15'
updated: '2026-06-18T10:33:29.221122'
workarea: workarea-0012
priority: P1
description: |
  清退旧 TaskPlan / Task / SubTask 工作对象事实源、Code 兼容入口、Web 展示入口和当前规范中的兼容维护规则。
  此次变更不保留旧对象兼容能力，因为项目尚未推广且没有外部使用者；历史事实由 Git 历史承载，
  当前事实源只保留 active 工作模型。
success_criteria: |
  - [x] `ldvh-base/taskplans/`、`ldvh-base/tasks/`、`ldvh-base/subtasks/` 不再作为当前事实源目录存在
  - [x] Code 不再把 `taskplan`、`task`、`subtask` 作为可读、可写、可校验或可迁移工作对象类型
  - [x] Web 不再暴露 TaskPlan / Task / SubTask 的默认入口、详情布局、兼容夹具或测试假设
  - [x] specs 中旧对象兼容、迁移保留、历史夹具维护等当前规则痕迹已清理或改写为当前事实规则
  - [x] Python、Web 和事实源校验通过，删除造成的残留问题已分流到 Spark 或后续 WorkCase
source: |
  Plan creation requested by dmh2002 on 2026-06-18:
  用户明确要求“不需要保留兼容能力，这个项目还没推广，没有其他人用”，并要求清理历史痕迹；
  后续又要求把事项拆成三个 WorkCase，审批后依次执行，中间问题记录到 Spark，最后总结经验用于完善流程。
  独立 Code/Web 清退审批子 Agent 已返回 APPROVE；本计划仍需 Human 明确批准后才能开始执行。
orchestration:
  mode: sequential
  execution_items:
  - id: item-1
    title: 记录旧对象清退范围和 Human Gate
    role: controller
    mode: single
    input_refs:
    - rules/LDVH-WORKSPACE-ENTRY.md
    - rules/LDVH-MAINTAINER-ENTRY.md
    - 用户 2026-06-18 明确授权
    expected_output: WorkCase 记录旧对象清退目标、成功标准、授权和执行分解。
    status: done
    result_summary: 已创建 workcase-0069，并根据用户要求缩窄为旧对象清退计划；独立 Code/Web 清退审批子 Agent 已批准，等待
      Human 执行批准。
    evidence_refs:
    - ldvh-base/workcases/workcase-0069-legacy-cleanup-and-header-contract.yaml
    - ldvh-base/workareas/workarea-0012-ldvh-internal-governance.yaml
    blocking_reason: null
  - id: item-2
    title: 清退旧工作对象事实源和 Code 兼容入口
    role: code
    mode: sequential
    input_refs:
    - code/fact_cli.py
    - code/fact_validate.py
    - ldvh-base/taskplans/
    - ldvh-base/tasks/
    - ldvh-base/subtasks/
    expected_output: 旧对象目录删除，CLI 和 Validator 不再接受旧对象类型，相关测试更新。
    status: done
    result_summary: 已删除旧 taskplans/tasks/subtasks 事实源目录和 workcase_migration 兼容脚本；fact_cli、fact_validate、fix_block_scalar、field_registry
      均收敛到 active 工作对象集合，并用测试断言旧对象类型不可创建、不可 show、不可作为当前字段。
    evidence_refs:
    - code/fact_cli.py
    - code/fact_validate.py
    - code/fix_block_scalar.py
    - code/spec_checks/field_registry.py
    - tests/code/test_fact_cli.py
    - tests/code/test_fact_validate.py
    - tests/code/specs_validate_checks/test_field_registry.py
    - /tmp/ldvh-legacy-delete-list.txt
    blocking_reason: null
  - id: item-3
    title: 清退 Web 旧对象展示入口和测试夹具
    role: web
    mode: sequential
    input_refs:
    - web/src/pages/ObjectDetail.tsx
    - web/src/components/ReadingPanel.tsx
    - web/src/i18n/locales.ts
    - tests/web/
    expected_output: Web 默认对象集合和详情阅读布局只面向 active 工作模型，旧对象夹具删除或替换。
    status: done
    result_summary: Web API、Dashboard、ObjectDetail、ObjectList、ReadingPanel、ReferenceCard、字段格式、颜色、状态和本地化入口已移除旧对象消费；Web
      API 测试改为断言旧对象 ID 不可作为对象引用或详情入口。
    evidence_refs:
    - web/api/routes/objects.ts
    - web/api/routes/dashboard.ts
    - web/api/services/facts.ts
    - web/api/services/pytools.ts
    - web/src/pages/ObjectDetail.tsx
    - web/src/pages/ObjectList.tsx
    - web/src/components/ReadingPanel.tsx
    - web/src/components/ReferenceCard.tsx
    - web/src/utils/fieldFormats.ts
    - tests/web/api/facts.test.ts
    blocking_reason: null
  - id: item-4
    title: 清理 specs 历史痕迹
    role: specs
    mode: sequential
    input_refs:
    - specs/05.02-工作模型字段内容与格式规范.md
    - specs/21-WorkCase-工作项.md
    expected_output: 当前规则不再要求维护旧对象兼容、旧夹具或迁移保留能力。
    status: done
    result_summary: 05.01 明确旧 TaskPlan/Task/SubTask 不再作为当前 active 工作模型或 Code/Web 兼容对象；21
      仅保留 TaskFlow 命名债和 WorkCase 关闭流程经验作为后续治理事项。
    evidence_refs:
    - specs/05.02-工作模型字段内容与格式规范.md
    - specs/21-WorkCase-工作项.md
    - code/docs/01-Code参考实现结构规范.md
    - web/docs/01-全局设计约束.md
    - web/docs/02-Dashboard.md
    - web/docs/04-ObjectDetail.md
    - web/docs/06-Changelog.md
    - web/docs/07-内容可读性深度研究.md
    - web/docs/08-网站整体性多角色审视研究.md
    - web/docs/09-图标语义规范.md
    blocking_reason: null
  review:
    controller_self_check: true
    specialist_review:
      required: true
      role: code-and-web-cleanup-review
      expected_output: 从 Code 可校验性和 Web 可表达性两个视角确认旧对象入口已退出当前产品面。
    human_closure_review: true
verification_evidence: |
  ## 验证计划

  历史证据格式规范化：检查原 验证证据 是否保留可复查的验证对象、结果或关闭判断；本次仅补齐四段式结构，不新增或改变历史事实语义。

  ## 验证命令

  历史记录未单独记录完整的验证命令；如原字段包含命令、脚本、检查入口或人工检查动作，保留在下方原始证据摘录中。

  ## 验证结果

  原始证据摘录：

  原文标题：验证结果

  旧对象事实源清退：
  - 删除清单 `/tmp/ldvh-legacy-delete-list.txt` 记录旧目录文件 129 个，覆盖 `ldvh-base/taskplans/` 与 `ldvh-base/tasks/`；本次也删除旧 Web 夹具 `tests/web/fixtures/taskplan-with-subtasks/`。
  - `code/workcase_migration.py` 与 `tests/code/test_workcase_migration.py` 已删除，不再维护迁移兼容入口。

  确定性校验：
  - `python3 code/fact_validate.py ldvh-base/`：files=108 errors=0 warnings=0。
  - `python3 code/specs_validate.py all --fail-on-diagnostics`：通过，diagnostics=0。
  - `python3 -m pytest tests/code -q`：194 passed。
  - `npm run test:web:api`：通过。
  - `npm run web:check`：通过。
  - `npm run web:build`：通过；仅保留 Vite chunk size warning，非本次对象契约问题。

  残留扫描：
  - `rg "taskplan|TaskPlan|subtask|SubTask|blocked_by|deliverables|source_tasks|related_taskplans|related_tasks|affected_docs|workcase_migration|22-Task|23-SubTask" code tests web/src web/api specs/05.02-工作模型字段内容与格式规范.md specs/21-WorkCase-工作项.md web/docs code/docs`
    结果仅剩 05.01 清退规则、fact_validate 移除字段诊断和“旧对象应被拒绝”的测试断言。
  - 当前事实源中的旧结构字段已清除；少量 WorkCase 历史叙述仍保留旧 Task 字样，作为迁移来源说明，不作为当前机器消费入口。

  原文标题：结论

  0069 的 Code/Web/specs/事实源清退目标已通过机器校验；TaskFlow 组件命名仍是 Web 内部命名债，已分流到 0071 做流程经验记录，不阻断本轮契约清退。

  ## 结论

  本次仅完成证据字段四段式格式规范化；对象状态、关闭判断、验证结果和历史事实语义以原始证据摘录为准，未额外伪造历史验证过程。
closure_evidence: |
  ## 验证计划

  历史证据格式规范化：检查原 关闭证据 是否保留可复查的验证对象、结果或关闭判断；本次仅补齐四段式结构，不新增或改变历史事实语义。

  ## 验证命令

  历史记录未单独记录完整的验证命令；如原字段包含命令、脚本、检查入口或人工检查动作，保留在下方原始证据摘录中。

  ## 验证结果

  原始证据摘录：

  原文标题：关闭判断

  0069 已完成旧工作对象事实源与兼容入口清退。当前 active 工作对象集合、CLI、Validator、Web API、Web 前端、字段注册表和测试夹具均不再把 TaskPlan / Task / SubTask 作为当前可消费对象。

  独立子 Agent 复审结论：
  - code 子 Agent：通过。确认旧对象目录不存在，fact_cli / fact_validate / specs_validate / tests/code 主链路已清退；`python3 code/fact_validate.py ldvh-base --format json` 为 files=108 errors=0 warnings=0；仍有 TaskFlow 命名债，但不属于旧对象入口、字段、目录或测试夹具依赖。
  - web 子 Agent：通过。确认 Web/API/前端字段消费视角未发现 `taskplan/task/subtask` 或 `related_taskplans/related_tasks/source_tasks/deliverables/affected_docs` 仍作为可消费契约存在；`tests/web/api/facts.test.ts` 中旧 ID 仅用于负向断言。

  后续分流：
  - `TaskFlow*`、`TaskFlowBar` 的 `tasks` prop、`sortPlanTasks`、`detail-task` class 和少量未消费旧文案 key 属于命名债，进入 workcase-0071 的流程经验与后续规范准备，不阻断 0069。
  - 历史 WorkCase、ADR、Spark、Study 中追溯迁移来源的 Task 字样不作为当前机器消费入口，本轮不做大规模文本改写。

  原文标题：结论

  0069 已完成执行和独立专业复审，可转入 review_needed 等待 Human closure review。

  ## 结论

  本次仅完成证据字段四段式格式规范化；对象状态、关闭判断、验证结果和历史事实语义以原始证据摘录为准，未额外伪造历史验证过程。
related_docs:
- specs/05.02-工作模型字段内容与格式规范.md
- specs/21-WorkCase-工作项.md
related_adrs: []
related_sparks:
- spark-0005
- spark-0009
related_pitfalls: []
related_workcases: []
review_requested_at: '2026-06-18T10:33:29.221116'
