# Trae-Solo AskUserQuestion使用规范

> 创建日期：2026-06-01
> 定位：定义 Trae Solo 环境机制中 AskUserQuestion 工具在 LDVH 中的使用方式、触发条件和跨 AI 知识传递路径
> 适用范围：所有接入 LD Vibe Harness 且运行于 Trae 平台的项目
> 上位依据：`specs/00-LD-Vibe-Harness理念与纲要.md` §3.3
> 相关规范：`specs/02-LDVH术语规范.md`、`specs/10-事实源边界与承载规范.md`、`specs/14-LDVH行动模型基础规范.md`、`specs/refs/12-Trae-AskUserQuestion问询工具调研.md`

---

---
## 1. 本文解决的问题

本文定义 Trae Solo 环境机制中 AskUserQuestion 工具在 LDVH 中的使用方式。具体包括：

1. AskUserQuestion 在 LDVH 架构中的定位与使用边界；
2. 何时应触发 AskUserQuestion 实现 Human Gate；
3. 问询问题的格式、选项设计和取消友好要求；
4. 用户取消后 AI 应遵循的降级策略；
5. 如何让不了解 AskUserQuestion 的 AI 知道如何使用。

本文不定义 Trae Solo 其他机制（如 Rules、Skill、Agent、Task、RunCommand 等）的使用方式，也不重新定义 AskUserQuestion 工具本身的参数细节。参数细节详见 `specs/refs/12-Trae-AskUserQuestion问询工具调研.md`。

---

## 2. 与 00 总纲的关系

00 §3.3 定义 Trae Solo 环境机制为 LDVH 五类构成要素之一，并声明 LDVH 对 Trae 机制的态度是"利用不重新定义"。本文依据 00 §3.3 声明利用 AskUserQuestion 工具实现 Human Gate 机制，不重新定义 Trae 平台提供的 AskUserQuestion 工具本身。

00 §5.3 定义人类确认闭环：关键决策、高风险操作、状态流转和事实源变更需要人类确认。本文定义人类确认闭环在 Trae 平台中通过 AskUserQuestion 工具实现的技术路径。

---

## 3. 定位与边界

### 3.1 利用不重新定义

本文只定义 AskUserQuestion 工具在 LDVH 中的：

1. **何时触发**：什么场景应调用 AskUserQuestion；
2. **如何设计**：问题格式、选项设计、取消友好要求；
3. **取消后如何处理**：降级策略和禁止重复追问规则；
4. **如何传递给 AI**：通过 Rules、Skill 和规范文档让 AI 了解用法。

本文不重新定义：

1. AskUserQuestion 的参数契约（见 refs/12 §2.1）；
2. Trae IDE 的弹窗渲染逻辑；
3. 用户选择后 IDE 如何封装返回消息。

### 3.2 与 refs/12 的关系

| 维度 | 本文（specs/05） | refs/12 |
|---|---|---|
| 性质 | LDVH 正式规范，定义使用方式 | 外部资料调研，定义工具细节 |
| 内容 | 触发场景、设计规范、降级策略、知识传递路径、refs 内容分工约束 | 参数契约、平台适配性、最佳实践、信息来源 |
| 权威性 | LDVH 强制要求 | 参考资料，不直接构成强制规则 |
| 引用关系 | 本文引用 refs/12 作为参数细节来源 | refs/12 不得反向引用 specs/ 目录内文件 |

AI 需要构造 AskUserQuestion 参数时，应同时参考本文（触发条件和设计规范）和 refs/12（参数契约和约束条件）。

### 3.3 refs 内容分工约束

refs/12 是外部资料引用，只包含平台层面的客观事实（参数契约、交互流程、平台适配性、实测结果等）。以下类型的内容**必须**写在本文或其他相关规范中，**不得**写入 refs/：

| 内容类型 | 应写入 | 不得写入 |
|---|---|---|
| 何时触发 AskUserQuestion | 本文 §5 | refs/12 |
| 问询设计规范和选项规则 | 本文 §6 | refs/12 |
| 取消后降级策略（强制规则） | 本文 §7 | refs/12 |
| 与 Human Gate 的映射关系 | 本文、14 §6.3 | refs/12 |
| 跨 AI 知识传递路径 | 本文 §8 | refs/12 |
| L1/L2 Rules 中 AskUserQuestion 声明模板 | 本文 §8.3 | refs/12 |
| 对 LDVH 规范的影响分析或建议 | 本文、ADR | refs/12 |
| 参数契约、返回值格式、平台适配性 | — | refs/12 ✓ |
| 实测案例（用户行为、返回值） | — | refs/12 ✓ |

本文 §10 合规检查项中"引用一致性"的判定标准之一：refs/12 不得包含上表"不得写入"列的内容。

---

## 4. AskUserQuestion 工具

### 4.1 定位

AskUserQuestion 是 Trae IDE 提供给 AI 的交互式问询工具，用于在关键决策点暂停 AI 执行，通过 UI 弹窗向用户展示选择题，用户点击选项后，选择结果返回给 AI，直接影响后续执行路径。

在 LDVH 中，AskUserQuestion 是实现 Human Gate 的首选技术方式。

### 4.2 参数契约摘要

AskUserQuestion 接收 `questions` 数组，每个问题包含以下字段：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `question` | string | 是 | 问题描述，以问号结尾 |
| `header` | string | 是 | 问题标签，chip/tag 显示，最多 12 字符 |
| `multiSelect` | boolean | 否 | 是否允许多选，默认 false |
| `options` | array | 是 | 选项列表，2-4 个选项 |

每个选项包含：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `label` | string | 是 | 选项显示文本，1-5 词 |
| `description` | string | 是 | 选项说明，解释选择后果 |

完整参数契约见 refs/12 §2.1。

### 4.3 约束条件

| 约束 | 值 |
|---|---|
| 问题数量 | 1-4 个 |
| 选项数量 | 每个问题 2-4 个 |
| header 长度 | 最多 12 字符 |
| label 长度 | 1-5 词 |
| 选项互斥 | multiSelect: false 时只能选一个 |

---

## 5. 触发场景

### 5.1 Human Gate 实现

当行动模型规范定义 Human Gate 触发条件时，应通过 AskUserQuestion 实现用户确认。

触发场景示例：

| 场景 | 问询示例 |
|---|---|
| 创建新事实对象 | "即将创建 ADR-001，是否确认创建？" |
| 关键状态流转 | "将 Task-001 从 proposed 转为 accepted，是否确认？" |
| 删除事实对象 | "即将删除 Memo-003，是否确认删除？" |
| 高风险操作 | "此变更影响 5 个文件，是否继续？" |

### 5.2 模式选择

当 AI 判断存在多种可行执行模式但难以取舍时，应通过 AskUserQuestion 交由用户选择。

示例（依据 51 §7.3）：

```text
header: "模式选择"
question: "难以判断是否需要升级，请选择执行模式："
options:
  - 轻量模式: 串行分析，共用上下文，快速得到结果
  - Agent 模式: 并行分析，独立上下文，结论隔离
  - 暂停讨论: 先详细说明差异，由用户决定
```

### 5.3 技术选型

当存在多个可行技术方案且无明确最优解时，应通过 AskUserQuestion 交由用户选择。

示例：

```text
header: "技术选型"
question: "[技术点] 有多种方案，请选择："
options:
  - [方案 A]: [后果说明]
  - [方案 B]: [后果说明]
  - 暂缓讨论: 暂不决策，先详细说明差异
```

### 5.4 高风险操作

当操作可能影响多个文件、模块或事实源时，应在执行前通过 AskUserQuestion 确认。

示例：

```text
header: "高风险"
question: "[操作描述]，影响 [N] 个文件，是否继续？"
options:
  - 继续: 执行操作
  - 暂停: 暂停执行
  - 查看影响详情: 先展示影响范围再决定
```

---

## 6. 问询设计规范

### 6.1 问题格式

| 规则 | 说明 |
|---|---|
| 以问号结尾 | question 字段必须以 `?` 或 `？` 结尾 |
| 场景前置 | 先描述场景，再提出问题 |
| 不含糊 | 问题应明确指向需要用户做的决策 |
| header 简洁 | 用 2-4 词概括问题类型，如"技术选型"、"方案确认"、"高风险" |

### 6.2 选项设计

| 规则 | 说明 |
|---|---|
| 选项互斥 | 各选项之间不重叠，用户能明确区分 |
| 后果说明 | 每个选项的 description 必须说明选择后果 |
| 覆盖完整 | 确保选项覆盖所有合理选择，不要遗漏"取消"或"暂缓"选项 |
| 推荐标注 | 如需推荐某选项，在 label 末尾添加 "(推荐)" |
| 数量适中 | 2-4 个选项，不超过 4 个 |

### 6.3 取消友好

| 规则 | 说明 |
|---|---|
| 必含暂缓选项 | 每个问询必须包含"取消"、"暂缓"或"暂停"等效选项 |
| 避免二选一强迫 | 只有两个选项时，应增加第三选项（如"详细说明"或"暂缓"） |
| 取消即暂缓 | 将取消行为理解为"暂缓执行，等待用户主动指示" |

---

## 7. 取消与降级处理

### 7.1 取消信号识别

用户点击取消时，系统返回的 answer 为取消标识（实际验证中返回 `暂停`）。AI 应识别以下信号为取消：

| 返回值 | 含义 |
|---|---|
| `暂停` | 用户点击取消 |
| 其他取消标识 | 取决于平台实现 |

### 7.2 降级策略

用户取消问询后，AI 必须遵循以下降级规则：

1. **不重复追问**：不得再次弹出相同或类似的问询；
2. **暂停执行**：输出"已暂停，等你进一步指示"或类似表述；
3. **等待用户指示**：由用户决定下一步，AI 不得自行继续；
4. **记录取消事件**：在证据或 Change 中记录用户取消行为（如适用）。

### 7.3 禁止重复追问

AI 在以下情况下不得重复弹出 AskUserQuestion：

| 场景 | 禁止行为 |
|---|---|
| 用户已点击取消 | 不得再次弹出相同问询 |
| 用户已选择某选项 | 不得因"不满意"而重新弹出 |
| 用户已明确下一步指示 | 不得回到旧问询 |

---

## 8. 跨 AI 知识传递

### 8.1 问题描述

AskUserQuestion 是 Trae 平台特有工具，其他 AI 模型不具备此能力。即使在 Trae 平台内，不同 AI 模型也可能不了解此工具的存在和用法。因此，需要通过 LDVH 的知识传递机制让 AI 了解如何使用。

### 8.2 知识传递路径

| 路径 | 机制 | AI 如何获知 |
|---|---|---|
| L0/L1/L2 Rules | 会话初始化时自动注入 | 平台自动注入 |
| Skill 模板 | 按需展开问询模板 | 语义匹配触发 |
| 规范文档引用 | AI 读取 05 或 refs/12 | 文件读取 |

### 8.3 Rules 注入

项目 L1 或 L2 Rules 中应声明 AskUserQuestion 的使用要求：

```markdown
## Human Gate 实现

本项目使用 Trae AskUserQuestion 工具实现 Human Gate 机制。
触发条件和设计规范见 specs/05-Trae-Solo AskUserQuestion使用规范.md §5-6。
取消处理见 §7。
```

### 8.4 Skill 模板

Skill 可提供结构化问询模板，降低 AI 构造参数的认知负担。Skill 模板应引用 05 §6（设计规范）和 §7（取消处理）。

### 8.5 规范文档引用

当 AI 需要详细了解 AskUserQuestion 工具参数时，引导读取 refs/12：

```text
参数契约详见 specs/refs/12-Trae-AskUserQuestion问询工具调研.md §2.1。
```

---

## 9. 机制关系声明

| 关联机制 | 关联实体 | 关系类型 | 同步触发 |
|---|---|---|---|
| Rules | `../.trae/rules/ldvh-l0-rules.md` | L0 工作区规则引用 | 本文的 AskUserQuestion 触发场景、设计规范或降级策略变化时 |
| Rules | `.trae/rules/ldvh-l1-rules.md` | L1 项目规则引用 | 本文的 Human Gate 实现方式或跨 AI 知识传递路径变化时 |
| Rules | `.trae/rules/ldvh-l2-specs-rules.md` | L2 场景规则引用 | 本文的问询设计规范或取消处理规则变化时 |
| 行动模型 | `specs/14-LDVH行动模型基础规范.md` §9 | Human Gate 上位定义 | 本文的触发场景与 14 §9 不一致时 |
| 行动模型 | `specs/51-multi-role-thinking-多角色思考.md` §7.3 | 模式选择规则 | 51 §7.3 的模式选择问询设计变化时 |
| 调研 | `specs/refs/12-Trae-AskUserQuestion问询工具调研.md` | 参数契约来源 | refs/12 新增参数、约束或平台行为变化时 |

---

## 10. Human Gate 与检查要求

### 10.1. Human Gate

新增或修改以下事项时，应评估 Human Gate：

1. 新增 AskUserQuestion 触发场景；
2. 修改降级策略或取消处理规则；
3. 在 L1/L2 Rules 中新增或移除 AskUserQuestion 使用要求。

### 10.2. 合规检查

本文合规检查项：

| 检查项 | 标准 |
|---|---|
| 利用不重新定义 | 本文未重新定义 AskUserQuestion 参数契约或平台行为 |
| 引用一致性 | 本文引用 refs/12 作为参数来源，refs/12 未反向引用 specs/ |
| refs 内容分工 | refs/12 未包含 §3.3 表中"不得写入"列的内容 |
| 取消友好 | 所有触发场景示例都包含"取消"或"暂缓"选项 |
| 降级策略 | §7.2 定义了不重复追问、暂停执行、等待指示、记录事件 4 条规则 |
| 编号分区 | 05 位于 00-09 总纲区段，符合 Trae 环境机制的定位 |

---

## 11. 待补齐事项

1. AskUserQuestion 与 NotifyUser 在 /plan 和 /spec 模式下的配合使用时机；
2. 子 Agent 是否可以调用 AskUserQuestion（平台待确认）；
3. AskUserQuestion 在 SOLO 模式下的行为是否与 Builder 模式一致；
4. 多问题批量询问（1-4 个问题同时弹出）的最佳实践和 UX 验证。
