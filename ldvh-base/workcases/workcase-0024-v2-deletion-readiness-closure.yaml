id: workcase-0024
type: workcase
title: V2 删除准备度收口
goal: |
  在准备删除本地 V2 前，完成 LDVH V3 的删除准备度收口：清除 active specs、事实对象和实现域文档中仍把本地 V2 当作后续迁移来源、稳定运行线或整体切换前提的活依赖，并建立可复查的验证检查，确保 V2 删除后 V3 主线仍自足。

  本 WorkCase 不以“继续迁移更多 V2 内容”为默认目标，而是逐项判断现有 V2 活引用应归入已迁入、不迁入或转为 V3 自身待办。Git history 仅作为审计追溯、争议复核或历史取证的备份渠道，不作为活依赖收口方式。只有仍支撑当前 V3 规则成立、Code 消费或事实对象关闭判断的内容，才进入 V3 正式规范或事实对象。

  2026-07-07 Human 进一步收紧判断标准：V3 `specs/00` 是已审核目标；本 WorkCase 不全量审计 V2，也不从 Git history 反推迁移清单。执行范围只限当前 V3 specs、事实对象、README 和实现域文档中已经暴露的 V2 / legacy / migration / deletion-readiness 活口径；每条必须回到 00 的 AI-first、事实源边界、保障与衔接层、五类构成要素、V1-V9 和 Human Gate 判断。缺漏后续按 V3 00 重写，不把 V2 重新升格为权威来源。
status: closed
created: '2026-07-07T12:48:59+08:00'
updated: '2026-07-08T23:42:00+08:00'
priority: P0
description: |
  Human 指出：既然迁移模板预期是迁移完成后 V2 准备删除，那么当前 V3 中仍存在“后续从 V2 筛选 / 判断 / 迁入”的待补齐事项就需要重新审核。

  2026-07-07 已完成三路只读审计：
  - specs/attachments 审计结论：不具备删除准备度；P0 阻断集中在 `specs/03`、`specs/05`、`specs/06` 仍把 V2 附件或行动成员写成后续筛选来源。
  - Code/tests/runtime 审计结论：通过；未发现删除本地 V2 会坏掉的活依赖，旧路径主要是 stale path 检测和测试 fixture。
  - README/事实对象/实现域文档审计结论：不具备删除准备度；`spark-0039` 和 `workcase-0022` 仍以未关闭状态保留“v2 继续稳定服务 / 待整体迁移”的活承诺。

  2026-07-07 复核当前事实对象状态：`workcase-0022` 已关闭，并明确把“V2 关键内容迁移完整性尚未确认”分流给本 WorkCase；`spark-0039` 仍是 `pending`，其中“v2 继续作为当前稳定运行线”等旧路线承诺仍需由本 WorkCase 判定为历史背景、改写或归档。

  本 WorkCase 用于承接后续逐步修补，不替代具体 specs 修改、事实对象关闭、测试或最终删除决策。
success_criteria: |
  - [x] 建立 V3-current V2-related 待办判定表；来源只限当前 V3 specs、事实对象、README 和实现域文档中已经暴露的待补齐事项或活口径，不从 V2 / Git history 主动挖迁移清单。
  - [x] 每条 V2-related 条目按 00 价值锚点归类为 `must_close_before_v2_delete`、`covered`、`archive_only` 或 `human_decision`，并说明它服务或影响的 AI-first、事实源、保障、五类构成要素、V1-V9 或 Human Gate。
  - [x] `specs/03-事实源与Git溯源规范.md` 不再把本地 V2 `07.Att.*` 写成后续筛选来源；commit type/scope/body 已迁入部分、非事实源排除清单、过程输出回写、读取策略等逐项判定为已迁入、不迁入或 V3 后续待办。
  - [x] `specs/05-事实模型基础规范.md` 不再把本地 V2 `02.Att.*`、20-24 成员文件或 WorkCase orchestration 长表写成活迁移来源；已由 `05.Att.01`、`21.Att.01`、20-24 成员规范、Code/tests schema 承接的内容完成收口。
  - [x] `specs/06-行动模板基础规范.md` 不再写“后续从 V2 30-36 和 WorkCase 流程筛选”；候选行动模板改为 V3 自身候选清单、明确后续 WorkCase 或明确不迁入。
  - [x] `specs/07`、20/22/23/24、21 的过期待补齐事项完成清理，避免未来 AI 误以为仍需回本地 V2 做字段或实现来源判断。
  - [x] `spark-0039` 的旧路线活承诺被关闭、废弃或改写为历史背景；`workcase-0022` 已关闭，只作为“完整性担忧已分流到本 WorkCase”的来源证据，不再作为未关闭阻断重复处理。
  - [x] README、code/docs 和实现域保留必要 stale V2 path 检测，但不把旧 V2 路径、旧插件或旧运行线写成当前依赖。
  - [x] 新增或更新 Code/tests 检查，阻止 active specs 再出现“后续从本地 V2 筛选 / 判断 / 迁入”的活依赖句式，并阻止把 Git history 写成未迁内容承接或验收出口；允许 `migration_sources: v2:...`、历史审计语境和 stale path 检测。
  - [x] 最终验证包含 specs validation、相关 targeted tests、子 agent 复审和工作区 clean 检查。
source: |
  来源：2026-07-07 当前会话。

  Human 关键输入：
  - “那按照这个要求是不是之前制定的 v2 迁移计划要重新审核？我的模板是这次迁移完成后 v2 要准备删除了”
  - “那你重新审核吧，我觉得安排子 agent 一起重新审核然后可以互补一下”
  - “你看有什么要记录的，先做一个记录，我们逐步完成”

  审计依据：
  - `README.md`
  - `specs/03-事实源与Git溯源规范.md`
  - `specs/05-事实模型基础规范.md`
  - `specs/06-行动模板基础规范.md`
  - `specs/07-Code确定性执行规范.md`
  - `specs/20-Spark-火花.md`
  - `specs/21-WorkCase-工作项.md`
  - `specs/22-ADR-决策.md`
  - `specs/23-Pitfall-踩坑经验.md`
  - `specs/24-Study-研究报告.md`
  - `ldvh-base/sparks/spark-0039-v3-sidecar-action-guide-compiler.yaml`
  - `ldvh-base/workcases/workcase-0022-v3-sidecar-action-guide-compiler-phase1.yaml`
  - `code/environment_entry_audit.py`
  - `tests/code/test_ldvh_specs_validate.py`
orchestration:
  mode: sequential
  execution_items:
    - id: item-0
      title: 建立 V3-current V2-related 待办判定表
      role: planning
      mode: single
      input_refs:
        - specs/00-理念与构成.md
        - README.md
        - specs/03-事实源与Git溯源规范.md
        - specs/05-事实模型基础规范.md
        - specs/06-行动模板基础规范.md
        - specs/07-Code确定性执行规范.md
        - specs/08-Web信息同步规范.md
        - specs/09-测试与验证规范.md
        - specs/10-安装与配置规范.md
        - specs/20-Spark-火花.md
        - specs/21-WorkCase-工作项.md
        - specs/22-ADR-决策.md
        - specs/23-Pitfall-踩坑经验.md
        - specs/24-Study-研究报告.md
        - specs/30-安装配置与验证行动模板.md
        - specs/33-环境插件编写与更新行动模板.md
        - ldvh-base/sparks/spark-0039-v3-sidecar-action-guide-compiler.yaml
        - ldvh-base/workcases/workcase-0022-v3-sidecar-action-guide-compiler-phase1.yaml
      expected_output: |
        只从当前 V3 文件中的待补齐事项和活口径抽取 V2-related 条目，按 00 价值锚点判定 `must_close_before_v2_delete` / `covered` / `archive_only` / `human_decision`。不得从 V2 / Git history 主动挖迁移清单。
      status: done
      result_summary: |
        初步判定表：
        - `specs/03` §12.1：`must_close_before_v2_delete`。当前文本仍写“后续应从 V2 `07.Att.*` 中筛选”，影响事实源、commit contract、非事实源排除和验证回写边界，直接服务 V3、V6、V7。
        - `specs/05` §12.1/§12.2/尾段：`must_close_before_v2_delete`。当前文本仍把 V2 `02.Att.*`、成员完整字段和 WorkCase orchestration 长表写成后续筛选来源；其中 WorkCase orchestration 已由 `21.Att.01` 基本覆盖，但 05 的活口径仍需改写成 V3-owned 字段契约后续。
        - `specs/06` §11.1/§11.2：`must_close_before_v2_delete`。行动模板候选仍以 V2 30-36 和 WorkCase 流程为后续筛选来源，需要改成 V3 当前候选、明确后续 WorkCase 或明确不迁入。
        - `specs/07` §13：`covered`。当前文本已声明无未承接体系缺口，V2 runtime / hook material 只作为 AI lifecycle Hook 或 external-adapter 候选边界，真实环境入口按 01、09、10 和 Human Gate 后置。
        - `specs/08` §12.1：`covered` / `archive_only`。Web DTO/API/source/cache/Spark quick create 轻写入回归已由 Web 实现和 tests 承接；未授权长表只作为历史审计材料。
        - `specs/09` §12.1：`covered` / `archive_only`。测试归属、验证声明字段和失败阻断清单父层规则已由 09 和 `09.Att.01` 承接；未闭环条目只保留为历史审计材料。
        - `specs/10`、`specs/30`、`specs/33` 与 README stale V2 path 语境：`covered`。这些文本是旧插件、旧路径和 stale path 检测 / 安装升级 guardrail，不是当前 V2 活依赖；必须保留诊断价值。
        - `specs/20`、`specs/22`、`specs/23`、`specs/24` 尾部字段/示例/frontmatter 待办：`human_decision`。这些可能仍有价值，但应作为 V3 字段契约、实例校验、Web/API 或行动模板后续判断，不应继续依赖本地 V2 作为来源。
        - `specs/21`：`covered`。WorkCase 顶层字段、`orchestration` 最小契约和状态条件必填已由 `21.Att.01` 承接；后续枚举、子字段和历史诊断必须维护 field-contract-first 边界。
        - `spark-0039`：`must_close_before_v2_delete`。该 Spark 仍为 `pending`，旧路线承诺仍可能被 AI 当作当前主线承诺，需要归档、改写或明确被 README / 0024 替代。
        - `workcase-0022`：`covered`。该 WorkCase 已 `closed`，并已声明关闭不等于确认 V2 关键内容完整迁入；完整性担忧由本 WorkCase 继续承接。
      evidence_refs:
        - specs/00-理念与构成.md
        - README.md
        - specs/03-事实源与Git溯源规范.md
        - specs/05-事实模型基础规范.md
        - specs/06-行动模板基础规范.md
        - ldvh-base/sparks/spark-0039-v3-sidecar-action-guide-compiler.yaml
        - ldvh-base/workcases/workcase-0022-v3-sidecar-action-guide-compiler-phase1.yaml
      blocking_reason: ''
    - id: item-1
      title: 收口 specs/03 的 V2 07.Att 活依赖
      role: specs
      mode: single
      input_refs:
        - specs/03-事实源与Git溯源规范.md
        - specs/attachments/03.Att.01-Commit-Message契约字段表.md
        - v2:specs/attachments/07.Att.01-事实归属矩阵.md
        - v2:specs/attachments/07.Att.06-过程输出回写核对表.md
        - v2:specs/attachments/07.Att.10-事实源读取策略核对表.md
        - v2:specs/attachments/07.Att.11-非事实源排除清单.md
        - v2:specs/attachments/07.Att.12-事实承载介质选择表.md
      expected_output: |
        03 的待补齐事项不再要求从本地 V2 `07.Att.*` 筛选；commit message 已承接内容、非事实源排除清单、过程输出回写、读取策略和承载介质边界逐项落入 V3 正文、附件、Code/tests 后续或明确不迁入。
      status: done
      result_summary: |
        `specs/03` §7 / §12.1 已从“V2 07.Att.* 作为迁移来源 / 后续筛选”改为删除准备度收口口径：commit type、scope、body 条件和 message 字段由 `03.Att.01`、Code 和 tests 承接；非事实源排除、过程输出回写、读取策略和承载介质边界由 03、01、09 和 Code source_refs 承接。旧 V2 `07.Att.*` 未进入当前 V3 specs、附件、Code/tests 或明确 WorkCase 的条目只保留为历史审计语境，不作为当前规则、验收依据或删除阻断；后续缺漏按 V3 00 新建 V3-owned 待办。
      evidence_refs:
        - specs/03-事实源与Git溯源规范.md
        - specs/attachments/03.Att.01-Commit-Message契约字段表.md
      blocking_reason: ''
    - id: item-2
      title: 收口 specs/05 与成员字段迁移活口径
      role: specs
      mode: single
      input_refs:
        - specs/05-事实模型基础规范.md
        - specs/attachments/05.Att.01-字段注册表结构.md
        - specs/attachments/21.Att.01-orchestration字段契约表.md
        - specs/20-Spark-火花.md
        - specs/21-WorkCase-工作项.md
        - specs/22-ADR-决策.md
        - specs/23-Pitfall-踩坑经验.md
        - specs/24-Study-研究报告.md
        - v2:specs/attachments/02.Att.*
      expected_output: |
        05 和 20/22/23/24 的待补齐事项改为 V3 自身字段契约、实例校验和 Web/API 后续；不再要求保留本地 V2 `02.Att.*` 作为后续筛选来源。WorkCase orchestration 长表已由 `21.Att.01` 承接的事实同步到 05。
      status: done
      result_summary: |
        `specs/05` §7 / §12 已改为 V3-owned 字段契约口径：Spark、WorkCase、ADR、Pitfall、Study 与真实 `ldvh-base/` 实例按当前 V3 成员规范、`05.Att.01`、`21.Att.01`、Code/tests schema 维护。成员完整字段表后续只能作为 V3-owned 字段契约、Code/tests schema 或明确 WorkCase 推进；本地 V2 `02.Att.*`、20-24 成员文件和事实对象 CLI 仅保留为历史审计语境，不再作为后续来源、当前规则、验收依据或删除阻断。`specs/20`、`21`、`22`、`23`、`24` 也已把后续字段 / frontmatter / 实例样例判断改为当前 V3 契约补充判断，不再以本地 V2 成员文件作为默认来源。
      evidence_refs:
        - specs/05-事实模型基础规范.md
        - specs/20-Spark-火花.md
        - specs/21-WorkCase-工作项.md
        - specs/22-ADR-决策.md
        - specs/23-Pitfall-踩坑经验.md
        - specs/24-Study-研究报告.md
        - specs/attachments/05.Att.01-字段注册表结构.md
        - specs/attachments/21.Att.01-orchestration字段契约表.md
      blocking_reason: ''
    - id: item-3
      title: 收口 specs/06 行动模板候选活依赖
      role: specs
      mode: single
      input_refs:
        - specs/06-行动模板基础规范.md
        - specs/30-安装配置与验证行动模板.md
        - specs/31-Git提交行动模板.md
        - specs/33-环境插件编写与更新行动模板.md
        - v2:specs/30-36
      expected_output: |
        06 不再写“后续从 V2 30-36 和 WorkCase 流程筛选”；候选行动模板改为当前 V3 已知候选、具体 WorkCase 后续或明确不迁入。30/31/33 的正式模板地位与未迁候选边界清楚。
      status: done
      result_summary: |
        `specs/06` §11 已从“后续从 V2 30-36 和 WorkCase 流程中逐项筛选”改为当前 V3 候选来源：候选行动模板来自当前 V3 需求、已识别 WorkCase / Spark 和 Human 明确目标；不得以本地 V2 30-36 或 WorkCase 流程作为默认筛选清单。30/31/33 仍按正式模板成员维护，未进入当前 V3 的旧行动成员不构成删除阻断。
      evidence_refs:
        - specs/06-行动模板基础规范.md
        - specs/30-安装配置与验证行动模板.md
        - specs/31-Git提交行动模板.md
        - specs/33-环境插件编写与更新行动模板.md
      blocking_reason: ''
    - id: item-4
      title: 收口未关闭事实对象中的旧路线承诺
      role: facts
      mode: single
      input_refs:
        - ldvh-base/sparks/spark-0039-v3-sidecar-action-guide-compiler.yaml
        - ldvh-base/workcases/workcase-0022-v3-sidecar-action-guide-compiler-phase1.yaml
        - ldvh-base/studies/study-0010-spark-study-absorption-index.md
        - ldvh-base/workcases/workcase-0001-runtime-entry-user-input-contract.yaml
      expected_output: |
        pending / open 事实对象不再承诺 “v2 继续稳定服务” 或 “待整体迁移后切换”。历史语境可保留，但必须标明已被 V3 主线替代、由本 WorkCase 承接或明确不迁入；不得把 Git history 写成完成依据。
      status: done
      result_summary: |
        `spark-0039` 已从 `pending` 改为 `resolved`，并明确其中“v2 继续作为当前稳定运行线”“待 specs + Code 成熟后整体迁移”等旧路线承诺只保留为历史背景，不再作为当前 V3 主线承诺、删除阻断或后续迁移来源；后续缺漏按 V3 00 新建 V3-owned 待办。`workcase-0022` 已关闭，只作为完整性担忧已分流到本 WorkCase 的来源证据。
      evidence_refs:
        - ldvh-base/sparks/spark-0039-v3-sidecar-action-guide-compiler.yaml
        - ldvh-base/workcases/workcase-0022-v3-sidecar-action-guide-compiler-phase1.yaml
      blocking_reason: ''
    - id: item-5
      title: 增加删除准备度防回归检查
      role: code-tests
      mode: single
      input_refs:
        - code/ldvh_specs.py
        - tests/code/test_ldvh_specs_validate.py
        - README.md
      expected_output: |
        新增或更新只读 validator / tests：active specs 不得出现“后续从 V2 / 本地 V2 筛选、判断、迁入”的活依赖；Git history 不得作为未迁内容承接或验收出口；允许 `migration_sources: v2:...`、历史来源说明、审计追溯语境和 stale path 检测。
      status: done
      result_summary: |
        `code/ldvh_specs.py` 已新增 `V2_DELETION_ACTIVE_DEPENDENCY_FORBIDDEN` 检查：active specs 正文不得再出现“后续从 V2 / 本地 V2 筛选、判断、迁入”“只能作为迁移来源”“再判断是否迁入”等活依赖句式；检查范围避开附件，并继续允许 migration_sources、历史来源说明、审计追溯语境和 stale path 检测。`tests/code/test_ldvh_specs_validate.py` 已补正向防回归用例和同义残留用例。
      evidence_refs:
        - code/ldvh_specs.py
        - tests/code/test_ldvh_specs_validate.py
      blocking_reason: ''
  plan_review:
    orchestration_owner: main_controller
    workflow_ref: null
    review_policy:
      selection_reason: |
        本工作直接影响 V2 删除准备度和 V3 主线自足性。若处理不完整，删除本地 V2 后会留下规范待办、事实对象和 README 之间的冲突；若处理过度，则可能误删合理的历史追溯和 stale path 检测。
      required_perspectives:
        - specs_attachments_deletion_readiness
        - code_tests_runtime_deletion_readiness
        - facts_readme_docs_deletion_readiness
      optional_perspectives:
        - commit_gate_contract
        - environment_plugin_stale_path_detection
      tool_method_requirements:
        - 子 agent 审计只读，不修改文件。
        - 必须区分 active V2 活依赖和允许的历史追溯。
        - 必须区分本地 V2 文件依赖和历史审计查询。
        - 必须保留 stale V2 path 检测，不得当作删除对象。
      aggregation_rule: |
        任一 required perspective 发现 `blocks_v2_deletion`，不得声明本地 V2 可删除。所有阻断必须转为已迁入、不迁入或 V3 自身待办后，才能进入删除准备度完成判断；Git history 只能作为审计追溯、争议复核或历史取证备份，不能作为阻断收口方式。
    review_items:
      - id: review-1
        role: specs-attachments
        agent_name: McClintock
        requested_at: '2026-07-07T12:00:00+08:00'
        prompt_context: |
          只读审计 `specs/` 和 `specs/attachments/` 是否仍保留本地 V2 删除阻断。
        result:
          status: fail
          summary: |
            当前还不具备 specs/attachments 删除准备度。P0 阻断集中在 `specs/03`、`specs/05`、`specs/06`：它们仍把 V2 附件或 V2 行动成员写成后续筛选 / 迁入来源。`specs/07` 和 20/22/23/24 还有需要决策的活口径，`specs/21` 有过期待补齐事项。
          key_findings:
            - specs/03 仍要求后续从 V2 `07.Att.*` 筛选 commit type、scope、body 条件和非事实源排除清单。
            - specs/05 仍要求后续筛选 V2 `02.Att.*`，且上文仍把 20-24 和 WorkCase orchestration 长表写成后续判断来源。
            - specs/06 仍写后续从 V2 30-36 和 WorkCase 流程筛选正式行动模板候选。
            - migration_sources 和 stale path 处理属于可接受历史引用，不阻断删除。
          required_changes:
            - 收敛 03/05/06 P0 活依赖。
            - 决策 07 与成员规范中过期待补齐事项。
          evidence_refs:
            - specs/03-事实源与Git溯源规范.md
            - specs/05-事实模型基础规范.md
            - specs/06-行动模板基础规范.md
      - id: review-2
        role: code-tests-runtime
        agent_name: Mill
        requested_at: '2026-07-07T12:00:00+08:00'
        prompt_context: |
          只读审计 `code/`、`tests/`、`hooks/`、`web/` 和根配置是否依赖本地 V2。
        result:
          status: pass
          summary: |
            Code/tests/runtime 范围未发现会因删除本地 V2 目录而坏掉的活依赖。旧 `ld-vibe-harness`、`hook_adapter.py`、`hook_dispatch.py` 路径主要用于 stale path 诊断和测试 fixture，应保留。
          key_findings:
            - "`code/environment_entry_audit.py` 的旧路径匹配是 legacy detection。"
            - "`tests/code/test_ldvh_specs_validate.py` 和 install verification 中旧路径是 fixture。"
            - Codex 插件 README 写“来自 V2 icons”是 stale wording，不阻断。
          required_changes:
            - 可后续清理图标来源文案。
            - 保留 stale path 检测和负例测试。
          evidence_refs:
            - code/environment_entry_audit.py
            - tests/code/test_ldvh_specs_validate.py
            - hooks/environment-plugins/codex-ldvh-v3/README.md
      - id: review-3
        role: facts-readme-docs
        agent_name: Ptolemy
        requested_at: '2026-07-07T12:00:00+08:00'
        prompt_context: |
          只读审计 README、code/docs 和事实对象是否仍保留本地 V2 删除阻断。
        result:
          status: fail
          summary: |
            README 已宣称 V2 关闭为历史来源，但 pending/open 事实对象仍保留“v2 继续稳定服务 / 待整体迁移”的活承诺，形成口径冲突。`spark-0039` 和 `workcase-0022` 是 P1 删除阻断，`study-0010` 和 `workcase-0001` 需要删除前决策。
          key_findings:
            - "`spark-0039` 仍是 pending，并写 V2 继续作为当前稳定运行线。"
            - "`workcase-0022` 仍是 result_self_checking，并写 V2 保持当前稳定运行、待 specs + Code 成熟后再整体迁移。"
            - README 口径与上述事实对象冲突，需要先改写或关闭事实对象。
          required_changes:
            - 收口 `spark-0039` 和 `workcase-0022`。
            - 处理 `study-0010` 中 “v2 当前需求” 表述。
            - 明确 `workcase-0001` 的 v2 字段格式只是历史来源。
          evidence_refs:
            - README.md
            - ldvh-base/sparks/spark-0039-v3-sidecar-action-guide-compiler.yaml
            - ldvh-base/workcases/workcase-0022-v3-sidecar-action-guide-compiler-phase1.yaml
    controller_resolution:
      status: plan_confirmed
      summary: |
        Human 已将执行标准收紧为：V3 `specs/00` 是已审核目标；只整理当前 V3 文件里已经暴露的 V2-related 待办和活口径；不全量审计 V2，不从 Git history 反推迁移清单。主控已据此新增 `item-0` 判定表，并将后续执行收敛到 03/05/06、`spark-0039`、防回归检查和必要的人判项。
      required_changes_handling:
        - 03/05/06 仍按 P0 删除准备度阻断处理。
        - "`workcase-0022` 已关闭，不再作为未关闭阻断重复处理；其完整性担忧由本 WorkCase 承接。"
        - "20/22/23/24 尾部字段细节先标为 `human_decision`，后续按 V3 字段契约或实例校验判断，不回退为 V2 权威。"
      unresolved_items:
        - 本轮 03/05/06、`spark-0039`、防回归检查、子 agent 复审、验证结果记录和 Human closure 判断均已完成；实际删除本地 V2 仍需另行授权。
    human_confirmation:
      decision: execute_v3_current_plan
      confirmed_at: '2026-07-07'
      confirmer: Human
      scope: |
        Human 确认按“V3 00 为锚点、只抽取当前 V3 中已经暴露的 V2-related 待办和活口径、不全量审计 V2、不把 Git history 当迁移清单”的计划执行。
      constraints:
        - "V3 `specs/00` 是当前已审核目标和最高价值锚点。"
        - 不默认继续迁移更多 V2 内容。
        - 不从 V2 或 Git history 主动生成迁移 checklist。
        - 后续发现缺漏时按 V3 00 重写或新建 V3-owned 待办，不恢复 V2 权威。
  result_review:
    orchestration_owner: main_controller
    workflow_ref: null
    review_policy:
      selection_reason: |
        本 WorkCase 已完成 03/05/06、`spark-0039`、防回归检查、测试验证、外部审核、子 agent 复审和 Human closure 判断。本结果复核记录只作为关闭证据，不授权立即删除本地 V2。
      required_perspectives:
        - specs_closure_result
        - facts_closure_result
        - code_tests_guardrail_result
      tool_method_requirements:
        - 修补后必须复跑 specs validation 和 targeted tests。
        - 必须确认 active specs 不再含本地 V2 活依赖句式。
        - 必须确认 README 与事实对象状态口径一致。
    controller_self_check: {}
    review_items:
      - id: result-review-1
        role: external-ai-review
        agent_name: external-ai
        requested_at: '2026-07-08T23:00:00+08:00'
        prompt_context: |
          Human 将 5 个 specs 收敛与 V2 删除准备度提交交给外部 AI 只读审核，要求按 P0/P1/P2 判断是否可进入 Human closure review。
        result:
          status: pass_with_followups
          summary: |
            外部审核结论为 P0 无、P1 无，5 个提交整体可保留，可以进入 Human closure review；仍不能声明“已授权立即删除 V2”。外部审核列出的 P2 中，`study-0010` 当前化 v2 需求表述和 Codex README 图标来源文案已在后续提交中清理；事实模型共享字段契约已记录为 V3-owned 后续工作，不作为 V2 删除准备度阻断。
          evidence_refs:
            - specs/03-事实源与Git溯源规范.md
            - specs/05-事实模型基础规范.md
            - specs/06-行动模板基础规范.md
            - specs/07-Code确定性执行规范.md
            - specs/20-Spark-火花.md
            - specs/21-WorkCase-工作项.md
            - specs/22-ADR-决策.md
            - specs/23-Pitfall-踩坑经验.md
            - specs/24-Study-研究报告.md
            - ldvh-base/sparks/spark-0039-v3-sidecar-action-guide-compiler.yaml
            - ldvh-base/studies/study-0010-spark-study-absorption-index.md
      - id: result-review-2
        role: subagent-result-review
        agent_name: Helmholtz
        requested_at: '2026-07-08T16:40:00+08:00'
        prompt_context: |
          只读总复审能力匹配、自锁处置、职责边界、V2 收口提交是否整体自洽。
        result:
          status: pass_with_followups
          summary: |
            子 agent 总复审未发现 P0/P1；指出 `specs/21` 仍有“后续继续逐篇判断 Spark、ADR、Pitfall 和 Study 是否进入 V3 成员规范”的陈旧 P2，已由提交 `e3346ee7` 清理。
          evidence_refs:
            - specs/21-WorkCase-工作项.md
      - id: result-review-3
        role: subagent-p2-cleanup-review
        agent_name: Jason
        requested_at: '2026-07-08T23:20:00+08:00'
        prompt_context: |
          只读复审 `study-0010` 和 Codex README 历史文案残留清理。
        result:
          status: pass
          summary: |
            子 agent 复审确认 `study-0010` 当前承接表述已改为 V3-owned / 当前 V3-owned，历史语境仍保留；Codex README 图标资产来源已改为当前样例包内资产；无 P0/P1/P2。
          evidence_refs:
            - ldvh-base/studies/study-0010-spark-study-absorption-index.md
            - hooks/environment-plugins/codex-ldvh-v3/README.md
      - id: result-review-4
        role: subagent-shared-field-followup-review
        agent_name: Zeno
        requested_at: '2026-07-08T23:31:00+08:00'
        prompt_context: |
          只读复审事实模型共享字段契约是否被正确记录为 V3-owned 后续工作。
        result:
          status: pass
          summary: |
            子 agent 复审确认共享字段契约在 05 和本 WorkCase 中均写为 V3-owned 后续工作，并明确不作为 V2 删除准备度阻断；无 P0/P1/P2。
          evidence_refs:
            - specs/05-事实模型基础规范.md
            - ldvh-base/workcases/workcase-0024-v2-deletion-readiness-closure.yaml
    controller_resolution:
      status: closure_confirmed
      summary: |
        主控接受外部审核和子 agent 复审结论：当前已识别的 V3 active specs、事实对象、README 和实现域文档中的本地 V2 活依赖已收口；防回归检查已建立；旧路线承诺和历史文案残留已处理或转为 V3-owned 后续项。P0/P1 均为无，P2 已处理或明确不阻断关闭。
      required_changes_handling:
        - 外部审核 P2 `study-0010` 当前化 v2 需求表述已清理为当前 V3-owned 需求。
        - 外部审核 P2 Codex README “来自 V2 icons” 已清理为当前样例包内资产。
        - 外部审核 P2 事实模型共享字段契约已记录为 V3-owned 后续工作，不作为 V2 删除准备度阻断。
      unresolved_items:
        - 关闭本 WorkCase 不等于确认 V2 关键内容全量无缺漏；后续发现缺漏时按 V3 00 新建 V3-owned 待办。
        - 关闭本 WorkCase 不等于授权立即删除本地 V2；删除动作仍需 Human 单独授权。
    human_closure_confirmation:
      decision: close
      confirmed_at: '2026-07-08T23:42:00+08:00'
      confirmer: Human
      scope: |
        Human 已确认关闭 `workcase-0024`。关闭范围仅限 V2 删除准备度收口：当前已识别的 V3 active 文件不再把本地 V2 作为后续规则、迁移、验收或实现来源。
      summary: |
        Human 接受当前验证、外部审核、子 agent 复审和主控处理结论，确认本 WorkCase 以 completed 结果关闭。该确认不授权立即删除本地 V2；删除动作需另行决策。
plan_confirmed_at: '2026-07-07'
review_requested_at: '2026-07-08T23:00:00+08:00'
verification_evidence: |
  记录创建前已完成只读审计：
  - specs/attachments 子审计：不通过，发现 03/05/06 P0 删除阻断。
  - Code/tests/runtime 子审计：通过，未发现删除本地 V2 会坏掉的活依赖。
  - README/事实对象/实现域文档子审计：不通过，发现 `spark-0039`、`workcase-0022` 活承诺冲突。

  2026-07-07 已按 Human 新标准建立 V3-current V2-related 待办判定表，见 `orchestration.execution_items.item-0.result_summary`。本次判定只覆盖当前 V3 文件中已经暴露的活口径，不声明 V2 关键内容全量完整迁入。

  2026-07-08 本轮修补完成：
  - `specs/03`、`specs/05`、`specs/06` 已清除“后续从本地 V2 筛选 / 判断 / 迁入”的 active 依赖口径，并改为 V3-owned 待办与已承接来源。
  - `specs/07`、`specs/20`、`specs/21`、`specs/22`、`specs/23`、`specs/24` 已把后续实现 / 字段 / 实例补充判断改为当前 V3-owned 口径，不以本地 V2 文件作为默认来源。
  - `spark-0039` 已改为 `resolved`，旧 v2 稳定运行线 / 整体迁移承诺仅保留为历史背景。
  - `code/ldvh_specs.py` 和 `tests/code/test_ldvh_specs_validate.py` 已新增 active V2 dependency 防回归检查，并覆盖“后续从 V2”和“只能作为迁移来源 / 再判断是否迁入”同义残留。
  - `python3 code/specs_validate.py all --format text --fail-on-diagnostics` 通过，diagnostics 为 0。
  - `python3 -m pytest tests/code/test_ldvh_specs_validate.py -q --tb=short -k "v2_deletion_readiness or current_specs_validate_without_diagnostics"` 通过，4 passed。
  - `python3 code/test_runner.py smoke` 通过。
  - `python3 -m pytest tests/code -q -m 'not slow' --durations=10 --tb=short` 通过，227 passed / 135 deselected；该轮在同义残留测试新增前启动，作为既有 code fast 回归补充证据。
  - `python3 code/test_runner.py targeted ... --slow skip --dry-run` 输出选择层为 quick_structure、fact_instance_validator、targeted_code_fast，并声明 runtime/e2e/environment/web/full-build 为未验证范围。
  - 子 agent 首轮复审发现 03/05 仍残留“V2 作为迁移来源 / 再判断是否迁入”同义活口径；本轮已修正 03/05 正文并扩展 validator/tests。修正后 active specs 禁止句式搜索无命中。
closure_evidence: |
  ## 关闭判断

  本 WorkCase 的目标是完成 V3 当前文件中已识别的本地 V2 活依赖收口，并建立防回归检查。关闭本 WorkCase 表示“删除准备度收口已完成，可以进入删除决策 review”；不表示 V2 关键内容全量无缺漏，也不授权立即删除本地 V2。

  ## 已完成范围

  - `specs/03`、`specs/05`、`specs/06` 已清除“后续从本地 V2 筛选 / 判断 / 迁入”的 active 依赖口径。
  - `specs/07`、`specs/20`、`specs/21`、`specs/22`、`specs/23`、`specs/24` 已改为 V3-owned 后续实现 / 字段 / 实例补充判断，不以本地 V2 文件作为默认来源。
  - `spark-0039` 已转为 `resolved`，旧 V2 稳定运行线 / 整体迁移承诺只保留为历史背景。
  - `study-0010` 当前化 v2 需求表述已改为当前 V3-owned 需求；Codex 插件 README “来自 V2 icons” 已改为当前样例包内资产。
  - `code/ldvh_specs.py` 与 `tests/code/test_ldvh_specs_validate.py` 已建立 active V2 dependency 防回归检查，并覆盖“后续从 V2”“只能作为迁移来源”“再判断是否迁入”等同义残留。
  - 事实模型共享字段契约已记录为 V3-owned 后续工作，不作为 V2 删除准备度阻断。

  ## 验证与复审

  - `python3 code/specs_validate.py all --format text --fail-on-diagnostics` 通过，diagnostics 为 0。
  - `python3 code/test_runner.py smoke` 通过。
  - `python3 -m pytest tests/code/test_ldvh_specs_validate.py -q --tb=short -k "v2_deletion_readiness or current_specs_validate_without_diagnostics"` 通过，4 passed。
  - `python3 -m pytest tests/code/test_ldvh_specs_validate.py -q --tb=short -k "v2_deletion_readiness or current_specs_validate_without_diagnostics or workcase"` 通过，20 passed。
  - `python3 -m pytest tests/code -q -m 'not slow' --durations=10 --tb=short` 通过，227 passed / 135 deselected。
  - 外部 AI 审核结论：P0 无、P1 无，5 个提交整体可保留，可以进入 Human closure review；仍不能声明“已授权立即删除 V2”。
  - 子 agent 复审结论：最终总复审、P2 清理复审、共享字段契约后续项复审均无 P0/P1；closure patch 首轮复审发现 stale closure 文案，主控已修正。

  ## 后续分流 / 收口结果

  - 若后续发现 V2 未迁内容确实支撑当前 V3 规则成立、Code 消费或事实对象关闭判断，应按 V3 00 新建 V3-owned 待办，不重新把本地 V2 升格为权威来源。
  - 事实模型共享字段契约后续可单独建 WorkCase，统一 Spark、WorkCase、ADR、Pitfall、Study 等成员中语义相同字段的字段名、定义、证据边界和 Code/tests 消费口径。
  - 本地 V2 的实际删除是独立高影响动作，需要 Human 另行授权和执行计划；本 WorkCase 的关闭不替代该授权。

  ## 结论

  Human 已确认关闭；本 WorkCase 以 `completed` 结果收口。当前关闭结论仅覆盖 V2 删除准备度收口，不覆盖立即删除本地 V2。
closure_requested_at: '2026-07-08T23:42:00+08:00'
closed_at: '2026-07-08T23:42:00+08:00'
closure_outcome: completed
human_closure_confirmation:
  decision: close
  confirmed_at: '2026-07-08T23:42:00+08:00'
  confirmer: Human
  scope: |
    Human 已确认关闭 `workcase-0024`。关闭范围仅限 V2 删除准备度收口：当前已识别的 V3 active 文件不再把本地 V2 作为后续规则、迁移、验收或实现来源。
  summary: |
    Human 接受当前验证、外部审核、子 agent 复审和主控处理结论，确认本 WorkCase 以 completed 结果关闭。该确认不授权立即删除本地 V2；删除动作需另行决策。
residual_risks: |
  - 本轮已清除当前已识别 active specs / facts 的 V2 活依赖，但这不等于声明 V2 关键内容全量完整迁入；后续发现缺漏时按 V3 00 新建 V3-owned 待办。
  - 事实模型共享字段契约仍是 V3-owned 后续工作：它的目标是统一 Spark、WorkCase、ADR、Pitfall、Study 等成员中语义相同字段的命名、定义、证据边界和 Code/tests 消费口径，避免各成员规范各自构造同义字段；该工作不作为 V2 删除准备度阻断。
  - 删除准备度检查需要允许审计追溯语境、`migration_sources: v2:...` 和 stale path 检测，不能简单禁止所有 V2 字符串；但不得把 Git history 写成未迁内容承接或验收出口。
  - `workcase-0024` 已由 Human 确认关闭；本关闭结论仍不得解释为“本地 V2 已可立即删除”的授权。
followup_refs:
  - workcase-0022
  - spark-0039
revision_history:
  - at: '2026-07-07'
    summary: |
      Human 收紧删除准备度判断标准：以已审核的 V3 00 为锚点，只抽取当前 V3 文件中已经暴露的 V2-related 待办和活口径；不全量审计 V2，不用 Git history 反推迁移清单。已新增 item-0 初步判定表，并将 WorkCase 状态推进为 executing。
  - at: '2026-07-07T12:48:59+08:00'
    summary: |
      创建 V2 删除准备度收口 WorkCase，记录三路只读审计结论、P0/P1 阻断、执行项和后续验证要求。
  - at: '2026-07-08T16:23:00+08:00'
    summary: |
      完成本轮 V2 删除准备度修补：03/05/06 改为 V3-owned 口径，`spark-0039` 旧路线承诺改为 resolved 历史来源，Code/tests 新增 active V2 dependency 防回归检查。当时尚未授权实际删除本地 V2，后续需继续完成结果复审和 Human closure。
  - at: '2026-07-08T23:42:00+08:00'
    from_status: executing
    to_status: closed
    actor: Human
    reason: |
      Human 确认关闭本 WorkCase；关闭范围仅限 V2 删除准备度收口，不授权立即删除本地 V2。
    changed_fields:
      - status
      - updated
      - success_criteria
      - orchestration.result_review
      - review_requested_at
      - closure_evidence
      - closure_requested_at
      - closed_at
      - closure_outcome
      - human_closure_confirmation
      - revision_history
related_docs:
  - README.md
  - specs/03-事实源与Git溯源规范.md
  - specs/05-事实模型基础规范.md
  - specs/06-行动模板基础规范.md
related_adrs: []
related_sparks:
  - spark-0039
related_pitfalls: []
related_workcases:
  - workcase-0022
