# V4 05、06 与三行动模板机制 1–4 专项审计

> 审计时间：2026-07-15 07:53:49 +0800（Asia/Shanghai）
>
> 审计者：GPT SOL
> 审计性质：对本次四阶段目标的提交前完成性审计；只以当前 Working Tree、真实 CLI、tests 和构建结果为证据。

## 1. 审计结论

四阶段目标的规范、Code、测试和机制工作已经完成，提交候选可以形成。总体边界评价为：**清晰**。

当前责任链为：

```text
05：事实模型共同准入、共同协议、Helper 事实领域操作
└─ 20–24：五类事实对象的合法初态、状态/阶段、转换、关系、字段条件与证据唯一权威

06：行动模板共同准入、声明、发现、内容读取和消费边界
├─ 30：Git 提交行动
├─ 31：事实对象判定与受控创建行动
└─ 32：事实对象生命周期变更与承接处置行动

Helper/Code：机械发现、精确读取和已定义的确定性操作
AI：自然语言适用、领域语义、来源、证据与授权判断
Human：实际来源保留的方向、授权、风险与验收决定
```

## 2. 目标 1：05 与 20–24 的事实规则权威边界

### 结论

完成。05 明确不负责具体类型的合法初态、状态、阶段和转换边；20–24 是相应语义唯一权威。05 后续 Helper 契约只定义跨类型协议、CAS/原子能力边界和结果语义，不再独立枚举类型状态机。

### 已修复冲突

1. WorkCase 初始状态：21 允许在具体阻塞证据成立时以 `blocked` 创建；05 和创建 Code 已同步，不再只允许 `open`。
2. WorkCase F1 投影：05 已补齐 21 要求的 `phase` 和从 `work_items` 派生、不得写回对象的 `work_item_counts`。
3. 05 §11.7：已删除 Spark、WorkCase、ADR、Pitfall、Study 的具体单对象转换枚举及 WorkCase 版本失效规则副本，改为精确引用 20–24，保留单对象 CAS 不处理 `supersedes` 的能力边界。
4. Code 派生：创建初态从事实类型实现适配层统一取得，移除创建操作内部第二份 `_INITIAL_STATUSES` 表；新增 `blocked` WorkCase 实际创建回归。

### 证据

- `specs/05-事实模型基础规范.md`
- `specs/21-WorkCase-工作项.md`
- `code/ldvh/facts/contracts.py`
- `code/ldvh/helper/operations/fact_creation_operation.py`
- `code/tests/helper/test_fact_creation_operation.py`

## 3. 目标 2：两个事实域行动模板

### 结论

完成。新建并激活：

1. `specs/31-事实对象判定与受控创建行动模板.md`，稳定 key 为 `fact-object-controlled-creation`；
2. `specs/32-事实对象生命周期变更与承接处置行动模板.md`，稳定 key 为 `fact-object-lifecycle-change`。

31 负责召回查重、是否对象化、选型、类型来源、授权、草案、单对象创建和回读。已有对象可更新时转交 32；需要 `supersedes` 或其它多对象共同成立时，不先创建孤立对象。

32 负责既有对象的事实更正、普通更新、单对象生命周期变化和多对象承接处置。它不定义任何状态机；多对象公开操作和原子实现尚未成立时保持对象原状并报告能力缺口。

两份模板的独立责任、数量、正反场景、承载比较、净价值和既有独立 subagent 意见已经固化在 `docs/v4-architecture/investigations/V4-事实对象行动模板准入与边界调查.md`。

## 4. 目标 3：三模板通用机制验证

### 结论

完成。30、31、32 均由各自当前来源的唯一 `行动模板声明` 暴露，通用解析器实际发现三个稳定 key，没有中央手工模板目录。

06 新增两个只读公开操作：

1. `read-action-template-candidates`：返回机械候选、来源、定义范围和未自动证明的准入条件；
2. `read-action-template-content`：按精确 key 和请求顺序返回定义章节、完整来源规范及两个 SHA-256。

真实 CLI 一次读取三个模板，`scope.requested == scope.completed`，`not_completed` 为空，`changes` 为空。候选命中只证明机械可发现；Helper 不作自然语言模板适用、授权、能力或执行判断。

## 5. 目标 4：通用 Helper Code、测试和审计

### Code 结果

- 新增 `action_template_request.py`：封闭解析候选与内容请求，拒绝替代路径、非空授权、observed context 和 disclosure。
- 新增 `action_template_operation.py`：复用同一次 `RepositoryInspection`，不重新打开不同 Working Tree 快照；实现候选、内容、partial/unavailable、capabilities 和来源证据。
- 在公开实现注册中绑定两个来源声明的操作；Code 实现不能自行创建公开操作身份。
- 更新真实仓库组合、CLI、三模板内容、精确顺序、未知 key、双指纹、空输入和可用性测试。

### 验证结果

| 验证 | 结果 |
|---|---|
| Code tests | 550 项收集并全量通过 |
| Ruff lint | 通过 |
| Ruff format check | 113 个 Python 文件通过 |
| Web API tests | 8 项通过 |
| TypeScript check | 通过 |
| Web production build | 退出码 0；2747 modules transformed |
| Git diff check | 通过 |
| 三模板真实候选读取 | `ok`，三个 key 全部完成 |
| 三模板真实内容读取 | `ok`，定义、完整来源和双指纹全部返回 |

Web 构建保留已有“大于 500 kB chunk”警告；它不由本次行动模板变更引入，也不影响本次构建成功结论。

## 6. 边界遗漏检查

没有发现本次目标范围内仍无人负责的内容：

- “什么事实变化合法”由 20–24 负责；
- “如何组织一次事实创建或变化”由 31/32 负责；
- “机械读取和确定性状态变更”由 Helper/Code 负责；
- “是否适用、来源与授权是否成立”由 AI/Human 按当前来源负责；
- “是否提交 Git”由另行授权后的 30 负责。

以下是明确保留的后续能力缺口，不是本次遗漏：

1. 事实对象多对象原子事务与 `supersedes` 变更；
2. 环境 Hook/Skill/adapter 接入；
3. 其它候选行动模板的逐项准入；
4. 跨 Git 历史的 template key tombstone 自动检查；
5. 模板语义自动选择与自动执行——当前架构明确保留给 AI，不因 Helper 读取成立而默认需要 Code 化。

## 7. 提交前判断

本次变化共同服务“建立三个真实行动模板的统一读取和事实域边界”这一主要目的；规范、Code、tests、调查和当前推进状态可以共同说明、共同验证和共同回退。候选不包含已知无关变化，可以形成一个高影响 `feat` 提交；提交 body 必须列出关键变更和实际验证结果。commit 创建、回读和剩余 Working Tree 检查仍由 30 在本审计之后执行。

— GPT SOL
