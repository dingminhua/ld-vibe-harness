# 事实源边界与承载规范

> 创建日期：2026-05-29
> 定位：LD Vibe Harness 最终事实源、单一事实源、事实承载和回写边界规范
> 适用范围：LD Vibe Harness 项目及所有接入 LD Vibe Harness 的管辖项目
> 上位依据：`docs/specs/00-LD-Vibe-Harness理念与纲要.md`
> 相关规范：`docs/specs/01-目录说明.md`、`docs/specs/02-术语规范.md`、`docs/specs/03-文档基础规范.md`、`docs/specs/03.01-规范文档规范.md`、`docs/specs/03.02-管辖项目文档规范.md`、`docs/specs/04.01-规范落地声明规范.md`、`docs/specs/05-工作模型基础规范.md`、`docs/specs/06-工作流程基础规范.md`、`docs/specs/07-Code实现规范.md`、`docs/specs/08-Web信息同步规范.md`、`docs/specs/10-运行闭环测试规范.md`、`docs/specs/11-非LDVH来源内容治理规范.md`

---
## 1. 本文解决的问题

本文解决 LD Vibe Harness 中哪些内容可以成为最终事实源、同一事实只能由哪里权威承载、聊天、工具缓存、数据库派生视图和 UI 状态为什么不能替代 Git 文件事实源，以及事实应如何选择 Markdown、YAML 或其他介质承载的问题。

LD Vibe Harness 的核心目标之一，是让 AI 不依赖聊天记忆持续推进项目，而是围绕 Git 可追踪文件读取、执行、检查和回写。本文为这一目标定义事实源边界和承载原则。

各目录在事实源体系中的性质声明，由 `docs/specs/01-目录说明.md` 依据本文原则承载。

---

## 2. 与 00 总纲的关系

00 定义 LD Vibe Harness 的价值实现标准和五类构成要素，并要求 AI 的执行结果能留下可追溯证据、稳定事实能按边界回写到 Git 文件事实源。本文承接该要求，专门定义最终事实源、非事实源边界、单一事实源原则、事实承载方式和工具输出回写规则。

本文是 LD Vibe Harness 事实源权威性判断原则的权威位置。00 只保留事实源闭环的顶层价值要求；01 依据本文原则声明各目录在事实源体系中的性质，但不定义判断原则本身。

---

## 3. 事实源边界职责

本文的权威领域是最终事实源定义、单一事实源原则、非事实源边界、事实承载介质选择、工具输出回写和事实归属判断。

---

## 4. 最终事实源原则

LD Vibe Harness 的最终事实源必须是 Git 可追踪文件。一个事实只有写入其对应权威文件位置后，才成为可追溯、可审查、可被 AI 稳定读取和可被工具解析的 LD Vibe Harness 事实。

基础原则：

1. 事实必须能被 AI 读取；
2. 事实必须能被人审查；
3. 事实必须能被工具解析或聚合；
4. 事实必须能通过 Git 追溯变化；
5. 同一事实只能有一个权威位置，原则上不能出现多个事实源。

聊天记录、临时 UI 状态、工具缓存、数据库派生视图、运行时索引、命令输出和模型临时上下文都不能成为 LD Vibe Harness 的最终事实源。

---

## 5. 单一事实源规则

同一事实只能有一个权威定义位置。其他文件、工具页面、缓存、报告或实践说明需要使用该事实时，只能引用或聚合权威位置，不得复制并维护一份独立事实。

典型规则：

1. docs/specs/ 定义 LDVH 规范，不承载具体事实实例；
2. ldvh-base/承载结构化对象实例，不承载规范正文；
3. docs/ 承载管辖项目文档事实，不替代 ldvh-base/中的动态对象状态；
4. docs/evals/ 和 docs/refs/ 不直接成为 LD Vibe Harness 强制规则；
5. docs/evals/ 和 docs/refs/ 不直接成为管辖项目正文事实源；
6. web/ 和 tools/ 可以实现工具能力，但不能独立维护与 Git 文件事实源冲突的状态。

当两个位置出现同一事实的不同表述时，应依据本文和对应对象规范判断权威位置，非权威位置应改为引用、摘要或删除重复事实。

---

## 6. 事实承载介质选择

YAML、Markdown、Python、JavaScript 等文件格式和编程语言是 LD Vibe Harness 使用的行业标准承载形式。LD Vibe Harness 不重新定义这些介质本身的语法，只定义事实承载选择原则。

### 6.1 Markdown

Markdown 更适合承载：

1. 背景、原因、解释和设计语境；
2. 理念、规范说明、项目资料、调研和报告；
3. 人类需要阅读、审查和理解的长文本内容；
4. 不需要高频程序解析的说明性事实。

### 6.2 YAML

YAML 更适合承载：

1. 任务、状态、结构化关系和审计字段；
2. 可解析决策、证据摘要、变更记录和对象实例；
3. 需要被工具校验、聚合、筛选或受控写入的事实；
4. 高频被 AI 读取且需要稳定字段契约的事实。

### 6.3 代码文件

Python、JavaScript 或其他代码文件主要用于 Code 实现、校验器、聚合器、Web 信息同步实现和辅助脚本。代码文件可以是工具实现事实，但不能替代规范、对象实例、证据、决策或任务状态的权威事实源。

### 6.4 派生数据

JSON、数据库表、运行时索引、页面状态、缓存文件和命令输出可以作为工具运行过程中的派生数据或中间结果，但不得成为唯一事实源。派生数据必须能从 Git 文件事实源重新生成，或在形成稳定事实后回写到对应权威位置。

### 6.5 事实源查询与读取策略

AI 查询和读取事实源时，应遵循读取精准原则（依据 `docs/specs/06-工作流程基础规范.md` §6.1.1），避免上下文过载。

1. **查询优先于全文读取**：AI 应先使用 Code、搜索工具或索引工具定位事实源、对象、章节和关系，再按最小必要范围读取权威文件原文；
2. **定位优先于全文**：AI 应先定位目标段落、对象字段或引用关系，再按行范围读取；不得在未定位的情况下全文读取超过 200 行的规范文档；
3. **章节锚点**：LDVH 规范文档使用 `§` 编号体系，章节标题可作为精准读取入口；AI 应先搜索章节标题（如 `^## `、`^### `），再读取目标段落；
4. **工具辅助事实源查询**：AI 可通过 Code 或脚本生成规范索引、工作对象列表、对象详情、关键词搜索、状态统计、关联关系、落地要求聚合、平台清单检查和管辖项目配置检查，辅助定位事实源；
5. **查询输出边界**：事实源查询工具输出应能回指事实源类型、权威路径、对象 ID 或章节锚点、状态摘要、关联对象、引用关系、诊断信息或建议读取位置；查询输出只作为导航、聚合和诊断结果，不替代 Git 文件事实源；
6. **事实源读取粒度**：AI 应根据任务复杂度选择读取粒度——简单事实查询使用工具定位、搜索定位和行范围读取；跨章节理解任务可逐步扩展读取范围；完整上下文构建任务才考虑全文读取。

---

## 7. 工具输出回写规则

工具可以查询、读取、解析、校验、聚合、展示和受控写入 Git 文件事实源。工具的价值是降低人和 AI 操作事实源的成本，而不是替代事实源。

工具输出只有满足以下条件后，才成为 LD Vibe Harness 稳定事实：

1. 已写入对应权威事实源文件；
2. 写入位置符合本文和对应对象规范的事实源边界；
3. 必要时通过对应 Human Gate；
4. 可以被 Git 追溯；
5. 不与已有权威事实冲突。

工具缓存、数据库派生视图、运行时索引和页面状态不得成为任务状态、证据、决策、规则或审计结果的唯一来源。

---

## 8. 典型事实归属

| 事实类型 | 权威位置 |
|---|---|
| LD Vibe Harness 理念、价值标准和构成要素 | docs/specs/00 |
| 项目目录规划、目录用途和目录事实源性质声明 | docs/specs/01 |
| 术语边界、机制英文专名和术语变更治理 | docs/specs/02 |
| 文档工作区基础规则、命名、引用和吸收规则 | docs/specs/03 |
| LDVH 规范文档规则 | docs/specs/03.01 |
| 管辖项目文档规则 | docs/specs/03.02 |
| 索引文档规则 | docs/specs/03.03 |
| 规范落地与环境适配基础规则 | docs/specs/04 |
| 规范落地要求、环境适配映射和运行投影规则 | docs/specs/04.01-04.05 |
| 工作模型基础规范 | docs/specs/05 |
| 工作字段内容格式规范 | docs/specs/05.01 |
| 工作流程基础规范 | docs/specs/06 |
| Code 实现规范 | docs/specs/07 |
| Web 信息同步规范 | docs/specs/08 |
| 事实源边界、事实承载和回写规则 | docs/specs/09 |
| 运行闭环测试规范 | docs/specs/10 |
| 非 LDVH 来源内容治理规范 | docs/specs/11 |
| 最佳实践 | docs/specs/12 |
| 工作模型集合索引和具体工作模型规范 | docs/specs/20-39（20、21、22、23、24、25、26、27 为 active；28-34 按 20 的 removed、deferred 或 reserved 状态处理） |
| 工作流程集合索引和具体工作流程规范 | docs/specs/40-59（40 工作流程集合索引、41 规范落地统筹和 44 多角色思考为 active；其余编号按 40 的 planned、candidate 或 reserved 状态处理） |
| 结构化对象实例 | ldvh-base/ |
| 管辖项目正文文档、资料和审计结果 | docs/ |
| 管辖项目内部分析和方案评估 | docs/evals/ |
| 管辖项目外部资料和第三方参考 | docs/refs/ |
| LDVH 外部资料和第三方参考 | docs/refs/ |
| Web 信息同步实现 | web/ |
| Code 实现、工具、脚本、校验器和命令入口 | tools/ |

审计流程属于 LDVH 工作流程，应进入 40-59；审计结果属于具体管辖项目事实，应进入 docs/；审计发现转化出的任务、决策、备忘、踩坑或检查结果，应进入对应 ldvh-base/ 工作对象；管辖项目外部审计方法资料应进入 docs/refs/，LDVH 规范自身外部资料应进入 docs/refs/。

---

## 9. 规范落地要求

本文通过以下规范落地要求说明相关要求的同步、检查或审计触发条件。

| 落地要求 | 要求内容 | 保障机制 | 同步类型 | 触发条件 |
|---|---|---|---|---|
| 上位约束承接要求 | 稳定事实必须按本文选择权威位置，不得由聊天、缓存、派生视图或 UI 状态替代 Git 可追踪事实源 | 本文 §4-§8、01 目录事实源性质声明、工作模型和工作流程规范 | 事实源治理 | 新增、接管或回写稳定事实时 |
| 入口可见要求 | AI 判断事实归属、目录写入、refs/evals 吸收或事实源查询方式时应能定位本文 | 相关规范引用、目录说明、文档修改执行顺序、AI 统一入口 | AI 执行入口提示 | 目录规则、事实源边界、吸收规则、查询入口或回写规则变化时 |
| 确定性执行要求 | 可机械化的事实源查询、路径、引用、字段、缓存和派生视图检查应由 Code 校验或记录缺口 | `docs/specs/07-Code实现规范.md`、正反样例、事实源查询和检查 Code | 校验实现 | 事实源路径、查询输出、工具输出、受控写入、对象字段或引用规则变化时 |
| Human 交互要求 | 改变权威位置、提升非事实源为事实源或高影响写入时应评估 Human Gate | Human Gate、影响范围说明、回写证据 | 人工确认 | 事实源归属、写入权限、目录边界或高影响事实变化时 |
| 生命周期触发要求 | 事实源边界变化后，应检查 01、03、05、06、07、08、10 和 11 是否需要同步 | 目录检查、文档检查、Code 检查、运行闭环测试、非 LDVH 来源接管检查 | 触发保障 | 本文最终事实源原则、单一事实源规则、承载介质规则或工具输出回写规则变化时 |

---

## 10. Human Gate 与检查要求

以下情况应评估 Human Gate：

1. 改变某类事实的权威位置；
2. 改变目录是否为最终事实源的判断原则；
3. 将原本属于 ldvh-base/的对象实例事实改放到 docs/specs/、docs/、web/ 或 tools/；
4. 将工具缓存、数据库派生视图、运行时索引或 UI 状态提升为唯一事实源；
5. 将 docs/refs/ 外部资料直接作为 LDVH 强制规范依据；
6. 将 docs/evals/ 或 docs/refs/ 直接作为管辖项目正文事实源；
7. 在多个位置并行维护同一稳定事实。

当 AI 识别到上述情况时，至少必须暂停当前变更动作，向人提醒该操作可能触发 Human Gate，并说明涉及的目录、事实源、影响范围和建议确认事项。

事实源检查至少包括：

| 检查项 | 标准 |
|---|---|
| 最终事实源 | 稳定事实已写入 Git 可追踪文件 |
| 单一事实源 | 同一事实只有一个权威位置 |
| 目录事实源性质 | 写入位置符合 01 目录事实源性质声明 |
| 承载介质 | Markdown、YAML 或代码文件选择符合事实类型 |
| 工具边界 | 工具缓存和派生视图未成为最终事实源 |
| refs/evals 边界 | 外部资料和内部分析未直接成为强制规则或正文事实源 |

---

## 11. 待补齐事项

1. ldvh-base/下各类事实实例的具体承载格式，待 20-39 LDVH 工作模型规范稳定后补齐；
2. 工具受控写入的具体校验规则，待 `docs/specs/07-Code实现规范.md` 和 `docs/specs/08-Web信息同步规范.md` 稳定后补齐；
3. 管辖项目 docs/ 中审计结果、报告、docs/evals 和 docs/refs 的事实源回写细则，待工作流程和管辖项目实践稳定后补齐。
