id: task-0067
type: task
title: 实现最小 landing apply 受控写入入口
status: closed
created: '2026-06-11T00:38:21.829407'
updated: '2026-06-11T02:05:00'
description: 实现最小 landing apply 受控写入入口，只执行 plan 中明确授权的写入目标，并在写入前检查 Human Gate、测试设计和事实源边界。
source: intent-0012；docs/research/21-LDVH下一阶段推进方向-受控落地执行闭环.md；用户确认创建任务批次。
acceptance: '- [x] apply 默认拒绝无 plan、无授权或无测试设计的写入；- [x] apply 只允许写入 plan 中列明目标；- [
  x] apply 能输出执行摘要和待验证事项；- [x] 失败时不静默通过且不自动关闭 Task；- [x] 测试覆盖无授权、目标越界、成功写入或 dry-run
  场景。'
blocked_by:
- task-0066
deliverables:
- tools/specs_validate.py
- tests/test_specs_validate.py
source_intent: intent-0012
related_adrs:
- adr-0007
related_docs:
- docs/research/21-LDVH下一阶段推进方向-受控落地执行闭环.md
verification: |
  ## 验证计划

  先补失败/阻断测试，再实现最小 apply；运行 pytest、fact_validate 和相关 specs_validate 命令。

  ## 验证结果

  - `python3 -m pytest tests/test_specs_validate.py -k landing_apply` 结果 4 passed。
  - `python3 -m pytest` 结果 192 passed。
  - `python3 tools/fact_validate.py ldvh-base` 首次发现 task-0062/0063/0064 使用旧状态值；已将 reviewing/completed 修正为当前状态机允许值。
  - `python3 tools/specs_validate.py all docs/specs --specs-dir docs/specs` 完成结构检查；命令因既有 Markdown 路径 warning 返回非零，未发现本次 landing-apply 相关错误。

  ## 结论

  task-0067 已完成；最小 landing-apply 入口已覆盖 Human Gate、test_design、write_targets、dry-run、真实写入和阻断摘要。
closed_at: '2026-06-11T02:05:00'
closure_evidence: |
  ## 验证结果

  - `python3 -m pytest tests/test_specs_validate.py -k landing_apply` 结果 4 passed。
  - `python3 -m pytest` 结果 192 passed。
  - `python3 tools/fact_validate.py ldvh-base` 结果 errors=0 warnings=0。
  - `python3 tools/specs_validate.py all docs/specs --specs-dir docs/specs` 因既有 Markdown 路径 warning 返回非零，未发现本次 landing-apply 相关错误。

  ## 结论

  最小 landing-apply 受控写入入口已实现并通过核心自动化验证；残留 spec warning 属于既有平台入口路径引用问题。
