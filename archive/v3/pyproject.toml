[tool.pytest.ini_options]
testpaths = ["tests/code"]
pythonpath = ["code"]
markers = [
    "slow: runtime/e2e tests that are intentionally excluded from fast targeted runs",
    "runtime: runtime facade, preflight, manual adapter, environment entry, or completion claim tests",
    "e2e: static end-to-end rehearsal tests",
    "hook_adapter: hook and environment entrypoint wiring checks",
    "runtime_slow: known long-tail runtime/e2e test checks",
]
