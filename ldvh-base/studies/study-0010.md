---
title: Codex 子代理与 Worktree 协作：LDVH 的并行判断
status: active
urls:
- ref: https://learn.chatgpt.com/docs/agent-configuration/subagents
  title: OpenAI Codex Subagents
  summary: 说明子代理适合并行、独立和读取为主的工作，同时明确并行写入的协调与冲突代价。
- ref: https://learn.chatgpt.com/docs/environments/local-environment#use-built-in-git-tools
  title: OpenAI Codex Local environments
  summary: 说明工作树用于隔离并发改动，并指出每个工作树仍需要独立完成环境准备。
- ref: https://learn.chatgpt.com/docs/config-file/config-reference#configtoml
  title: OpenAI Codex Configuration Reference
  summary: 说明项目配置、并发上限与子代理继承的权限/配置边界，避免把运行时能力误写成项目纪律。
research_intent: 为 LDVH 未来涉及研究、核验与实现的协作选择一个清楚的判断方式：何时应拆分并行任务，何时应保留单一写入与汇总责任。
research_question: Codex 的子代理和 Git worktree 能如何帮助 LDVH 处理可并行的研究、核验与实现工作，同时避免把产品能力误写为固定流程？
abstract: 官方资料表明：子代理适合边界清晰、可独立汇总的读密集任务；并行写入需要额外协调；工作树隔离并发改动但不替代环境准备、权限控制或人类决策。对 LDVH 的启发是把并行性视作按任务选择的执行策略，并始终显式保留实际工作树、写入所有者和汇总责任。
recommendation_summary: 把并行视为按任务可分性选择的工作方式：先写清输出、汇总人与不修改范围；共享写入保留单一所有者；只有在确有需求时，以有界 WorkCase 验证 worktree 准备与交接，并由 Human 决定是否固化为长期默认。
object_id: study-0010
fact_type_key: study
created_at: '2026-07-23T20:14:44.651335+08:00'
updated_at: '2026-07-23T20:56:13.598244+08:00'
---

## 研究问题

### 项目问题

LDVH 未来会同时遇到资料阅读、规则核对、代码实现和验证等工作。问题并不是是否“应该使用更多代理”，而是：一项工作何时真的可以拆开，拆开后谁对共享改动、交接信息和最终判断负责。

这关系到项目推进的节奏。若把工具提供的并行能力直接当成流程，读者会看见许多局部产出，却无法知道哪一个结论进入了项目、哪一处改动由谁汇总；反过来，若把一切都留给单一执行者，独立的阅读与核验又会不必要地串行。本研究为后续具体任务提供判断框架，不设定长期流程。

### 外部问题

Codex 的子代理和 Git worktree 能如何帮助 LDVH 处理可并行的研究、核验与实现工作，同时避免把产品能力误写为固定流程？

## 输入与边界

### 已读外部资料

| 资料 | 本次用来回答什么 | 不能据此推出什么 |
|---|---|---|
| Codex Subagents | 哪些有清楚输入、可独立完成且主要产出阅读或核验结果的工作，适合分给子代理；并行写入仍有协调成本。 | 不能推出所有任务都应拆分，或子代理可以替主线程作项目判断。 |
| Codex Local environments | Git worktree 可以把并发改动放入独立检出目录，减少直接互相覆盖的机会。 | 不能推出目录隔离已经完成依赖准备、测试、交接或合并判断。 |
| Codex Configuration Reference | 子代理运行时会继承父任务的权限和配置上下文；配置可约束并发和角色。 | 不能推出配置文件能够分配项目写入责任，或替代 Human 的授权与验收。 |

三份资料都来自 OpenAI 当前文档。本研究把它们当作了解产品能力和使用边界的外部线索，而不是把文档文本转写为 LDVH 规则。

### 本次边界

本报告于 2026-07-23 重新阅读上述资料，只讨论 Codex 的子代理、worktree 与运行时配置对协作方式的启发。它不比较其它 agent 产品，不测量并行的实际收益，也不声称任何当前项目已采用、验证或授权这些做法。

特别地，worktree 是否需要环境准备、哪些文件可以并发修改、何时汇总结果，取决于未来具体任务和当前工作树；这些都不能从产品文档自动得出。本报告也不把 v3 的历史结论当作本次来源。

## 关键发现

### 可并行的是有独立交付面的阅读与核验，不是“角色数量”

Subagents 文档把代码库探索、测试、分诊和资料汇总这类任务列为适合并行的场景。它们的共同点不是任务名称，而是可以先说明输入问题、预期输出和不碰触的范围：一份资料比较、一个只读代码定位或一项测试观察，都能由主线程在随后统一吸收。

这对 LDVH 的启发是，先问“结果交回后能否由一个人清楚比较和采用”，再决定是否拆分。一个可用的最小交接是：要回答的问题、输出形式、允许读取的范围、不得修改的范围，以及谁负责汇总。这样并行服务于推进判断，而不是制造更多过程记录。

它不等于代理越多越好。多个任务若争夺同一份事实对象、同一个 Schema 或同一处代码设计，表面上可以分派，实质上仍要共享判断；这类工作过早并行，往往只是把冲突从开始时移到汇总时。

### Worktree 解决的是检出隔离，不是交接和可交付性

Local environments 文档说明 worktree 可把并发改动放在独立目录中。这使两个确实需要不同改动面的任务可以减少直接覆盖，也让主工作目录不必承担所有实验性改动。

对 LDVH 而言，真正需要保留的是“实际在哪个工作树写入”和“谁负责把结果带回主线”两件事。独立目录仍可能缺依赖、生成文件、环境变量或本地准备；即使测试在一个目录通过，也不自动说明结果适合另一个目录，更不说明可以合并。

因此，worktree 是在具体 WorkCase 中可验证的手段，而不是 Study 可以提前宣布的默认流程。若任务没有真实的并行写入需求，额外建 worktree 反而增加准备与交接成本。

### 运行时权限和项目责任是两层不同的东西

Configuration Reference 说明子代理会继承父任务的沙箱与权限上下文，并允许以配置控制并发或专用角色。这解释了运行时能做什么、会受到什么限制。

但项目责任仍需在任务语境里明确：谁能修改共享文件、谁吸收多路发现、何时需要 Human 判断方向或接受风险。运行时允许一次写入，不表示这次写入已经有项目层面的理由；运行时能同时启动多个角色，也不表示结果之间已经一致。

这一区分让 LDVH 不必把产品配置抄成规范。配置是当次执行条件，项目责任则由目标、规则与 Human 决定共同限定；两者都应在需要时被看见，但不应互相替代。

## 建议

### 先定义交接，再决定是否并行

对未来的研究、代码定位或测试核验，先写出每个子任务的输入问题、预期交付、汇总人和不修改范围。只有这些信息能够让汇总人比较结果、并且任务之间不需要频繁互相改写时，才值得并行。

这是一项工作方式建议，不是新建模板或强制门禁。一次短小的单线程工作不需要为了满足格式而拆出角色；当交接信息写不清时，也应把它当作暂不并行的信号。

### 共享写入保留单一所有者

涉及同一 Study、同一规则、同一 Schema 或同一代码切面的变更，应明确一个写入所有者。其他并行工作可以提供资料、定位、测试观察或建议，但由所有者结合当前工作树和约束决定如何落地与验证。

这样并不排斥协作：它把协作的重点从“同时改同一处”移到“提供可吸收的输入”。若未来确有独立改动面，worktree 可以作为隔离手段，但仍需明确合并、测试和回收责任。

### 只在真实需求出现时试行 Worktree 协作

当 Human 要求试行并行实现，并且任务已有清楚目标、独立改动面和可接受的交接成本时，可建立有界 WorkCase，记录实际的 worktree 准备、交接和汇总结果。该试行的目的不是证明并行天然正确，而是判断它是否在当前项目中带来净收益。

是否把角色划分、worktree 使用或并行阈值固化为长期默认，仍应由 Human 在看到真实成本和适用范围后决定；本 Study 不替代那项取舍。

## 后续分流

| 分流类别 | 触发条件 | 下一步或不创建理由 |
|---|---|---|
| 无需对象化 | 当前只是一次任务内的临时协作安排，交接与写入所有者仍然清楚 | 直接把本研究作为判断线索，不因调用子代理或创建工作树另建治理对象。 |
| Spark | 多次出现“并行产出难以汇总”或“共享写入责任不清”的具体症状 | 记录发生环境、症状和待判断问题；它是待研究线索，不预设解决方案。 |
| WorkCase | Human 要求试行并行实现，且存在明确目标、独立改动面与汇总责任 | 以有界行动验证 worktree 准备、交接和最终验证如何发生，不把试行结果预先写成结论。 |
| ADR | Human 正在考虑把协作方式固化为长期默认，且存在真实取舍 | 由 ADR 记录选项、依据和后果；在此之前不把本 Study 的建议当作已决定规则。 |
