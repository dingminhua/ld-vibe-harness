# V4 V3 事实对象全量逐条迁移决定

> 记录性质：本文记录 Human 对 V3 事实实例进入 V4 的明确范围决定及后续实施边界。它不是规则源，不自行授权迁移写入，也不证明迁移规范、行动模板、Code、清单或任何对象迁移已经完成。
> 决定日期：2026-07-15
> 当前状态：只记录，暂不实施

## 1. Human 决定

V3 的全部事实对象都必须依据 V4 当前事实对象模型逐条迁移，包括已经关闭、完成、解决、丢弃或处于其它终态的对象。不得因为对象已关闭、内容较旧、状态不再使用、历史实现已经归档或当前默认召回不包含终态，就把它排除在迁移范围之外。

该决定改变的是 V3 实例收口范围，不恢复 V3 Schema、字段、状态、路径、orchestration、关系或实现效力。V3 内容必须按 05 与 20–24 的当前语义重新审核和承载，不能把旧文件改名后直接放入 V4 当前位置。

## 2. 冻结库存

当前 `archive/v3/ldvh-base/` 共发现 92 个事实对象：

| 类型 | 数量 | V3 状态分布 |
|---|---:|---|
| Spark | 49 | 46 `pending`、2 `resolved`、1 `discarded` |
| WorkCase | 24 | 13 `closed`、4 `subagents_plan_reviewing`、3 `human_closure_confirming`、2 `human_plan_confirming`、2 `result_self_checking` |
| Study | 17 | 17 `active` |
| Pitfall | 2 | 2 `active` |
| ADR | 0 | 无 V3 实例 |

本表只是当前文件库存。正式迁移基线仍须为每个源文件计算内容指纹并绑定可解析 Git 版本，防止计划与实施期间源输入漂移。

## 3. “逐条迁移”的完成含义

未来迁移必须建立 92 项闭集清单，每个 V3 源对象各有且只有一个主清单项，并至少记录：

1. V3 类型、ID、规范化源路径、内容指纹、Git 版本和原状态；
2. 当前语义审核、仍有价值的内容、应退出的旧实现细节和证据缺口；
3. V4 目标类型、稳定引用和实际承载路径；
4. 发生拆分或合并时，全部 V3 source 与全部 V4 target 的双向可追溯映射；
5. 未迁内容及理由、来源/时间/关系/证据修复、机械和语义验证结果；
6. 实际写入、回读、Git 历史锚点和失败/残留状态。

逐条审核不强制旧对象与 V4 对象机械 1:1。根据当前语义可以 1:1 重建、拆分为多个 V4 对象、多个来源合并为一个不丢失来源的 V4 对象，或重新判断应由另一 V4 事实类型承载。但每个 V3 对象都必须能从清单回指至少一个实际 V4 承接结果；单纯标记 `history-only`、留在 archive、不再适用或“不准入”默认不算完成迁移。某一对象确实无法由任何 V4 类型合法承接时必须暂停该项并交还 Human，不能静默豁免。

## 4. 终态和历史边界

旧状态不得按名称机械映射。特别是：

- V3 Spark `resolved` 不自动等于 V4 `routed`；
- V3 WorkCase `closed` 不自动满足 V4 的结果包、验证、独立审核、Human 关闭确认和剩余责任承接；
- V3 `active` Study/Pitfall 不自动成为 V4 `active` 当前入口；
- 旧对象缺少时区、关闭时间、来源版本、适用边界、证据或当前关系时，必须补充可验证依据、缩小声明或进入 Human Gate，不得编造历史决定。

已经关闭的 V3 对象仍然必须迁移，但迁移不得伪造 V4 当时实际发生过的审核、批准或验证。如何让历史终态在不冒充 V4 原生生命周期的前提下取得合法 V4 承载，是后续迁移契约必须先解决的 blocker。

## 5. 当前能力缺口

当前 `create-fact-object` 只允许具体类型的合法新建初态，并由 Code 分配当前 ID 和时间；`update-fact-object` 只更新已经存在的 V4 对象。当前没有保留历史身份/时间、终态导入、拆分/合并映射、多对象原子迁移、可恢复批处理或逐项 receipt 能力。31 和 32 也明确排除迁移实现。

因此，在迁移契约、92 项基线清单、终态审核边界、历史 ID/时间策略、行动承载和原子写入能力成立前，不创建或改写任何 V4 迁移对象。库存完成、字段转换草案、dry-run 或部分批次成功都不得声明为 V3 事实迁移完成。

## 6. 后续实施顺序

本决定现在只记录，后续与 V4 开源交付主线一起按以下顺序实施：

1. 保持 Web Spark 创建表现层不变，把 POST/读取接口从 V3 直写/读取改为 V4 受控创建与投影，从而冻结 V3 迁移源增量；
2. 完成普通安装契约和原生 Windows 安全文件能力；
3. 形成迁移专项调查、92 项机器基线清单、规则差距和 Code 实现规划；
4. 判断是否需要专门的事实对象迁移行动模板，并在需要时补齐 05、20–24 的迁移契约；
5. 实现无写入 dry-run、映射校验、可恢复/原子写入与完整回读；
6. 先用同时覆盖非终态、终态、拆分、合并、时间缺失和关系修复的小样本验证；
7. 分批迁移全部 92 项，每批独立审核、验证、提交并更新闭集清单；
8. 使用迁移后的真实事实验证 bootstrap/resume、跨会话 dogfood 和 V4 Web；
9. 在 92 项均有实际 V4 承接、验证和 Git 锚点后关闭 V3 实例迁移 Gate。

## 7. 完成边界

V3 事实迁移完成必须同时满足：92 个源对象无遗漏；每项有可回指清单、实际 V4 承接、来源与证据处置、机械与语义验证及 Git 锚点；全部拆分/合并关系闭合；终态对象没有被跳过或冒充 V4 原生历史；没有未交还的失败、残留或无法承接对象。

本文落盘和提交只证明 Human 决定已经被记录，不证明上述任何实施步骤已经开始。
