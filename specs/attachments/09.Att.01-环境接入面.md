# 环境接入面

```yaml
ldvh_attachment:
  attachment_key: "environment-integration-surface"
  attachment_id: "09.Att.01"
  title: "环境接入面"
  status: "active"
  canonical_path: "specs/attachments/09.Att.01-环境接入面.md"
  positioning: "列出 LDVH 源码接入模式要求的环境无关入口与资产，并逐项标明当前实际交付状态"
```

> 文件状态：`active`。本文列出 LDVH 源码接入模式必须交付的环境无关入口与资产，并如实登记当前实际交付状态。默认接入不得构建或安装 LDVH wheel、sdist、editable distribution，也不得以 pip 安装 LDVH 本体或 Python console entry point 作为前提。本文不定义任何厂商事件、payload、机器安装路径或兼容结论。

## 要求入口与当前交付状态

| surface_key | 源码模式要求 | 当前实际交付状态 | 作用与调用边界 | 输出与失败 | 规则来源 |
|---|---|---|---|---|---|
| `helper-cli` | 已确认源码仓库根目录的 `ldvh` 稳定 dispatcher：`ldvh capabilities`、`ldvh call` | **已交付** | 用于 capabilities 与当前 Helper 公开操作；UTF-8 JSON 从 stdin 输入 | 合同为 `ldvh-helper-cli/2`；源码身份不可确认或依赖不可得时失败，不改投不明 PATH | `specs/04-Helper CLI 服务规范.md` |
| `worktree-bootstrap` | 已确认源码仓库根目录的依赖无关 launcher：`ldvh worktree-bootstrap [--check]` | **已交付** | 仅为当前实际 Git worktree 创建或检查本地 `.venv`、已声明第三方依赖及 Helper capabilities；不猜测、借用或搜索相邻 worktree 环境 | 合同为 `ldvh-worktree-bootstrap/1`；`ready` 只证明当前 worktree 的源码运行条件，`not_ready`/`unavailable` 必须交还缺口；不构建或安装 LDVH wheel、sdist、editable distribution 或 Python package | `requirements.txt`、`requirements-dev.txt`、`specs/09-环境接入规范.md` §6.2 |
| `skill-template` | `skill/SKILL.md`（canonical 文件） | **已交付** | 薄 Skill 唯一内容来源；只读复制且部署件必须逐字节一致 | 不一致即过期：重新部署并重新验证受影响范围 | `specs/09-环境接入规范.md` §5.2、§5.3 |
| `skill-icons` | `icons/`（canonical 目录） | **已交付** | 仅在目标环境当前资料确认支持时使用；图标不进入 SKILL.md | 无机制或无法确认时如实交还，不影响核心路由合同 | `specs/09-环境接入规范.md` §5.3 |
| `work-context-core` | 可选入口，消费宿主原生生命周期事件 | **已交付**：`ldvh work-context` | 仅当宿主原生提供所需事件对象时可用；不可用或宿主不提供时，精确读取路径（09 §5.4）是唯一规则引导路径 | 合同为 `ldvh-work-context/1`；不可用时保留实际范围和缺口 | `specs/01-规范模型基础规范.md`、`specs/04-Helper CLI 服务规范.md`、`specs/09-环境接入规范.md` |
| `context-recovery` | 稳定源码 launcher | **已交付**：`ldvh context-recovery` | 仅在 AI 已按 05 §8.1 明确进入事实消费分支后使用；不是新会话默认入口 | 合同为 `ldvh-context-recovery/1`；不可形成可信投影时不得输出伪成功 | `specs/05-事实模型基础规范.md`、`specs/09-环境接入规范.md` |
| `git-commit-msg-gate` | 由真实 Git Hook 调用的稳定源码 Git Gate 入口 | **已交付**：`ldvh git-commit-msg` | 必须由 Git Hook 交付并由核心重新验证 actual worktree、当次 Index 与 commit message | 结果为 allow/block 与诊断；入口缺失或绑定不可验证时阻断 | `specs/03-事实源与信息溯源规范.md` |
| `git-hook-manager` | common-dir 级 Git Hook bundle 管理入口；一次部署覆盖共享 common-dir 的主/现有/后续 linked worktree | **已交付**：`ldvh git-hook` | 从显式 actual worktree 解析并验证 common-dir；将 `prepare-commit-msg` 与 `commit-msg` 作为同一版本 bundle 管理；仅迁移可证明由 LDVH 管理的旧覆盖；未知 Hook 或配置 fail-closed、零写入；独立 clone 另行部署 | install 前要求 Human Gate；写入或切换前以临时 Hook、Index 和 message 完成两个入口的语法、注入 no-op/有效快照与真实 block/allow、所有权预检；失败则两成员回到原样 | `specs/09-环境接入规范.md`、`specs/33-环境接入行动模板.md` |
| `doctor` | 读取源码仓库 identity/revision 与源码入口状态的诊断 launcher | **已交付**：`ldvh doctor` | 只读检查源码、Helper、显式配置/管辖和入口静态状态 | 合同为 `ldvh-doctor/1`；不证明 Skill 递达、Hook 部署或真实事件验证 | `specs/09-环境接入规范.md` |

源码模式的所有入口与资产只由已确认身份与修订的 LDVH 源码仓库提供；不得从已安装包、缓存副本或不明 PATH 入口替代。入口已交付不等于某个目标环境已经部署、加载、触发或完成四项验证。

四项工作中的“管辖配置”不新增独立环境入口：其配置来源、字段与解析算法唯一回指 02，当次解析经已交付的 `helper-cli` 公开操作执行；不得借此新建另一 AI 路由、配置 writer 或环境 adapter。

## Helper 最小调用形态

请求 JSON 经 stdin 传入；从已确认的源码仓库根目录使用最小形态：

```bash
printf '%s' '{"arguments": {}}' | ./ldvh call <operation_key>
```

无请求输入的入口（如通用 capabilities）同样无条件读 stdin 至 EOF：交互终端之外的调用必须闭合 stdin。不得把不明 PATH 命令写成当前入口。

共同请求字段闭集与默认值以 `specs/attachments/04.Att.01-Helper CLI 请求与响应字段表.md` 的当次内容为准；各操作自己的必填与可选参数先经源码 launcher 的通用 capabilities 或指定操作 capabilities 现取，再按回指章节的当次内容构造 `arguments`。试调返回 `invalid_request` 时读取响应 `gaps` 与 `diagnostics` 中的契约问题并据以修正，不得变换参数值盲目重试。

## 技能登记字段值

目标环境登记技能要求提供字段时，各字段值的权威来源：技能名称、描述与指令正文照抄 `skill/SKILL.md`（`skill-template` 行）frontmatter 与正文的当次内容，不重述不改写；许可证与作者署名见源码仓库 `LICENSE` 当次版权声明行；源码身份与版本以 `git remote`（存在时）、当前 revision 和工作树状态为准；图标素材见 `skill-icons` 行。环境私有扩展字段（厂商专属 frontmatter 键或 sidecar 文件）不由 LDVH 预填，按目标环境当次官方资料在接入时判定。技能可见会话范围按 09 §5.2 的默认部署范围为用户级；仅在 Human 明确要求限制可见范围时才改为项目级或工作区级。本附件不登记任何厂商目录、表单或字段名。

## 入口选择

目标环境 AI 必须先读取当前环境资料，再判断原生事件与上述入口的语义是否对应：

1. 规则引导的唯一路径是经 Helper `read-specification-content` 精确读取 00 §8.1 与 §8.2（09 §5.4）。宿主原生提供生命周期事件且 `work-context-core` 可用时，也可以经该入口取得相同内容。不得仅因事件、cwd 或任务文字调用 `context-recovery`。
2. AI 在理解 Human 目标后明确需要项目事实时，才可使用 `context-recovery`；也可以按来源定义直接使用相应 Helper 事实读取能力。两种入口都不判断事实适用、行动允许或完成。
3. AI 主动读取或变更 LDVH 能力时，使用已解析且状态为已交付的源码 Helper launcher，不从环境事件猜测 Helper 字段，也不使用不明 PATH 命令。
4. 只有实际 Git `commit-msg` 事件才能连接 `git-commit-msg-gate`；AI 环境中名称相似的阻断事件不能自动视为等价。每次事件必须保留 actual worktree、当次 Index 与 commit message 的独立绑定。
5. `git-hook-manager` 只管理承接 Git Gate 的 Git Hook，不是通用 AI 环境 adapter。目标能力按 common-dir 部署并覆盖共享它的主/现有/后续 linked worktree；纳入接入目标的独立 clone 必须单独部署。
6. 具体环境来源必须先声明当前单元实际承接的原生事件及版本/范围；每次行动再按 `specs/33-环境接入行动模板.md` 为四项工作与这些事件形成一次性的验证矩阵。入口清单不维护跨厂商事件登记，也不证明任何事件已真实触发。
7. 只有环境接入、管辖配置、Git Hook 部署与验证所需的当前入口和链路都可成立，才按 `specs/09-环境接入规范.md` 和 Human Gate 形成完整方案；任一必需入口尚未交付或链路缺失时保持 `unverified`/`unsupported`，不得以 manifest、环境 Hook、插件、启动脚本或 adapter 补造另一接入层。

不得为了适配方便另造统一厂商 payload、通用事件路由、默认语义或 adapter 登记。若当前入口不能承接目标环境真实输入和反馈，应保持 `unverified` 或依据肯定证据报告 `unsupported`，不能改写入口契约；`context-recovery` 不是环境 Hook 对新能力不兼容时的替代路径。

## 环境侧实现边界

LDVH 接入面不存在环境 Hook、插件或 adapter 层（`specs/09-环境接入规范.md` §5.1）；本仓库不收纳、不维护任何具体 AI 环境的环境侧实现。目标环境不能直接加载 canonical SKILL.md 或连接所需原生 Git 事件时，按 09 §8 如实判定能力，不新增并行接入形态。
