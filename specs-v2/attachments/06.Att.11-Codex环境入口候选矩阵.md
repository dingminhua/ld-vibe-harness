# Codex环境入口候选矩阵

```yaml
v2_attachment:
  attachment_id: "06.Att.11"
  title: "Codex环境入口候选矩阵"
  status: "draft"
  authority: "not_active_until_parent_and_human_approved"
  parent_spec: "specs-v2/06-运行时扩展规范.md"
  canonical_path: "specs-v2/attachments/06.Att.11-Codex环境入口候选矩阵.md"
  purpose: "承载 06 授权的 Codex 环境入口候选矩阵"
  migration_sources:
    - "specs/04.03-环境入口适配与部署规范.md"
  code_consumption:
    - "codex_environment_entry_candidates"
```

> 文件状态：本文当前位于 `specs-v2/`，尚未切换为 active；正式 Codex 适配规则仍以 active `specs/04.03-环境入口适配与部署规范.md` 为准。本文是 `06-运行时扩展规范.md` 授权附件，必须随父规范经过 Human 单篇确认后才可生效，不记录当前用户安装状态或非 Codex 适配。

## 1. 定位

本文承载 06 授权的 Codex 环境入口候选矩阵。

## 2. 候选矩阵

| 能力意图 | 环境入口候选 | 位置 | 接入方式 | 适配检查 | 禁止声明 |
|---|---|---|---|---|---|
| 用户级配置 | Codex 配置 | `~/.codex/config.toml` | 配置适配 / Human Gate | 检查文件存在、格式有效、配置层级清楚 | 未确认读取前不得声明生效 |
| 用户级 Hooks | Codex hooks | `~/.codex/hooks.json` | Hook 配置 / Human Gate | 检查 hooks 文件存在、事件名有效、命令可信 | 不得声明未信任 hook 已自动运行 |
| 用户级 AGENTS 入口 | Codex 用户级 AGENTS | `~/.codex/AGENTS.md` | 用户手动薄引用 / Human Gate | 检查入口可定位、已有内容未覆盖、LDVH 段落只指向 Rules | 不得把 AGENTS 写成 LDVH Rules 资产 |
| 项目级配置 | 项目 `.codex/config.toml` | `<repo>/.codex/config.toml` | 项目专属配置 / 显式降级 | 检查项目配置层是否被信任和读取 | 不得当作通用入口默认落点 |
| 项目级 Hooks | 项目 `.codex/hooks.json` | `<repo>/.codex/hooks.json` | 项目专属 Hook / 显式降级 | 检查 hooks 定义、命令来源、权限和信任状态 | 不得绕过用户审查和信任流程 |
| 软入口 | instructions、AGENTS、项目说明、会话入口 | 依当前 Codex 能力确认 | 指令适配 / 人工降级 | 检查是否稳定读取该入口 | 不得声明持久入口完整支持 |

## 3. 待补齐事项

1. Codex App 当前版本证据确认后，应复核候选入口、路径和 trust 规则；
2. Windows 路径仍需当前 Codex App 实测确认。
