# Web 测试实现记录（V3 历史）

> 本文原先记录 V3 Web 测试的目录、对象和写入假设。其文件名为历史遗留，不表示本文是 V4 规范，也不授予任何测试、事实类型、字段、状态或写入能力 V4 身份。

V4 Web 测试的当前实施入口是 [`V4-工作推进总纲.md`](../../docs/v4-architecture/V4-工作推进总纲.md)；[`docs/web/README.md`](../../docs/web/README.md) 只保留目录与运行导航。稳定要求来自：

1. [`03-事实源与信息溯源规范.md`](<../../specs/03-事实源与信息溯源规范.md>)；
2. [`07-Code 实践与测试规范.md`](<../../specs/07-Code 实践与测试规范.md>) 中被 08 直接采用的共同测试工程要求；
3. [`08-Web 呈现与交互规范.md`](<../../specs/08-Web 呈现与交互规范.md>)。

## 当前测试位置与入口

Web tests 位于 `web/tests/`，不再使用 V3 的根级 `tests/web/`。当前入口：

```bash
cd web
npm test
```

当前首批测试只覆盖实际列明的 HTTP/API 与提交 DTO 特征，不覆盖页面、组件、视觉、响应式、辅助技术或 V4 领域适配。夹具是测试所需的临时输入，不是事实源或 Schema 来源。

## V3 内容的处置

原文中的 WorkCase、Spark、priority、importance、Confirm UI、写入白名单、状态和字段测试映射均来自 V3 实践，未自动获得 V4 身份。历史测试保存在 `archive/v3/tests/web/`，只能作为调查输入逐项审核，不能整体恢复。

尤其是 Spark quick create：当前源码和页面入口仍存在，只能说明 V3 实现尚未隔离；在具体 V4 来源、当次授权和实际 Code 受控能力成立前，不得称为 V4 正式写入能力，也不得用恢复旧测试的方式补造其契约。
