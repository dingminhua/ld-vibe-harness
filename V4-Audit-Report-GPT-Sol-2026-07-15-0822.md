# V4 架构方向独立审计报告

- 审计模型：GPT Sol
- 审计时间：2026-07-15 08:22:24 +08:00（Asia/Shanghai）
- 审计对象：`/Users/dmh2002/poker_hud_projects/ld-vibe-harness-v4`
- 审计方式：严格只读检查；报告形成后按用户要求写入本文件

## 1. 总判定

**基本正确，但需要立即校正。**

V4 的根方向仍然正确，尚未发生不可逆的结构性偏航：

- `specs/00-理念与构成.md` 明确把 AI 执行者作为第一服务对象；
- 五类构成要素的职责划分总体合理；
- Code/AI 边界清楚，Code 没有明显越权解释自然语言；
- Working Tree、来源引用、确定性校验、CAS 更新、受控事实写入、分层读取等已经有真实实现，而非纯文档设想；
- V4 WorkCase 已删除 V3 中泛化的 orchestration、角色、日志和任意历史结构，这是实质性减法。

但是，**当前建设重心已经从“尽快闭合 AI 的真实使用路径”，偏向“继续闭合规范、字段、模板、审核与证明体系”**。最关键的反差是：

- 仓库已有 21 个规范候选、约 6230 行规范、81 个登记字段、550 个已收集 Python 测试；
- 但当前没有一个 Git 跟踪的 `facts/**` 实例；
- 没有真实 WorkCase 中断恢复样本；
- 没有完成环境 adapter / 非 editable 部署；
- Web 仍在读取和写入 V3 对象；
- 没有证明一个新 AI 能以较低成本完成“定位—理解—行动—验证—回写—交接—恢复”的纵向闭环。

所以，现在不是继续扩建更多规范或行动模板的时候。如果仍按当前顺序推进，V4 将很可能从“基本正确但失衡”转为“规范闭合、产品路径不闭合”的结构性偏航。

## 2. 对 00 核心目标和 V1–V8 的证据矩阵

| 00 要求 | 当前证据 | 判定 |
|---|---|---|
| 最终目标：Human、AI、Code 各展所长，并支持环境、模型、使用者自由组合 | 根目标明确见 `specs/00-理念与构成.md:56-59`。当前 Code 核心较真实，但环境和模型组合尚未落地 | **部分成立** |
| AI 执行者是第一服务对象 | 明确定义于 `specs/00-理念与构成.md:63-91`；Helper 的 10 个公开操作确实面向 AI。但建设证据主要仍是规范、测试和审核闭合，而非 AI 实际消费数据 | **原则成立，实践未充分证明** |
| Helper 是统一基础服务入口 | `specs/00-理念与构成.md:93-111` 定义正确；当前 10 个操作均有实现。缺口是没有环境 adapter、普通 wheel 规则源定位和一条低成本启动路径 | **实现基础成立，接入未闭合** |
| 事实源让 AI 少读、少猜、少漂移 | 目标见 `specs/00-理念与构成.md:113-125`；事实契约、校验、创建、更新已实现。但当前 `git ls-files facts` 为零，没有真实消费和恢复证据 | **模型成立，产品价值未验证** |
| 五类构成要素职责清楚 | `specs/00-理念与构成.md:127-139` 的划分总体正确。Code 没有明显进行自然语言语义裁决；行动模板没有承载运行状态。Web 的 V3 直写则违反了该边界 | **总体正确，Web 有现实冲突** |
| Human Gate 只保留 Human 决策 | 上位语义见 `specs/00-理念与构成.md:185-224`，明确不替代技术验证。下位 WorkCase 和 Web 将 Gate 扩展得过密 | **上位正确，下位偏重** |
| Stop Conditions 最小范围暂停 | `specs/00-理念与构成.md:226-265` 清楚限定最小受影响范围并保留普通求解自主性 | **设计较强，尚缺真实恢复验证** |
| V1 快速定位 | 规范候选、行动模板候选、治理范围和事实发现均可调用；但 README 存在断链和操作数过期，启动仍需多次跨层调用 | **部分成立** |
| V2 充分理解 | Helper 可以返回来源、治理信息、事实卡片和规则内容；但空事实查询仍可返回约 14.8K 字符，单模板内容约 10.7K 字符，存在固定上下文成本 | **部分成立，披露仍不够经济** |
| V3 边界识别 | 规则来源、治理范围、Human Gate、Stop、模板适用条件的表达完整，Code 保留语义判断给 AI | **较强** |
| V4 稳定推进 | 三个行动模板和确定性事实操作提供了基础；但没有真实 AI 完成纵向任务的验收，模板机制完成不等于稳定推进已经成立 | **部分成立** |
| V5 据实判断 | 来源回指、诊断、CAS、回读、能力状态和验证边界都较强；本次实际调用的 10 个公开操作均存在 | **较强** |
| V6 工作接续 | WorkCase 模型支持责任、阶段、证据和恢复；但当前没有任何 V4 WorkCase 实例，也没有跨模型/跨会话恢复演练 | **弱，尚未实证** |
| V7 清晰沟通 | Human Gate 交还字段和 WorkCase 审核结构很完整；但可能以过多审核节点增加 Human 等待和 AI 整理成本 | **机制充分，净价值未证** |
| V8 持续积累 | ADR、Pitfall、Study、Spark 等事实类型具备模型；当前零实例，无法证明经验真正被后续 AI 复用 | **弱，主要停留在模型层** |
| V1–V8 净收益门槛 | `specs/00-理念与构成.md:145-160` 要求新增机制有可辨认净收益。当前大量机制只有静态闭合证据，没有与简单 Markdown 交接或无 LDVH 路径的对照 | **尚未满足验证责任** |

## 3. “真正服务 AI”与“主要服务规范维护/审计”的比例

这不能从 LOC 精确计算。按当前建设重心和 AI 实际消费路径判断：

> **直接服务 AI 的内容约处于四成档位；主要服务规范维护、字段治理、审核与自证的内容约处于六成档位。**

这是定性判断，不是统计结论。

真正服务 AI 的部分包括：

- 10 个可调用 Helper 公开操作；
- 确定性的规范、治理、事实发现与事实创建/更新；
- 内容指纹、来源回指、诊断、CAS 和回读；
- F0–F4 渐进式披露框架；
- 行动模板与临时执行现场分离；
- WorkCase 中的责任、状态、证据和恢复意图；
- Code 不重释自然语言规则的边界。

主要服务维护/审计的部分包括：

- 21 个规范候选和大量交叉归属、依据、效力与建设顺序规则；
- 81 个统一登记字段，其中约 45 行适用于 WorkCase、约 41 个字段键以 WorkCase 为前缀；
- 每个字段提案要求完整准入审核和独立复核，见 `specs/05-事实模型基础规范.md:177-194` 和 `specs/attachments/05.Att.01-事实对象统一字段登记.md:195`；
- 术语和规范职责的多层审核，见 `specs/01-规范模型基础规范.md:545-596`、`:617-696`；
- 大量“完成、封闭、审核通过”证据没有对应真实事实对象和 AI 使用验收；
- 最近十个提交约增加 6681 行、删除 317 行，扩建明显快于减法和真实使用反馈。

单看运行时 Code，其价值结构优于整个项目；失衡主要发生在**建设顺序和治理投入**，不是 Code 核心实现本身。

## 4. 分级问题

### P0

**当前没有独立 P0。**

现有问题尚未使根架构失效，也没有证据表明 Code 正在系统性越权解释规则。但以下 P1 必须成为继续扩建前的门槛，否则会发展成结构性偏航。

### P1-1：建设顺序已经背离“先真实闭环、后扩模板”的既有处置决定

证据：

- 总纲明确环境接入、Web V4、端到端 dogfood 尚未完成，见 `docs/v4-architecture/active/V4-工作推进总纲.md:90-103`；
- 双审计处置记录要求先做真实事实、恢复和 WorkCase dogfood，再推进模板，见 `docs/v4-architecture/active/V4-全架构方向双审计处置记录.md:60,74-85`；
- 随后提交却优先增加 Git 提交闭环和三个行动模板机制；
- 总纲当前下一步仍在“模板或环境 adapter”之间摇摆，见 `V4-工作推进总纲.md:326`。

失败场景：继续完成更多模板、规范和测试，新的 AI 进入项目后仍不知道如何以低成本启动、读取真实对象、恢复中断或通过 Web/Human 完成交接。

AI 实际影响：形成“规范使用路径闭合，但 AI 工作路径不闭合”。

最小修正：冻结新的事实类型、字段、模板和全局审核项目；把当前唯一下一步改为真实纵向闭环。

### P1-2：WorkCase 的核心价值正确，但完整审核链被设为不可裁剪，尚未证明净收益

证据：

- WorkCase 只有 Human 明确选择进入项目记录后才适用，见 `specs/21-WorkCase-工作项.md:66-80`；
- 一旦进入，则完整链对简单、短时、单项工作也不可跳过，见 `specs/21-WorkCase-工作项.md:26-32`；
- 包含创建独立审核、计划审核、Human 计划确认、控制者检查、结果独立审核、Human 关闭，见 `specs/21-WorkCase-工作项.md:212-256`；
- 对象外还有是否纳入正式记录的 Human 决定，对象内再有两个 Human Gate，见 `specs/21-WorkCase-工作项.md:198-203,378-389`。

失败场景：Human 只是希望保存一个可恢复的长期工作项，却被迫选择完整的多模型、多审核、两次对象内 Human Gate 流程。

AI 实际影响：恢复成本可能降低，但创建、版本级联、审核等待和交还成本可能超过简单事实记录或 Markdown handoff。

最小修正：保留一个精简 WorkCase 核心——责任、目标、状态、当前依据、下一步、阻塞、版本和交接；把双独立审核和双 Human Gate 变为由风险、来源规则或项目策略触发的控制层，而不是对象存在的必备条件。

### P1-3：Helper 已经有能力原语，但还不是低成本 AI 启动路径

证据：

- 固定响应外壳始终包含大量顶层字段和空 `follow_up` 结构，见 `code/ldvh/helper/responses.py:52-108`；
- 单个行动模板读取同时返回精确定义和完整来源正文，见 `code/ldvh/helper/operations/action_template_operation.py:119-125`；
- `specs/06-行动模板基础规范.md:310-316` 又要求 AI 消费定义、正文和依赖；
- 当前零事实的 F1 返回仍约 14.8K 字符，候选规范约 12.4K 字符，能力发现约 14K 字符；
- 普通 wheel 如何找到规则源没有定义，`code/ldvh/helper/rule_source.py:19-33` 依赖代码和 `specs` 位于同一工作树。

失败场景：新 AI 必须先理解多个操作、治理范围、规则候选、模板候选和事实卡片，再自行组合启动上下文；即使仓库没有事实，仍要消费较大固定响应。

AI 实际影响：定位和理解成本从“搜索文件”转成“理解多个结构化大响应”，渐进披露只部分成立。

最小修正：增加一个机械性的启动/恢复 bundle 或薄 adapter，只组合当前治理范围、能力摘要、F0/F1 摘要和来源指针；不替 AI 做语义匹配。空字段应在 compact 模式省略，完整模板正文改为按需读取。

### P1-4：Web 当前仍能产生与 V4 不兼容的 V3 事实

证据：

- Web 硬编码五个 V3 事实目录和 `ldvh-base`，见 `web/api/services/facts.ts:17-30`；
- 读取 `id/type/created/updated`，而 V4 使用 `object_id/fact_type_key/created_at/updated_at`，见 `web/api/services/facts.ts:115-128`；
- Spark 路由通过 `writeFileSync` 直接写 V3 对象，见 `web/api/routes/sparks.ts:78-112`；
- WorkCase UI 状态仍是 V3 的 `subagents_plan_reviewing` 等，见 `web/src/utils/workcaseStatus.ts:1-40`；
- V4 Web 规范要求写入必须经过实际 Code 能力，见 `specs/08-Web 呈现与交互规范.md:165-175`。

失败场景：Human 在当前 Web 创建 Spark，仓库出现 Helper/validator 不认可的“事实”。

AI 实际影响：同一仓库产生两个事实模型，AI 无法确认 Web 展示与 V4 事实源谁可信。

最小修正：立即禁用或显式 feature-gate V3 Spark 写入口；在 V4 Code 写能力接入前，Web 只读。

### P1-5：“唯一控制面”自身已产生断链和互相矛盾的当前状态

证据：

- 总纲声明自身是唯一控制面，见 `docs/v4-architecture/active/V4-工作推进总纲.md:3-18`；
- 同一文件分别出现 8 个和 10 个操作、462/466/550 个测试、14/19/21 个规范候选等不同时点信息，见 `V4-工作推进总纲.md:139-187,315-324`；
- 根 README 的总纲、基线和 V3 清单链接已经失效，见 `README.md:10-13`；
- README 仍称只有 8 个操作且行动模板操作尚未实现，见 `README.md:25`。

失败场景：AI 按 README 或总纲建立当前状态，得出错误的能力数量、完成范围和下一步判断。

AI 实际影响：V1 快速定位和 V5 据实判断首先被项目自身的控制文档破坏。

最小修正：修复根导航；删除易过期的手工数量，改为从 `capabilities` 和收集结果生成；总纲只保留日期明确的观察，不重复维护能力清单。

### P2-1：统一字段和规范职责治理的准入成本过高

证据：

- 每个正式字段都要求完整结构化准入审核、与现用及退役字段比较和独立复核，见 `specs/05-事实模型基础规范.md:177-194`；
- 术语和规范职责也有类似的多段审核要求，见 `specs/01-规范模型基础规范.md:545-596,679-696`。

失败场景：一个仅属于单一类型、非破坏性的字段，也触发与共享跨类型字段相同的治理流程。

AI 实际影响：AI 花费大量时间证明“字段没有漂移”，却没有生成可供后续任务消费的事实。

最小修正：保留全局唯一键、定义、承载位置和 tombstone；完整独立审核仅用于共享字段、跨类型提升、语义变更和破坏性迁移。类型局部字段使用轻量比较。

### P2-2：Git 提交确定性能力投入很大，但还没有成为 AI 可消费能力

证据：

- `code/ldvh/commits` 已实现契约、索引、校验和执行；
- 但当前 10 个 Helper 公开操作中没有 commit 操作；
- 最近 Git 提交闭环约新增三千行代码和测试，却未解决环境 adapter 和真实 dogfood。

失败场景：仓库内部拥有确定性提交实现，AI 实际工作时仍只能自行调用 Git 或按长模板人工拼装步骤。

AI 实际影响：Code 投资没有转化为 V4 的统一服务入口，增加了体系体积但没有立即降低执行成本。

最小修正：暂停扩建；真实 dogfood 后，如果重复出现确定性提交需求，再通过受授权的窄 Helper/adapter 入口暴露。否则保持内部库并降低“能力已交付”的声明范围。

### P2-3：Web Human Gate 范围过宽

证据：

- `specs/08-Web 呈现与交互规范.md:195-209` 强调行为保持；
- `specs/08-Web 呈现与交互规范.md:235` 将有意的视觉、文案、导航、交互、API、响应式和可访问性改变普遍纳入 Human Gate。

失败场景：Human 已批准“把 Web 迁移到 V4 只读事实模型”，AI 在每项可观察实现变化上仍可能反复进入 Gate。

AI 实际影响：Human 等待增加，AI 无法在已明确授权的产品方向内自主完成技术迁移。

最小修正：Human Gate 保留给产品语义、取舍和验收；允许 Human 一次批准有明确范围的迁移包，包内技术实现和修复由 AI 自主推进。

### P3：完成证据的时点和口径不一致

证据：

- 总纲出现 462、466、550 等测试数字；
- `docs/code/README.md:23-32` 仍只列 8 个操作。

失败场景：AI 将“已收集 550 个测试”误读为“当前环境已全部通过 550 个测试”，或把旧操作列表当成当前能力面。

AI 实际影响：主要是额外核查和轻度误判，不直接破坏核心架构。

最小修正：证据必须带提交、时间和口径，明确区分 `collected`、`passed`、`implemented`、`integrated`；当前能力列表由 Code 生成。

## 5. 最值得保留的设计

1. **00 的 AI-first 和净收益门槛。** 这是 V4 最重要的方向锚点，不应弱化。
2. **规则源、事实源、Working Tree 和来源回指。** 它们为 AI 的判断提供了可证伪基础，而不是依赖聊天记忆。
3. **Code 与 AI 的语义边界。** `specs/00-理念与构成.md:136` 对“Code 不重释自然语言”的限制是正确的。当前抽查没有发现 Code 大规模越权。
4. **事实结构由统一登记和类型声明派生，而非多处复制。** 唯一键、定义、来源和 tombstone 应保留；需要简化的是准入流程，不是删除治理锚点。
5. **CAS、原子更新、回读、诊断和内容指纹。** 这些是实际降低 AI 判断风险的确定性能力。
6. **行动模板不承载运行状态。** 模板、事实对象和临时执行现场的分层是正确的。
7. **V4 WorkCase 对 V3 orchestration 的减法。** V3 当前归档的 24 个 WorkCase YAML 合计约 11842 行，平均约 493 行。V4 去掉通用 orchestration、日志、角色和任意历史是正确方向；现在应继续减审核流程，而不是恢复 V3 膨胀。

## 6. 应删除、合并、简化或后移的内容

立即删除或禁用：

- Web 的 V3 Spark 直接写入口；
- README 和总纲中无法可靠维护的手工能力数、测试数和候选数。

合并：

- 字段准入、术语审核、规范职责审核中的重复“比较—影响—复核—封闭”记录；
- 对共享或破坏性变化保留一份完整变更记录，普通局部变化不应复制整套治理证明。

简化：

- WorkCase 为“精简责任记录 + 条件控制层”；
- compact Helper 响应中的空字段和重复来源；
- 行动模板精确定义应尽量自足，完整规范正文按需读取；
- 总纲只做当前路线和未闭合能力控制，不再兼任历史审计汇编。

后移：

- 新行动模板；
- 新事实类型和更多统一字段；
- 多对象操作、自动 supersedes 等高级机制；
- 大规模 Web 交互和写能力；
- 新一轮全架构审核。

提前：

- 环境 adapter、规则源部署；
- 机械启动/恢复 bundle；
- 真实事实对象和真实 WorkCase dogfood；
- 最小 V4 Web 只读路径。

## 7. 当前下一步与未来 2–3 个增量

### 当前下一步

**不要再在“下一个模板”和“环境 adapter”之间选择。应明确选择：环境接入 + AI 启动路径 + 真实纵向验收。**

### 增量 1：闭合 AI 入口

交付：

- 定义非 editable 环境中的规则源配置或打包方式；
- 建立一个薄环境 adapter；
- 提供机械性的 bootstrap/resume bundle；
- 修复 README、控制面和能力清单；
- 禁用 Web V3 写入口。

验收不是测试数量，而是：一个不了解仓库的新 AI 能在有界读取次数内定位治理范围、相关规则、事实、能力和下一步，并清楚知道哪些内容尚未验证。

### 增量 2：做真实中断恢复 dogfood

在一个真实受管辖的验收项目中完成：

- 创建和读取实际事实对象；
- 更新、回读和 Git 溯源；
- 执行中途强制中断；
- 换会话或换模型恢复；
- 完成交接和关闭。

至少比较两组：

- 一个使用精简责任记录的普通长期事项；
- 一个确实需要完整审核链的高风险事项。

记录调用数、读取文件数、响应体量、无产出判断次数、Human 等待点、恢复遗漏和重复调查，并与简单 Markdown handoff 对照。

### 增量 3：以 dogfood 结果做减法，再交付最小 Web

- 根据实测简化 WorkCase、字段准入和 Helper 外壳；
- Web 只做 V4 事实的只读列表、详情、风险和待 Human 决定事项；
- 只有真实工作中重复出现的行动，才晋升为新行动模板；
- 多对象写入和高级自动化继续后移。

## 8. 与已有外部审计的差异

本报告在形成独立判定后才阅读已有审计。

- `V4-Audit-Report-GLM5.2-2026-07-14-1524.md` 判为基本正确，并把渐进披露/响应外壳列为 P0。其部分事实已经过期：当时公开操作更少，L3 和 compact 还未达到当前状态。因此本报告不保留其 P0 严重度。
- `V4-Audit-Report-GPT-5.6-SOL-极高-2026-07-14-1533.md` 判为结构性偏航。它对 WorkCase、零事实、Web V3 和治理重心的风险判断有较强依据；但当前已有 10 个真实公开操作、有效治理解析、事实创建/更新/CAS 和三层规则读取，因此本报告认为尚未达到结构性偏航。
- 本报告与既有处置记录最大的差异是：记录已经正确提出“先真实事实和恢复，后模板”，但实际提交顺序没有遵守。这个执行偏差本身，比审计文件写得是否完整更值得处理。

## 9. 已读范围、实际检查和不确定性

完整通读或结构化通读：

- `specs/00`；
- `specs/01–08`；
- `specs/20–24`；
- 统一字段登记及相关附件；
- `specs/30–32` 当前行动模板；
- V4 工作推进总纲；
- 当前 WorkCase 规范、封闭记录、外部审计交接和处置记录；
- 两份全架构外部审计及处置记录；
- 05/06 和三行动模板专项审计；
- 根 README、Code README、Web README。

重点抽查：

- Helper service、请求响应、规则源定位、规范发现、行动模板读取；
- governance resolver；
- facts contracts、schema、validation、relations、transitions、creation/update；
- Git commit package；
- 相关 Python 测试；
- Web facts service、Spark 写路由、WorkCase 状态和相关测试；
- `archive/v3` 的 WorkCase 规范、附件和代表性对象。

实际只读检查：

- `capabilities`：成功，10 个公开操作；
- `resolve-governance-scope`：成功，`governed_single`，无当前诊断；
- `read-specification-candidates`：21 个候选；
- `read-action-template-candidates`：3 个模板；
- `read-action-template-content`：验证了精确定义与完整来源的重复体量；
- `find-fact-object-candidates`：成功，15 类状态计数均为零；
- `ruff check --no-cache code`：通过；
- `PYTHONDONTWRITEBYTECODE=1 pytest --collect-only -q -p no:cacheprovider`：收集 550 个测试；
- 检查最近 Git 历史和提交统计；
- 审计前后 `git status` 均为干净状态。

未覆盖和不确定性：

- 为遵守严格只读要求，**没有实际运行完整测试套件**，因为部分测试会创建临时文件或 Git 仓库；因此本报告只确认“收集 550 个测试”，不声明其全部通过；
- 没有启动 Web、浏览器或执行构建；
- 没有逐行阅读每个 Code、test、Web 和 V3 archive 文件；
- 当前没有 V4 事实实例，无法实证跨模型恢复、Human Gate 等待成本和 Web/事实一致性；
- 没有真实多模型 token、延迟和任务成功率数据，因此 AI/治理比例与净收益判断是有证据的定性判断，不是精确测量。

除按用户要求新增本报告文件外，本次审计没有修改、删除、暂存或提交任何仓库文件。
