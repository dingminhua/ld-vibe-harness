# 关联提交派生优先级表

```yaml
v2_attachment:
  attachment_id: "07.Att.09"
  title: "关联提交派生优先级表"
  status: "draft"
  authority: "not_active_until_parent_and_human_approved"
  parent_spec: "specs-v2/07-事实源边界与Git追溯规范.md"
  canonical_path: "specs-v2/attachments/07.Att.09-关联提交派生优先级表.md"
  purpose: "承载 07 授权的关联提交派生优先级"
  migration_sources:
    - "specs/10-Git提交规范.md"
  code_consumption:
    - "related_commit_derivation"
    - "git_history_projection"
```

> 文件状态：本文当前位于 `specs-v2/`，尚未切换为 active；正式关联提交派生规则仍以 active `specs/10-Git提交规范.md` 为准。本文是 `07-事实源边界与Git追溯规范.md` 授权附件，必须随父规范经过 Human 单篇确认后才可生效，不独立创建对象字段、Git 查询实现或 Web 页面契约。

## 1. 定位

本文承载 07 授权的关联提交派生优先级。

07 正文定义 Git commit records 的事实源修改追溯性质；本文只说明如何派生“关联提交”，不得要求工作对象手写提交清单，不得定义 Git 查询实现、Web DTO 或知识地图输出 Schema。

## 2. 派生优先级

工作对象、规范、Code 和 Web 需要“关联提交”时，应按以下优先级派生：

1. commit touched files 与对象事实源路径、规范路径、Code/Web 文件路径的匹配；
2. commit description 或 body 中自然出现的对象 ID、规范编号、规范路径或文件路径；
3. Git 提交时间线、相邻提交、分支上下文和人工审查结论；
4. 必要时由用户或 AI 在当前分析中临时筛选，但不得回写为手填提交字段。

对象 ID、规范编号或路径可以出现在 description 或 body 中，但不得要求 AI 为了追溯而补写固定 `Refs:` 字段。Git 变更由 Git 自身的 hash、文件路径、diff 和时间线追踪。

## 3. 禁止事项

1. 不得在工作对象中手写维护 `related_changes` 或等价提交清单字段；
2. 不得把 Web 展示的关联提交列表回写为对象事实；
3. 不得把知识地图派生边写回 Git commit records；
4. 不得把一次临时筛选结果写成永久关联事实；
5. 不得用固定 `Refs:` trailer 替代 body 自然语义。

## 4. 辅助核对表

| 核对项 | 标准 |
|---|---|
| 来源可追溯 | 派生结果能回到 commit hash、文件路径、diff 或正文自然文本 |
| 优先级清楚 | touched files 和自然文本优先于人工临时筛选 |
| 不手写字段 | 对象事实源没有维护提交清单字段 |
| 不强制 trailer | 不要求固定 `Refs:` 字段作为唯一追溯入口 |

## 5. 待补齐事项

1. Code v2 Git 查询实现确认后，应补充关联提交派生的输出字段和降级诊断；
2. Web v2 提交展示确认后，应检查关联提交显示是否仍回指 Git commit records；
3. 知识地图 Git history 能力后置完成前，本文只作为派生原则，不要求运行时图谱实现。
