# V4 架构 05 / 06 基础规范边界专项审计

- 审计人：GPT SOL
- 审计时间：2026-07-15 02:37:32 CST（UTC+08:00，Asia/Shanghai）
- 审计性质：只读架构审计
- 主要审计对象：`specs/05-事实模型基础规范.md`、`specs/06-行动模板基础规范.md`
- 下级规范核对范围：`specs/20-Spark-火花.md` 至 `specs/24-Study-研究报告.md`

## 一、结论摘要

边界总体评价：**基本清晰**。

05 与 06 的核心语义分工清楚：05 管“长期事实是什么、如何成立和保存”，06 管“重复行动如何组织和执行”。06 基本保持了纯基础规范定位；05 前半部也是清晰的基础规范，但后半部混入了五类具体事实类型及其 Helper 操作契约，形成层级反向依赖和重复定义风险。

主要结论如下：

1. 05 与 06 没有根本性的领域职责重叠；两者的连接点是“行动模板可以组织事实的读取和回写，但不得定义事实 Schema、状态或成立条件”。
2. 05 不是纯框架：§10 写入了当前五类事实类型已经确认的架构决定，§11 又硬编码了五类的初始状态、候选投影、默认召回集合、状态转换和部分 WorkCase 版本规则。
3. 06 基本是纯框架：当前没有声明任何具体 `template_key`，也没有正式的 31–39 具体行动模板。
4. 20–24 总体遵循“05 基础规范 → 具体事实类型规范”的分层，但 05 §11 又反向复制 20–24 的具体规则，因此当前依赖并非完全单向。
5. 06 与未来具体行动模板的概念分层清晰，但尚未通过真实具体模板验证；多模板组合、冲突、依赖和机器召回信息仍需补充。

## 二、问题 1：各自的定位

### 1. 05 的定位

05 是事实模型的元规范或基础规范，主要定义：

1. 什么稳定事实值得进入事实模型；
2. 事实类型、事实对象、`fact_type_key` 的共同身份；
3. 公共字段、来源、证据、关系、状态和对象变更的共同边界；
4. 具体事实类型的唯一定义来源必须回答哪些问题；
5. 事实模型消费方和实现不得反向取得事实语义定义权。

05 明确声明不负责：

1. 定义某个具体事实类型是否成立；
2. 定义具体类型的对象范围、增量字段、状态值、关系类型、目录和文件格式；
3. 判断具体领域证据是否足够；
4. 定义通用行动授权；
5. 定义可复用行动步骤或具体环境接入。

上述定位在 §3“职责边界”和 §10“具体事实类型的责任边界”中表达得较完整。问题不在缺少声明，而在 05 后文没有完全遵守该声明。

### 2. 06 的定位

06 是行动模板的元规范或基础规范，主要定义：

1. 什么重复行动结构值得成为具体行动模板；
2. `template_key`、行动模板声明和唯一定义来源；
3. 适用条件、输入、能力、授权、步骤、失败分流、输出、验证、回写和交还的共同语义；
4. 模板选择、适用、执行、验证和完成互不等价；
5. 模板与规则、事实、一次性计划、Code、Helper 和环境 adapter 的分工。

06 明确声明不负责：

1. 定义某个具体行动模板的 `template_key`、载体、步骤、命令、字段、格式或当前效力；
2. 定义领域规则、事实类型、Schema、状态闭集、证据充分性或完成条件；
3. 定义 Helper CLI 请求、响应、发现接口或错误语义；
4. 定义 Code 模块、测试实现或运行时状态；
5. 定义具体厂商事件、Hook payload、安装路径、权限映射或具体环境接入契约；
6. 保存单次计划、执行日志和没有独立事实类型承接的运行进度。

### 3. 两者是否有职责重叠

核心职责没有重叠：

| 关注点 | 05 | 06 |
|---|---|---|
| 主要回答 | 什么事实长期成立 | 某类行动如何重复执行 |
| 稳定状态 | 可以由具体事实类型定义并保存 | 不保存模板运行状态 |
| 字段与 Schema | 定义事实字段共同规则和类型组合规则 | 只引用，不定义 |
| 行动步骤 | 不定义 | 组织可复用步骤 |
| 回写 | 定义事实对象成立和变更边界 | 决定何时调用既有来源进行回写 |
| 授权 | 不授予行动权限 | 模板适用也不等于行动获权 |

两者的合理接口是：06 可以组织读取或更新事实的行动，但必须服从 05 和具体事实类型，不能复制事实字段、状态或证据充分性规则。

存在的是治理结构上的重复，而不是事实与行动的领域语义重叠。05 和 06 都定义了：

- 稳定 key；
- 分布式声明表；
- 唯一定义来源；
- 拆分、合并、替代与退出；
- Code 不得形成第二权威。

这种平行治理当前尚未冲突，但长期存在两个声明协议漂移的风险。

## 三、问题 2：框架与实例

### 1. 05 不是纯框架

05 前半部属于基础框架。公共字段、稳定引用和统一字段登记虽然具体，但它们是跨类型基础契约，不属于类型实例泄漏。

真正越过基础层的是以下内容：

1. §10 直接写入“Human 已确认当前 V4 需要 Spark、WorkCase、ADR、Pitfall 和 Study 五个类型”。这是当前架构推进决定，不是长期共同语义。
2. §11 定义了五个实际 Helper 公开操作，使 05 同时承担“事实模型基础规范”和“当前事实服务操作契约”。
3. §11 的操作契约硬编码了具体类型内容，包括：
   - 只接受当前五类的 `fact_type_key`；
   - Spark、WorkCase、ADR、Pitfall、Study 的具体初始状态；
   - 默认召回状态及 F1 固定扫描 ADR、WorkCase；
   - 五类各自的 F1/F2 字段投影；
   - 五类具体状态转换；
   - WorkCase 的 phase、`plan_version`、`result_version` 和失效字段规则。

这些内容不是具体事实对象实例，但属于具体事实类型集成内容。更合适的承载位置是：

1. 20–24 各类型规范，分别定义自己的投影、初始状态和转换规则；或者
2. 独立的“事实对象 Helper 操作规范”或“事实服务契约规范”，聚合并消费各类型来源，而不成为第二类型权威。

### 2. 06 基本是纯框架

06 没有声明任何实际 `template_key`，也没有定义某个具体模板的步骤。文中的 WorkCase、Skill、Agent、Hook 等内容只用于说明边界，不构成具体行动模板实例。

当前 `specs/` 中没有正式的 31–39 具体行动模板。因此：

1. 06 到具体行动模板的分层目前只是制度设计；
2. 尚未通过一个真实具体模板验证其可操作性；
3. “31–39”不是 06 规定的固定编号段；
4. 按 06，具体模板可以放在已有规范责任内的 H2/H3，也可以在确有独立普通规范责任时新建规范。

## 四、问题 3：边界是否清晰

### 1. 是否有明确的“不负责定义”声明

有。05 和 06 都有结构清楚的“负责定义”和“不负责定义”列表。

- 05：§3“职责边界”；
- 06：§3“职责边界”。

### 2. 声明是否足够覆盖实际越界场景

06 基本足够，05 不完全足够。

05 的主要问题不是声明缺失，而是声明和正文实际内容不一致：前文说不定义具体类型状态、Schema、目录和文件格式，后文却在事实服务契约中复制五类的状态、载体差异、字段投影和转换规则。

06 存在一个较小的表述歧义：

1. §3 表示不负责“环境接入契约”；
2. §9.4 又规定一个环境一个 LDVH 接入单元、薄 adapter 及其禁止承载的内容。

从全文判断，实际意图应是：

- 06 负责跨环境的共同架构不变量；
- 06 不负责某个具体环境的事件、payload、权限、安装位置和 adapter 契约。

这个意图合理，但“不负责环境接入契约”的原措辞范围过宽。

### 3. 有没有内容被两份规范同时定义

存在共同治理协议的重复定义：

1. key 的稳定性与不得改派；
2. 声明表结构和解析失败隔离；
3. 唯一定义来源；
4. 拆分、合并、替代和退出；
5. 派生视图不得成为第二权威。

这些重复当前没有形成领域冲突，但可以考虑由 01 抽象共同协议，同时保留事实类型只允许 H2、行动模板允许 H2/H3 等领域差异。

### 4. 有没有两份规范都没有定义的内容

当前没有明显的事实与行动接口语义无人归口，但存在以下待定义项：

1. 多个行动模板同时适用时的选择、组合、排序、嵌套和冲突处理；
2. 模板之间的依赖、调用、排斥或替代关系；
3. 模板自动召回所需的最小机器可发现信息；
4. 模板发现与内容读取的 Helper 契约；
5. 新增第六种事实类型时，如何接入 05 §11 当前硬编码的五类服务契约。

其中第 4 项已被 06 明确推迟，属于已识别的待定义内容，不是无意遗漏。

## 五、问题 4：与下级规范的关系

### 1. 05 与 20–24 的边界

预期分层是清楚的：

**05 负责：**

1. 事实类型共同准入；
2. 公共身份和稳定引用；
3. 公共字段入口及统一字段登记；
4. 来源、证据、关系和状态的一般边界；
5. 具体事实类型必须定义的责任清单。

**20–24 负责：**

1. 本类型的目的、对象粒度和相邻类型边界；
2. 本类型字段绑定和专属字段；
3. 载体、目录和对象 ID 格式；
4. 具体状态值、状态机和关系语义；
5. 具体证据要求、主动召回、验证、Human Gate 和 Stop Conditions。

20–24 实际上也遵循这套结构：每份规范都声明了唯一 `fact_type_key`，并定义本类型的字段绑定、载体、状态和生命周期。

当前的问题是 05 §11 又反向复制了 20–24 的具体内容：

```text
理想依赖：05 → 20–24 → Helper 派生消费

当前依赖：05 → 20–24
          05 §11 又复制并硬编码 20–24 的具体内容
```

因此，“基础规范 → 具体规范”的语义层次清晰，但契约承载没有保持完全单向。

### 2. 06 与未来具体行动模板的边界

预期边界清晰：

**06 负责：**

1. 模板准入；
2. 稳定身份和声明；
3. 共同结构；
4. 适用、授权、执行、验证、回写与交还边界；
5. 模板与规则、事实、Code 和环境的分工。

**具体行动模板负责：**

1. 某个重复场景的进入条件；
2. 实际步骤关系和分支；
3. 失败分流和停止位置；
4. 验证安排；
5. 交还方式；
6. 对实际规则、事实和能力来源的引用。

领域规则、状态和完成条件仍由相应正式来源定义，具体模板只能引用和组织消费。

目前只能评价为“概念层次清晰，实践层次尚未验证”，原因是尚无正式具体模板，无法验证：

1. 共同要求是否会让具体模板过重；
2. 声明表是否足以支持自动召回；
3. 多模板组合是否产生歧义；
4. 具体模板能否在不复制 WorkCase 或其它事实类型语义的前提下完整表达行动。

## 六、问题 5：遗漏与说明不清

### 1. 应该定义但尚未充分定义的内容

1. 05 应定义新事实类型接入事实服务的扩展方式，避免每增加一种类型就修改基础规范中的五类闭集。
2. 06 应补充多个模板并用、组合、冲突和选择规则。
3. 06 可以增加一份集中式“具体行动模板唯一定义来源至少必须回答的问题”，类似 05 §10；当前要求分散在 §5–§9。
4. 未来模板自动发现需要明确最小机器可发现信息由谁定义，例如进入信号、排除摘要和所需能力是否进入结构化契约。
5. 两份规范共同使用的“稳定 key + 声明表 + 唯一来源 + 退出承接”协议可以考虑由 01 抽象共同规则。

### 2. 已经定义但说明不够清楚的内容

1. 05 到底是纯基础规范，还是同时承担事实服务领域契约，目前标题、定位和实际内容不完全一致。
2. 06 的“环境共同不变量”与“具体环境接入契约”界线需要更精确的措辞。
3. WorkCase 阶段事实与行动模板运行状态是最高风险接缝。21 已明确区分长期阶段目标和环境内部步骤，但未来具体模板仍需用真实实例验证，避免模板把运行事件写回 WorkCase，或者 WorkCase 反过来成为行动执行引擎。

## 七、边界总体评价

### 05：基本清晰

优点：

- 共同事实语义、字段归口、具体类型责任和消费边界非常完整；
- 与 20–24 的预期职责分工明确；
- 对来源、证据、状态、关系和实现权威边界覆盖充分。

主要扣分点：

- §10 混入当前五类已经确认的推进决定；
- §11 混入事实服务操作契约；
- §11 复制和硬编码 20–24 的具体状态、投影和转换规则；
- 新事实类型扩展会迫使修改基础规范。

### 06：清晰，但尚未通过具体模板验证

优点：

- 框架定位稳定；
- 对规则、事实、授权、一次性计划和运行状态的排除很清楚；
- 没有建立固定阶段或全局运行对象；
- 没有夹带具体模板实例。

主要待完善点：

- 多模板组合和冲突规则未定义；
- 自动召回的结构化入口较弱；
- 环境共同边界与具体 adapter 契约的措辞有歧义；
- 尚无真实具体模板验证基础要求是否可用。

### 05 与 06 之间：清晰

两者的核心分界可以概括为：

> 05 定义可长期消费的事实身份、状态和证据；06 定义可重复使用的行动结构。行动可以读取或改变事实，但行动模板不能取得事实定义权；事实对象可以保存长期工作状态，但不能成为模板运行引擎。

## 八、具体建议

按优先级建议如下：

1. **拆分或重新定位 05 §11。**优先把具体 Helper 操作移入独立的事实服务契约规范；如果保留在 05，则需要修改标题、定位和“不负责定义”声明，明确 05 同时承担当前事实服务领域契约。
2. **移出“当前五类已经确认”的推进决定。**由审计记录、路线文档及 20–24 的有效声明证明当前类型集合，不写入长期基础规范。
3. **消除五类规则复制。**F1/F2 投影、默认状态、初始状态和允许转换应由 20–24 唯一定义；聚合操作只读取这些来源，不再在 05 复制一份。
4. **建立事实类型到服务操作的扩展机制。**新类型应能通过自身来源声明载体、投影和机械转换边界，而不是修改基础规范的固定五类分支。
5. **明确 06 的环境边界。**把“不负责环境接入契约”收窄为“不负责具体环境 adapter 的事件、payload、权限和安装契约”；保留单一接入单元、薄 adapter 和不得复制模板语义作为共同架构约束。
6. **在首个具体模板准入前补充组合规则。**至少定义多个模板同时适用时的选择、并用、嵌套、冲突、失败隔离和交还责任。
7. **增加具体模板的集中责任清单。**将 §5–§9 中分散的强制要求汇总为类似 05 §10 的可审计清单。
8. **不要把 31–39 当作固定层级要求。**具体模板应按规范责任决定放入已有规范章节或新建规范；编号只是文件管理结果，不是架构语义。
9. **可选地抽取共同声明协议。**将事实类型和行动模板共同使用的稳定 key、声明表、唯一来源和退出承接规则抽到 01，同时保留各领域的必要差异。

## 九、最终判断

05 与 06 的领域分工基本正确，事实与行动之间没有根本性职责冲突。06 的基础规范定位较稳；05 则需要优先处理“基础规范内嵌当前五类事实服务契约”的问题。若该问题得到拆分或重新定位，整体边界可从“基本清晰”提升为“清晰”。
