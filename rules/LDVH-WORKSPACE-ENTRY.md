# Deprecated LDVH Workspace Entry

This file is a compatibility redirect for historical LDVH fact references.

The active LDVH runtime entry is:

```text
rules/LDVH-RUNTIME-PROTOCOL.md
```

Do not treat this file as an active Rules asset. It exists so older WorkCase,
Spark, Study, and diagnostic references have a stable target while the system
uses the unified Runtime Protocol entry.

