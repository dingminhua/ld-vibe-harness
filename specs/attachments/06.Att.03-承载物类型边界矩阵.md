# 承载物类型边界矩阵

```yaml
v2_attachment:
  attachment_id: "06.Att.03"
  title: "承载物类型边界矩阵"
  status: "active"
  authority: "active_with_parent_spec"
  parent_spec: "specs/06-运行时扩展规范.md"
  canonical_path: "specs/attachments/06.Att.03-承载物类型边界矩阵.md"
  purpose: "承载 06 授权的 Rules、Skills、Agents、Hooks 类型边界"
  migration_sources:
    - "history/specs-v1/04.02-LDVH能力资产与保障机制规范.md"
  code_consumption:
    - "runtime_extension_type_boundaries"
```

> 文件状态：本文是 active 授权附件；随父规范生效，不独立改变父规范正文授权之外的原则、状态机、事实源边界或流程 Gate。

## 1. 定位

本文承载 06 授权的 Rules、Skills、Agents、Hooks 类型边界矩阵。

## 2. 类型边界矩阵

| 类型 | 允许承载 | 不得承载 |
|---|---|---|
| Rules | 入口判断、场景分流、STOP 点、最小读取顺序、官方 Code/检查工具导航、交接、缺口提示、事实源边界提示和知识地图导航入口 | 环境薄引用、完整 specs、长期状态、工作对象事实、偶发任务细节、本地 shell 策略、环境权限策略、部署状态、知识地图缓存 |
| Skills | 可复用执行流程、标准输入输出、失败处理、验证入口和主控交还 | 新增规范规则、默认调度 Agent、绕过 Human Gate 或 Code 校验 |
| Agents | 隔离上下文分析、专项审查、并行视角、复杂争议和权限边界差异判断 | 输出直接生效、无限分派子 Agent、独立事实源状态、越权写入 |
| Hooks | 生命周期检查入口、提交前后检查提示、工具调用前后检查提示、会话停止检查提示和缺口提示 | 替代 Code 校验、替代 Human Gate、替代长期记忆、替代 Git commit records |

## 3. 同名机制分流

Codex `.rules`、sandbox、approval、permission profiles、MCP 授权、插件安装状态、Claude path rules、Git Hook 管理器配置和 CI branch protection 属于环境接入、运行约束、工具能力或部署事实，不自动成为 LDVH 固定运行时扩展承载物。

## 4. 待补齐事项

1. 若新增运行时扩展类型，应先回到 06 Human Gate；
2. 非 Codex 环境适配时，应用本文区分环境机制和 LDVH 承载物。
