# 工作模型集合索引（迁移待删除）

> 创建日期：2026-06-09
> 定位：旧工作模型集合索引的迁移说明
> 适用范围：`specs/20-39` 工作模型区段删除旧手写索引前的过渡说明
> 上位依据：`specs/05-工作模型基础规范.md`
> 相关规范：`specs/01-目录说明.md`、`specs/03.03-工作模型文档规范.md`、`specs/05-工作模型基础规范.md`、`specs/09-事实源边界与承载规范.md`

---
## 1. 迁移状态

ADR-0007 已接受删除手写工作模型集合索引文档。工作模型编号区段为 `20-39` 具体工作模型规范；成员清单、编号、状态、路径、事实源目录和规则锚点由各成员主文件自描述承载。

本文仅作为迁移期间的历史入口保留，不得新增工作模型集合事实、状态清单、关系映射或检查输入。完成引用迁移、成员自描述补齐和必要的后续校验能力后，本文应删除。

---
## 2. 当前权威入口

工作模型集合读取应使用：

1. `specs/01-目录说明.md` 的编号分区；
2. `specs/05-工作模型基础规范.md` 的基础规则；
3. `specs/03.03-工作模型文档规范.md` 的成员自描述契约；
4. `specs/20-39` 具体工作模型主文件的 `ldvh_member` 自描述块。

当前目标顺序已经迁移为：

| 编号 | 工作模型 | 主文件 |
|---|---|---|
| 20 | WorkArea / 工作域 | `20-WorkArea-工作域.md` |
| 21 | TaskPlan / 任务计划 | `21-TaskPlan-任务计划.md` |
| 22 | Task / 任务 | `22-Task-任务.md` |
| 23 | SubTask / 子任务 | `23-SubTask-子任务.md` |
| 24 | ADR / 决策 | `24-ADR-决策.md` |
| 25 | Pitfall / 踩坑 | `25-Pitfall-踩坑.md` |
| 26 | Memo / 备忘 | `26-Memo-备忘.md` |
| 27 | Change / 变更 | `27-Change-变更.md` |

`28-39` 暂未占用，未来新增工作模型必须通过 Human Gate。

---
## 3. 删除前置条件

删除本文前，应确认：

1. 正式 specs 和 Rules 不再要求读取本文定位工作模型；
2. `20-27` 主文件均已补齐 `ldvh_member` 自描述；
3. 旧编号引用已从正式规范和入口规则迁移；
4. `03.03` 和 `05` 已承接成员自描述、编号占用和 Human Gate 规则；
5. 删除本文不会导致工作模型集合事实失去权威入口；
6. 删除操作已经通过 Human Gate。

---
## 4. 禁止事项

迁移期间不得：

1. 在本文继续维护第二份工作模型清单；
2. 用本文覆盖成员主文件自描述；
3. 把本文作为新增工作模型编号分配依据；
4. 把本文作为 Code/Web/Skill/Agent 的字段契约或状态机事实源；
5. 在 Code 拆分期间把未实现的 Code 派生索引当作当前事实源。

---
## 5. 待补齐事项

1. 删除本文前继续清理残留硬引用；
2. Code 整体拆分完成后，再评估是否需要派生工作模型集合索引和校验能力；
3. 物理删除本文时，应同步检查 `01`、`03.03`、`05`、`09` 和 Rules 入口。
