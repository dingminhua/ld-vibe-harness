# 04 系列落地讨论上下文暂存

> 创建日期：2026-06-13
> 定位：临时记录本轮关于 04 系列、环境适配、ECC 吸收和落地可行性的沟通上下文，便于上下文丢失后快速恢复当前状态
> 性质：临时会话上下文暂存，不构成正式 specs、ADR、Task、适配记录或稳定事实源
> 使用边界：只用于恢复讨论脉络；稳定结论必须另行进入 specs、ADR、Task、Code、Web、适配卡片或其它对应 Git 文件事实源后才具备效力

---

## 1. 当前讨论目标

本轮讨论围绕 04 系列是否能够承担 LDVH 的规范落地与环境适配方法展开。

当前核心问题是：

1. 04 系列当前方法是否可行；
2. 是否缺少让 AI 稳定执行的字段、流程、检查和证据闭环；
3. ECC 的组织方法对 04 系列有什么启发；
4. 后续如果修改 specs，应如何保持以 specs 术语为主，并避免把 docs、temp、研究材料直接提升为正式依据。

---

## 2. 已完成的审查与判断

### 2.1 04 系列语义审查

已审查 `specs/04*.md` 六个文件：

1. `specs/04-规范落地与环境适配基础规范.md`；
2. `specs/04.01-规范落地声明规范.md`；
3. `specs/04.02-LDVH能力保障规范.md`；
4. `specs/04.03-环境适配规范.md`；
5. `specs/04.04-环境适配措施实践.md`；
6. `specs/04.05-个人环境特别要求规范.md`。

基础验证已执行：

1. `python3 code/specs_validate.py all`，退出码为 0；
2. `python3 -m pytest tests/code/test_specs_validate.py`，98 passed。

判断：Code 通过只代表结构验证通过，不代表语义、职责边界和 AI 可执行性没有问题。

### 2.2 04 系列当前主要问题

已识别的主要问题包括：

1. `待补齐事项` 在 04 系列中普遍承载状态实例；
2. 04 父规范存在 `V10`、`Learn 阶段工作流程尚未创建` 等阶段或历史信息；
3. 04.04 位于 specs 中，但承载具体环境状态、适配方式、降级记录和待补齐事项，与规则态 specs 存在张力；
4. 04.04 适配卡片字段不够结构化，AI 和 Code 难以稳定消费；
5. 04.03 / 04.04 的来源字段允许 studies、sources 进入消费字段，容易被误读为规范依据；
6. 04.03 与 04.04 在新增环境、Human Gate、检查规则上存在重复；
7. 04.01 对落地要求字段使用“建议”，但后续检查按必填处理；
8. 规范落地要求表格字段统一，但语义值未枚举，Code 只能浅校验；
9. 04.02 能力保障概念清楚，但缺少“要求 → 能力 → 环境适配 → 降级 → Human Gate”的可执行判定链；
10. 04.05 不应承载具体个人环境记录，但可查询记录位置未定。

### 2.3 04 方法落地可行性判断

当前判断：04 方法论方向可行，但还不是稳定闭环。

可行部分：

1. 04.01 能声明规范落地要求；
2. 04.02 能提供能力保障分类和边界；
3. 04.03 能定义环境能力清单、环境适配状态和环境适配规则；
4. 04.04 已有适配卡片雏形；
5. 04.05 已明确个人环境特别要求不应提升为 LDVH 通用规范。

当前缺失：

1. 缺少资产层、事实源层、环境适配层之间的桥接说明；
2. 缺少从规范落地要求到实例化能力保障的判定字段；
3. 04.04 适配卡片不够结构化；
4. 缺少 42 / Code 消费 04 的契约；
5. Human Gate 缺少证据闭环；
6. 环境适配状态、检查过程状态和工作对象状态容易混淆；
7. 04.05 缺少个人环境特别要求的稳定查询位置；
8. Claude Code CLI 等第三环境可作为扩展方向，但不是当前 04 方法可行性的前置条件；
9. Command、Hook、MCP、Context / Memory / Session 等适配面在 specs 中表达仍不完整。

---

## 3. ECC 相关启发

已读取或参考的 ECC 相关临时材料包括：

1. `temp/07-LDVH对ECC-Claude-Code插件的借鉴评估.md`；
2. `temp/08-LDVH吸收ECC机制的五项落地建议深描.md`；
3. `temp/09-ECC跨平台同功能复用机制任务参考.md`；
4. `temp/20-LDVH对ECC运行系统组织方法的借鉴评估与深度调研.md`；
5. `temp/22-LDVH跨平台能力复用与薄投影落地计划.md`；
6. `temp/23-LDVH资产层与04系列重构计划.md`；
7. `temp/26-LDVH环境适配技术经验吸收研究.md`。

ECC 对 LDVH 的主要启发：

1. 学组织方法，不学内容规模或安装器；
2. 学可声明性、可计划性、可验证性、可观察性；
3. 学 manifest 思想，但转译为 LDVH 的规范落地与适配声明，不直接做安装器；
4. 学 target / adapter 思想，让不同环境映射回同一组 LDVH 要求；
5. 学 compliance matrix 思想，防止虚假完整支持；
6. 学 plan / apply / verify 分段，但 LDVH apply 必须 Human Gate；
7. 学 command contract，让命令、Skill、Code、适配措施有输入、输出、STOP 点、验证和写入边界；
8. 学 doctor / status / audit 的诊断思想，但 LDVH 检查输出只能是过程诊断，不是最终事实源；
9. 学共享内核 + 环境薄入口，保持 specs、Code、Web、工作对象和事实源边界不按环境分叉。

LDVH 不应吸收：

1. ECC 式完整安装器；
2. install state 或长期运行状态库；
3. 无授权 repair；
4. 第三方 skills / commands / rules / agents 原文资产库；
5. 把 manifest、status、doctor、landing-plan 或检查报告作为最终事实源；
6. 为了跨环境完整性提前建设庞大 CLI 或多环境自动分发系统。

---

## 4. 当前建议的补齐顺序

当前建议按以下顺序处理：

1. 补齐 04 父规范中的桥接模型，说明规范事实源、能力保障、环境适配、适配措施、事实源回写之间的关系；
2. 补齐 04.02 的实例化判定规则，让每条规范落地要求能回答执行机制、触发时机、输入、输出、验证、降级、回写和 Human Gate；
3. 重整 04.04 卡片结构，把环境状态、适配方式、证据、检查、降级、待补齐事项拆开；
4. 补齐 04.03 / 04.04 与 42 / Code 的消费契约，明确 Code 能检查什么、不能检查什么，42 如何映射检查状态；
5. 补齐 Human Gate 证据规则，包括确认内容、暂停点、证据位置、拒绝或接受降级后的处理；
6. 明确 04.05 的可查询记录位置或查询优先级；
7. 再考虑 Claude Code CLI 或三环境矩阵扩展。

---

## 5. 术语使用约束

后续沟通和文档修改应以 specs 当前术语为主。

优先使用：

1. 规范落地要求；
2. 能力保障；
3. 环境能力清单；
4. 环境适配；
5. 适配措施；
6. 适配降级；
7. Human Gate；
8. Code；
9. Web；
10. 人工降级；
11. Git 文件事实源；
12. 规则事实源；
13. 工作对象事实源。

docs / temp 中的以下词可以作为参考概念，但进入 specs 时应谨慎转译：

1. 承接；
2. 承接矩阵；
3. 薄投影；
4. 共享内核；
5. asset layer；
6. adapter；
7. manifest；
8. compliance matrix；
9. doctor / status / audit。

---

## 6. 当前未执行的事项

截至本文创建时，尚未修改 specs 正文。

尚未执行：

1. 未改 04 父规范；
2. 未改 04.01；
3. 未改 04.02；
4. 未改 04.03；
5. 未改 04.04；
6. 未改 04.05；
7. 未创建 ADR；
8. 未创建 Task；
9. 未修改 Code；
10. 未修改 Web。

---

## 7. 新增讨论：以部署入口作为 04 落地起点

用户补充了当前重要判断：LDVH 目前还不存在“让用户配置功能”的正式说法。当前 LDVH 的能力不是可选安装项，而是应安装、应准备、应可检查的必备内容。未来可以提供配置能力，例如在 Web 中列出大量 Skill 并让用户勾选，再由 AI 按配置措施安装和验证；但当前阶段即使做 Web 页面，也只能作为清单展示，不能表达为用户可选择启用或禁用。

当前更适合作为起点的问题是“部署入口”。用户认为当前最缺的是入口：既没有明确要求，也没有明确安装适配内容。

用户提出当前需要部署的入口能力可先收敛为四类：

1. Rules；
2. Skill；
3. Agent；
4. Hook。

用户倾向认为其它内容不属于部署入口。例如 CLI、Code、Web 等更像 LDVH 自身需要准备的能力资产或执行能力，不应与环境入口部署混在一起。

当前疑点：

1. Trae 似乎可以通过 Skill 兼容出类似 Hook 的效果；
2. Codex App 若要 Hook，可能需要使用其自身 Hook 能力；
3. Hook 在不同环境下是否属于真实 Hook、Skill 模拟、Command 触发、人工降级或 CLI 侧能力，目前边界混乱；
4. 对纯 CLI 环境不熟悉，需要进一步判断只用 CLI 时 Rules、Skill、Agent、Hook 分别如何表达。

该讨论对 04 的启发：

1. 当前不应把 LDVH 能力表达为用户可选功能配置；
2. 可以先定义“部署入口清单”或“环境入口适配清单”，但其语义应是必备项检查，不是用户选择项；
3. 入口部署对象先聚焦 Rules、Skill、Agent、Hook；
4. Code、Web、CLI 更适合作为 LDVH 能力保障或检查执行能力，不一定属于环境入口部署对象；
5. Hook 需要单独澄清真实 Hook、模拟 Hook、生命周期触发、人工降级之间的边界。

四类必备入口的边界补充：

1. Rules、Skill、Agent、Hook 是当前 LDVH 面向 AI 开发环境的必备入口能力，不是用户可选功能配置；
2. 四类必备入口不等于把所有相关资产都复制到每个管辖项目，也不等于每个环境都必须具备同名原生能力；
3. Rules 的边界是让 AI 稳定看到 LDVH 入口、规则约束、STOP 点和目标项目识别要求；
4. Skill 的边界是承载可复用 SOP 或操作能力，原生 Skill 不存在时可以转译为指令化流程、命令模板或人工步骤，但仍需可检查；
5. Agent 的边界是承载独立上下文、专项审查或分角色执行能力，原生 Agent 不存在时可以由主控 AI 顺序模拟，但必须显式记录目标、输入和输出；
6. Hook 的边界是生命周期触发和检查入口，可能由原生 Hook、Skill 模拟、命令触发或人工检查清单降级实现，不能把降级实现误写成原生支持；
7. Code、Web、CLI、MCP 和文档不是同一层级的部署入口；它们可以是能力保障、检查执行、展示入口、连接器或事实承载位置，但不应混入四类入口清单；
8. 四类入口的部署与检查必须保留现有 specs 中的边界：事实源边界、Human Gate、环境适配状态查询、管辖项目目标边界和降级证据边界都不能遗漏。

---

## 8. 新增讨论：管辖项目概念必须纳入部署入口

用户指出需要叉开讨论“管辖项目”概念。部署入口如果不区分 LDVH 自身项目、管辖项目和 AI 开发环境入口，会导致入口部署语义混乱。

当前临时判断：部署入口不是单纯部署到 LDVH 仓库，也不是单纯部署到某个业务项目，而是部署到 AI 执行时能够识别当前管辖项目，并按 LDVH 规则治理它的入口链路里。

需要区分至少三层：

1. LDVH 自身项目：承载 specs、Code、Web、规则、能力保障和 LDVH 自身工作对象规范；
2. 管辖项目：被 LDVH 管理、检查、落地和记录工作对象的目标项目；
3. AI 开发环境入口：Trae、Codex、Claude Code、CLI 等环境中承载 Rules、Skill、Agent、Hook 的入口。

该概念对部署入口的影响：

1. 不应把 LDVH 的 Rules、Skill、Agent、Hook 错装成每个管辖项目的一套完整副本；
2. 不应在每个管辖项目复制 specs 正文形成第二事实源；
3. Skill 应优先被理解为 LDVH 通用能力，执行时根据当前管辖项目 id/path 读取对应项目事实源；
4. Agent 输入必须包含管辖项目边界，避免跨项目误读事实源；
5. Hook 触发时必须知道作用对象是 LDVH 自身、工作区入口，还是某个管辖项目；
6. 管辖项目入口应尽量薄，只用于识别受 LDVH 管辖、定位管辖项目配置和指向 LDVH 统一入口。

后续需要读取 specs 中关于管辖项目的正式定义，尤其是目录说明和管辖项目配置规范，再判断部署入口规则应如何接入。

已读取的正式规范包括：

1. `specs/00-LD-Vibe-Harness理念与纲要.md`；
2. `specs/01-目录说明.md`；
3. `specs/03.05-管辖项目配置规范.md`。

从 specs 得到的正式边界：

1. LDVH 需要区分 LDVH 自身项目和管辖项目；
2. LDVH 自身项目承载和维护 `specs/`、Code、Web、文本能力资产和 dogfood 事实实例；
3. 管辖项目是安装并使用 LDVH 进行工程治理的用户项目；
4. 管辖项目不默认创建 `specs/` 或等价规范工作区；
5. 管辖项目的项目文档位置由项目自身约定决定，工作对象事实源是 `{path}/ldvh-base`；
6. 工作区根目录 `LDVH-GOVERNED-PROJECTS.yaml` 只登记当前 LDVH 管理实例，以及管辖项目的 `id`、`path`、可选 `name`、可选 `description`；
7. `LDVH-GOVERNED-PROJECTS.yaml` 不记录初始化状态、落地状态、检查状态、环境能力、适配措施或缺口处理状态；
8. 管辖项目接入后，应优先检查工作区根目录配置是否登记该项目，并检查对应项目的 `ldvh-base/` 和必要的工作区级薄入口；
9. 管辖项目不默认创建环境规则文件、项目指令或等价项目级入口；确需例外时，必须 Human 明确授权并记录影响范围；
10. 项目入口文档可以是 README，但不得承载 LDVH 稳定规范规则、工作对象状态机、字段契约或验收事实；
11. 管辖项目 `docs/studies` 和 `docs/sources` 结论必须吸收后才成为项目稳定事实；
12. 工作模型实例由项目拥有，存放在项目结构化事实源目录中。

对部署入口讨论的修正：

1. 部署入口首先应面向工作区级 / 环境级入口，让 AI 能识别 LDVH 管理实例和管辖项目清单；
2. 不应默认在每个管辖项目创建项目级 Rules、Skill、Agent、Hook 入口；
3. 若某个管辖项目确需项目级入口，应作为例外适配措施处理，并触发 Human Gate；
4. Rules / Skill / Agent / Hook 的部署状态应区分“LDVH 通用入口已部署”和“某管辖项目例外入口已部署”；
5. 管辖项目配置文件不是部署状态事实源，部署状态、缺口和检查结论应由当前检查报告、Task、适配卡片、Code 输出或其他对应事实源承载。

多管辖项目带来的新增判断：

1. 多个管辖项目是 LDVH 管理实例的必然场景，不应为了降低复杂度而假设只有一个管辖项目；
2. 多项目本身不是问题，默认选择才是问题；
3. 管辖项目配置只回答“当前工作区中的 LDVH 管哪些项目”，不回答“本次任务要处理哪个项目”；
4. 凡是动作会读取、检查、修改或回写管辖项目事实源，AI 必须先确定目标管辖项目；
5. 目标管辖项目可以来自用户明确指定的 `project_id`、明确路径、当前工作目录唯一匹配、或当前流程上下文中的唯一项目；
6. 如果当前路径匹配多个嵌套项目，应优先使用最长路径匹配，但高影响写入前仍应 Human Gate；
7. 如果当前路径不在任何登记项目下，或用户说“当前项目”但无法唯一匹配，AI 不得默认选择第一个项目，应暂停并询问 Human；
8. 如果用户要求处理所有项目，可以遍历检查，但写入和缺口关闭仍应明确授权范围，并按项目隔离输出；
9. 多项目检查报告必须逐项目标注 `project_id`、`project_path`、检查范围、缺口和降级，不得混成一个整体结论；
10. Code 命令和部署入口应支持 `project_id` / `project_path` / `all projects` 等目标模式；没有目标参数时，只应做项目清单检查或要求确认，不应直接检查或修改某个项目；
11. LDVH 自身 dogfood 项目不得被解释为用户安装后的默认管辖项目；
12. Rules、Skill、Agent、Hook 在多项目场景下必须携带或解析目标项目边界，避免跨项目误读、误写和事实源污染。

管辖项目设置后的行为调整：

1. 设置管辖项目后，用户需要通过 Web 或等价 Human-facing 入口知道发生了什么变化；
2. 设置管辖项目可以建立或检查 LDVH 需要的数据文件夹或结构化事实源目录，用于承载 LDVH 工作对象实例；
3. 管辖项目的项目文档位置、结构和维护方式由项目自身约定决定，不属于管辖项目设置动作的默认变化；
4. 管辖项目接入后的默认变化应收敛为：登记项目、建立或检查 LDVH 数据/事实源目录、让 Web 展示接入结果和缺口；
5. 如果某个任务确实需要读取、引用或修改项目文档，应由具体任务、用户明确指令、项目约定或 Human Gate 触发，而不是由“设置管辖项目”自动触发。

已执行的正式规范变更：

1. 已修改 `specs/01-目录说明.md`，将管辖项目文档边界调整为项目自有文档位置，并明确项目文档位置、结构和维护方式由项目自身约定决定；
2. 已删除 `specs/03.02-管辖项目文档规范.md`，03 系列不再单列管辖项目文档专题规范；
3. 已将 03 系列后续子文档编号顺移：03.03 索引文档规范变为 03.02，03.04 工作模型文档规范变为 03.03，03.05 工作流程文档规范变为 03.04，03.06 管辖项目配置规范变为 03.05；
4. 已修改 `specs/03.05-管辖项目配置规范.md`，取消由 `path` 固定推导 docs 路径的规则，只保留 `ldvh-base` 作为固定推导的工作对象事实源目录，并要求 Web 或等价 Human-facing 入口展示管辖项目接入结果、缺口和后续动作；
5. 已同步更新 specs、Code、Code 测试、入口 Rules 和临时上下文中对 03 系列编号的引用；
6. 已运行 `python3 code/specs_validate.py all`，退出码为 0；
7. 已运行 `python3 -m pytest tests/code/test_specs_validate.py`，98 passed；
8. 已运行 `git diff --check`，退出码为 0；
9. 已提交 `1a8ef11`，提交信息为“调整管辖项目文档边界”；
10. 已提交 `898854b`，提交信息为“删除管辖项目文档规范并顺移03编号”；
11. 提交后 `git status --short` 无输出，工作区干净。

当前 03 系列正式子文档清单：

1. `specs/03.01-规范文档规范.md`；
2. `specs/03.02-索引文档规范.md`；
3. `specs/03.03-工作模型文档规范.md`；
4. `specs/03.04-工作流程文档规范.md`；
5. `specs/03.05-管辖项目配置规范.md`。

下一步建议安排：

1. 回到 04 系列，先定义“部署入口”作为 04 落地起点；
2. 明确当前 LDVH 不存在用户可选功能配置，Rules、Skill、Agent、Hook 是必装、必备、必检查入口能力；
3. 在 04.02 / 04.03 / 04.04 中区分四类入口能力的 LDVH 要求、环境原生能力、适配方式、检查方式、适配降级和 Human Gate；
4. 把管辖项目配置接入入口链路：AI 先识别 LDVH 管理实例和目标管辖项目，再执行 Rules、Skill、Agent、Hook 相关能力；
5. 明确设置管辖项目后的默认变化：登记项目、建立或检查 `ldvh-base`、Web 展示接入结果和缺口；
6. 再评估 Code / Web 是否需要支持管辖项目接入结果展示、入口部署清单展示和多项目目标解析。

---


## 10. 04 系列部署入口小改执行记录

本轮已按“小改、先补入口模型、避免大重构”的方式推进 04 系列部署入口设计。

已执行的 specs 修改：

1. `specs/04-规范落地与环境适配基础规范.md`：在三层主轴后补入部署入口模型，明确部署入口是能力保障落到 AI 开发环境时的入口表达，不构成第四治理层；当前必备部署入口限定为 Rules、Skill、Agent、Hook；Code、Web、CLI、MCP、Command、CI 和文档不是同一层级的部署入口；
2. `specs/04.02-LDVH能力保障规范.md`：新增“必备部署入口”小节，定义四类入口的 LDVH 要求、环境承载方式和缺失处理；将原 Rules / Skill / Agent 边界细则扩展为 Rules / Skill / Agent / Hook 部署入口边界细则；补齐 Hook 的允许承载、禁止承载、交还、回写和 Human Gate 边界；
3. `specs/04.03-环境适配规范.md`：在环境能力清单边界中加入四类必备部署入口适配判断表；在环境适配字段中加入条件必填的“部署入口类型”，用于区分 Rules、Skill、Agent、Hook 与其它能力保障项；
4. `specs/04.04-环境适配措施实践.md`：新增“部署入口类型”字段；将适配措施轻度分组为入口总卡片、四类必备部署入口适配措施、非部署入口能力保障适配措施；拆分 Skill 与 Command，明确 Command 不是第五类部署入口；明确 Code / CI 和 Web / Human Gate 是检查、执行或交互能力，不属于四类部署入口；
5. `specs/04.01-规范落地声明规范.md`：补充边界，说明落地要求中的保障机制只是候选方向，是否进入四类必备部署入口由 04.02 判断，规范落地要求不得直接指定必须由某环境原生能力实现；
6. `specs/04.05-个人环境特别要求规范.md`：补充边界，说明个人环境特别要求可以影响四类入口在当前个人环境中的本地承载、命令验证或人工降级方式，但不得提升为 LDVH 通用部署入口规则。

本轮语义边界：

1. 四类必备部署入口是 Rules、Skill、Agent、Hook；
2. 四类入口必备、必检查、可降级，但不要求每个环境具备同名原生能力；
3. Skill 可以模拟 Hook 提醒或检查流程，但不得写成 Hook 原生完整支持；
4. Command、Code、Web、CLI、MCP、CI 和文档可支撑入口能力，但不得作为第五类部署入口；
5. 环境适配必须区分原生承载、配置适配、指令适配、人工降级和待确认；
6. 入口部署和检查不得遗漏事实源边界、Human Gate、环境状态查询、管辖项目目标边界和降级证据边界。


## 11. 最小入口资产落地记录

在 04 系列部署入口模型入库后，已继续把四类必备入口推进到最小文本能力资产层。

已完成：

1. Rules：已提交 `d02b563`，提交信息为“同步Rules入口部署入口模型”；`rules/LDVH-AI-ENTRY.md` 已投影 Rules、Skill、Agent、Hook 四类必备部署入口模型，并修正 04.02 重复引用为 04.03；
2. Skill：新增 `skills/ldvh-spec-change-check/SKILL.md`，作为 LDVH 规范变更检查 Skill 的最小文本能力资产；
3. Agent：新增 `agents/ldvh-spec-semantic-review.md`，作为 LDVH 规范语义审查 Agent 的最小文本能力资产；
4. Hook：新增 `hooks/ldvh-lifecycle-check.md`，作为 LDVH 生命周期检查 Hook 的最小文本能力资产；
5. 三个新增入口资产均只承载定位、触发、输入输出、检查步骤、STOP 点、Human Gate、降级和交还规则，不新增稳定规范规则，不声明任何环境具备原生完整支持。

本轮入口资产边界：

1. `skills/ldvh-spec-change-check/SKILL.md` 是 Skill 部署入口资产，用于 specs 或入口资产变更后的最小治理检查；
2. `agents/ldvh-spec-semantic-review.md` 是 Agent 部署入口资产，用于独立语义审查，输出必须回到主控或 Human，不得直接生效；
3. `hooks/ldvh-lifecycle-check.md` 是 Hook 部署入口资产，用于生命周期检查入口，覆盖原生 Hook、Skill 模拟、Command 触发、CI 触发和人工检查清单等承载方式；
4. Command、Code、Web、CLI、MCP、CI 和文档仍不作为第五类部署入口；
5. Skill 模拟 Hook 只能作为模拟或指令适配，不能声明原生 Hook 完整支持；
6. Hook 触发不等于检查通过，Hook 输出必须交还主控 AI 或 Human。


## 12. 部署入口资产清单落地记录

已新增 `rules/LDVH-DEPLOYMENT-ENTRIES.md`，作为四类必备部署入口资产的当前仓库清单。

本轮已完成：

1. 新增 `rules/LDVH-DEPLOYMENT-ENTRIES.md`，列出 Rules、Skill、Agent、Hook 四类入口的当前资产、位置、状态、使用时机和降级方式；
2. 更新 `rules/LDVH-AI-ENTRY.md`，指向 `rules/LDVH-DEPLOYMENT-ENTRIES.md`；
3. 明确该清单只是入口资产定位清单，不是 specs 正式规范，不是环境安装状态记录，不代表当前环境已经安装、启用或原生完整支持；
4. 明确后续 Code / Web 可以读取该清单做检查或展示，但正式规则仍以 specs 为准，环境能力状态仍必须按 04.03、04.04、环境适配记录、Code 输出或 Human Gate 查询。

当前部署入口资产链路：

1. AI 先读 `rules/LDVH-AI-ENTRY.md`；
2. 再通过 `rules/LDVH-DEPLOYMENT-ENTRIES.md` 定位四类入口资产；
3. 根据任务类型读取 `skills/ldvh-spec-change-check/SKILL.md`、`agents/ldvh-spec-semantic-review.md` 或 `hooks/ldvh-lifecycle-check.md`；
4. 使用入口资产时仍受 specs、事实源边界、Human Gate 和验证命令约束。

## 13. 部署入口资产清单 Code 检查落地记录

已在 `code/specs_validate.py` 中新增部署入口资产清单一致性检查，并纳入 `all` 总检查。

本轮已完成：

1. 新增 `deployment-entries` 子命令，用于检查 `rules/LDVH-DEPLOYMENT-ENTRIES.md` 是否存在、是否包含 Rules、Skill、Agent、Hook 四类必备入口，以及清单中资产路径是否存在；
2. 检查 `rules/LDVH-AI-ENTRY.md` 是否引用 `rules/LDVH-DEPLOYMENT-ENTRIES.md`；
3. 检查清单中不得把 Code、Web、CLI、MCP、Command、CI 或文档写成第五类部署入口；
4. 将 `rules/LDVH-DEPLOYMENT-ENTRIES.md` 纳入运行投影默认检查路径；
5. 在 `tests/code/test_specs_validate.py` 中补充部署入口资产清单的通过、缺失清单、缺失必备类型、缺失资产路径、禁止入口类型和 CLI 正向测试。

本轮验证：

1. `python3 code/specs_validate.py deployment-entries`，退出码为 0；
2. `python3 -m pytest tests/code/test_specs_validate.py`，103 passed；
3. `python3 code/specs_validate.py all`，退出码为 0。

当前意义：

1. 四类部署入口从 specs 规则、Rules 投影、入口资产、资产清单推进到 Code 自动检查；
2. 后续移动、删除、重命名入口资产，或误把非入口能力写成第五类部署入口，会被 Code 检查暴露；
3. 该检查仍是项目内结构一致性检查，不证明任何外部 AI 开发环境已经安装、启用或原生完整支持四类入口。

## 14. 部署入口资产清单 Web 需求规范化记录

用户指出 Web 相关需求应写入 Web 相关规范，由负责 Web 的 AI 后续处理。因此本轮只把“部署入口资产清单 Web 只读展示”写入规范和 Web 实现文档，不直接改 Web 代码。

本轮已完成：

1. 在 `specs/08-Web信息同步实现规范.md` 中明确 Web 可以展示规范落地、能力保障、部署入口资产清单和环境适配缺口的 Human-facing 派生态势；
2. 在 `specs/08-Web信息同步实现规范.md` §6.1 明确部署入口资产清单、landing-plan、landing-report、Validate 结果和 Code 检查输出在 Web 中属于派生展示或过程输出；
3. 在 `specs/08-Web信息同步实现规范.md` §10 和 §14 增加部署入口资产清单 Web 使用场景和落地要求；
4. 在 `specs/04.02-LDVH能力保障规范.md` 中补充 Web / Human 入口可以展示部署入口资产清单，但不得表达为用户可选配置、环境安装状态或完整支持声明；
5. 在 `web/docs/02-Dashboard.md` 中记录 Dashboard 可增加部署入口资产清单摘要区域，展示 Rules、Skill、Agent、Hook 四类入口资产和检查状态。

边界：

1. Web 展示只读，不提供开关、安装向导或可选配置；
2. Web 展示不得把 Code、Web、CLI、MCP、Command、CI 或文档展示为同级第五类部署入口；
3. Web 展示不得声明任何外部 AI 开发环境已经安装、启用或原生完整支持四类入口；
4. 若清单或 Code 检查失败，Web 应显示错误态或降级提示，并引导回 Validate、Code 输出或 Git 文件事实源。

## 15. 部署入口认知校正记录

用户进一步澄清：四类部署入口资产不是因用户变化而变化的运行清单，已知 AI 开发环境的能力画像和适配方案也不应被简单理解为每个用户临时变化的内容。LDVH 应提前准备固定入口要求、已知环境类型适配方案和用户本地实例验证边界，避免把固定产品内容误拆成噪声文件或临时运行投影。

最新认知应按三层区分：

1. LDVH 固定入口要求：Rules、Skill、Agent、Hook 是 LDVH 必备部署入口，入口资产属于 LDVH 固定产品内容，应由 specs 承载，不应由 rules 下的独立清单反向定义；
2. 已知环境类型预置适配：Trae、Codex、Claude Code、Cursor 等已知 AI 开发环境的 Rules / Skill / Agent / Hook 承载方式、原生支持情况、模拟方式、Command / CI 辅助方式和降级策略，应作为 LDVH 预置适配方案沉淀，而不是让每个用户临时判断；
3. 用户本地环境实例状态：用户机器、工作区和项目是否已经按预置方案安装、启用、授权和验证，才是因用户和本地环境变化的实例状态，应通过检查、记录或 Human Gate 确认。

据此，后续应避免以下噪声：

1. 不再把固定部署入口资产写成一个会随用户变化的运行清单；
2. 不再把 Trae / Codex 等已知环境类型适配方案描述为每个用户都要重新判断的临时事项；
3. 不再让 `rules/LDVH-DEPLOYMENT-ENTRIES.md` 承担规范事实、字段契约或 Code / Web 契约来源；
4. 不再把 Code 输出契约、Web 展示契约或入口资产字段含义优先归到实现层；这些业务语义应先回到业务规范；
5. 不再把 Web 展示数据来源简单绑定到 `rules/LDVH-DEPLOYMENT-ENTRIES.md`；更合理的来源应是 specs 定义的固定入口要求、已知环境适配方案和 Code 检查输出。

此前行为中不符合当前认知或需要后续修正的地方：

1. 新增 `rules/LDVH-DEPLOYMENT-ENTRIES.md` 作为当前仓库部署入口资产清单，虽然当时已标注不是 specs、不是环境安装状态，但从最新认知看，它把固定 LDVH 产品内容拆成了 rules 运行投影，容易制造中间事实源；
2. 更新 `rules/LDVH-AI-ENTRY.md` 指向 `rules/LDVH-DEPLOYMENT-ENTRIES.md`，会强化 AI 通过 rules 清单定位固定入口资产的路径，后续应改为直接指向正式 specs 中的固定入口定义；
3. 在 `code/specs_validate.py` 中新增的 `deployment-entries` 检查以 `rules/LDVH-DEPLOYMENT-ENTRIES.md` 是否存在为核心前提，这与“固定入口资产应由 specs 定义，Code 检查固定资产与规范承接”的方向不一致，后续应调整为固定部署入口资产检查；
4. 将 `rules/LDVH-DEPLOYMENT-ENTRIES.md` 纳入运行投影默认检查路径，会延续该文件作为长期运行投影的定位，后续应重新评估是否删除该文件或将其降为极薄入口摘要；
5. 在 Web 规范和 `web/docs/02-Dashboard.md` 中把部署入口资产清单展示的数据来源写向 `rules/LDVH-DEPLOYMENT-ENTRIES.md`，后续应改为展示 specs 定义的固定入口资产、已知环境适配方案和 Code 检查状态；
6. 曾建议先由 07 定义 JSON 输出契约，这混淆了“Code 生成输出”和“业务规范定义字段语义”；正确方向应是业务语义先由 04 系列承接，07 只定义确定性实现与输出方式，08 只定义 Web 消费边界。

当前建议的修正方向：

1. 先在 04 系列中归位固定入口资产定义和已知环境类型适配方案边界；
2. 再决定删除 `rules/LDVH-DEPLOYMENT-ENTRIES.md`，或将其改成只指向 specs 的极薄 AI 运行摘要；
3. 调整 `rules/LDVH-AI-ENTRY.md`、Code 检查和 Web 规范，使它们不再依赖该清单作为权威来源；
4. 最后再设计 Code JSON 输出和 Web 展示消费，确保它们只是实现和展示 specs 定义的业务结构。

## 16. 固定入口资产与环境类型适配规范归位记录

本轮按最新认知先做 04 系列规范归位小改，暂不清理 Rules、Code 和 Web 对 `rules/LDVH-DEPLOYMENT-ENTRIES.md` 的依赖。

已完成：

1. 在 `specs/04.02-LDVH能力保障规范.md` 的必备部署入口章节中增加 LDVH 自身固定部署入口资产表，明确 Rules、Skill、Agent、Hook 对应的固定入口资产路径、职责和维护边界；
2. 在 04.02 中明确固定入口资产是 LDVH 产品内容的一部分，不是用户配置项、不是环境安装状态记录，也不随单个用户环境变化；
3. 在 `specs/04.03-环境适配规范.md` 中加入三层事实区分：LDVH 固定入口要求、已知环境类型预置适配、用户本地环境实例状态；
4. 在 04.03 中明确不得把已知环境类型预置适配当成用户本地实例已安装，也不得把用户本地实例失败反向写成环境类型或 LDVH 固定入口要求不可用；
5. 在 `specs/04.04-环境适配措施实践.md` 中明确 Trae Work CN 与 Codex App 是已知环境类型，本文记录的是 LDVH 可复用的环境类型预置适配方案，不表示任一用户机器、工作区或项目已经完成安装、启用、授权或现场验证。

本轮尚未处理：

1. 尚未删除或降级 `rules/LDVH-DEPLOYMENT-ENTRIES.md`；
2. 尚未修改 `rules/LDVH-AI-ENTRY.md` 对该清单的引用；
3. 尚未调整 `code/specs_validate.py deployment-entries` 的检查前提；
4. 尚未调整 Web 规范和 Dashboard 文档中对该清单作为展示来源的引用。

后续顺序建议：先清理 Rules 入口依赖，再调整 Code 检查，最后调整 Web 规范和 Web 文档，避免实现层继续依赖已被归位为 specs 事实的中间清单。

## 17. 部署入口中间清单依赖清理记录

本轮按第 16 节后的后续顺序，清理此前围绕 `rules/LDVH-DEPLOYMENT-ENTRIES.md` 形成的中间清单依赖。

已完成：

1. 删除 `rules/LDVH-DEPLOYMENT-ENTRIES.md`，固定部署入口资产不再由 rules 运行投影清单承载；
2. 修改 `rules/LDVH-AI-ENTRY.md`，将四类必备部署入口和 LDVH 固定入口资产定义直接指向 `specs/04.02-LDVH能力保障规范.md`；
3. 修改 `code/specs_validate.py`，将 `deployment-entries` 检查从“部署入口资产清单一致性检查”调整为“固定部署入口资产检查”，检查 04.02 是否定义 Rules、Skill、Agent、Hook 和四类固定资产路径，以及资产文件是否存在；
4. 从运行投影默认检查路径中移除 `rules/LDVH-DEPLOYMENT-ENTRIES.md`；
5. 更新 `tests/code/test_specs_validate.py`，测试 04.02 固定入口资产定义缺失、资产路径缺失、非入口能力误列、Rules 未引用 04.02 和 CLI 正向输出；
6. 修改 `specs/08-Web信息同步实现规范.md` 和 `web/docs/02-Dashboard.md`，Web 展示来源改为 04.02 固定入口资产定义、Rules 统一入口和 Code 固定部署入口资产检查结果。

当前口径：

1. 固定部署入口资产是 LDVH 产品内容，由 04.02 承载；
2. Rules 文件只做 AI 运行入口和导航，不再维护固定入口资产清单；
3. Code 只检查 04.02 规范定义和实际资产是否一致，不再要求额外 rules 清单存在；
4. Web 只读展示 04.02 定义的固定入口资产和 Code 检查状态，不把展示写成配置、安装状态或完整支持声明。

## 18. 入口术语校正记录

用户进一步确认：此前把 LDVH 自己准备的 Rules、Skill、Agent、Hook 内容称为“入口”并不准确。更准确的区分是：LDVH 自己准备的是能力资产；入口是各个 AI 开发环境里实际读取、加载、调用或触发这些资产的机制或位置；薄引用是放在环境入口里的最小引用，用来指向 LDVH 能力资产。

已确认术语：

| 术语 | 含义 | 示例 |
|---|---|---|
| LDVH 能力资产 | LDVH 自己定义和维护的能力内容 | `rules/LDVH-AI-ENTRY.md`、Skill、Agent、Hook |
| 环境入口 | AI 开发环境提供的读取、加载、调用、触发位置或机制 | Trae rules 目录、Trae skill 目录、Codex hook 配置 |
| 薄引用 | 放在环境入口中，指向 LDVH 能力资产的最小引用 | Trae rules 入口里的 LDVH 引用文件 |
| 环境适配 | 把 LDVH 能力资产接入环境入口的方案 | 复制、引用、软链、配置、人工降级 |
| 用户本地实例状态 | 某个用户当前环境是否已完成接入和验证 | 本机 Trae 是否已放入薄引用 |

据此应稳定以下认知：

1. Rules、Skill、Agent、Hook 在 LDVH 侧首先是能力资产类型，不是环境入口本身；
2. Trae、Codex、Cursor、Claude Code 等环境各自提供的 rules、skill、agent、hook、instructions、配置、目录或触发机制，才是环境入口；
3. LDVH 能力资产进入环境入口时，应优先通过薄引用、配置、软链或安装方式连接，避免复制完整资产正文形成第二事实源；
4. 环境适配要回答“这个环境入口如何接入 LDVH 能力资产”，而不是把 LDVH 资产本身叫入口；
5. 用户本地实例状态只回答“当前用户环境是否已经完成接入和验证”，不能反向定义 LDVH 能力资产或环境类型入口能力。

此前上下文中第 15-17 节的“部署入口”“固定部署入口资产”“入口资产”等说法，需要视为阶段性表述，不再作为后续术语依据。后续正式规范重构前，应先把术语切换为“LDVH 能力资产 / 环境入口 / 薄引用 / 环境适配 / 用户本地实例状态”。

当前规划状态：后续 04.03 / 04.04 / 04.05 / 04.06 的命名和职责仍未稳定，不应继续扩散到 Code JSON、Web 实现或更多规范改写。下一步应先围绕上述术语重新整理 04 后半段边界，得到用户确认后再执行正式迁移。

## 19. 04.04 适配、部署与实时检查一体化认知

用户进一步确认：用户本地实例状态不应作为长期记录型规范内容；最终 AI 是根据 04.04 帮用户执行部署，因此“环境入口如何适配”“如何部署”“如何实时检查是否生效”本质上应放在同一闭环中讨论。

已确认方向：

1. 04.04 不应只是环境适配说明或环境类型卡片集合，而应成为环境入口适配、部署执行和实时检查的一体化规范；
2. 04.04 应回答：某个环境入口在哪里、它如何接入 LDVH 能力资产、AI 如何帮用户执行部署、部署后如何实时检查、失败时如何降级或触发 Human Gate；
3. 用户本地实例状态不是长期事实源，不应写成“已安装 / 已启用 / 已验证”的稳定记录；它应来自每次部署前、部署后或声明完成前的实时检查输出；
4. 实时检查方式应由 04.04 随环境入口适配一起定义，检查结果由 Code 输出、Web 当前视图、Human Gate 记录、Task / Memo / Pitfall 等承载，不写入通用 specs 正文；
5. 若环境入口无原生能力，应在 04.04 中定义模拟、配置、薄引用、软链、复制、人工降级或不可声明完整支持的处理方式；
6. 04.05 不宜设计为“环境实例验证规范”。若保留 04.05，应继续聚焦个人环境特别要求；用户本地接入是否有效由 04.04 定义实时检查方法。

04.04 后续建议字段方向：

| 字段 | 含义 |
|---|---|
| 环境类型 | Trae Work CN、Codex App、Cursor、Claude Code 等 |
| 环境入口 | 该环境实际读取、加载、调用或触发的位置或机制 |
| 对应 LDVH 能力资产 | Rules、Skill、Agent、Hook 等 LDVH 能力资产 |
| 接入方式 | 薄引用、配置、安装、软链、复制、人工降级等 |
| 部署步骤 | AI 如何在授权边界内帮助用户把 LDVH 能力资产接入环境入口 |
| 写入权限 | 是否允许自动写入，是否需要 Human Gate |
| 实时检查方式 | 如何现场判断当前环境入口已经接入对应 LDVH 能力资产 |
| 成功条件 | 满足什么才能声明本次部署或接入有效 |
| 失败处理 | 缺入口、无权限、不可读、环境不支持时如何处理 |
| 降级方式 | 没有原生入口时如何模拟或人工降级 |
| 禁止声明 | 哪些情况下不得声明原生完整支持或部署完成 |
| 证据输出 | AI、Code 或 Web 应向 Human 呈现哪些检查证据 |

据此，04 后半段当前更稳定的候选结构是：

1. 04.03：LDVH 能力资产规范，回答 LDVH 自己有哪些能力资产、职责和边界；
2. 04.04：环境入口适配与部署规范，回答各环境入口如何接入、部署、实时检查 LDVH 能力资产；
3. 04.05：个人环境特别要求规范，回答个人或当前执行环境的特殊约束如何影响部署和检查，但不记录长期安装状态。

当前仍需讨论后再执行：04.03 / 04.04 / 04.05 的最终命名、是否顺移现有文件、如何迁移当前 04.03 / 04.04 / 04.05 内容，以及如何修正已提交的 Code / Web 引用。暂不继续扩散到 JSON 输出、Web 实现或更多代码修改。

## 20. 04 后半段边界确认稿

本节是基于第 18、19 节已确认术语形成的讨论稿，不是正式 specs 结论。目标是先把 04.03、04.04、04.05 的内容边界讨论清楚，再决定是否执行正式迁移。

### 20.1 当前已稳定的前半段

1. `04`：回答 04 系列整体链路是什么，即 specs 落地要求如何通过 LDVH 能力保障进入 AI 开发环境；
2. `04.01`：回答 specs 如何声明落地要求；
3. `04.02`：回答落地要求如何映射到 LDVH 能力保障；
4. 04.02 不应长期承担 LDVH 能力资产文件路径、环境入口、部署步骤或实时检查细节。

### 20.2 后半段候选结构

当前更稳定的候选结构：

1. `04.03-LDVH能力资产规范.md`；
2. `04.04-环境入口适配与部署规范.md`；
3. `04.05-个人环境特别要求规范.md`。

暂不新增“环境实例验证规范”。用户本地实例状态通过 04.04 定义的实时检查产生即时输出，不作为长期规范正文维护。

### 20.3 04.03 LDVH 能力资产规范

04.03 应回答：LDVH 自己定义和维护哪些能力资产，这些资产的职责、边界和可被环境入口接入的方式是什么。

04.03 应负责：

1. 定义 LDVH 能力资产的概念；
2. 定义 Rules、Skill、Agent、Hook 等 LDVH 能力资产类型；
3. 说明这些资产是 LDVH 产品内容，不是环境入口本身；
4. 说明这些资产不替代 specs、Git 文件事实源、Code 校验或 Human Gate；
5. 说明资产进入环境入口时应通过薄引用、配置、安装、软链或受控复制等方式接入；
6. 说明资产被 Code 检查和 Web 只读展示的边界。

04.03 不应负责：

1. 不定义 Trae / Codex / Cursor / Claude Code 的环境入口位置；
2. 不写具体部署步骤；
3. 不记录用户本地是否已安装、启用或验证；
4. 不定义个人环境特殊限制；
5. 不定义 Code JSON 输出或 Web 页面布局。

### 20.4 04.04 环境入口适配与部署规范

04.04 应回答：各 AI 开发环境提供哪些环境入口，这些入口如何接入 LDVH 能力资产，AI 如何执行部署，部署后如何实时检查是否有效。

04.04 应负责：

1. 定义环境入口、薄引用、接入方式、部署步骤、实时检查和证据输出字段；
2. 记录 Trae Work CN、Codex App、Cursor、Claude Code 等已知环境类型的预置适配与部署方案；
3. 对每个环境入口说明对应的 LDVH 能力资产、接入方式、部署步骤、写入权限、实时检查方式、成功条件、失败处理、降级方式、禁止声明和证据输出；
4. 规定 AI 根据 04.04 帮用户执行部署时，哪些动作可以自动做，哪些必须 Human Gate；
5. 规定用户本地实例状态只能由实时检查输出产生，不写成长期“已安装 / 已启用 / 已验证”状态；
6. 规定环境无原生入口时如何模拟、配置、薄引用、软链、受控复制或人工降级。

04.04 不应负责：

1. 不定义 LDVH 能力资产本身；
2. 不降低 04.02 的能力保障要求；
3. 不把一次检查结果写成通用规范事实；
4. 不把某个用户机器状态写成环境类型结论；
5. 不定义个人偏好或当前 Human 特殊约束。

### 20.5 04.05 个人环境特别要求规范

04.05 应回答：个人或当前执行环境的特殊约束如何影响部署、检查和验证，但不升级为 LDVH 通用规则。

04.05 应负责：

1. 定义个人环境特别要求的概念、字段和查询方式；
2. 记录个人工具路径、命令偏好、权限限制、不可触碰目录、网络限制、版本限制等特殊约束的治理规则；
3. 说明个人环境特别要求如何约束 04.04 中的部署步骤和实时检查；
4. 说明个人环境特别要求不得反向修改 LDVH 能力资产、环境入口定义或环境类型预置方案。

04.05 不应负责：

1. 不定义 LDVH 能力资产；
2. 不定义环境入口适配方案；
3. 不记录长期安装、启用或验证状态；
4. 不替代 Human 当前指令、项目 README 或环境实时检查结果。

### 20.6 迁移影响清单

若用户确认上述边界，后续正式迁移至少涉及：

1. 从 04.02 移出 LDVH 能力资产文件路径和资产职责内容，迁入新 04.03；
2. 将当前 04.03 中环境适配字段、状态、三层事实边界等内容迁入新 04.04，并按“环境入口、部署步骤、实时检查”重写；
3. 将当前 04.04 中 Trae Work CN、Codex App 的环境适配措施迁入新 04.04，改为环境入口适配与部署卡片；
4. 保留并必要时顺移当前 04.05 为个人环境特别要求规范；
5. 更新 04 父规范的子文档边界；
6. 更新 `rules/LDVH-AI-ENTRY.md`、`code/specs_validate.py`、`tests/code/test_specs_validate.py`、`specs/08-Web信息同步实现规范.md` 和 `web/docs/02-Dashboard.md` 中已提交但基于旧术语的引用；
7. 更新临时上下文，明确此前“部署入口 / 固定部署入口资产 / 入口资产”的阶段性表述已被新术语替换。

### 20.7 暂缓事项

在上述边界未确认前，暂缓：

1. Code JSON 输出契约；
2. Web 页面实现；
3. 新增环境类型卡片；
4. 用户本地实例状态记录格式；
5. 更多 Rules / Skill / Agent / Hook 资产扩展。

## 21. 04 文档命名与草案优先策略

用户进一步确认：能力保障的依据是能力资产，因此能力资产与落地保障可以放在同一个 04.02 中。用户提出 `04.02-LDVH能力资产与落地保障规范.md`，该命名比“LDVH能力保障与能力资产规范”更符合当前认知：先说明 LDVH 拥有什么能力资产，再说明这些资产如何保障规范落地。

当前候选文件结构更新为：

1. `04-规范落地与环境适配基础规范.md`：04 系列整体链路；
2. `04.01-规范落地声明规范.md`：specs 如何声明落地要求；
3. `04.02-LDVH能力资产与落地保障规范.md`：LDVH 如何用能力资产与能力组合保障规范落地；
4. `04.03-环境入口适配与部署规范.md`：各 AI 环境如何通过环境入口接入、部署并实时检查 LDVH 能力资产；
5. `04.04-个人环境特别要求规范.md`：个人或当前执行环境的特殊约束如何影响部署和检查。

与第 20 节相比，本轮修正点：

1. 不再单独规划 `04.03-LDVH能力资产规范.md`；
2. 能力资产与落地保障合并到 04.02；
3. 04.03 专注环境入口适配、部署和实时检查；
4. 04.04 继续承接个人环境特别要求；
5. 不新增“环境实例验证规范”，用户本地实例状态仍由 04.03 定义的实时检查产生即时输出。

用户建议：可以先写一份这些文档的草案，然后再替换现在的正式文档。该策略更稳，应优先采用。

草案优先策略：

1. 先在临时或草案位置写出 04、04.01、04.02、04.03、04.04 的完整草案，不直接覆盖现有 specs；
2. 草案先解决内容边界、术语一致性、章节结构、迁移归属和引用口径；
3. 草案阶段允许保留 TODO、迁移说明和差异标注，但不得作为正式规范依据；
4. 用户确认草案后，再执行正式替换、文件重命名、引用更新、Code / Web 校验调整；
5. 正式替换前不继续扩散到 JSON 输出、Web 实现或新增环境类型卡片。

草案建议路径待确认。候选：

1. `temp/04-restructure-drafts/`：适合临时讨论，不进入 specs 权威位置；
2. `docs/drafts/04-restructure/`：适合较完整的草案评审；
3. 直接在本临时上下文中写章节级草案：上下文集中，但不适合承载完整文件正文。

当前建议采用 `temp/04-restructure-drafts/`，先生成完整草案文件，确认后再替换正式 specs。

## 22. 04 重构草案产出记录

已按第 21 节的草案优先策略，在 `temp/04-restructure-drafts/` 创建五份非正式草案文档：

1. `temp/04-restructure-drafts/04-规范落地与环境适配基础规范.md`；
2. `temp/04-restructure-drafts/04.01-规范落地声明规范.md`；
3. `temp/04-restructure-drafts/04.02-LDVH能力资产与落地保障规范.md`；
4. `temp/04-restructure-drafts/04.03-环境入口适配与部署规范.md`；
5. `temp/04-restructure-drafts/04.04-个人环境特别要求规范.md`。

草案定位：

1. 这些文件是讨论草案，不是正式 specs 权威来源；
2. 草案用于先评审 04 系列结构、术语、章节边界和迁移方向；
3. 正式替换前不更新 Code JSON、Web 实现或新增环境类型卡片；
4. 后续若用户确认草案，再执行正式 specs 替换、文件重命名、引用更新、Code / Web 校验调整。

本轮草案关键调整：

1. 04.02 采用 `LDVH能力资产与落地保障规范`，合并能力资产与落地保障，不再单独拆出 LDVH 能力资产规范；
2. 04.03 采用 `环境入口适配与部署规范`，一体化承接环境入口、薄引用、部署步骤、写入权限、实时检查、成功条件、失败处理、降级方式和证据输出；
3. 04.04 保留个人环境特别要求定位，强调其约束 04.03 的部署和实时检查，但不记录长期安装、启用或验证状态；
4. 草案统一使用“LDVH 能力资产 / 环境入口 / 薄引用 / 环境适配 / 实时检查 / 个人环境特别要求”术语。

## 23. 04 正式重构与同步执行记录

用户确认目录列表可以更新、术语可以增加，并要求先由子 Agent 审核当前问题后再行动。本轮已按该要求执行。

已完成的正式化动作：

1. 将 04 系列正式 specs 收敛为 `04`、`04.01`、`04.02`、`04.03`、`04.04` 五份文档；
2. `04.02-LDVH能力资产与落地保障规范.md` 成为 LDVH 能力资产与落地保障的正式承载位置；
3. `04.03-环境入口适配与部署规范.md` 成为环境入口、薄入口、部署步骤、适配检查和适配降级的正式承载位置；
4. `04.04-个人环境特别要求规范.md` 成为个人或当前执行环境特殊约束的正式承载位置；
5. 旧 `04.02-LDVH能力保障规范.md`、`04.03-环境适配规范.md`、`04.04-环境适配措施实践.md`、`04.05-个人环境特别要求规范.md` 已不再作为正式文件名；
6. 目录、术语、03 系列、Rules 入口、Code 检查、测试、Web 规范和 Dashboard 文档已同步到新文件名和新术语。

当前术语基线：

1. LDVH 自己定义和维护的是 LDVH 能力资产；
2. AI 开发环境提供的是环境入口；
3. 环境入口中只应放薄入口或薄引用；
4. 用户本地实例状态不写入长期 specs，只来自当前适配检查输出；
5. “部署入口”“固定部署入口资产”“入口资产”等此前阶段性表述只作为历史上下文，不再作为后续正式术语依据。

## 24. 04 子文档收敛为 3 个文件记录

用户进一步确认：个人设置的目标是让 AI 在创建或读取 Rules 时直接看见并执行，不需要新增独立事实源；04.04 不应作为独立规范文件，04 系列子文档保持 3 个文件。

本轮执行方向：

1. 删除正式 `specs/04.04-个人环境特别要求规范.md`；
2. 将个人环境特别要求的写入规则、字段、约束类型、优先级和 Human Gate 并入 `specs/04.03-环境入口适配与部署规范.md`；
3. 04.03 同时负责环境入口适配、部署、适配检查、适配降级和个人环境特别要求写入 Rules 入口；
4. 具体个人设置应写入对应 Rules 入口的“个人环境特别要求”段落，不新增 `ldvh-base/` 或其它中间事实源；
5. 04 系列正式子文档收敛为 `04.01-规范落地声明规范.md`、`04.02-LDVH能力资产与落地保障规范.md`、`04.03-环境入口适配与部署规范.md`。

## 25. 环境适配调研与快速输入功能不建设记录

用户确认：关于在不同平台快速输入“按你计划执行”的问题，应先联网调研；调研后决定不建设该功能，只把认知写入 docs 参考文档，作为“环境适配调研”的一项内容，后续还可以追加其它环境适配调研项。

已完成正式参考文档：

1. 新增 `docs/studies/20-环境适配调研.md`；
2. 文档标题为“环境适配调研”；
3. 定位为环境适配相关问题的参考调研汇总，不直接构成强制规则；
4. 第一项内容是“快速输入‘按你计划执行’的平台适配调研”；
5. 本轮只覆盖 Trae Work CN、Codex App，以及 macOS / Windows 系统层输入辅助边界。

本轮联网调研结论：

1. Trae Work CN 官方 Commands 机制可封装常用 Prompt，并通过 `/` 或命令图标显式触发，适合作为“按你计划执行”的候选快捷触发方式；
2. Codex App 未找到官方支持用户自定义 slash command / 自定义 Commands 的明确证据；搜索到的 slash commands 多为内置会话管理指令，不得据此声明 Codex 支持用户自定义固定 Prompt 快捷命令；
3. macOS 官方支持自定义短语 / 文本替换，可把短码替换为完整短语，但这是系统输入辅助能力，不是 Trae 或 Codex 的环境入口能力，并且需要在具体 App 输入框实测；
4. Windows 未找到 Microsoft 官方明确说明系统原生提供与 macOS 自定义短语等价的通用文本替换能力；只能保留输入法短语、AutoHotkey、第三方文本扩展工具等候选方向，不声明原生支持；
5. 不使用 Codex Hook 自动改写用户输入，除非未来有明确官方证据、风险评估和 Human Gate；当前不作为默认方案。

本轮决策：

1. 不建设 LDVH 快速输入“按你计划执行”的功能；
2. 不写入 `specs/04.03-环境入口适配与部署规范.md`；
3. 不创建 Trae Command；
4. 不创建 Codex Hook 或 Codex 自定义命令配置；
5. 不把 macOS 文本替换或 Windows 第三方工具升级为 LDVH 通用规则；
6. 仅把调研认知保留在 `docs/studies/20-环境适配调研.md`，供后续评估。

后续若继续扩展该参考文档，可追加：Trae Work CN 用户级 Rules 入口具体路径、Codex App 用户级 hooks / config 当前版本行为、Trae 全局 Skill 部署与加载实测、Codex 压缩前后 Hook 与备忘恢复、不同平台环境入口 Human Gate 风险分级等内容。

## 26. 环境入口与薄引用术语修正记录

用户进一步确认：环境入口不单单是 Rules，Skill、Agent、Hook、Command、MCP、instructions、配置文件、用户级目录、项目级目录、页面或自动触发机制等都可能有各自环境入口。此前把“环境薄入口”“薄入口文件”等说法混用，会把“环境提供的入口”和“LDVH 采用的接入措施”混在一起。

本轮正式修正方向：

1. 术语权威先修 `specs/02-术语规范.md`；
2. “环境入口”定义为 AI 开发环境提供的读取、加载、调用、触发、配置、展示或执行某类环境能力的位置或机制；
3. “薄引用”定义为 LDVH 接入环境入口时采用的最小引用措施；
4. “薄引用”不是环境入口本体；环境入口是平台提供的位置或机制，薄引用是 LDVH 写入、配置或安装到该入口中的接入措施；
5. “环境入口名称”是环境中某个入口的名称，不应写成“环境薄入口名称”；
6. 环境规则入口文件不是 LDVH Rules 资产；它只是某个环境中的入口。LDVH 采用薄引用的方式，在该环境入口中引入 LDVH Rules 资产文件；
7. LDVH Rules 资产当前仍是 `rules/LDVH-AI-ENTRY.md`，不改名；
8. Rules、Skill、Agent、Hook 等 LDVH 能力资产都可能需要接入各自环境入口，不能把 Rules 环境入口误认为全部环境入口。

本轮同步修改范围：

1. `specs/02-术语规范.md`：修正环境入口、环境适配、适配措施、工作区级入口、项目级入口、个人环境特别要求、薄引用等定义；
2. `specs/04.03-环境入口适配与部署规范.md`：将“薄入口”改为“薄引用”，将个人环境特别要求写入对象从 Rules 入口扩展为对应环境入口；
3. `rules/LDVH-AI-ENTRY.md`：补充环境入口不是 LDVH Rules 资产，环境入口通过薄引用指向 LDVH AI 统一入口 Rules 资产；
4. `specs/04-规范落地与环境适配基础规范.md`、`specs/04.01-规范落地声明规范.md`、`specs/04.02-LDVH能力资产与落地保障规范.md`、`specs/01-目录说明.md`、`specs/03.01-规范文档规范.md`、`specs/05-工作模型基础规范.md`、`specs/06-工作流程基础规范.md` 同步调整相关表述。

后续再讨论实际写入 Trae CN / Codex App 环境入口时，应先基于上述术语：先识别环境入口，再选择薄引用、配置、安装、软链、受控复制或人工降级等适配措施。Trae CN 用户级 Rules 入口路径由用户确认可采用 `C:\Users\<user>\.trae-cn\user_rules.md`；实际写入前还需要按当前环境识别 CN / 国际版。

## 9. 后续更新规则

本文件用于临时保持上下文。

后续如发生以下事项，应更新本文：

1. 用户确认新的方向、边界或优先级；
2. 完成新的审查；
3. 修改 specs、Code、Web、ADR、Task 或适配措施；
4. 发现新的 P0 / P1 级缺口；
5. 改变 04 系列重构顺序；
6. Human Gate 通过、暂停或拒绝某项关键动作；
7. 上下文恢复需要新增当前状态摘要。

更新本文不得替代正式事实源回写。稳定结论仍必须进入对应正式位置。
