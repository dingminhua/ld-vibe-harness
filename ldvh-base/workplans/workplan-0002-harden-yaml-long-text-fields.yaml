id: workplan-0002
type: workplan
title: 固化 YAML 长文本字段安全写入规则
status: closed
created: '2026-06-18T07:40:04'
updated: '2026-06-18T07:40:04'
workarea: workarea-0001
priority: P2
description: 由历史 Task task-0002 迁移生成的任务计划。计划目标以关联 Task 的 description 和 acceptance
  为准，避免复制多个权威事实。
success_criteria: '- [x] 关联任务 task-0002 满足其 acceptance、verification 和 closure_evidence。'
source: |-
  Legacy TaskPlan migration from taskplan-0002; source path: ldvh-base/taskplans/taskplan-0002-harden-yaml-long-text-fields.yaml
  Human Gate confirmed by dmh2002: User said 继续吧 after TaskPlan-to-WorkPlan migration preview on 2026-06-18; write WorkPlan facts and WorkArea backlinks only, preserve legacy TaskPlan/Task facts.
orchestration:
  mode: single
  execution_items:
  - id: task-0002
    title: 固化 YAML 长文本字段安全写入规则
    role: legacy-task
    mode: single
    input_refs:
    - task-0002
    expected_output: |
      - [ ] ldvh-intake、ldvh-close 或相关写入说明明确提示长文本字段优先使用 YAML 块标量
      - [ ] Fact Validator 或测试覆盖含冒号长文本的合法样例
      - [ ] 相关验证命令通过
    status: done
    result_summary: |-
      verification: python3 tools/check_fact_model.py ldvh-base/intents/ ldvh-base/tasks/ ldvh-base/evidence/ && python3 -m pytest tests/tools/test_check_fact_model.py
      ## 验证结果

      Fact Validator files=5 errors=0 warnings=0；test_check_fact_model.py 13 passed；tests/tools 98 passed。

      ## 结论

      验证通过。

      closure_evidence: ev-0002 记录 task-0002 验证通过：ldvh-intake/ldvh-close 已加入 YAML 长文本块标量写入要求；Fact Validator 已增强 YAML_PARSE_ERROR 提示；测试已覆盖含冒号长文本块标量合法样例。
      验证结果：Fact Validator files=5 errors=0 warnings=0；test_check_fact_model.py 13 passed；tests/tools 98 passed。
      ## 验证结果

      ldvh-intake/ldvh-close 已加入 YAML 长文本块标量写入要求；Fact Validator 已增强提示；测试覆盖含冒号长文本合法样例；Fact Validator files=5 errors=0 warnings=0。

      ## 结论

      任务完成，人工确认关闭。
    evidence_refs: []
    blocking_reason: null
  review:
    controller_self_check: true
    specialist_review:
      required: false
      role: null
      expected_output: null
    human_closure_review: true
verification_evidence: |
  ## 验证计划

  历史证据格式规范化：检查原 验证证据 是否保留可复查的验证对象、结果或关闭判断；本次仅补齐四段式结构，不新增或改变历史事实语义。

  ## 验证命令

  历史记录未单独记录完整的验证命令；如原字段包含命令、脚本、检查入口或人工检查动作，保留在下方原始证据摘录中。

  ## 验证结果

  原始证据摘录：

  原文标题：验证结果

  历史任务计划迁移聚合证据：
  - task-0002: ev-0002 记录 task-0002 验证通过：ldvh-intake/ldvh-close 已加入 YAML 长文本块标量写入要求；Fact Validator 已增强 YAML_PARSE_ERROR 提示；测试已覆盖含冒号长文本块标量合法样例。

  ## 结论

  本次仅完成证据字段四段式格式规范化；对象状态、关闭判断、验证结果和历史事实语义以原始证据摘录为准，未额外伪造历史验证过程。
closure_evidence: |
  ## 验证计划

  历史证据格式规范化：检查原 关闭证据 是否保留可复查的验证对象、结果或关闭判断；本次仅补齐四段式结构，不新增或改变历史事实语义。

  ## 验证命令

  历史记录未单独记录完整的验证命令；如原字段包含命令、脚本、检查入口或人工检查动作，保留在下方原始证据摘录中。

  ## 验证结果

  原始证据摘录：

  原文标题：验证结果

  历史任务计划迁移聚合证据：
  - task-0002: ev-0002 记录 task-0002 验证通过：ldvh-intake/ldvh-close 已加入 YAML 长文本块标量写入要求；Fact Validator 已增强 YAML_PARSE_ERROR 提示；测试已覆盖含冒号长文本块标量合法样例。

  ## 结论

  本次仅完成证据字段四段式格式规范化；对象状态、关闭判断、验证结果和历史事实语义以原始证据摘录为准，未额外伪造历史验证过程。
review_requested_at: '2026-06-18T07:40:04'
closed_at: '2026-06-18T07:40:04'
related_docs: []
related_adrs: []
related_sparks: []
related_pitfalls: []
related_workplans: []
