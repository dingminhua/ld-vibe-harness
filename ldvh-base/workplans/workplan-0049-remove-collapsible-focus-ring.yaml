id: workplan-0049
type: workplan
title: 移除可折叠字段的白色focus ring动画
status: closed
created: '2026-06-06T00:30:38.636904'
updated: '2026-06-06T00:32:24.729918'
workarea: workarea-0006
priority: P2
description: 由历史 Task task-0049 迁移生成的任务计划。计划目标以关联 Task 的 description 和 acceptance
  为准，避免复制多个权威事实。
success_criteria: '- [x] 关联任务 task-0049 满足其 acceptance、verification 和 closure_evidence。'
source: |-
  Legacy TaskPlan migration from taskplan-0049; source path: ldvh-base/taskplans/taskplan-0049-remove-collapsible-focus-ring.yaml
  Human Gate confirmed by dmh2002: User said 继续吧 after TaskPlan-to-WorkPlan migration preview on 2026-06-18; write WorkPlan facts and WorkArea backlinks only, preserve legacy TaskPlan/Task facts.
orchestration:
  mode: single
  execution_items:
  - id: task-0049
    title: 移除可折叠字段的白色focus ring动画
    role: legacy-task
    mode: single
    input_refs:
    - task-0049
    expected_output: |
      - [x] 页面加载/路由切换时不再出现白色圆形轮廓动画
      - [x] ReadingPanel 面板开关的 margin 过渡正常工作
    status: done
    result_summary: |-
      verification: ## 验证结果

      历史补齐：该 Task 在早期关闭时未单独维护 verification 字段；本次状态边界收敛补齐该字段。具体验证结果和证据见 closure_evidence。

      ## 结论

      历史关闭记录有效，当前补齐 verification 字段以满足关闭边界。

      closure_evidence: 根因定位：通过 Playwright 连续帧截图 + CSS dump 确认，白色圆圈动画由 ObjectDetail.tsx
      主容器（第198行）的 `transition-all duration-300` 导致。Chrome 对该元素设置默认 border-radius=8251.97px，
      transition-all 使其在页面加载时从大值过渡到实际值（0px），产生"圆→方框"轮廓动画。

      修复措施：
      1. ObjectDetail.tsx 第198行：`transition-all` → `transition-[margin]`（仅过渡 margin）+ 添加 `rounded-none`
      2. index.css：全局清除 focus ring、tap-highlight 和浏览器默认按钮样式

      验证结果：Playwright 帧截图对比确认修复后无圆形轮廓；ReadingPanel margin 过渡正常。
      经验已沉淀至 pitfall-0007。

      ## 验证结果

      根因确认为 transition-all 导致 border-radius 动画，修复后 Playwright 帧截图对比确认无圆形轮廓，ReadingPanel margin 过渡正常。

      ## 结论

      任务完成，人工确认关闭。
    evidence_refs:
    - web/src/pages/ObjectDetail.tsx
    - web/src/index.css
    blocking_reason: null
  review:
    controller_self_check: true
    specialist_review:
      required: false
      role: null
      expected_output: null
    human_closure_review: true
verification_evidence: |
  ## 验证计划

  历史证据格式规范化：检查原 验证证据 是否保留可复查的验证对象、结果或关闭判断；本次仅补齐四段式结构，不新增或改变历史事实语义。

  ## 验证命令

  历史记录未单独记录完整的验证命令；如原字段包含命令、脚本、检查入口或人工检查动作，保留在下方原始证据摘录中。

  ## 验证结果

  原始证据摘录：

  原文标题：验证结果

  历史任务计划迁移聚合证据：
  - task-0049: 根因定位：通过 Playwright 连续帧截图 + CSS dump 确认，白色圆圈动画由 ObjectDetail.tsx

  ## 结论

  本次仅完成证据字段四段式格式规范化；对象状态、关闭判断、验证结果和历史事实语义以原始证据摘录为准，未额外伪造历史验证过程。
closure_evidence: |
  ## 验证计划

  历史证据格式规范化：检查原 关闭证据 是否保留可复查的验证对象、结果或关闭判断；本次仅补齐四段式结构，不新增或改变历史事实语义。

  ## 验证命令

  历史记录未单独记录完整的验证命令；如原字段包含命令、脚本、检查入口或人工检查动作，保留在下方原始证据摘录中。

  ## 验证结果

  原始证据摘录：

  原文标题：验证结果

  历史任务计划迁移聚合证据：
  - task-0049: 根因定位：通过 Playwright 连续帧截图 + CSS dump 确认，白色圆圈动画由 ObjectDetail.tsx

  ## 结论

  本次仅完成证据字段四段式格式规范化；对象状态、关闭判断、验证结果和历史事实语义以原始证据摘录为准，未额外伪造历史验证过程。
review_requested_at: '2026-06-06T02:10:00'
closed_at: '2026-06-06T02:10:00'
related_docs: []
related_adrs: []
related_memos: []
related_pitfalls:
- pitfall-0007
related_workplans: []
