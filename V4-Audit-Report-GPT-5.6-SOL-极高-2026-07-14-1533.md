# V4 架构方向独立审计报告

- 审计者：GPT-5.6 SOL（推理强度：极高）
- 审计时间：2026-07-14 15:33:33 CST（Asia/Shanghai）
- 审计项目：`/Users/dmh2002/poker_hud_projects/ld-vibe-harness-v4`
- 审计性质：独立、以 `specs/00-理念与构成.md` 为最高判断基线的架构方向审计
- 审计方式：先形成独立结论，再阅读既有外部审计与处置记录

## 1. 总判定

**已发生结构性偏航。**

这里的“偏航”不是指 `specs/00-理念与构成.md` 的方向错误。相反，00 对 AI 第一服务对象、V1–V8、Human Gate、Stop Conditions、Code/Web 边界的定义清楚而且值得保留。偏航发生在当前建设路径：

- 规范闭合、字段治理、审核证明和 WorkCase 生命周期，已经优先于真实 AI 消费路径。
- V4 尚不能可靠地用自身能力管理自身工作，却准备继续建设行动模板。
- WorkCase 将 V3 orchestration 的相当一部分改写为声明式字段、阶段、审核和 Human Gate；形态变轻了一些，运行负担并未得到实证下降。
- Helper 的成功响应主要在输出资格缺口和审计元数据，没有实现 00 所说的“少读、少猜、少漂移”。
- Code 内部质量较强，但部署、配置、更新、跨会话接续、Web V4 读取和端到端 dogfood 尚未闭合。

因此，不应推倒 V4；应立即停止继续扩张规范面，转入“压缩消费成本 + 打通最小纵向闭环”的校正阶段。

## 2. 从 00 独立提取的判断基线

在阅读其他设计和既有审计前，先完整阅读了 00。其核心并不是“建立一套可审计规范”，而是：

- 第一服务对象是 AI 执行者；支持必须可消费、可验证、可接续。见 `specs/00-理念与构成.md:63`。
- Helper 是统一入口，但未命中规则时，AI 普通求解路径必须保持自主。见 `specs/00-理念与构成.md:93`。
- 事实对象的目的，是让 AI “少读、少猜、少漂移”。见 `specs/00-理念与构成.md:113`。
- 规范、事实、行动模板、Code、Web 是分工组件；Code 负责可确定机械行为，Web 服务 Human。见 `specs/00-理念与构成.md:127`。
- 新机制只有给 AI 带来可辨认的净收益，才有纳入基础。见 `specs/00-理念与构成.md:143`。
- Human Gate 适用于上层目标、重大选择、授权和验收，不是所有普通工作项的固定流程。见 `specs/00-理念与构成.md:185`。
- Stop Conditions 应限制在最小受影响范围；未受影响的普通路径继续自主推进。见 `specs/00-理念与构成.md:226`。

当前 V4 最大的问题，是这些原则写在 00 中，但没有成为后续机制的硬性成本约束。

## 3. 00 核心目标与 V1–V8 证据矩阵

| 目标 | 当前判定 | 支持证据 | 反向证据 |
|---|---|---|---|
| AI 第一服务对象 | **实现层偏离** | Helper 单入口、Git 事实源、确定性 Code 都面向 AI | 实际响应大量服务资格证明、字段审计和流程闭合；缺少真实 AI 使用成本验证 |
| V1 快速定位 | **部分满足** | 有 7 个公开操作、来源引用、候选列表 | 候选操作刻意不使用任务语义；实测默认候选响应约 46 KB、33 个 gaps，AI 仍需二次筛选。见 `specs/01-规范模型基础规范.md:323-330` |
| V2 充分理解 | **未满足** | 有 L0–L4、投影和关系字段 | 实测读取 00 的一个 L3 小节退化为整个 265 行 L4、约 36 KB；规则允许无法机械证明时直接扩至 L4。见 `specs/01-规范模型基础规范.md:463` |
| V3 边界识别 | **部分满足，基础较强** | Working Tree、来源、AI/Code/Human 边界写得较清楚 | WorkCase 将独立审核和两次 Human Gate 普遍化，模糊了“普通路径自主”与“重大决策暂停”的边界 |
| V4 稳定推进 | **较弱** | Fact create/read 的原子性、确定性错误处理较好 | 没有 update/CAS 状态推进闭环；当前外部配置不能解析本 V4；没有模板、环境能力和跨会话 dogfood |
| V5 据实判断 | **当前最强项** | Working Tree 优先、source refs、能力状态区分、Code 不解释自然语言 | 448 tests 仅有文档声明，本审计因只读约束未重跑；没有 CI；路线图中外部配置状态存在相互矛盾陈述 |
| V6 工作接续 | **设计充分、产品未验证** | WorkCase 有责任、摘要、恢复字段；F0–F4 有覆盖概念 | 没有真实 V4 WorkCase 实例、更新能力和跨模型恢复实验；每次恢复读取全部 active ADR 和 open/blocked WorkCase 会随项目线性增重。见 `specs/05-事实模型基础规范.md:248` |
| V7 清晰沟通 | **较弱** | 00 的 Human Gate 信息要求合理 | 当前 Web 仍是 V3 数据模型；WorkCase 又将 Human 参与扩张成固定生命周期，产生无必要交还 |
| V8 持续积累 | **部分满足** | Spark/ADR/Pitfall/Study 分型和关系方向合理 | 当前仓库没有实际 V4 `facts/**` 对象；主要通过 fixtures 证明模型，而非用真实项目事实证明复用价值 |

总体上，V3、V5 的底层意图比较扎实；V1、V2 的真实消费表现不合格；V4、V6、V7、V8 仍主要停留在“模型可表达”，没有达到“AI 可低成本使用”。

## 4. 真正服务 AI 与主要服务规范维护/审计的比例

这不是 LOC 精确统计，而是基于公开能力、默认响应、近期建设内容和真实使用路径的定性判断：

- **真正直接服务 AI：约三至四成**
- **主要服务规范维护、结构闭合和审核证明：约六至七成**

直接服务 AI 的部分主要包括：

- Working Tree 与 Git 事实源。
- 事实对象的确定性 create/read、原子创建、不覆盖和来源回指。
- Helper 单入口及显式能力集合。
- L0–L4、coverage、cursor、fingerprint 等渐进披露基础。
- Spark/ADR/Pitfall/Study 的事实分型。
- Code 不越权解释自然语言的原则。

偏规范维护/审计的证据包括：

- 当前统一登记达到 8 个结构、81 个字段；新字段、类型和成员需要逐项准入审计与独立审核。见 `specs/attachments/05.Att.01-事实对象统一字段登记.md:195-225`。
- Helper 响应强制 14 个顶层字段，包括大量空集合、空值和通用 `follow_up`。见 `specs/attachments/04.Att.01-Helper CLI 请求与响应字段表.md:66-87`。
- 成功读取规则时仍附带所有无法证明的全局资格条件；实测普通候选调用产生 33 个 gaps。
- WorkCase 每项工作都要求计划、独立创建审核、Human 计划确认、控制者检查、独立结果审核和 Human 关闭。见 `specs/21-WorkCase-工作项.md:217-242`。
- 最近 15 个提交约增加 13,136 行、删除 121 行，主要集中在 Code、测试、规范和封闭记录；没有 Web V4 适配。这只能作为方向信号，但与“体系扩张快于消费闭环”一致。
- 总纲已经明确部署、CI、配置写入、update/status、环境能力、Web V4 和 E2E 都未完成，却仍把下一阶段放在第一个行动模板。见 `docs/v4-architecture/V4-工作推进总纲.md:292-310`。

## 5. 职责划分审计

### 5.1 规范模型

职责定义基本正确：提供可定位、可回指、层次化披露的规范来源。

当前问题是规范模型已开始承担“证明所有潜在适用条件均被考虑”的职责。它优化了审计覆盖率，却没有优化 AI 当次任务的相关性与输入规模。

### 5.2 事实模型

Git、Working Tree、来源引用、对象类型和关系方向基本正确。

主要偏差发生在 WorkCase：长期责任事实与计划审批、执行阶段、审核反馈、Human 关闭被混入同一个基础事实类型，导致事实模型承担流程控制职责。

### 5.3 行动模板

基础规范中“不设全局固定阶段”“内部自动消费”“未命中时普通求解继续自主”等边界是正确的。

但当前第一个模板准备围绕尚未完成实际纵向使用验证的事实创建路径展开。此时模板更可能包装未验证的 CRUD/审核流程，而不是沉淀已经重复出现的有效行动结构。

### 5.4 Code

Code 不解释自然语言、只执行确定性机械行为的边界正确。原子创建、路径治理、managed fields 和读取回验均值得保留。

但 Code 当前没有提供足够的机械压缩：能力契约发现与可用性检查耦合、成功响应过宽、精确小节读取退化、全局 gaps 无差别附加。AI 因而承担了 Code 本可可靠减少的筛选和去噪工作。

### 5.5 Web

Web 服务 Human、默认只读、写入经 Code 的目标正确。

当前实现仍是 V3 schema，并直接写 V3 Spark，因而既不能可靠呈现 V4 状态，也绕开了 V4 Code 边界。

## 6. 分级问题

## P0：方向阻断项

### P0-1 WorkCase 恢复了普遍化 orchestration

**文件与行号**

- `specs/00-理念与构成.md:93-99,143-160`
- `specs/21-WorkCase-工作项.md:197-242,360-369`

**问题**

- 新 WorkCase 需要先完成计划和独立创建审核，并从 `human_plan_confirming` 开始。
- 定义了七个强制阶段。
- 创建和关闭都有独立审核及 Human Gate。
- 这与普通路径保持自主、只有净收益机制才应纳入的原则冲突。

**失败场景**

一个普通但跨会话的重构任务，在没有 Subagent 或 Human 即时可用时，无法正常创建、开始或关闭 WorkCase；计划变化还会触发版本级联和复审。

**对 AI 的实际影响**

增加上下文、等待、重复证明和交接次数；把 WorkCase 从“恢复工作所需的事实”变成“工作必须服从的控制流程”。

**最小修正方向**

- 基础 WorkCase 只保留可独立关闭的责任、目标、范围、成功条件、当前状态、摘要和恢复入口。
- `work_items` 可选。
- plan review、result review、Human approval 改为风险或来源触发的可选证据，不再是每个 WorkCase 的固定组成。
- 阶段流程移入可选行动模板或项目政策，不作为事实类型的普遍语义。

### P0-2 当前建设顺序优先扩展模板，而非证明 AI 闭环

**文件与行号**

- `docs/v4-architecture/V4-工作推进总纲.md:90-105,292-310`

**问题**

阶段 7 部署未闭合，8 环境能力未实现，9 Web 仅 V3 基线，10 E2E 未验证；update/status、多对象操作、外部配置治理和 CI 也尚未完成，但当前下一步仍是阶段 6 的首个事实对象行动模板。

**失败场景**

先给一个尚未可部署、不可更新、不能跨会话自举的能力包裹模板，后续为了适应真实使用再增加适配层和字段。

**对 AI 的实际影响**

AI 继续面对“规范完整但没有一条可实际闭合的使用路径”；新增模板只会扩大要读和要维护的表面。

**最小修正方向**

暂停阶段 6 和新字段准入，先完成一个“发现规则 → 创建/读取/更新事实 → 中断恢复 → 验证”的最小纵向闭环。

## P1：高优先级产品价值问题

### P1-1 渐进式披露在真实输出上失效

**文件与行号**

- `specs/01-规范模型基础规范.md:323-330,389,463`
- `code/ldvh/helper/specification_candidates.py:315-335`
- `code/ldvh/helper/specification_content.py:382-390`
- `docs/v4-architecture/V4-工作推进总纲.md:176`

**失败场景**

AI 只是想确认一个局部要求，却必须解析全文及大量与当次任务无关的资格缺口。

**实际检查**

- 默认规则候选：18 个 L0 项、33 个 gaps、约 46.5 KB。
- 读取 00 中一个 L3 小节：返回整个 265 行文档、约 36.3 KB。

**对 AI 的实际影响**

V1/V2 由“快速定位和不过载理解”退化为“统一格式的全文搜索”。

**最小修正方向**

成功响应只保留操作局部必要 gaps；精确返回命中小节及依赖引用；完整审计信息改为显式 diagnostic 模式。

### P1-2 事实与接续能力没有完成自举 dogfood

**文件与行号**

- `docs/v4-architecture/V4-工作推进总纲.md:174-178,296-302`
- `README.md:23-25`

**问题与失败场景**

- 当前 Git 跟踪文件中没有实际 V4 `facts/**` 对象，只有 V3 archive。
- 实测 `resolve-governance-scope` 外层调用成功，但结果是 `config_status=invalid`、`scope_status=scope_unknown`，旧登记路径不是 Git worktree。
- 尚无 update/status 能力。

V4 尝试用自身能力登记或恢复当前 V4 工作时，连管辖范围都无法确定；即使创建对象，也不能可靠推进状态。

**对 AI 的实际影响**

V6 只是字段可表达，尚不是可使用的工作接续能力。

**最小修正方向**

提供受支持的开发安装与配置路径；在 V4 本身创建一个真实事实对象；先补单对象 CAS/update，再做一次真实中断恢复。

### P1-3 统一字段治理和 F1 默认读取会随项目规模持续增重

**文件与行号**

- `specs/05-事实模型基础规范.md:160-194,248`
- `specs/attachments/05.Att.01-事实对象统一字段登记.md:195-225`
- `specs/21-WorkCase-工作项.md:312-323`

**失败场景**

当项目累积几十个 WorkCase 和数百个 ADR 后，每次恢复都先读取全体摘要；新增一个类型局部字段也要走全局准入和独立审核。

**对 AI 的实际影响**

项目积累越多，AI 启动成本越高，正好与 V6/V8 的目标相反。

**最小修正方向**

- 全局登记只治理共享身份字段、跨类型字段和破坏性变更。
- 类型局部字段保留在类型规范内。
- F1 改为有界索引、分页和当前目标相关集合；全体覆盖状态可声明，但不要求每次全文消费。

### P1-4 当前 Web 会误导 Human，并可能写出 V3 事实

**文件与行号**

- `web/api/services/facts.ts:17-30,115-128`
- `code/ldvh/facts/contracts.py:38-77`
- `web/api/routes/sparks.ts:17-50,151-174`
- `docs/web/V4-Web-浏览器表现基线.md:17-23`

**失败场景**

Code 创建了合法 V4 对象，Web 看不到；Human 又可能通过 Web 创建 V3 Spark。

**对 AI 的实际影响**

V7 沟通界面提供错误状态；Human 基于错误展示做决定，并可能产生分叉事实源。

**最小修正方向**

立即禁用旧 Spark 写入；先交付最小 V4 只读列表/详情，复用 Code 的 schema 或共享 DTO；用一个真实 V4 fixture 做端到端读取测试。

### P1-5 审核机制在证明自身一致，而不是证明净 AI 价值

**文件与行号**

- `docs/v4-architecture/V4-WorkCase设计外部审计交接包.md:65-75,134`
- `docs/v4-architecture/V4-WorkCase专项外部审计处置记录.md:7-20,49-59`

**失败场景**

审核问题被定义为“是否忠实实现强制流程”，而不是“强制流程是否比简单责任记录更降低 AI 成本”。

**对 AI 的实际影响**

可以形成严密的自我证明闭环，却无法发现产品方向已经偏离 00。

**最小修正方向**

架构 Gate 必须包含真实 AI 场景和消费成本对照；字段闭合、测试通过只能证明内部一致，不能单独证明 V1–V8 净收益。

## P2：应尽快修正的问题

### P2-1 能力契约发现存在循环依赖

**文件与行号**

- `docs/code/README.md:55-62`
- `code/ldvh/helper/service.py:273-355`

文档称 `capabilities <operation>` 可显示请求契约，但实现会立即按该操作解析必填输入。实测无输入调用 `capabilities read-specification-content` 和 `capabilities find-fact-object-candidates` 均返回 `invalid_request`。

**失败场景与 AI 影响**

AI 必须先知道契约，才能请求契约。

**最小修正方向**

分离 `describe operation` 与 `check availability for request`，前者不要求业务输入。

### P2-2 外部环境状态被写成长期路线图事实

**文件与行号**

- `docs/v4-architecture/V4-工作推进总纲.md:174-178`
- `specs/03-事实源与信息溯源规范.md:142`

总纲一处承认旧路径未迁移，另一处又称 V4 worktree 与 linked scope 已解析为同一项目；实测后者当前不成立。

**失败场景与 AI 影响**

AI 信任路线图中的瞬时外部状态，作出错误的能力可用性判断。

**最小修正方向**

路线图只记录带时间和环境范围的观察；当前配置状态必须由实时 Helper 返回，不进入长期控制面结论。

### P2-3 测试证据尚不能支持集成完成声明

**文件与行号**

- `docs/v4-architecture/V4-工作推进总纲.md:182-184,297,310`

总纲报告 448 tests 和 Ruff 通过，但 CI 尚未建立。这不说明测试无效；它说明当前证据只支持“本地实现曾通过”，不支持跨环境集成或产品价值。

**失败场景与 AI 影响**

后续 AI 将测试数量误当作端到端能力和产品方向已验证的证据。

**最小修正方向**

建立最小 CI，记录 commit-bound 结果；另设真实 AI 使用验收，不把单元测试数量作为 V1–V8 的代理。

### P2-4 Helper 成功信封过宽

**文件与行号**

- `specs/attachments/04.Att.01-Helper CLI 请求与响应字段表.md:66-87`
- `code/ldvh/helper/responses.py:73-103`

14 个顶层字段被规定为始终存在，实现也始终输出空数组、空值和通用 follow-up。

**失败场景与 AI 影响**

高频调用不断消耗上下文去解析无信息字段，真正结果被审计外壳淹没。

**最小修正方向**

保留小型稳定核心；空字段省略或放进 `diagnostics`；详细资格、来源和后续建议按需展开。

## P3：低风险但应避免继续固化

### P3-1 关系校验存在事实类型耦合

**文件与行号**

- `code/ldvh/facts/relations.py:252-262`

`_valid_supersedes_source` 将合法来源状态硬编码为 `active`。当前调用类型受限，因此尚不是现实故障；但函数表面上较通用，未来扩展到不同状态模型时容易产生隐蔽错误。

**失败场景与 AI 影响**

新增事实类型复用该校验时，合法关系可能被机械拒绝，AI 只能回头调查实现细节。

**最小修正方向**

将约束收窄为明确支持的事实类型，或把状态条件放入类型布局定义。

## 7. 最值得保留的设计

1. 00 的核心价值观和净收益门槛。
2. Working Tree 而非 HEAD 作为当前事实观察面。
3. Git 事实源、source refs、可回指依据及声明范围控制。
4. Code 不解释自然语言，只做确定性机械处理。
5. 事实创建的原子性、不覆盖、managed fields 和读取回验。
6. Spark、ADR、Pitfall、Study 的分型及关系方向。
7. F0–F4 的分层思想、coverage/cursor/fingerprint 等机制，但需缩小默认消费范围。
8. 行动模板基础规范中的正确边界：不设全局固定阶段、内部自动消费、未命中时普通求解继续自主。
9. WorkCase 中应保留：一个可独立关闭的责任、goal/scope/success criteria、摘要和恢复线索；不保存角色、模式、命令日志和重复 revision history。
10. Web 默认只读、直接服务 Human 的定位。

## 8. 最应删除、合并、简化或后移的内容

### 删除或退出默认路径

- 每个成功响应中的全局 qualification gaps。
- 空字段和通用 `follow_up` 填充。
- Web 的 V3 Spark 写入。
- 基础 WorkCase 中强制性的 review feedback、plan approval、result approval。

### 合并

- WorkCase 七阶段和双版本控制，合并成可选流程策略；对象并发控制交给 Code CAS。
- 字段准入表、独立审核表和封闭记录，合并为仅针对共享或破坏性变更的一份 change record。

### 简化

- Helper 成功响应。
- F1 默认读取集合。
- WorkCase 基础字段。
- 能力描述与可用性检查。
- 规则内容精确小节读取。

### 后移

- 第一个具体行动模板。
- 新事实类型和更多字段。
- 多模型审核制度扩张。
- 广泛 Web 功能恢复。
- 跨 clone allocator、完整模板注册体系等，直到最小纵向闭环证明存在重复需求。

## 9. 下一步与未来三个增量

### 9.1 立即动作

**冻结阶段 6、WorkCase 新字段和新增封闭记录。**

先定义三个真实 AI 场景，并记录：

- 调用次数；
- 返回字节或 token 量级；
- 读取文件数；
- 无产出判断次数；
- Human 等待次数；
- 中断后恢复所需信息和时间。

### 9.2 增量 1：打通可消费的 Helper 纵向切片

1. 独立的 operation describe。
2. 精简成功响应和 operation-local gaps。
3. 精确小节读取。
4. 可用的开发部署及 V4 管辖配置。
5. 用 V4 自身创建并读取一个真实 Spark/ADR/Pitfall。
6. 重启会话后通过 Helper 找回它。

这实际上应优先完成阶段 7 和阶段 10 的最小部分。

### 9.3 增量 2：简化 WorkCase，并实现可靠更新

- 把 WorkCase 缩为长期责任与恢复事实。
- 实现单对象 CAS/update/status。
- 用一个持续多会话的真实任务 dogfood。
- 强制发生一次上下文压缩或执行者切换。
- 与“一份普通 Git Markdown 交接记录”比较恢复成本，只保留能证明净收益的字段和 Gate。

### 9.4 增量 3：薄行动适配器 + 最小 V4 Web

- 先禁用旧写入。
- Web 只读展示真实 V4 facts、当前 gaps 和需要 Human 决定的事项。
- 写入统一经过 Code。
- 只有当多个真实场景反复出现同一种行动结构时，才重新启动阶段 6 的具体模板。

## 10. 与既有外部审计及 V3 的差异

本审计先形成上述结论，之后才读取旧审计报告的 Git 历史版本和处置记录。

旧报告的判定是“有条件通过、最高 P2”。它对局部字段一致性、状态转换和实现覆盖的检查有价值，也确实促成了一个 status/phase 冲突修复。但它：

- 接受了“独立审核 + 双 Human Gate”作为前置要求；
- 主要检查模型是否闭合；
- 将 update、环境、Web 等视为后续能力；
- 没有验证真实 Web 数据模型；
- 没有测量 AI 获取一个答案实际需要读取多少内容。

因此，它的局部结论与本审计并不直接矛盾；只是没有回答本次更上层的问题：**这套闭合模型是否给 AI 带来净收益。**

与 V3 相比，V4 的确删除了不少角色、模式、输入引用、空审核占位和显式 revision log，这是进步。但 WorkCase 仍保留并强化计划审核、阶段、结果审核、双 Human Gate 和版本失效。V3 的流程没有消失，而是被重新编码为事实结构。当前又没有真实 V4 WorkCase 实例证明实际对象更轻，所以不能认定已摆脱 V3 orchestration 膨胀。

## 11. 已读范围、实际检查与不确定性

### 11.1 完整阅读

- `specs/00-理念与构成.md`。
- `specs/01–08`。
- `specs/20–24`。
- `specs/attachments/01、02、04、05`。
- V4 工作推进总纲、字段治理、五类型归并、WorkCase 封闭、外部审计交接与处置、行动资产设计。
- README、Code README、Web README。
- 关键 Helper、facts、relations 实现。
- Web facts、Spark 路由、WorkCase 状态和页面相关代码。
- V3 WorkCase 规范、附件及三个实例。
- 最近 Git 历史和旧外部审计的 Git 历史版本。

### 11.2 实际运行的只读或无写入检查

- `.venv/bin/ruff check --no-cache .`：通过。
- `.venv/bin/ruff format --check --no-cache .`：91 个文件符合格式。
- `git diff --check`：通过。
- `npm run check`：通过。
- `npm run lint -- --no-cache`：11 errors、14 warnings，与总纲一致。
- 多个 Helper capabilities/call 实测。
- Git 状态、历史、变更统计、文件及测试静态清点。

### 11.3 未覆盖与不确定性

- 未运行 Pytest、Web API tests、build。它们会创建缓存、临时文件或构建产物，不符合本轮原始审计的严格只读约束。
- 因此没有独立确认“448 passed”，只确认该数字被文档多次声明、测试代码规模与之并不明显矛盾。
- 没有进行真实多会话/多模型恢复实验，因为当前配置和 update 路径未闭合。
- 没有审阅审计期间新出现的 `V4-Audit-Report-GLM5.2-2026-07-14-1524.md`。

## 12. 最终结语

V4 当前最重要的资产不是 81 个字段、448 个测试或多轮封闭记录，而是 00 中已经表达清楚的判断标准，以及 Code 中已经具备的确定性基础。

下一阶段不应继续问“模型还缺哪些字段和审核”，而应问：

> 一个刚进入项目、上下文有限、可能随时中断的 AI，能否用更少的读取、更少的猜测、更少的无产出判断，完成定位、理解、推进、验证和接续？

只有当这个问题通过真实路径得到肯定答案，V4 才能从“规范闭合”回到“产品闭环”。
