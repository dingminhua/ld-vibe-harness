# Web回归入口表

```yaml
v2_attachment:
  attachment_id: "08.Att.06"
  title: "Web回归入口表"
  status: "draft"
  authority: "not_active_until_parent_and_human_approved"
  parent_spec: "specs-v2/08-测试基础规范.md"
  canonical_path: "specs-v2/attachments/08.Att.06-Web回归入口表.md"
  purpose: "承载 08 授权的 LDVH Web 当前回归入口和优先级"
  migration_sources:
    - "specs/11-测试基础规范.md"
    - "specs/08-Web信息同步实现规范.md"
    - "web/docs/11-Web测试实现规范.md"
  code_consumption:
    - "web_regression_entry"
```

> 文件状态：本文当前位于 `specs-v2/`，尚未切换为 active；正式 Web 测试入口仍以 active `specs/11-测试基础规范.md`、active `specs/08-Web信息同步实现规范.md` 和当前 `web/docs/` 为准。本文是 `08-测试基础规范.md` 授权附件，必须随父规范经过 Human 单篇确认后才可生效。

## 1. 定位

本文承载 08 授权的 LDVH Web 当前回归入口和优先级。

## 2. 回归入口表

| 对象 | 回归入口 | 优先级 |
|---|---|---|
| Web API contract | `tests/web/api/` 或等价 API contract 检查 | 最高 |
| 高风险 API | `POST /api/sparks`、ProjectFiles、Validate、Objects 对应测试或等价验证 | 最高 |
| Web 暂停实施期间的回归线 | DTO、Spark 创建白名单、WorkCase orchestration 展示、提交记录展示、Confirm UI 边界、派生态势原因语义、测试/构建/API contract 入口 | 最高 |
| 页面空态、错误态、降级态、只读边界、来源呈现 | 页面测试、组件测试、人工降级检查 | 中 |
| Confirm UI、受控轻写入白名单 | API、组件和人工确认边界检查 | 高 |
| i18n、图标语义、Human-facing 文案 | 页面检查、截图说明或人工降级检查 | 中 |
| E2E、视觉回归、复杂浏览器流程 | Playwright 或等价浏览器流程 | 后置 |

## 3. 边界

Web v2 暂不实施时，也必须保留 DTO、API contract 或人工降级检查的回归线。Web 回归线不得反向定义事实模型、Code 输出、行动编排流程或事实源边界。

## 4. 待补齐事项

1. 05 单篇核对后，应同步 Web 回归入口和白名单范围；
2. Web 实现恢复推进后，应检查本文是否需要记录实际 npm script。
