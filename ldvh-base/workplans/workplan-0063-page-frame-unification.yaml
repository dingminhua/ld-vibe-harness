id: workplan-0063
type: workplan
title: 统一页面框架
status: closed
created: '2026-06-11T00:00:00.000000'
updated: '2026-06-11T18:45:15'
workarea: workarea-0012
priority: P2
description: 由历史 Task task-0063 迁移生成的任务计划。计划目标以关联 Task 的 description 和 acceptance
  为准，避免复制多个权威事实。
success_criteria: '- [x] 关联任务 task-0063 满足其 acceptance、verification 和 closure_evidence。'
source: |-
  Legacy TaskPlan migration from taskplan-0063; source path: ldvh-base/taskplans/taskplan-0063-page-frame-unification.yaml
  Human Gate confirmed by dmh2002: User said 继续吧 after TaskPlan-to-WorkPlan migration preview on 2026-06-18; write WorkPlan facts and WorkArea backlinks only, preserve legacy TaskPlan/Task facts.
orchestration:
  mode: single
  execution_items:
  - id: task-0063
    title: 统一页面框架
    role: legacy-task
    mode: single
    input_refs:
    - task-0063
    expected_output: |
      - [x] 所有主要页面头部结构统一为 `PageHeader` 或对象详情专用头部
      - [x] Dashboard/Workbench 支持态势信号条
      - [x] ObjectDetail 支持对象类型差异化阅读重点
      - [x] 移动端响应式布局完整
      - [x] npm run check 通过
      - [x] npm run build 通过
    status: done
    result_summary: |-
      verification: ## 验证结果

      - Dashboard、Workbench、Validate、Gate、Changelog、AttentionTest、ProjectFiles 均使用 `PageHeader`。
      - ObjectDetail 保留对象详情专用头部，用于承载对象类型、状态、路径复制和筛选返回语境。
      - Dashboard 已在校验错误时展示 `StatusBanner`。
      - Workbench 已在校验失败时展示 `StatusBanner` 并可跳转 Validate。
      - 浏览器验证 Gate、Workbench、Changelog 页面主流程正常，页面级宽度未超过视口。
      - `npm run check`、`npm run lint` 通过；最终收口前运行 `npm run build`。

      ## 结论

      页面框架已收敛到统一结构。对象详情页因信息密度和返回筛选语境保留专用头部实现。

      closure_evidence: ## 交付内容

      - 统一页面头部：`web/src/components/PageHeader.tsx` 及各页面接入。
      - 态势信号条：Dashboard/Workbench 使用 `StatusBanner` 表达校验失败或风险信号。
      - 对象详情阅读重点：ObjectDetail/ReadingPanel 按对象类型组织字段阅读顺序。

      ## 验证结果

      - `npm run check` 通过。
      - `npm run lint` 通过。
      - 浏览器验证 Gate、Workbench、Changelog 主流程正常。

      ## 结论

      task-0063 完成并关闭。
    evidence_refs:
    - web/src/components/PageHeader.tsx
    - web/src/pages/Dashboard.tsx
    - web/src/pages/ObjectList.tsx
    - web/src/pages/Changelog.tsx
    - web/src/pages/ObjectDetail.tsx
    - web/src/components/ReadingPanel.tsx
    blocking_reason: null
  review:
    controller_self_check: true
    specialist_review:
      required: false
      role: null
      expected_output: null
    human_closure_review: true
verification_evidence: |
  ## 验证计划

  历史证据格式规范化：检查原 验证证据 是否保留可复查的验证对象、结果或关闭判断；本次仅补齐四段式结构，不新增或改变历史事实语义。

  ## 验证命令

  历史记录未单独记录完整的验证命令；如原字段包含命令、脚本、检查入口或人工检查动作，保留在下方原始证据摘录中。

  ## 验证结果

  原始证据摘录：

  原文标题：验证结果

  历史任务计划迁移聚合证据：
  - task-0063: ## 交付内容

  ## 结论

  本次仅完成证据字段四段式格式规范化；对象状态、关闭判断、验证结果和历史事实语义以原始证据摘录为准，未额外伪造历史验证过程。
closure_evidence: |
  ## 验证计划

  历史证据格式规范化：检查原 关闭证据 是否保留可复查的验证对象、结果或关闭判断；本次仅补齐四段式结构，不新增或改变历史事实语义。

  ## 验证命令

  历史记录未单独记录完整的验证命令；如原字段包含命令、脚本、检查入口或人工检查动作，保留在下方原始证据摘录中。

  ## 验证结果

  原始证据摘录：

  原文标题：验证结果

  历史任务计划迁移聚合证据：
  - task-0063: ## 交付内容

  ## 结论

  本次仅完成证据字段四段式格式规范化；对象状态、关闭判断、验证结果和历史事实语义以原始证据摘录为准，未额外伪造历史验证过程。
review_requested_at: '2026-06-11T18:45:15'
closed_at: '2026-06-11T18:45:15'
related_docs: []
related_adrs: []
related_memos: []
related_pitfalls: []
related_workplans: []
