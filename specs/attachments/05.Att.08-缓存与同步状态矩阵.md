# 缓存与同步状态矩阵

```yaml
v2_attachment:
  attachment_id: "05.Att.08"
  title: "缓存与同步状态矩阵"
  status: "active"
  authority: "active_with_parent_spec"
  parent_spec: "specs/05-Web信息同步规范.md"
  canonical_path: "specs/attachments/05.Att.08-缓存与同步状态矩阵.md"
  purpose: "承载 05 授权的缓存、同步、问题原因和冲突展示状态"
  migration_sources:
    - "history/specs-v1/08-Web信息同步实现规范.md"
  code_consumption:
    - "web_cache_sync_states"
```

> 文件状态：本文是 active 授权附件；随父规范生效，不独立改变父规范正文授权之外的原则、状态机、事实源边界或流程 Gate。

## 1. 定位

本文承载 05 授权的缓存与同步状态矩阵。

## 2. 状态矩阵

| 状态 | 含义 | 展示要求 |
|---|---|---|
| fresh | 当前响应来自最新可读事实源或 Code 输出 | 可显示同步时间和来源 |
| stale | 当前数据可能过期 | 显示过期原因和刷新入口 |
| limited | 部分输入不可用、能力受限或不可证明范围存在 | 显示问题原因、不可证明范围和后续承接位置 |
| error | 读取、解析、同步或 API 出错 | 显示错误来源和修复线索 |
| conflict | Web 派生结果与事实源或 Code 输出冲突 | 退回事实源和诊断，不以页面覆盖事实源 |
| no-store | 知识地图等查询不得持久化缓存 | 不使用浏览器持久化缓存 |

历史 Web/API 兼容状态 `degraded` 只读映射到 `limited`。新增展示、DTO 或缓存状态不得把 `degraded` 作为 Human-facing 主展示态。

## 3. 边界

缓存只能改善性能和交互体验，不得成为事实实例、决策、状态、审计或验收的唯一来源。

## 4. 待补齐事项

1. Web 实现恢复后，应核对缓存策略和 API header；
2. 知识地图运行时推进前，应复核 no-store 语义。
