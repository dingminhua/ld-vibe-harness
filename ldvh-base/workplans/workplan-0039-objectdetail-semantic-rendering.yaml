id: workplan-0039
type: workplan
title: ObjectDetail 字段语义化渲染升级 等 3 个关联任务
status: closed
created: '2026-06-05T22:04:53.452736'
updated: '2026-06-05T23:01:16.997434'
workarea: workarea-0006
priority: P2
description: 由历史 Task task-0039 迁移生成的任务计划。计划目标以关联 Task 的 description 和 acceptance
  为准，避免复制多个权威事实。
success_criteria: |-
  - [x] 关联任务 task-0039 满足其 acceptance、verification 和 closure_evidence。
  - [x] 关联任务 task-0040 满足其 acceptance、verification 和 closure_evidence。
  - [x] 关联任务 task-0042 满足其 acceptance、verification 和 closure_evidence。
source: |-
  Legacy TaskPlan migration from taskplan-0039; source path: ldvh-base/taskplans/taskplan-0039-objectdetail-semantic-rendering.yaml
  Human Gate confirmed by dmh2002: User said 继续吧 after TaskPlan-to-WorkPlan migration preview on 2026-06-18; write WorkPlan facts and WorkArea backlinks only, preserve legacy TaskPlan/Task facts.
orchestration:
  mode: sequential
  execution_items:
  - id: task-0039
    title: ObjectDetail 字段语义化渲染升级
    role: legacy-task
    mode: single
    input_refs:
    - task-0039
    expected_output: |
      - [x] ChecklistCard 组件实现：acceptance 字段显示完成比例（如 3/6）和每项勾选状态
      - [x] ReferenceCard 组件实现：blocked_by/source_intent/parent_task 显示对象 ID、标题、状态徽章
      - [x] SummaryText 组件实现：长文本字段默认显示前 3 行，点击展开全文
      - [x] DocPreviewLink 组件实现：related_docs 条目可点击，触发右侧扩展区
      - [x] EvidenceBlock 组件实现：closure_evidence 突出显示验证命令和文件路径
      - [x] Task 类型对象详情页字段顺序调整为优先展示 acceptance、blocked_by、related_docs
      - [x] 所有组件支持中英双语切换
      - [x] TypeScript 编译和 ESLint 通过
    status: done
    result_summary: |-
      verification: ## 验证结果

      历史补齐：该 Task 在早期关闭时未单独维护 verification 字段；本次状态边界收敛补齐该字段。具体验证结果和证据见 closure_evidence。

      ## 结论

      历史关闭记录有效，当前补齐 verification 字段以满足关闭边界。

      closure_evidence: 5 个语义化组件已实现：ChecklistCard（完成比例+进度条）、ReferenceCard（ID+标题+状态徽章）、
      SummaryText（150字符截断+展开/收起）、DocPreviewLink（自定义事件ldvh:doc-preview）、
      EvidenceBlock（命令和路径高亮）。Task类型字段顺序调整为acceptance→blocked_by→related_docs优先。
      独立agent验证8项acceptance全部通过，ESLint未使用变量已修复。

      ## 验证结果

      5 个语义化组件（ChecklistCard、ReferenceCard、SummaryText、DocPreviewLink、EvidenceBlock）已实现，Task 类型字段顺序调整完成，8 项 acceptance 全部验证通过。

      ## 结论

      任务完成，人工确认关闭。
    evidence_refs:
    - web/docs/04-ObjectDetail.md
    blocking_reason: null
  - id: task-0040
    title: 右侧扩展阅读区基础能力
    role: legacy-task
    mode: single
    input_refs:
    - task-0040
    expected_output: |
      - [x] 桌面端 ObjectDetail 页面布局改为三段式：左侧导航 + 中间主内容 + 右侧扩展阅读区
      - [x] 点击 related_docs 条目时右侧面板预览 Markdown 文件内容
      - [x] 点击 related_tasks/related_adrs 条目时右侧面板展示对象摘要卡片（ID、标题、状态）
      - [x] 右侧面板支持关闭按钮，关闭后恢复双栏布局
      - [x] 移动端（< 1024px）右侧扩展区降级为底部抽屉
      - [x] 右侧面板不写入任何事实源，只读取已有数据
      - [x] TypeScript 编译和 ESLint 通过
    status: done
    result_summary: |-
      verification: ## 验证结果

      历史补齐：该 Task 在早期关闭时未单独维护 verification 字段；本次状态边界收敛补齐该字段。具体验证结果和证据见 closure_evidence。

      ## 结论

      历史关闭记录有效，当前补齐 verification 字段以满足关闭边界。

      closure_evidence: ReadingPanel 组件实现三段式布局，桌面端右侧320px面板滑出，移动端底部抽屉50vh+拖拽关闭。
      监听 ldvh:doc-preview 和 ldvh:ref-preview 事件，文档预览通过 GET /api/docs 获取内容，
      对象摘要通过 GET /api/objects/:type/:id 获取。ReferenceCard 桌面端阻止导航改为打开面板。
      TypeScript编译通过。

      ## 验证结果

      ReadingPanel 组件实现三段式布局，桌面端右侧面板和移动端底部抽屉均可正常工作，文档预览和对象摘要功能已验证，TypeScript 编译通过。

      ## 结论

      任务完成，人工确认关闭。
    evidence_refs: []
    blocking_reason: null
  - id: task-0042
    title: 新用户引导与解释性微文案
    role: legacy-task
    mode: single
    input_refs:
    - task-0042
    expected_output: |
      - [x] ObjectDetail 页面顶部增加对象类型一句话说明（中英双语）
      - [x] ObjectDetail 页面顶部增加"下一步"行动提示（基于当前状态，如"等待执行"/"待审查"）
      - [x] 状态徽章增加 tooltip 显示状态说明（中英双语）
      - [x] Human Gate 相关状态（review_needed）显示提示图标和说明
      - [x] 所有微文案通过 i18n 体系管理，支持中英切换
      - [x] TypeScript 编译和 ESLint 通过
    status: done
    result_summary: |-
      verification: ## 验证结果

      历史补齐：该 Task 在早期关闭时未单独维护 verification 字段；本次状态边界收敛补齐该字段。具体验证结果和证据见 closure_evidence。

      ## 结论

      历史关闭记录有效，当前补齐 verification 字段以满足关闭边界。

      closure_evidence: 新增 TYPE_DESCRIPTION_LOCALES（7种对象类型说明）和 STATUS_HINT_LOCALES（9种状态提示），
      ObjectDetail 顶部显示类型说明和状态行动提示，StatusBadge 增加 title tooltip，
      review_needed 状态显示琥珀色 Human Gate 提示条。TypeScript编译通过。

      ## 验证结果

      TYPE_DESCRIPTION_LOCALES 和 STATUS_HINT_LOCALES 已实现，ObjectDetail 顶部显示类型说明和状态行动提示，StatusBadge 增加 tooltip，review_needed 状态显示 Human Gate 提示条，TypeScript 编译通过。

      ## 结论

      任务完成，人工确认关闭。
    evidence_refs:
    - web/docs/01-全局设计约束.md
    blocking_reason: null
  review:
    controller_self_check: true
    specialist_review:
      required: false
      role: null
      expected_output: null
    human_closure_review: true
verification_evidence: |
  ## 验证计划

  历史证据格式规范化：检查原 验证证据 是否保留可复查的验证对象、结果或关闭判断；本次仅补齐四段式结构，不新增或改变历史事实语义。

  ## 验证命令

  历史记录未单独记录完整的验证命令；如原字段包含命令、脚本、检查入口或人工检查动作，保留在下方原始证据摘录中。

  ## 验证结果

  原始证据摘录：

  原文标题：验证结果

  历史任务计划迁移聚合证据：
  - task-0039: 5 个语义化组件已实现：ChecklistCard（完成比例+进度条）、ReferenceCard（ID+标题+状态徽章）、
  - task-0040: ReadingPanel 组件实现三段式布局，桌面端右侧320px面板滑出，移动端底部抽屉50vh+拖拽关闭。
  - task-0042: 新增 TYPE_DESCRIPTION_LOCALES（7种对象类型说明）和 STATUS_HINT_LOCALES（9种状态提示），

  ## 结论

  本次仅完成证据字段四段式格式规范化；对象状态、关闭判断、验证结果和历史事实语义以原始证据摘录为准，未额外伪造历史验证过程。
closure_evidence: |
  ## 验证计划

  历史证据格式规范化：检查原 关闭证据 是否保留可复查的验证对象、结果或关闭判断；本次仅补齐四段式结构，不新增或改变历史事实语义。

  ## 验证命令

  历史记录未单独记录完整的验证命令；如原字段包含命令、脚本、检查入口或人工检查动作，保留在下方原始证据摘录中。

  ## 验证结果

  原始证据摘录：

  原文标题：验证结果

  历史任务计划迁移聚合证据：
  - task-0039: 5 个语义化组件已实现：ChecklistCard（完成比例+进度条）、ReferenceCard（ID+标题+状态徽章）、
  - task-0040: ReadingPanel 组件实现三段式布局，桌面端右侧320px面板滑出，移动端底部抽屉50vh+拖拽关闭。
  - task-0042: 新增 TYPE_DESCRIPTION_LOCALES（7种对象类型说明）和 STATUS_HINT_LOCALES（9种状态提示），

  ## 结论

  本次仅完成证据字段四段式格式规范化；对象状态、关闭判断、验证结果和历史事实语义以原始证据摘录为准，未额外伪造历史验证过程。
review_requested_at: '2026-06-05T23:01:16.997442'
closed_at: '2026-06-05T23:01:16.997442'
related_docs: []
related_adrs: []
related_sparks: []
related_pitfalls: []
related_workplans: []
