# 固定运行时扩展登记表

```yaml
v2_attachment:
  attachment_id: "06.Att.02"
  title: "固定运行时扩展登记表"
  status: "active"
  authority: "active_with_parent_spec"
  parent_spec: "specs/06-运行时扩展规范.md"
  canonical_path: "specs/attachments/06.Att.02-固定运行时扩展登记表.md"
  purpose: "承载 06 授权的当前固定运行时扩展基线登记"
  migration_sources:
    - "history/specs-v1/04.02-LDVH能力资产与保障机制规范.md"
  code_consumption:
    - "fixed_runtime_extension_registry"
    - "runtime_extension_baseline"
```

> 文件状态：本文是 active 授权附件；随父规范生效，不独立改变父规范正文授权之外的原则、状态机、事实源边界或流程 Gate。

## 1. 定位

本文承载 06 授权的当前固定运行时扩展基线登记。

本文只做 v2 迁移基线回指。资产文件自身的 `ldvh_asset` 或等价自描述才是实例权威；本文不得维护环境安装状态、用户本地启用状态、检查通过状态或长期部署状态。

## 2. 登记表

| 类型 | 当前固定承载物 | 权威路径 | 边界 |
|---|---|---|---|
| Rules | 工作区入口 | `rules/LDVH-WORKSPACE-ENTRY.md` | 管辖项目识别、工作对象入口和 STOP 点；不维护 LDVH 产品资产 |
| Rules | 维护入口 | `rules/LDVH-MAINTAINER-ENTRY.md` | LDVH 产品资产维护入口；不直接处理管辖项目工作对象 |
| Skill | Git 提交 Skill | `skills/ldvh-git-commit/SKILL.md` | 提交准备、拆分判断、消息编写、预检和交还流程；不定义 commit message 本体规则 |
| Hook | Hook registry | `hooks/ldvh-hooks.yaml` | 统一登记 LDVH Hook 事件和触发命令；不替代 Code validator、CI 或 Human Gate |
| Agent | 无 active 固定 Agent | 无 | 环境 Agent 能力可被主控调用，但无登记 fixed Agent 时不得写成稳定承载物 |

## 3. 使用规则

未登记的 Rules、Skill、Agent 或 Hook 只能作为候选资产、临时过程输出、人工降级步骤或环境适配候选，不得被 Code、Web、Rules 或行动编排当成稳定保障机制。

新增、删除、移动或重命名固定承载物必须回到 06 Human Gate，并同步 01、README、Code 检查和入口规则。

## 4. 待补齐事项

1. Code 能解析 `ldvh_asset` 后，应由 Code 生成固定承载物索引，本文只保留基线说明；
2. 若后续新增 fixed Agent，应先补齐 Agent 自描述、输入输出、权限和主控回收边界。
