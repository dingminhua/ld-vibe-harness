id: task-0072
type: task
title: 建设最小统一 LDVH CLI 入口
status: closed
created: '2026-06-11T00:38:52.200694'
updated: '2026-06-11T03:22:00'
description: 建设最小统一 LDVH CLI 入口，优先包装 status、landing plan/apply/repair/verify/review、facts
  和 specs 校验，降低 AI 使用多个 Python 工具的发现成本。
source: intent-0012；docs/research/21-LDVH下一阶段推进方向-受控落地执行闭环.md；用户确认创建任务批次。
acceptance: '- [x] 定义最小命令树和输出合同；- [x] 包装现有 fact_cli、fact_validate、specs_validate 能力；-
  [x] 命令具备稳定 exit code；- [x] 写入命令显式检查 Human Gate 和计划边界；- [x] 测试覆盖 help、status、plan
  和至少一个失败路径。'
blocked_by:
- task-0066
deliverables:
- tools/ldvh_cli.py
- tests/test_ldvh_cli.py
- .trae/specs/minimal-ldvh-cli-entry/tasks.md
source_intent: intent-0012
related_adrs:
- adr-0007
related_docs:
- docs/research/21-LDVH下一阶段推进方向-受控落地执行闭环.md
verification: |
  ## 验证计划

  先补 CLI help/exit code 测试，再实现入口；运行 pytest 和关键 CLI 命令。

  ## 验证结果

  - `python3 -m pytest tests/test_ldvh_cli.py` 先失败于 `tools/ldvh_cli.py` 缺失；完成实现后结果 5 passed。
  - `python3 -m pytest` 结果 207 passed。
  - `python3 tools/ldvh_cli.py --help` 结果返回 0，输出 status、landing、facts、specs 最小命令树。
  - `python3 tools/ldvh_cli.py status --format json` 结果返回 0，输出 `ldvh-cli/v1` 合同、稳定 exit code 合同和写入 guard 来源。
  - `python3 tools/ldvh_cli.py landing plan --format json` 可委托 specs_validate landing-plan；当前项目存在既有 open/degraded 缺口时返回 1。
  - `python3 tools/ldvh_cli.py facts validate ldvh-base --format json` 结果返回 0。
  - `python3 tools/fact_validate.py ldvh-base` 结果 files=104 errors=0 warnings=0。
  - `python3 tools/ldvh_cli.py specs validate docs/specs --specs-dir docs/specs` 因既有平台入口 Markdown 路径 warning 返回 1，未发现本次 CLI 相关错误。

  ## 结论

  task-0072 已完成；最小统一 CLI 入口已包装 status、landing、facts 与 specs 能力，写入类 landing apply/repair 继续依赖 landing-plan/v1 的 Human Gate 与 write_targets 边界，不新增第二事实源。
closed_at: '2026-06-11T03:22:00'
closure_evidence: |
  ## 验证结果

  - `python3 -m pytest tests/test_ldvh_cli.py` 结果 5 passed。
  - `python3 -m pytest` 结果 207 passed。
  - `python3 tools/ldvh_cli.py --help` 结果返回 0。
  - `python3 tools/ldvh_cli.py status --format json` 结果返回 0。
  - `python3 tools/ldvh_cli.py facts validate ldvh-base --format json` 结果返回 0。
  - `python3 tools/fact_validate.py ldvh-base` 结果 files=104 errors=0 warnings=0。
  - `python3 tools/ldvh_cli.py landing plan --format json` 与 `python3 tools/ldvh_cli.py specs validate docs/specs --specs-dir docs/specs` 因既有 open/degraded 缺口或平台入口 Markdown 路径 warning 返回 1。

  ## 结论

  最小统一 LDVH CLI 入口已实现并通过核心自动化验证；残留非零验证均来自既有规范落地缺口或平台入口路径 warning。
