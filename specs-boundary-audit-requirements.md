# Specs 边界一致性审查要求

> 本文件是给审查 AI 的执行指令。审查完成后由主控 AI 检查结果并决定修订。

## 审查目标

对 `specs/` 下 00-10 共 11 篇正式 spec 做边界一致性审查，找出归口越界、内容重复、实现域溢入、临时内容污染和信息角色职责违反，确保每篇 spec 只管自己归口的内容，不复制来源规则，不承载实现域细节，不混入迁移期临时内容。

## 审查范围

| spec | 文件 |
|---|---|
| 00 | `specs/00-理念与构成.md` |
| 01 | `specs/01-保障与衔接.md` |
| 02 | `specs/02-AI行为规范.md` |
| 03 | `specs/03-事实源与Git溯源规范.md` |
| 04 | `specs/04-Specs基础规范.md` |
| 05 | `specs/05-事实模型基础规范.md` |
| 06 | `specs/06-行动模板基础规范.md` |
| 07 | `specs/07-Code确定性执行规范.md` |
| 08 | `specs/08-Web信息同步规范.md` |
| 09 | `specs/09-测试与验证规范.md` |
| 10 | `specs/10-安装与配置规范.md` |

附件（`specs/attachments/`）和 20+ 成员不在本次审查范围。

## 审查依据

- `specs/04-Specs基础规范.md` §5（正文结构）、§5.3（实现域实践边界）、§5.5（信息角色职责闭集）、§8（状态归口原则）
- `specs/06-行动模板基础规范.md` §5.1（引用与复制的判断标准）、§5.2（七结构粒度要求）

## 审查重点方向

以下 8 个方向基于已发现的问题提炼。每个方向列出具体检查项和已发现的实例。

### 方向 1：归口边界越界

**检查目标**：每篇 spec 的 §3 声明了归口边界，但正文里有没有写其他 spec 归口的内容。

**检查方法**：
1. 读每篇 spec 的 §3，列出它声明"归口"什么、"不归口"什么
2. 遍历主体规则区（§5 及之后），找有没有写 §3 声明"不归口"的内容
3. 特别注意：行动模板类 spec（06、30+）是否在正文里写了机器规则（应归 10/01/07）；事实模型类 spec 是否在正文里写了行动步骤（应归 06）

**已发现实例**：
- 30 §7.3 重述了 10 §5.1 的配置层级冲突规则
- 30 §7.4 重述了 10 §8.1 的 install_plan 字段内容
- 30 §7.5 重述了 10 §8.2 的 environment_strategy 枚举
- 06 原 §7.30 对 30 写了单独禁止规则（应归 30 自身或来源 spec）

**输出要求**：列出每篇 spec 的越界位置（精确到 §X.Y 和原文片段）、越界内容、应归哪篇 spec。

---

### 方向 2：specs 间内容重复

**检查目标**：同一内容在多个 specs 中重复定义，无法判断权威来源。

**检查方法**：
1. 找出在两篇以上 spec 中出现的相同或近乎相同的表格、列表、规则段落
2. 判断哪篇是权威来源（通常是被 `basis` 或 `related_specs` 引用的那篇）
3. 判断重复是否属于"引用"（按 06 §5.1 判断表允许）还是"复制"（违反 06 §5.1）

**已发现实例**：
- 10 §8 的 `ldvh-base/` 目录表（workcases/adrs/pitfalls/sparks/studies）和 30 §6 的同名表格内容几乎相同
- 06 原 §7 WorkCase 示范列举了 21 的状态枚举值（已删除）

**输出要求**：列出重复内容的位置、内容摘要、权威来源判断、应改为引用还是删除。

---

### 方向 3：实现域溢入

**检查目标**：specs 正文是否锁死了具体实现语言、框架、模块拆分、目录组织、页面组件、API 路由、测试文件清单等。

**检查依据**：04 §5.3 实现域实践边界——specs 只定义需求、规则、契约、边界、Human Gate、Stop Conditions 和验证要求；实现域细节由 `code/`、`code/docs/`、`web/`、`web/docs/`、`tests/` 承接。

**检查方法**：
1. 遍历每篇 spec 的主体规则区
2. 找有没有写具体 CLI 命令、文件路径（非 spec 路径）、模块名、函数名、页面组件、API 路由、测试框架名、fixtures 组织方式
3. 找有没有写表格列、图标、确认块格式、展示样式等呈现层细节

**已发现实例**：
- 30 §7.1 混入了表格列、图标、确认块格式、复杂度分级表等呈现层细节（应归 `code/docs/03`）

**输出要求**：列出溢入位置、溢入内容、应归哪个实现域文档。

---

### 方向 4：信息角色职责违反

**检查目标**：每篇 spec 的固定头部和固定尾部章节是否遵守了 04 §5.5 信息角色职责闭集。

**检查依据**：04 §5.5 定义的 10 个信息角色的"只负责/不负责"闭集。

**检查方法**：逐篇逐章节检查：

| 信息角色 | 检查重点 |
|---|---|
| 价值判断（§1） | 是否重写了 00 价值门？是否写了具体规则细节？ |
| 权威依据（§2） | 是否定义了本文管什么（应归 §3）？是否处理了具体规则内容？ |
| 归口边界（§3） | 是否用了无限排除清单？是否替相邻规范定义了规则？ |
| 适用范围（§4） | 是否手写了目录、关系索引、读取计划或 Code 覆盖状态？ |
| 主体规则区（§5+） | 是否重复了上位规范全文？是否承载了过程输出、Code 派生目录、读取计划、关系图或一次性检查结果？ |
| 保障措施 | 是否记录了一次性执行结果？是否写了全量审计清单？ |
| 验证方法 | 是否声明了当前已经通过？是否把 Code 诊断升级为授权？ |
| Human Gate | 是否写了 AI 自行放行条件？ |
| Stop Conditions | 是否替 Human 完成了暂停后的确认？ |
| 待补齐事项 | 是否写了泛泛优化愿望？是否隐藏了新规则？ |

**输出要求**：列出违反位置、违反的信息角色、具体问题、建议修订。

---

### 方向 5：固定头尾完整性

**检查目标**：每篇 spec 是否具备 04 §5.1 要求的固定头部四项和 §5.4 要求的固定尾部五项，且顺序正确。

**检查方法**：
1. 除 00 外，检查每篇 spec 是否有：价值判断、权威依据、归口边界、适用范围（固定头部，顺序一致）
2. 检查每篇 spec 是否有：保障措施、验证方法、Human Gate、Stop Conditions、待补齐事项（固定尾部）
3. 检查主体规则区是否替代了固定尾部（把保障/验证/Gate/Stop 内容藏在主体规则区里）
4. 检查 `role_sections` 是否与正文标题一致

**输出要求**：列出缺失或顺序错误的固定头尾项。

---

### 方向 6：临时内容污染

**检查目标**：迁移期临时内容是否长期存在于正文；过程输出是否冒充了规则；一次性检查结果是否进入了正文。

**检查依据**：04 §5.2 主体规则区不得承载过程输出、Code 派生目录、读取计划、关系图、一次性检查结果、review 结果、测试输出或迁移证据。

**检查方法**：
1. 找有没有"当前迁移判断"、"阶段 X 已迁入"、"阶段 9C/9D 已完成"等迁移过程记录长期存在于正文（应归待补齐事项或迁移材料）
2. 找有没有把某个 Code 诊断输出、某次 review 结论、某次测试结果写成了规则
3. 找有没有把临时执行结果（如"已检查 N 个项目"）写成了规则

**已发现实例**：
- 06 原 §7"模板候选与迁移边界"是迁移期临时章节，已删除并移到 §11 待补齐事项

**输出要求**：列出临时内容位置、内容摘要、应移到何处。

---

### 方向 7：状态归口违反

**检查目标**：是否只有 specs/attachments 生命周期状态和事实对象业务状态写成可记录状态；运行时输出、Code 诊断、环境审计等是否被写成第三类可记录状态。

**检查依据**：04 §8 状态归口原则。

**检查方法**：
1. 搜索每篇 spec 中的 `status`、`state`、`状态` 字样
2. 判断每个 status/state 是 specs/attachments 生命周期状态、事实对象业务状态，还是运行时输出/验证输出/展示输出
3. 运行时输出不得被写成可记录状态，不得形成独立状态闭集

**输出要求**：列出可能违反的位置、status/state 用法、是否属于违反。

---

### 方向 8：identity block 与正文一致性

**检查目标**：`ldvh_spec` identity block 的 `positioning`、`scope`、`role_sections`、`code_consumption` 是否与正文实际内容一致。

**检查方法**：
1. 读 identity block 的 `positioning` 和 `scope`，对比 §3 归口边界和 §4 适用范围
2. 读 `role_sections`，对比正文实际章节标题
3. 读 `code_consumption`，检查是否有已删除内容对应的项仍残留
4. 读 `related_specs`，检查引用关系是否与正文实际引用一致

**已发现实例**：
- 06 identity block 的 `scope` 原来包含"Git 提交行动示范"等已删除内容（已修正）
- 06 `code_consumption` 原来包含 `git_commit_action_template` 等已删除项（已修正）

**输出要求**：列出不一致的字段、当前值、正文实际情况、建议修订。

## 输出格式

每篇 spec 独立输出一份审查报告，格式如下：

```markdown
# [spec-id] 审查报告

## 基本信息
- 文件路径：
- 行数：
- 固定头部完整性：[通过/不通过]
- 固定尾部完整性：[通过/不通过]

## 方向 1：归口边界越界
[通过/有发现]
- [若发现] 位置：§X.Y 第 N 行
- 越界内容：[原文片段]
- 应归：[spec-id]
- 建议：[改为引用/删除/移动]

## 方向 2：specs 间内容重复
[通过/有发现]
...

（方向 3-8 同上）

## 汇总
- 通过方向数：X/8
- 需修订项数：N
- 严重程度：[高/中/低]
```

## 审查纪律

1. 只读审查，不修改任何文件
2. 必须读原文，不能根据摘要判断
3. 每个发现必须精确到 §X.Y 和原文片段
4. 引用 06 §5.1 判断表时必须说明判断依据
5. 不确定的发现标注"待确认"，不要自行下结论
6. 审查完成后将所有报告输出到 `reviews/specs-boundary-audit/` 目录下
