# v1 to v2 migration map

```yaml
migration_map:
  status: empty_stub
  active_fact_source: specs/
  rule: "迁移条目必须在 V1-UNDERSTANDING-GATE 通过且对应 v2 文档经 Human 单篇核对后填写"
```

## 1. 用途

本文后续记录 v1 `specs/` 到 v2 `specs-v2/` 的迁移覆盖关系。

当前不登记正式迁移结论，避免在未核对前形成第二事实源。

填写本文前，应先确认 `V1-UNDERSTANDING-GATE.md` 中对应 v1 机制的 v2 去向。

## 2. 待填写字段

| v1 来源 | v1 章节 | v2 目标 | 迁移动作 | 状态 | Human 核对记录 |
|---|---|---|---|---|---|
| 待核对 | 待核对 | 待核对 | migrate / merge / split / drop / defer | pending | 待核对 |
