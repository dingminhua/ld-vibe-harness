# 运行时扩展自描述字段表

```yaml
v2_attachment:
  attachment_id: "06.Att.01"
  title: "运行时扩展自描述字段表"
  status: "draft"
  authority: "not_active_until_parent_and_human_approved"
  parent_spec: "specs-v2/06-运行时扩展规范.md"
  canonical_path: "specs-v2/attachments/06.Att.01-运行时扩展自描述字段表.md"
  purpose: "承载 06 授权的运行时扩展自描述字段表"
  migration_sources:
    - "specs/04.02-LDVH能力资产与保障机制规范.md"
  code_consumption:
    - "runtime_extension_identity_fields"
    - "runtime_extension_asset_index"
```

> 文件状态：本文当前位于 `specs-v2/`，尚未切换为 active；正式运行时扩展承载物规则仍以 active `specs/04.02-LDVH能力资产与保障机制规范.md` 为准。本文是 `06-运行时扩展规范.md` 授权附件，必须随父规范经过 Human 单篇确认后才可生效，不独立改变运行时扩展准入或部署状态。

## 1. 定位

本文承载 06 授权的运行时扩展自描述字段表。

06 正文定义固定运行时扩展承载物必须具备自描述；本文只维护字段表，不得登记具体资产实例状态、环境安装状态、部署状态、Code 输出 Schema 或 Web 页面契约。

## 2. 字段表

| 字段 | 出现要求 | 含义 |
|---|---|---|
| `id` | 必须出现 | 承载物稳定 ID |
| `type` | 必须出现 | 承载物类型，例如 `rule`、`skill`、`agent`、`hook` |
| `status` | 必须出现 | 承载物状态 |
| `canonical_path` | 必须出现 | 权威文件路径 |
| `source_specs` | 必须出现 | 来源规范或保障要求 |
| `consumption_scenarios` | 必须出现 | 消费场景 |
| `inputs` | 必须出现 | 运行时输入或读取对象 |
| `outputs` | 必须出现 | 输出、交还对象或提示结果 |
| `handoff` | 条件出现 | 需要交还主控、Human 或其它入口时填写 |
| `verification` | 必须出现 | 验证命令、检查方式或人工降级检查 |
| `sync_triggers` | 必须出现 | 同步触发条件 |
| `deprecation` | 必须出现 | 废弃、替代或迁移规则 |

## 3. 使用规则

资产文件自身的自描述是实例事实权威；06 附件或派生索引只能读取、展示或诊断，不得反向维护资产状态。

Code 尚不能解析自描述字段时，应执行人工降级检查，并把 Code 缺口分流为 WorkCase、Spark 或 Code 需求。

## 4. 待补齐事项

1. Code v2 解析实现后，应检查字段名、必填性和诊断等级；
2. 若未来出现专题承载物专属字段，应回到 06 正文或对应承载物类型规范确认。
