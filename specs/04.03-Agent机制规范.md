# Agent 机制规范

> 创建日期：2026-05-30
> 所属主文档：`specs/04-LDVH-AI协作规范.md`
> 关系：父规范扩展
> 适用范围：LD Vibe Harness Agent 创建、调用、定义、调度、生命周期和事实源边界
> 上位依据：`specs/04-LDVH-AI协作规范.md`

---

## 一、本文定位

本文定义 LD Vibe Harness 如何使用 Trae Agent 机制处理独立上下文、结论隔离、并行委派和不同权限边界场景。

Agent 是高级可选能力。LDVH 不默认要求创建 Agent，不在本文中维护推荐自建 Agent 清单，也不把 PM Kit 推荐自建 Agent 清单迁移为 LDVH 正式规则。

## 二、父规范关系

`specs/04-LDVH-AI协作规范.md` 定义 AI 协作机制总边界、事实源回写总原则和 Rules / Skill / Agent 跨机制选择边界。本文只展开 Agent 机制。

当需要判断某项内容应进入 Rules、Skill 还是 Agent 时，必须以 `specs/04-LDVH-AI协作规范.md` 第五节为准；本文只检查已拟使用 Agent 时是否满足创建调用、调度、定义摘要、生命周期、权限边界和治理要求。

Agent 与其他机制的关系：

| 对象 | 关系 |
|---|---|
| Rules | Rules 可定义 Agent 创建、调用和 Human Gate 边界 |
| Skill | Skill 不调度 Agent，只能建议用户或主控判断是否使用 Agent |
| 行动模型 | Agent 调度、输入输出和事实回写应服从 40-69 行动模型 |

## 三、Agent 创建或调用治理检查

跨机制选择依据 `specs/04-LDVH-AI协作规范.md` 第五节，本文不另行判断某项内容是否应优先进入 Rules、Skill 或 Agent。已拟使用 Agent 时，应检查是否满足以下治理条件：

1. **独立上下文**：需要读取大量事实源，主上下文难以承载或容易污染当前任务；
2. **结论隔离**：需要不同角色独立形成判断，避免主上下文过早合并观点；
3. **并行委派**：需要多个专业角色并行分析或执行；
4. **不同权限边界**：需要与主上下文不同的权限范围；
5. **长期专业入口**：某类复杂审查、分析或执行场景高频出现，且已有稳定定义和边界。

Agent 治理检查还应确认：

1. 不是仅为单次简单问答或单文件小改创建 Agent；
2. 不用 Agent 承载稳定规范正文、项目事实源正文或完整教程；
3. 不是仅为了包装角色名称；
4. Agent 创建、修改、删除或权限调整已按 Human Gate 处理。

## 四、调度规则

Agent 应被动响应用户显式调用或主控编排，不得自行触发。

调度规则：

1. 用户可通过 Trae `@Agent` 显式调用已存在的 Agent；
2. SOLO Agent 或主控流程可在复杂任务中编排子 Agent；
3. Skill 不得调度 Agent；
4. Agent 默认不调度其他 Agent，除非其定义和主控授权明确允许；
5. Agent 默认只读分析，除非输入上下文明确授权写入；
6. Agent 发现 Human Gate 场景时必须标注并停止相关修改；
7. Agent 输出应返回主控或用户，由主控决定是否写回事实源。

## 五、Agent 定义摘要单一事实源

Trae 平台中的 Agent Prompt 是运行入口。LDVH 中已确认创建或导入的 Agent，应在项目 Git 文件事实源中维护定义摘要，避免关键行为只存在于平台配置中。

Agent 定义摘要应至少包含：

| 字段 | 内容 |
|---|---|
| Agent 名称 | Trae 平台显示名称 |
| 英文标识 | 稳定英文标识 |
| 角色定位 | 覆盖项目、核心职责、调度方 |
| 触发场景 | 触发条件、触发频率、停止条件 |
| 分析关注点 | 必须分析的维度和输出判断项 |
| 职责边界 | 只读/写入权限、Human Gate、上下游影响 |
| 必读文档 | 执行前必须读取的权威事实源 |
| 输入输出 | 输入上下文要求和输出结构 |
| 权限范围 | 权限范围和高风险权限说明 |
| Prompt 来源 | Trae Prompt 与定义摘要的同步关系 |
| 维护状态 | active / deprecated / planned |

Agent 定义摘要和 Trae 平台 Prompt 不得形成两个互相冲突的事实源。Prompt 应尽量引用定义摘要作为权威来源，只保留角色入口、执行约束和输出格式等必要内容。

## 六、生命周期

### 6.1 创建

1. 检查是否满足 Agent 创建或调用治理条件；
2. 说明为什么 Rules、Skill 或主上下文不足；
3. 说明拟创建 Agent 的角色、权限边界、触发方式和影响范围；
4. 触发 Human Gate 并等待用户确认；
5. 用户确认后在 Trae 中创建或导入 Agent；
6. 在 Git 文件事实源中维护 Agent 定义摘要；
7. 追加 Change 记录。

### 6.2 修改

1. 判断修改是否影响职责边界、触发条件、权限边界或 Prompt；
2. 涉及高影响变更时触发 Human Gate；
3. 同步修改 Trae 平台配置和 Git 文件定义摘要；
4. 检查引用和索引；
5. 追加 Change 记录。

### 6.3 删除或停用

1. 确认该 Agent 不再被流程、规则、Skill 或文档引用；
2. 触发 Human Gate；
3. 从 Trae 平台删除、禁用或停用 Agent；
4. 归档或更新 Git 文件定义摘要；
5. 更新引用和索引；
6. 追加 Change 记录。

## 七、Agent 输出与事实源回写

Agent 输出不是最终事实源。Agent 输出中的分析、建议、证据、风险、任务拆分、决策建议或变更说明，只有写入对应 Git 文件事实源后才成为稳定事实。

回写规则：

1. 规范规则写入 specs/ 或 `.trae/rules/` 对应位置；
2. 任务、证据、变更、风险等结构化对象写入 `ldvh-base/`；
3. 管辖项目报告或审计结果写入 docs/；
4. 外部资料或 Agent 模板参考写入 references/；
5. Agent 输出与事实源冲突时，以 Git 文件事实源为准；
6. Agent 不得自行宣称任务完成，必须由主控或用户依据事实源和证据判断。

## 八、推荐清单处理原则

PM Kit 的推荐自建 Agent 清单、Trae 官方一键导入 Agent 示例和社区 Agent 模板，只能作为参考输入。LDVH 正式规范不得直接维护推荐自建 Agent 清单。

如未来确需建立 LDVH Agent 参考清单，应满足：

1. 放入 70-89 内部调研区或经确认进入正式规范；
2. 明确调研边界和执行效力；
3. 不表示已创建任何 Trae Agent；
4. 不替代 Agent 创建门槛和 Human Gate；
5. 不绑定具体业务项目或技术栈，除非进入管辖项目文档。

## 九、Human Gate 与检查要求

以下情况应评估 Human Gate：

1. 创建、导入、启用、停用、删除或重命名 Agent；
2. 修改 Agent Prompt、职责边界、触发方式或定义摘要；
3. 调整 Agent 权限边界、写入权限、网络权限或可调用范围；
4. 允许 Agent 调度其他 Agent；
5. 将 Agent 输出提升为稳定事实但未回写 Git 文件事实源；
6. 将推荐 Agent 清单正式化；
7. 删除或改变 Agent 定义摘要的权威位置。

Agent 检查至少包括：

| 检查项 | 标准 |
|---|---|
| 创建门槛 | 满足独立上下文、结论隔离、并行委派或不同权限边界之一 |
| 轻量替代 | 已判断 Rules、Skill 或主上下文不足 |
| 调度边界 | 用户显式调用或主控编排，不自行触发 |
| Skill 边界 | 未由 Skill 调度 Agent |
| 权限最小化 | 权限范围最小必要 |
| 定义摘要 | Git 文件中有定义摘要或维护计划 |
| 事实源回写 | Agent 输出未替代 Git 文件事实源 |
| Change 记录 | Agent 变更已记录到 `ldvh-base/changes/` |

## 十、待补齐事项

1. 07 LDVH 行动模型稳定后，补齐 Agent 调度的上下文包和回写流程；
2. 如未来创建 Agent 定义摘要目录，应补齐目录位置和索引格式；
3. 如未来使用多 Agent 并行流程，应补齐输出合并、冲突处理和证据回写规则。
