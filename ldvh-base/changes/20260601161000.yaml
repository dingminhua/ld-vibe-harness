type: change
title: 统一 ADR Agent 子文档标题命名
author: ai
status: done
created: 2026-06-01
updated: 2026-06-01
context: |
  用户指出 `ADR — Agent 实践` 按约定应改成相同的名字。
  经检查，`specs/04-LDVH模型子文档规范.md` §5.1 将 `NN.03-Agent.md` 的内容维度定义为 `Agent 上下文隔离实践`。
  `specs/21.03-Agent.md` 标题和正文仍使用较泛化的 `Agent 实践`，与同组模型子文档命名维度不一致。
decision: |
  将 `specs/21.03-Agent.md` 标题改为 `ADR — Agent 上下文隔离实践`。
  将头部适用范围和本文定位中的对应表述同步调整为 `Agent 上下文隔离实践`。
consequences: |
  - ADR 的 21.03 子文档命名与 `NN.03-Agent.md` 约定保持一致。
  - 本次变更不改变 ADR Agent not-created 判断、检查项或事实模型核心规则。
affects:
  - specs/21.03-Agent.md
related_rules:
  - specs/04-LDVH模型子文档规范.md
  - specs/02-LDVH术语规范.md
human_gate:
  required: false
  confirmed_by: none
  confirmation_context: 仅统一既有子文档标题和局部表述，未新增、删除、重命名文件或调整编号体系。
validation: |
  1. 已检查 `specs/21.03-Agent.md` 标题为 `ADR — Agent 上下文隔离实践`。
  2. 已检查头部适用范围使用 `Agent 上下文隔离实践`。
  3. 已检查本文定位段落使用 `Agent 上下文隔离实践`。
