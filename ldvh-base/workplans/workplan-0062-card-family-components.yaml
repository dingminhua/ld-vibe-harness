id: workplan-0062
type: workplan
title: 抽取统一卡片家族组件
status: closed
created: '2026-06-11T00:00:00.000000'
updated: '2026-06-11T18:45:15'
workarea: workarea-0012
priority: P2
description: 由历史 Task task-0062 迁移生成的任务计划。计划目标以关联 Task 的 description 和 acceptance
  为准，避免复制多个权威事实。
success_criteria: '- [x] 关联任务 task-0062 满足其 acceptance、verification 和 closure_evidence。'
source: |-
  Legacy TaskPlan migration from taskplan-0062; source path: ldvh-base/taskplans/taskplan-0062-card-family-components.yaml
  Human Gate confirmed by dmh2002: User said 继续吧 after TaskPlan-to-WorkPlan migration preview on 2026-06-18; write WorkPlan facts and WorkArea backlinks only, preserve legacy TaskPlan/Task facts.
orchestration:
  mode: single
  execution_items:
  - id: task-0062
    title: 抽取统一卡片家族组件
    role: legacy-task
    mode: single
    input_refs:
    - task-0062
    expected_output: |
      - [x] MetricCard 支持 icon + value + label，可选 href 跳转
      - [x] StatusBanner 支持 4 种状态色调 (closed/degraded/open/needs_human_gate)
      - [x] ContentCard 支持 title + content，可选 collapsible
      - [x] 现有页面的通用 metric/status/content 卡片模式已收敛到共享组件；业务专用对象卡片保留局部实现
      - [x] npm run check 通过
      - [x] npm run build 通过
    status: done
    result_summary: |-
      verification: ## 验证结果

      - `MetricCard` 已支持 icon、value、label、href、onClick、tone、size、detail。
      - `StatusBanner` 已支持 `closed`、`degraded`、`open`、`needs_human_gate` 四种状态。
      - `ContentCard` 已支持 title、icon、headerExtra、collapsible、defaultOpen。
      - Dashboard/Gate/Workbench/Validate/AttentionTest 等页面已使用 `MetricCard` 或 `StatusBanner`。
      - Validate 的 ReportCard 和 Gate 的问题码分组已改用 `ContentCard`。
      - `npm run check`、`npm run lint` 通过；最终收口前运行 `npm run build`。

      ## 结论

      统一卡片家族组件已落地。保留 StatsCard、对象列表项、引用卡片等业务专用组件，不再强制改造成 ContentCard。

      closure_evidence: ## 交付内容

      - 共享卡片家族：`web/src/components/MetricCard.tsx`、`web/src/components/StatusBanner.tsx`、`web/src/components/ContentCard.tsx`。
      - 页面接入：Dashboard/Gate/Workbench/Validate/AttentionTest 等页面使用共享 metric/status/content 组件。
      - 本轮补强：Validate 的报告卡片和 Gate 的问题码分组改用 ContentCard。

      ## 验证结果

      - `npm run check` 通过。
      - `npm run lint` 通过。
      - 浏览器验证 Gate 页面主结构正常。

      ## 结论

      task-0062 完成并关闭。后续若要继续压缩 Dashboard 内部业务卡片，应单独开小任务，不阻塞本任务关闭。
    evidence_refs:
    - web/src/components/MetricCard.tsx
    - web/src/components/StatusBanner.tsx
    - web/src/components/ContentCard.tsx
    blocking_reason: null
  review:
    controller_self_check: true
    specialist_review:
      required: false
      role: null
      expected_output: null
    human_closure_review: true
verification_evidence: |
  ## 验证计划

  历史证据格式规范化：检查原 验证证据 是否保留可复查的验证对象、结果或关闭判断；本次仅补齐四段式结构，不新增或改变历史事实语义。

  ## 验证命令

  历史记录未单独记录完整的验证命令；如原字段包含命令、脚本、检查入口或人工检查动作，保留在下方原始证据摘录中。

  ## 验证结果

  原始证据摘录：

  原文标题：验证结果

  历史任务计划迁移聚合证据：
  - task-0062: ## 交付内容

  ## 结论

  本次仅完成证据字段四段式格式规范化；对象状态、关闭判断、验证结果和历史事实语义以原始证据摘录为准，未额外伪造历史验证过程。
closure_evidence: |
  ## 验证计划

  历史证据格式规范化：检查原 关闭证据 是否保留可复查的验证对象、结果或关闭判断；本次仅补齐四段式结构，不新增或改变历史事实语义。

  ## 验证命令

  历史记录未单独记录完整的验证命令；如原字段包含命令、脚本、检查入口或人工检查动作，保留在下方原始证据摘录中。

  ## 验证结果

  原始证据摘录：

  原文标题：验证结果

  历史任务计划迁移聚合证据：
  - task-0062: ## 交付内容

  ## 结论

  本次仅完成证据字段四段式格式规范化；对象状态、关闭判断、验证结果和历史事实语义以原始证据摘录为准，未额外伪造历史验证过程。
review_requested_at: '2026-06-11T18:45:15'
closed_at: '2026-06-11T18:45:15'
related_docs: []
related_adrs: []
related_memos: []
related_pitfalls: []
related_workplans: []
