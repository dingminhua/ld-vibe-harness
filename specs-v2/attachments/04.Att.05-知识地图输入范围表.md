# 知识地图输入范围表

```yaml
v2_attachment:
  attachment_id: "04.Att.05"
  title: "知识地图输入范围表"
  status: "draft"
  authority: "not_active_until_parent_and_human_approved"
  parent_spec: "specs-v2/04-Code确定性执行规范.md"
  canonical_path: "specs-v2/attachments/04.Att.05-知识地图输入范围表.md"
  purpose: "承载 04 授权的知识地图输入范围"
  migration_sources:
    - "specs/07-Code确定性执行实现规范.md"
  code_consumption:
    - "knowledge_map_input_scope"
```

> 文件状态：本文当前位于 `specs-v2/`，尚未切换为 active；正式知识地图输入规则仍以 v2 04 Human 确认后为准。本文是 `04-Code确定性执行规范.md` 授权附件，必须随父规范经过 Human 单篇确认后才可生效，不提前推进知识地图运行时能力。

## 1. 定位

本文承载 04 授权的知识地图输入范围。

## 2. 输入范围

| 范围 | 状态 | 规则 |
|---|---|---|
| `specs_v2` | 当前可诊断 | 只读解析已登记 v2 文件和身份块 |
| active `specs/` | 待双读 | 用于迁移审计，不被 v2 草案覆盖 |
| `governed_projects` | 后置 | v2 完整迁移和新事实源建立后再推进 |
| `git_history` | 后置 | 只作为 Git 追溯和证据层，不落盘为图谱事实 |
| `web_cache`、测试缓存、聊天过程 | 禁止 | 不作为权威输入 |
| `PLAN.md`、`MIGRATION-MAP.md`、临时审计 | 排除 | 可用于诊断或 review hints，不生成可信节点边 |

## 3. 待补齐事项

1. 管辖项目图谱、Git history 图谱和 raw 层能力在 v2 完整迁移后再评估；
2. Code 应对后置范围输出 degraded 诊断，不得静默忽略。
