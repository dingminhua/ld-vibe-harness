# V4 三份独立审计处置记录

> 记录性质：本文记录 2026-07-15 三份独立架构审计与当前工作树事实的合并核对、处置和顺序修正。它不是规则源，不直接修改 Specs、字段、公开操作或产品行为；当前状态与下一步仍以 `V4-工作推进总纲.md` 为唯一控制面。
> 核对时间：2026-07-15
> 核对基线：`dev-v4`，`d64381263000`

## 1. 输入与处置方法

本次逐项核对以下三份根目录报告：

1. `V4-Audit-Report-GPT-Sol-2026-07-15-0822.md`；
2. `V4-Audit-Report-Hy3-2026-07-15-0829.md`；
3. `V4-架构方向独立审计报告-GLM5.2-2026-07-15.md`。

三份报告的严重度口径、观察时点和覆盖范围不同，且均早于 09/33、Codex adapter 安装验证及其完整回归闭合。因此不继承任一报告的 P0–P3 等级，也不把审计建议自动写成规则。每项意见按以下口径处置：

- **接受并提前**：当前事实仍成立，且会直接破坏 AI 使用路径、安全边界或发布能力；
- **部分关闭**：审计后已有实现或证据关闭其中一部分，剩余部分继续进入路线；
- **证据后决定**：问题是合理假设，但直接修改正式规则会造成无实测依据的第二次设计；先通过 dogfood 收集数据；
- **不作为缺陷接受**：意见与当前已确认责任边界或 Human 决定冲突，或只凭行数、字段数和重复表述不足以证明设计错误；
- **时点失效**：报告描述的是审计时旧工作树、旧操作集或不同运行环境，不能继续作为当前状态。

## 2. 当前事实基线

本轮重新核对得到：

| 项目 | 当前事实 | 声明边界 |
|---|---|---|
| 规则源 | 当前发现 23 个候选：00、01–09、20–24、30–33 和四份授权附件 | `active` 与机械检查通过不等于所有语义和产品能力已验证 |
| Helper | 当次 `capabilities` 返回 10 项已绑定公开操作 | 操作存在不等于普通安装、所有环境接入或 AI 端到端闭环已完成 |
| Code 回归 | Python 3.12 开发环境中 567 passed；Ruff check 和 format check 通过 | 结果只对应 2026-07-15、提交 `d64381263000` 加当前文档工作树；不替代 Windows、Web 或真实项目验证 |
| 环境接入 | 09/33 已建立；本机当前用户 Codex 0.144.2 的 `SessionStart startup|resume` 已完成安装、信任、成功/失败触发及停用/恢复验证 | 只覆盖当前用户、机器、版本、插件版本、事件和工作区，不证明普通 wheel 或其它环境 |
| 事实实例 | `git ls-files 'facts/**'` 为 0 | 当前没有真实 V4 事实对象、WorkCase 关闭或跨会话恢复证据 |
| 普通安装 | Helper 仍只支持规则源与 Code 共置的源码运行或 editable install；shell PATH 中没有普通安装的 `ldvh` | Codex 插件真实工作不等于普通 sdist/wheel 安装闭合 |
| Web | API 仍从 `ldvh-base` 读取 V3 字段和状态，Spark POST 仍可用 `writeFileSync` 直接创建 V3 YAML | Web 不具备 V4 写入权威，并存在制造 V4 Helper 不认可对象的现实入口 |
| Windows | `fcntl`、`O_DIRECTORY`、`O_NOFOLLOW`、`dir_fd`、目录 fsync 和固定 `python3` 启动仍存在 | 原生 Windows 尚不受支持，WSL 不能代替原生 Windows 证据 |
| CI | 当前没有 `.github/workflows` workflow | CI 只应验证 LDVH 自身，不负责管辖项目；不应在跨平台命令尚未稳定时制造永久红灯 |

## 3. 三份审计的共同结论

三份报告虽使用了“需要校正”“早期结构性偏航”或更高严重度的不同表达，但共同事实判断一致：

1. 00 的 AI-first、Working Tree 事实边界、Helper 统一入口、渐进披露、Code/AI 语义边界、来源回指、CAS 和写后回读值得保留；
2. V4 在 V1 快速定位、V3 边界识别和 V5 据实判断上已有较强实现；
3. V4 在 V4 稳定推进、V6 工作接续、V7 真实 Human 交还和 V8 持续积累上仍缺真实纵向证据；
4. 规范、字段、测试和审计闭合速度快于真实事实消费、跨会话恢复、普通安装和 Web V4 适配；
5. 继续扩建新模板、事实类型、字段或全局审核机制，不能替代一次真实 AI 工作闭环。

本轮接受这一共同方向判断，但不接受“当前核心架构已经失效”的结论。09/33 和首个 Codex adapter 已经关闭了审计时的单环境接入空白，567 项回归也关闭了旧工作树中的脆弱集合断言；真正未关闭的是普通安装、低成本启动/恢复、真实事实与跨会话接续、Web V4 安全边界、原生 Windows 和发布工程。

## 4. 逐类处置

| 编号 | 合并问题 | 当前处置 | 理由与后续动作 |
|---|---|---|---|
| A1 | 0 个真实事实对象、0 次跨会话 WorkCase 恢复，核心 AI 价值未实证 | **接受并提前** | 阶段 10 必须进入近期主线；但不得为了证明流程制造虚假内部事实。应选择真实受管辖工作，在 Human 明确选择建立 WorkCase 后完成创建、读取、更新、中断、恢复、验证、交还和关闭 |
| A2 | 没有低成本 bootstrap/resume 路径，AI 仍需自行组合多个大响应 | **接受并提前** | 当前 Codex `SessionStart` 只完成最小管辖解析，不等于恢复 bundle。先调查能否由薄 adapter 机械组合现有操作；只有现有操作无法形成稳定边界时才新增 Helper 公开操作，不把语义判断塞给 Code |
| A3 | Web 仍可写入 V3 Spark，并读取 V3 类型、字段、目录和状态 | **接受并作为安全前置** | 在 V4 受控写入接入前，Spark 创建 UI 与 POST API 应同时 fail closed 或受默认关闭的明确 feature gate 约束；随后再建设 V4 只读列表、详情、风险和 Human 待决定事项。具体可见行为变化须按 08 取得 Human 范围授权 |
| A4 | 环境 adapter 和实际 CLI 不可用 | **部分关闭** | 本机 Codex adapter 已真实闭合，报告中的“适配器尚未构建”已经失效；普通 wheel、PATH、干净环境安装、规则源部署和其它环境仍开放，进入阶段 7 |
| A5 | README、控制面、操作数和测试数互相矛盾 | **部分关闭并在本增量校正** | 根 README 链接和 09/33 状态已在审计后修复；`docs/code/README.md` 仍漏列两个行动模板读取操作，总纲仍保留历史测试数字。开发导航补齐两项操作，控制面把数字明确标成带时点证据并新增当前 567 基线；运行时能力仍以 `capabilities` 为准 |
| A6 | WorkCase 7 个 phase、完整审核链和双 Human Gate 可能成本过高 | **证据后决定** | Human 已明确：一旦选择把工作建立为 WorkCase，完整质量链不因持续时间或 work item 数缩减。审计不能推翻该决定。真实 dogfood 必须记录读取量、等待点、版本失效、恢复遗漏和实际使用字段；如证据显示净负担，再向 Human 提交“保留完整链”与“精简核心 + 条件控制层”比较，不在本轮直接改 21 |
| A7 | 字段准入、术语、规范职责和重复 Gate/Stop/验证治理税过高 | **证据后决定** | 当前没有事实规模和变更频率数据，不能仅按行数判定。冻结与 dogfood 无关的新字段、新类型和全局治理扩建；记录 dogfood 中实际触发次数、无产出审核和跨文件同步成本，再决定局部字段轻量通道或共享/破坏性变化完整通道 |
| A8 | compact 响应仍有空字段、重复来源和较重信封 | **接受为测量项，不立即破坏契约** | 响应字段是正式契约，不能只按顶层字段数压平。bootstrap 与 dogfood 记录字节数、读取次数、空字段占比、AI 误判和缺失诊断；基于证据提出兼容演进或新版本，而不是直接删除字段 |
| A9 | 01 中公开操作契约、05 字段登记、各规范验证段应搬移或合并 | **不作为当前缺陷接受** | 这些内容承担规则源、授权和机械投影责任，行数长或 AI 不应直接阅读不等于责任放错。优先让 Helper/adapter 避免 AI 读取维护契约；只有 dogfood 证明责任重复、来源冲突或披露无法解决时才进入 Specs 重构 |
| A10 | Git 提交 Code 没有成为 Helper 公开操作 | **不作为当前缺陷接受** | 当前 30 是行动模板，Git 写入仍需 Human 当前授权；“实现存在”不自动要求公开为远程操作。真实使用重复证明统一入口有净收益后，再按 04/06/30 另行准入 |
| A11 | Code/AI 机械与语义校验边界使 AI 不知可以信任什么 | **未发现越权缺陷，保留为披露验收项** | 当前来源和响应会报告机械范围、未验证范围与资格缺口，三份报告也未发现 Code 系统性解释自然语言。dogfood 需要验证新 AI 能否正确理解这些边界；若不能，先改善输出与入门路径，不扩张 Code 语义权威 |
| A12 | 549 passed / 1 failed、8 个操作、21 个候选或 3 个模板 | **时点失效** | 当前复跑为 567 passed、10 个操作、23 个候选和 4 个模板。旧数字只保留在带日期的历史证据或原审计报告中，不再承担当前状态 |
| A13 | Web Human Gate 可能导致迁移中反复等待 | **按明确范围授权处理，不立即改 08** | 后续可将“关闭 V3 写入口”或“V4 只读迁移”作为边界明确的整包交给 Human 一次决定；包内不新增产品取舍的机械实现可持续推进。若 08 实际阻止这种执行，再以真实阻断进入规则修正 |
| A14 | 原生 Windows、开源 CI 和发布资料缺失 | **接受为阶段 7/12 门槛** | 现在固定跨平台设计约束；首个公开 V4 发布前完成三平台 LDVH 自身检查、真实 Windows 验证、构建/干净安装、支持矩阵、License、贡献、安全和发布资料。CI 不管理管辖项目 |

## 5. 顺序修正

在真实纵向证据形成前，暂停以下非阻断扩建：

- 新事实类型和与当前闭环无关的新字段；
- 第 34 项以后新的行动模板；
- 不由当前失败或发布边界驱动的新一轮全架构审核；
- 多对象事务、自动 `supersedes`、删除迁移等高级能力；
- 大规模 Web 写能力和交互扩张。

允许继续的范围限定为：修复当前真实性/安全缺口、普通安装与 Windows 基础、低成本 bootstrap/resume、真实 dogfood 所必需的最小变更、V4 Web 只读迁移、V3 覆盖收口和首个公开版本交付门槛。

后续按以下顺序推进：

1. **文档与安全校正**：完成当前审计处置同步；取得 Human 对 Web V3 Spark 写入口 fail-closed 范围的决定并实施 API + UI 双层关闭；
2. **普通安装契约**：定义 wheel 内规则源承载/定位、配置查找、安装/升级/卸载和错误边界，完成 sdist/wheel 与干净虚拟环境 CLI smoke test；
3. **原生 Windows 基础**：设计并实现跨平台锁、安全路径遍历、原子 no-overwrite/replace、目录持久化降级和 Python 启动方式；补盘符、UNC、大小写、反斜杠、reparse point/符号链接权限测试；
4. **低成本启动/恢复入口**：以薄 adapter 优先，机械组合管辖范围、能力摘要、F0/F1 和来源指针；测量有事实与空事实场景，避免复制规则或替 AI 判断适用性；
5. **真实纵向 dogfood**：在真实受管辖工作上取得 Human 的 WorkCase 建立决定，完成事实创建、读取、CAS 更新、强制中断、换会话恢复、验证、Git 溯源、结果交还和关闭；记录调用数、字节数、文件数、无产出判断、Human 等待点、重复调查和恢复遗漏；
6. **证据驱动减法 Gate**：根据第 5 步决定是否修改 WorkCase、字段准入、响应信封和规范披露；没有实测问题的正式规则不因审计偏好重写；
7. **V4 Web 最小只读闭环**：通过 V4 当前来源/Code 展示事实列表、详情、风险、证据和 Human 待决定事项；写入继续只走已获授权的 Code 能力；
8. **V3 覆盖收口**：逐项关闭历史资产的保留、重建、取消或延后结论，避免发布范围静默遗漏；
9. **开源发布 Gate**：建立 LDVH 自身的 Linux/Windows/macOS CI，稳定后设置主分支 required checks；补齐真实 Windows 证据、发布构建、版本、License、贡献、安全、支持矩阵和排障资料；
10. **首个 V4 可用版本验收**：按明确支持矩阵复核安装、公开调用、失败恢复、环境、Web、真实项目 dogfood、文档和未支持范围，再决定发布。

## 6. 后续需要 Human 决定的事项

当前不需要为了继续文档和普通安装调查而先作额外决定。到达相应 Gate 时需要 Human 明确：

1. 是否授权把 Web 的 V3 Spark 创建 UI 与 POST API 一并默认关闭；
2. 选择哪一项真实受管辖工作进入首个 WorkCase dogfood，并授权创建该项目记录；
3. dogfood 证据形成后，是否改变已确认的 WorkCase 完整质量控制链；
4. CI 稳定后，是否修改远端主分支保护并把三平台 checks 设为合并条件；
5. 首个公开版本的精确 Windows 支持矩阵和发布渠道。

除第 1、2 项在对应实现前会形成 Gate 外，其余事项现在提前决定都缺少必要证据。

## 7. 完成边界

本记录完成只表示三份审计已经经过事实核对并进入统一路线。它不表示审计问题已经全部修复，也不证明 dogfood、普通安装、原生 Windows、Web V4、CI 或发布已经完成。任何后续状态变化必须回写 `V4-工作推进总纲.md`，不得只修改本记录。
